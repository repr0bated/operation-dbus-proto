{
  "$defs": {
    "A2aServerConfig": {
      "description": "Inbound A2A discovery server configuration.",
      "properties": {
        "bind": {
          "description": "Optional advertise-only host override for card endpoint URLs. The\nroutes always serve on the gateway's own listener; this only changes\nthe host printed in advertised endpoints when A2A is fronted at a\ndifferent address. `None` derives from the gateway host.",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "default": false,
          "description": "Master switch for the inbound A2A surface. Default `false`: no\nwell-known route, no per-alias cards, no inbound endpoints.",
          "type": "boolean"
        },
        "port": {
          "description": "Optional advertise-only port override, paired with `bind`. `None`\nderives from the gateway port. Advertise-only: nothing binds here.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "public_base_url": {
          "default": "",
          "description": "Operator-supplied base URL advertised in agent card endpoints.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "A2aServerSection": {
      "description": "A2A section wrapper that leaves room for future sibling configuration.",
      "properties": {
        "server": {
          "$ref": "#/$defs/A2aServerConfig",
          "default": {
            "enabled": false,
            "public_base_url": ""
          },
          "description": "Inbound A2A discovery server (`[a2a.server]`)."
        }
      },
      "type": "object"
    },
    "AccessMode": {
      "description": "A cross-agent filesystem grant from a workspace allowlist entry.",
      "oneOf": [
        {
          "const": "read",
          "description": "Read access only. Cross-agent `file_read` is permitted; writes are not.",
          "type": "string"
        },
        {
          "const": "write",
          "description": "Write access only. Cross-agent `file_write` is permitted; reads are not.",
          "type": "string"
        },
        {
          "const": "read_write",
          "description": "Both read and write. The agent can `file_read` and `file_write` against\nthe target's workspace.",
          "type": "string"
        }
      ]
    },
    "AcpConfig": {
      "description": "ACP (Agent Client Protocol) server configuration (`[acp]` section).",
      "properties": {
        "default_agent": {
          "description": "Agent alias to use when `session/new` omits `agentAlias` and more than\none agent is configured. When exactly one agent exists it is\nauto-selected regardless of this field.",
          "type": [
            "string",
            "null"
          ]
        },
        "max_sessions": {
          "default": 10,
          "description": "Maximum number of concurrent ACP sessions. Default: `10`.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "session_timeout_secs": {
          "default": 3600,
          "description": "Idle session timeout in seconds. Sessions with no activity for this\nduration are eligible for eviction. Default: `3600` (1 hour).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "AgentA2aConfig": {
      "description": "Per-agent A2A publication and exposed-skill configuration.",
      "properties": {
        "exposed_skills": {
          "default": [],
          "description": "Filter selecting which resolved skill ids appear on this alias's\ncard. Empty = no skills advertised. Entries that do not resolve to\na real skill in the alias's bundles are dropped (the bundles are\ncanonical; this only selects from them).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "published": {
          "default": false,
          "description": "Publish this alias as a discoverable A2A agent. Default `false`:\nthe alias is excluded from the discovery catalog and serves no\nper-alias card even when the server is enabled.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "AgentAlias": {
      "description": "Reference to a configured `[agents.<type>.<alias>]` entry.\nEmpty value means \"no preference\" (opt-out). Non-empty values must\nresolve to a configured alias; `Config::validate()` enforces this.",
      "type": "string"
    },
    "AgentMemoryConfig": {
      "description": "Per-agent memory backend selection and its persistence contract.",
      "properties": {
        "backend": {
          "$ref": "#/$defs/MemoryBackendKind",
          "default": "sqlite",
          "description": "The backend kind this agent uses. Defaults to `Sqlite` for new\nagents; once an agent has on-disk data the value is locked."
        }
      },
      "type": "object"
    },
    "AgentWorkspaceConfig": {
      "description": "Per-agent workspace and cross-agent access configuration.",
      "properties": {
        "access": {
          "additionalProperties": {
            "$ref": "#/$defs/AccessMode"
          },
          "default": {},
          "description": "Cross-agent workspace allowlist. An empty map grants no sibling access.",
          "type": "object"
        },
        "path": {
          "description": "Optional explicit workspace path. `None` = derive from\n`<install>/agents/<alias>/workspace/`.",
          "type": [
            "string",
            "null"
          ]
        },
        "read_memory_from": {
          "default": [],
          "description": "Cross-agent memory allowlist. An empty list grants access only to local memory.",
          "items": {
            "$ref": "#/$defs/AgentAlias"
          },
          "type": "array"
        },
        "unrestricted_filesystem": {
          "default": false,
          "description": "Escape hatch: when `true`, the agent can read or write anywhere\nthe host filesystem permits. Off by default; flipping this on is\nauditable.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "Ai21ModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "AihubmixModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "AliasedAgentConfig": {
      "description": "Configuration for an aliased agent. Each `[agents.<alias>]` TOML\nblock deserializes into one of these. The `DelegateTool` looks up\nentries here to dispatch a subtask to a named sibling agent.",
      "properties": {
        "a2a": {
          "$ref": "#/$defs/AgentA2aConfig",
          "default": {
            "exposed_skills": [],
            "published": false
          },
          "description": "Per-agent A2A publication block (`[agents.<alias>.a2a]`). Gates\nwhether this alias is discoverable as a spec-conforming A2A agent\nand which resolved skills appear on its card. Default-closed\n(`published = false`, no exposed skills). See\n`crate::multi_agent::AgentA2aConfig`."
        },
        "acp_enable_mcp": {
          "default": false,
          "description": "Initialize this agent's `mcp_bundles` tools when it serves an ACP\n(`session/new`) session.\n\nOff by default: MCP servers are external processes/services that can\nblock startup while they connect, and ACP `session/new` is expected to\nreturn promptly. Enable it when this agent must call its `mcp_bundles`\ntools over ACP; `session/new` then pays the one-time MCP connection cost\n(bounded and non-fatal per server). Set per agent so each ACP profile\nopts in independently; when this agent is the ACP default\n(`acp.default_agent`, or the sole configured agent), the flag is picked\nup automatically for sessions that omit `agentAlias`.",
          "type": "boolean"
        },
        "channels": {
          "default": [],
          "description": "Channel aliases this agent handles (e.g. `[\"telegram.<alias>\", \"discord.<alias>\"]`).\nEach entry is a `ChannelRef` resolving through `[channels.<type>.<alias>]`;\n`Config::validate()` fails loud on dangling references.",
          "items": {
            "$ref": "#/$defs/ChannelRef"
          },
          "type": "array"
        },
        "classifier_provider": {
          "$ref": "#/$defs/ModelProviderRef",
          "default": "",
          "description": "Optional override for the per-message LLM reply-intent classifier\n(`classify_channel_reply_intent` in zeroclaw-channels). When non-empty,\nthe channel orchestrator routes the \"should this message be replied to?\"\nclassification call to `[providers.models.<type>.<alias>]` referenced\nhere, instead of reusing the main agent's `model_provider`.\n\nSource of truth for api_key / uri / model / temperature etc. is the\nreferenced `[providers.models.<type>.<alias>]` entry. This field is\na reference only (NEVER a copy), per AGENTS.md SINGLE SOURCE OF TRUTH.\n\nEmpty (`Default`) = inherit the main agent's resolved provider+model\n(preserves pre-PR behavior; backward compatible).\n\nUse case: classification is a cheap REPLY/NO_REPLY decision, doesn't\nneed a high-end model. Point this at a fast/free small model\n(e.g. `kimi-k2.5`, `qwen-turbo`) while `model_provider` stays on the\nexpensive answering model (e.g. `qwen3.6-plus`).\n\nNote: TOML table names cannot contain `.`, so alias `kimi-k2.5`\nmust be written as `[providers.models.custom.kimi-k2-5]`. The\nunderlying `model = \"kimi-k2.5\"` string can still contain dots.\n\nACP channels (IDE-direct) always reply and skip the classifier\nentirely, so this field has no effect on ACP traffic."
        },
        "cron_jobs": {
          "default": [],
          "description": "Cron job aliases. Each entry references `cron[key]`, a declarative\nscheduled job invoked by the scheduler on its configured trigger.\nWhen the cron fires, this agent is the actor that executes the job.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "delegate_same_risk_profile": {
          "default": true,
          "description": "Auto-allow delegation to every agent sharing this agent's risk\nprofile. Default `true` preserves the historical reach where any\nsame-profile peer is a delegation target. Set `false` to opt this\nagent out so only the explicit `delegates` list is reachable.\nGating (whether delegation is permitted at all) still lives on the\nrisk profile's `delegation_policy.mode`; this only narrows reach.",
          "type": "boolean"
        },
        "delegates": {
          "default": [],
          "description": "Explicit delegate roster: additional agent aliases this agent may\ndelegate to, beyond same-profile peers. Possibly empty. String\nentries are accepted for concise manual editing and load as bounded\ndelegates; saved config emits object entries with explicit modes.\nEntries may name agents on a different risk profile. `Config::validate()`\nfails loud on a dangling alias, duplicate alias, or self-reference.",
          "items": {
            "$ref": "#/$defs/DelegateTargetConfig"
          },
          "type": "array"
        },
        "enabled": {
          "default": true,
          "description": "Whether this agent is active. Set false to disable without removing the definition.",
          "type": "boolean"
        },
        "identity": {
          "$ref": "#/$defs/IdentityConfig",
          "default": {
            "aieos_inline": null,
            "aieos_path": null,
            "format": "openclaw"
          },
          "description": "Per-agent identity format (`[agents.<alias>.identity]`). Each\nagent renders its own IDENTITY.md / SOUL.md inside its\nper-agent workspace; this block selects the format (OpenClaw or\nAIEOS) and optional inline/file source for the agent's identity\ndocument."
        },
        "knowledge_bundles": {
          "default": [],
          "description": "Knowledge bundle aliases. Additive: the agent loads every listed\nbundle.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "mcp_bundles": {
          "default": [],
          "description": "MCP bundle aliases. Each entry references `mcp_bundles[key]`, a named\ngroup of MCP servers. Secure by default: an agent is granted only the\nservers named by its bundles. An agent with no `mcp_bundles` receives\nno MCP servers (omission is not a grant). See\n`Config::mcp_servers_for_agent`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "memory": {
          "$ref": "#/$defs/AgentMemoryConfig",
          "default": {
            "backend": "sqlite"
          },
          "description": "Per-agent memory backend selection (`[agents.<alias>.memory]`).\nThe `backend` field is locked at agent creation and immutable on\nsubsequent loads. Defaults to `Sqlite`. See\n`crate::multi_agent::AgentMemoryConfig`."
        },
        "model_provider": {
          "$ref": "#/$defs/ModelProviderRef",
          "default": "",
          "description": "Dotted model-provider alias (e.g. `\"anthropic.<alias>\"`).\nResolves through `model_providers.<type>.<alias>` at runtime;\n`Config::validate()` fails loud on dangling references."
        },
        "precheck": {
          "$ref": "#/$defs/ChannelPrecheckConfig",
          "default": {
            "enabled": true,
            "timeout_secs": 5
          },
          "description": "Per-agent reply-intent precheck controls. The classifier call reads\nthis block at message time; model/provider selection stays on\n`classifier_provider` so there is only one routing source of truth."
        },
        "risk_profile": {
          "$ref": "#/$defs/RiskProfileRef",
          "default": "",
          "description": "Risk profile alias (e.g. `\"default\"`). Resolves delegation guardrails at runtime."
        },
        "runtime_profile": {
          "$ref": "#/$defs/RuntimeProfileRef",
          "default": "",
          "description": "Runtime profile alias (e.g. `\"default\"`). Resolves agentic/iteration settings."
        },
        "skill_bundles": {
          "default": [],
          "description": "Skill bundle aliases. Each entry resolves to\n`skill_bundles[key].directory` at runtime; the agent loads every\nlisted bundle.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "summary_provider": {
          "$ref": "#/$defs/ModelProviderRef",
          "default": "",
          "description": "Per-agent override for the context-compression summarizer provider, as\na `providers.models.<type>.<alias>` reference. Empty (Default) = inherit\nthe runtime profile's `context_compression.summary_provider`, else the\nagent's own resolved provider+model. Reference only, never a copy;\nresolved by [`Config::effective_summary_provider`]. Validated in\n`Config::validate()`."
        },
        "transcription_provider": {
          "$ref": "#/$defs/TranscriptionProviderRef",
          "default": "",
          "description": "Transcription / STT provider as a dotted alias reference\n(`<type>.<alias>`, e.g. `\"groq.<alias>\"`). Resolves through\n`transcription_providers.<type>.<alias>`. Empty = agent has no\ntranscription preference; channels that ingest voice still need a\nresolved provider (there is no global default), so an inbound voice\nflow into an agent with empty `transcription_provider` errors loudly\nat the channel boundary."
        },
        "tts_provider": {
          "$ref": "#/$defs/TtsProviderRef",
          "default": "",
          "description": "TTS provider as a dotted alias reference (`<type>.<alias>`,\ne.g. `\"openai.<alias>\"`). Resolves through `tts_providers.<type>.<alias>`.\nEmpty = no TTS for this agent (there is no global default-provider concept;\nevery agent that wants TTS sets its own `tts_provider`)."
        },
        "workspace": {
          "$ref": "#/$defs/AgentWorkspaceConfig",
          "default": {
            "access": {},
            "read_memory_from": [],
            "unrestricted_filesystem": false
          },
          "description": "Per-agent workspace block (`[agents.<alias>.workspace]`).\nHolds the agent's filesystem path, cross-agent access allowlist,\nfilesystem-escape boolean, and cross-agent memory allowlist.\nDefault is fully jailed (no cross-agent access). See\n`crate::multi_agent::AgentWorkspaceConfig`."
        }
      },
      "type": "object"
    },
    "AmqpConfig": {
      "properties": {
        "amqp_url": {
          "description": "AMQP broker URL. Use `amqp://` for plain or `amqps://` for TLS\n(e.g. `amqps://fedora:@rabbitmq.fedoraproject.org/%2Fpublic_pubsub`).",
          "type": "string",
          "x-secret": true
        },
        "ca_cert": {
          "description": "Path to the CA certificate bundle for `amqps://` connections.",
          "type": [
            "string",
            "null"
          ]
        },
        "client_cert": {
          "description": "Path to the client certificate for broker mutual-TLS auth\n(Fedora Messaging requires a client cert).",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "client_key": {
          "description": "Path to the client private key matching `client_cert`.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "content_template": {
          "default": "",
          "description": "Template for the inbound message content. `{field}` placeholders are\ninterpolated from the JSON delivery body's top-level keys. When empty,\nthe raw delivery body is used verbatim.",
          "type": "string"
        },
        "dispatch": {
          "$ref": "#/$defs/SopDispatch",
          "default": "agent_loop",
          "description": "Where consumed deliveries are routed: drive an agent turn\n(`agent_loop`, default), dispatch to the SOP engine (`sop`), or both\n(`sop_and_agent_loop`). The `sop` and `sop_and_agent_loop` modes match\nthe delivery against SOP `amqp` triggers by routing key."
        },
        "durable_ack": {
          "default": true,
          "description": "Acknowledgement mode. When `true` (default), deliveries are acked only\nafter the message is durably handed to the agent loop, giving\nat-least-once semantics: a crash before hand-off redelivers the event.\nSet `false` for at-most-once (broker acks on dispatch), which silently\ndrops in-flight events on crash and is only appropriate for\nnon-side-effecting, drop-on-overload consumers.",
          "type": "boolean"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "exchange": {
          "description": "Exchange to bind the consumer queue to (e.g. `amq.topic`).",
          "type": "string"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "queue": {
          "description": "Queue name. Leave unset for a server-generated, transient,\nauto-deleted, exclusive queue. Set a stable name (UUID recommended)\nonly when durable delivery across reconnects is required.",
          "type": [
            "string",
            "null"
          ]
        },
        "routing_keys": {
          "default": [],
          "description": "Routing keys to bind. Scope these to the topics of interest; binding\n`#` consumes the entire exchange and is almost never what you want.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "sender_label": {
          "default": "amqp",
          "description": "Value placed in `ChannelMessage.sender` for every delivery from this\nsource (e.g. `anitya`). Lets the orchestrator's self-loop guard and\nper-channel routing identify the origin.",
          "type": "string"
        },
        "thread_id_field": {
          "default": "",
          "description": "Dotted path into the JSON delivery body whose value becomes the\nmessage `thread_ts`, correlating replies to the originating event\n(e.g. `message.project.name`). Empty disables threading.",
          "type": "string"
        }
      },
      "required": [
        "amqp_url",
        "exchange"
      ],
      "type": "object"
    },
    "AnthropicModelProviderConfig": {
      "description": "Anthropic model model_provider config. No family-specific extras yet — typed\nslot reserved for future Anthropic-only knobs (cache_control, beta\nheaders) so they land cleanly without another schema rework.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "AnyscaleModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "ApprovalGroupConfig": {
      "description": "A named approver group (`[sop.approval.groups.<name>]`).",
      "properties": {
        "members": {
          "default": [],
          "description": "Identity labels that belong to this group.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "ApprovalMode": {
      "description": "WHO may clear a SOP approval gate. Layered with execution_mode / priority /\nrequires_confirmation (all of which still apply). Closed set => serde enum.",
      "oneOf": [
        {
          "const": "both",
          "description": "Agent `sop_approve` OR an out-of-band principal both clear. Backward-compat default.",
          "type": "string"
        },
        {
          "const": "out_of_band_required",
          "description": "Only out-of-band principals (CLI / WS / HTTP / timeout) clear; `sop_approve`\nbecomes a no-op. Lets an operator require a separate approver without bricking runs.",
          "type": "string"
        },
        {
          "const": "agent_tool",
          "description": "Only the agent tool clears; out-of-band surfaces are read-only pending lists. Niche.",
          "type": "string"
        }
      ]
    },
    "ApprovalPolicyConfig": {
      "description": "A named approval policy (`[sop.approval.policies.<name>]`) the broker enforces.",
      "properties": {
        "escalation_route": {
          "default": null,
          "description": "Route to escalate to on timeout (the distinct \"second route\"). `None`/empty\nre-surfaces to the same route (today's `Escalate` behavior). When configured,\nthe route must use the `channel:recipient` format.",
          "type": [
            "string",
            "null"
          ]
        },
        "quorum": {
          "default": 0,
          "description": "Distinct approvers required before the gate clears. `0`/`1` both mean a\nsingle approval; `>= 2` requires a quorum of distinct approver identities.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "request_route": {
          "default": null,
          "description": "Channel to deliver the INITIAL approval request to when a run parks at a\ngate this policy governs, formatted `channel:recipient` (e.g.\n`discord.ops:123456789012345678`). The `channel` names a configured channel\n(the `<channel>.<alias>` / bare-`<channel>` key from the channel map); the\n`recipient` is that channel's addressee (a Discord channel id, a chat id,\netc.). Delivery is best-effort - the gate is the source of truth and a\ndelivery failure never blocks or clears it; approvals still come back\nthrough the normal HTTP/WS/tool surfaces. `None`/empty = no out-of-band\nrequest notice (today's behavior: only the originating surface is notified).\nThis is a DISTINCT lifecycle event from `escalation_route`: the request goes\nout when the run parks; the escalation goes out only if it later times out.\nWhen configured, the route must use the `channel:recipient` format.",
          "type": [
            "string",
            "null"
          ]
        },
        "required_group": {
          "default": null,
          "description": "Group whose members may satisfy this policy's gate. `None`/empty = any\nprincipal permitted by `approval_mode` (back-compat, no membership gate).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ApprovalRoute": {
      "description": "Routes tool approvals to a distinct approver channel with fail-closed defaults.",
      "properties": {
        "approver_channel": {
          "description": "A registered channel name (NOT the originator) — the distinct-approver hop.",
          "type": "string"
        },
        "on_no_approver": {
          "$ref": "#/$defs/OnNoApprover",
          "default": "deny",
          "description": "Behavior when the approver is unreachable. Fail-closed by default."
        },
        "timeout_secs": {
          "default": 120,
          "description": "Bound the approver's response window; a timeout denies (DoS guard). Default 120s.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "approver_channel"
      ],
      "type": "object"
    },
    "ApprovalTimeoutAction": {
      "description": "What happens to a SOP approval gate when it times out. Default is fail-closed:\nthe gate never self-approves. Closed set => serde enum.",
      "oneOf": [
        {
          "const": "escalate",
          "description": "Re-surface to the out-of-band approver and keep waiting; never self-approve.\nDefault, fail-closed.",
          "type": "string"
        },
        {
          "const": "cancel",
          "description": "Terminate the run (fail-safe cancel). Strict mode.",
          "type": "string"
        },
        {
          "const": "auto_approve",
          "description": "LEGACY: auto-approve on timeout. The ONLY path to the old fail-open behavior;\nopt-in only.",
          "type": "string"
        }
      ]
    },
    "ArceeModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "AssemblyAiSttConfig": {
      "description": "AssemblyAI STT model_provider configuration (`[transcription.assemblyai]`).",
      "properties": {
        "api_key": {
          "default": null,
          "description": "AssemblyAI API key.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        }
      },
      "type": "object"
    },
    "AssemblyAiTranscriptionProviderConfig": {
      "description": "Shared base for cloud transcription providers. Each cloud family\ncomposes this via `#[serde(flatten)] base: TranscriptionProviderConfig`.",
      "properties": {
        "api_key": {
          "default": null,
          "description": "API key for the transcription provider.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "initial_prompt": {
          "default": null,
          "description": "Whisper-style initial prompt to bias the model toward expected\nvocabulary (proper nouns, technical terms). Provider-specific support;\nsilently ignored where not applicable.",
          "type": [
            "string",
            "null"
          ]
        },
        "language": {
          "default": null,
          "description": "Optional language hint passed to the provider (ISO-639-1 like `\"en\"` /\n`\"ru\"`, or BCP-47 like `\"en-US\"` for Google). Most providers auto-detect\nwhen this is unset.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "AstraiModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "AtomicChatModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "AuditConfig": {
      "description": "Audit logging configuration",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Enable audit logging",
          "type": "boolean"
        },
        "log_path": {
          "default": "audit.log",
          "description": "Path to audit log file (relative to zeroclaw dir)",
          "type": "string"
        },
        "max_size_mb": {
          "default": 100,
          "description": "Maximum log size in MB before rotation",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "sign_events": {
          "default": false,
          "description": "Sign events with HMAC for tamper evidence",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "AuthMode": {
      "description": "Authentication mode for model model_provider families that support more than one\n(e.g. Qwen, Minimax can use API key OR OAuth). Families that only support a\nsingle auth flow simply omit this field from their config struct.",
      "oneOf": [
        {
          "const": "api_key",
          "description": "Standard API key authentication via the `api_key` field.",
          "type": "string"
        },
        {
          "const": "o_auth",
          "description": "OAuth flow — credential resolution defers to the family runtime impl\n(typically reading a vendor-specific token cache or env var).",
          "type": "string"
        }
      ]
    },
    "AutoClassifyConfig": {
      "properties": {
        "complex_hint": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "cost_optimized_hint": {
          "default": "cost-optimized",
          "type": "string"
        },
        "simple_hint": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "standard_hint": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "AutonomyLevel": {
      "description": "The agent's autonomy level, ordered from least to most autonomous.",
      "oneOf": [
        {
          "const": "readonly",
          "description": "Read-only: can observe but not act",
          "type": "string"
        },
        {
          "const": "supervised",
          "description": "Supervised: acts but requires approval for risky operations",
          "type": "string"
        },
        {
          "const": "full",
          "description": "Full: autonomous execution within policy bounds",
          "type": "string"
        }
      ]
    },
    "AvianModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "AzureModelProviderConfig": {
      "description": "Azure OpenAI model model_provider config. Carries the Azure-specific connection\nfields (`resource`, `deployment`, `api_version`) — the URI template\nsubstitutes `{resource}` and `{deployment}` at runtime. Operators can\nstill override the entire endpoint via `base.uri`.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "api_version": {
          "description": "Azure API version string (e.g. `2024-10-21`).",
          "type": [
            "string",
            "null"
          ]
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "deployment": {
          "description": "Azure deployment name: the deployment created in Azure AI Studio.",
          "type": [
            "string",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "resource": {
          "description": "Azure resource name (the `<resource>` part of `<resource>.openai.azure.com`).",
          "type": [
            "string",
            "null"
          ]
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "BackupConfig": {
      "description": "Backup tool configuration (`[backup]` section).",
      "properties": {
        "compress": {
          "default": true,
          "description": "Compress backup archives.",
          "type": "boolean"
        },
        "destination_dir": {
          "default": "state/backups",
          "description": "Output directory for backup archives (relative to workspace root).",
          "type": "string"
        },
        "enabled": {
          "default": true,
          "description": "Enable the `backup` tool.",
          "type": "boolean"
        },
        "encrypt": {
          "default": false,
          "description": "Encrypt backup archives (requires a configured secret store key).",
          "type": "boolean"
        },
        "include_dirs": {
          "default": [
            "config",
            "memory",
            "audit",
            "knowledge"
          ],
          "description": "Workspace subdirectories to include in backups.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_keep": {
          "default": 10,
          "description": "Maximum number of backups to keep (oldest are pruned).",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "schedule_cron": {
          "default": null,
          "description": "Optional cron expression for scheduled automatic backups.",
          "type": [
            "string",
            "null"
          ]
        },
        "schedule_timezone": {
          "default": null,
          "description": "IANA timezone for `schedule_cron`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "BaichuanModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "BasetenModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "BedrockModelProviderConfig": {
      "description": "AWS Bedrock model model_provider config. Carries the AWS region (the URI\ntemplate substitutes `{region}` from this field). Bedrock auth is\nSigV4 — credentials come from the standard AWS credential chain\n(env vars, instance metadata, profile), not from `api_key`.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "region": {
          "description": "AWS region for the Bedrock endpoint (e.g. `us-east-1`, `eu-west-1`).",
          "type": [
            "string",
            "null"
          ]
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "BlueskyConfig": {
      "description": "Bluesky channel configuration (AT Protocol).",
      "properties": {
        "app_password": {
          "description": "App-specific password (from Bluesky settings).",
          "type": "string",
          "x-secret": true
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "handle": {
          "description": "Bluesky handle (e.g. `\"mybot.bsky.social\"`).",
          "type": "string"
        }
      },
      "required": [
        "handle",
        "app_password"
      ],
      "type": "object"
    },
    "BrowserComputerUseConfig": {
      "description": "Computer-use sidecar configuration (`[browser.computer_use]` section).\n\nDelegates OS-level mouse, keyboard, and screenshot actions to a local sidecar.",
      "properties": {
        "allow_remote_endpoint": {
          "default": false,
          "description": "Allow remote/public endpoint for computer-use sidecar (default: false)",
          "type": "boolean"
        },
        "api_key": {
          "default": null,
          "description": "Optional bearer token for computer-use sidecar",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "endpoint": {
          "default": "http://127.0.0.1:8787/v1/actions",
          "description": "Sidecar endpoint for computer-use actions (OS-level mouse/keyboard/screenshot)",
          "type": "string"
        },
        "max_coordinate_x": {
          "default": null,
          "description": "Optional X-axis boundary for coordinate-based actions",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "max_coordinate_y": {
          "default": null,
          "description": "Optional Y-axis boundary for coordinate-based actions",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "timeout_ms": {
          "default": 15000,
          "description": "Per-action request timeout in milliseconds",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "window_allowlist": {
          "default": [],
          "description": "Optional window title/process allowlist forwarded to sidecar policy",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "BrowserConfig": {
      "description": "Browser automation configuration (`[browser]` section).\n\nControls the `browser_open` tool and browser automation backends.",
      "properties": {
        "allowed_domains": {
          "default": [
            "*"
          ],
          "description": "Allowed domains for `browser_open` (exact or subdomain match)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "allowed_private_hosts": {
          "default": [],
          "description": "Private/internal hosts allowed to bypass SSRF protection.\nExact and subdomain matches are supported; `[\"*\"]` permits **all** private/local\nhosts (RFC 1918, loopback, link-local, `.local`). Default: empty (deny).\nListed hosts also bypass `allowed_domains`. Both the `browser` tool and\n`browser_open` accept `http://` for listed hosts — internal services\nfrequently lack a public TLS cert.\nWarning: `[\"*\"]` also reaches link-local addresses, including the cloud metadata\nendpoint (`169.254.169.254`) — list specific hosts unless you accept that exposure.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "backend": {
          "default": "agent_browser",
          "description": "Browser automation backend: \"agent_browser\" | \"rust_native\" | \"computer_use\" | \"auto\"",
          "type": "string"
        },
        "computer_use": {
          "$ref": "#/$defs/BrowserComputerUseConfig",
          "default": {
            "allow_remote_endpoint": false,
            "api_key": null,
            "endpoint": "http://127.0.0.1:8787/v1/actions",
            "max_coordinate_x": null,
            "max_coordinate_y": null,
            "timeout_ms": 15000,
            "window_allowlist": []
          },
          "description": "Computer-use sidecar configuration"
        },
        "enabled": {
          "default": true,
          "description": "Enable `browser_open` tool (opens URLs in the system browser without scraping)",
          "type": "boolean"
        },
        "headed": {
          "default": null,
          "description": "Show browser window for agent_browser backend. When unset, inherits AGENT_BROWSER_HEADED.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "native_chrome_path": {
          "default": null,
          "description": "Optional Chrome/Chromium executable path for rust-native backend",
          "type": [
            "string",
            "null"
          ]
        },
        "native_headless": {
          "default": true,
          "description": "Headless mode for rust-native backend",
          "type": "boolean"
        },
        "native_webdriver_url": {
          "default": "http://127.0.0.1:9515",
          "description": "WebDriver endpoint URL for rust-native backend (e.g. `http://127.0.0.1:9515`)",
          "type": "string"
        },
        "session_name": {
          "default": null,
          "description": "Browser session name (for agent-browser automation)",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "BrowserDelegateConfig": {
      "properties": {
        "allowed_domains": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "blocked_domains": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "chrome_profile_dir": {
          "default": "",
          "type": "string"
        },
        "cli_binary": {
          "default": "claude",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "type": "boolean"
        },
        "task_timeout_secs": {
          "default": 120,
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "BuiltinHooksConfig": {
      "properties": {
        "command_logger": {
          "default": false,
          "description": "Enable the command-logger hook (logs tool calls for auditing).",
          "type": "boolean"
        },
        "webhook_audit": {
          "$ref": "#/$defs/WebhookAuditConfig",
          "default": {
            "enabled": false,
            "include_args": false,
            "max_args_bytes": 4096,
            "tool_patterns": [],
            "url": ""
          },
          "description": "Configuration for the webhook-audit hook.\n\nWhen enabled, POSTs a JSON payload to `url` for every tool invocation\nthat matches one of `tool_patterns`."
        }
      },
      "type": "object"
    },
    "CerebrasModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "ChannelPrecheckConfig": {
      "description": "Per-channel reply-intent precheck configuration.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "When false, the precheck is skipped entirely for this agent and every\naccepted channel message triggers the full agent loop. Default: `true`.",
          "type": "boolean"
        },
        "timeout_secs": {
          "default": 5,
          "description": "Hard ceiling (seconds) on the precheck LLM call. On timeout the\nprecheck fails open to REPLY. Default: `5`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "ChannelRef": {
      "description": "Reference to a configured `[channels.<type>.<alias>]` entry.\nEmpty value means \"no preference\" (opt-out). Non-empty values must\nresolve to a configured alias; `Config::validate()` enforces this.",
      "type": "string"
    },
    "ChannelsConfig": {
      "description": "Top-level channel configurations (`[channels]` section).\n\neach channel type is a keyed table of named instances (aliases).\n`[channels.telegram.default]` is the conventional single-instance key.\nAccess via `config.channels.telegram.get(\"default\")`.",
      "properties": {
        "ack_reactions": {
          "default": true,
          "description": "Whether to add acknowledgement reactions (👀 on receipt, ✅/⚠️ on\ncompletion) to incoming channel messages. Default: `true`.",
          "type": "boolean"
        },
        "amqp": {
          "additionalProperties": {
            "$ref": "#/$defs/AmqpConfig"
          },
          "description": "AMQP channel instances (`[channels.amqp.<alias>]`).",
          "type": "object"
        },
        "bluesky": {
          "additionalProperties": {
            "$ref": "#/$defs/BlueskyConfig"
          },
          "description": "Bluesky channel instances (`[channels.bluesky.<alias>]`).",
          "type": "object"
        },
        "clawdtalk": {
          "additionalProperties": {
            "$ref": "#/$defs/ClawdTalkConfig"
          },
          "description": "ClawdTalk voice channel instances (`[channels.clawdtalk.<alias>]`).",
          "type": "object"
        },
        "cli": {
          "default": true,
          "description": "Enable the CLI interactive channel. Default: `true`.",
          "type": "boolean"
        },
        "debounce_ms": {
          "default": 0,
          "description": "Inbound message debounce window in milliseconds. When a sender fires\nmultiple messages within this window, they are accumulated and dispatched\nas a single concatenated message. `0` disables debouncing. Default: `0`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "dingtalk": {
          "additionalProperties": {
            "$ref": "#/$defs/DingTalkConfig"
          },
          "description": "DingTalk channel instances (`[channels.dingtalk.<alias>]`).",
          "type": "object"
        },
        "discord": {
          "additionalProperties": {
            "$ref": "#/$defs/DiscordConfig"
          },
          "description": "Discord bot channel instances (`[channels.discord.<alias>]`).",
          "type": "object"
        },
        "email": {
          "additionalProperties": {
            "$ref": "#/$defs/EmailConfig"
          },
          "description": "Email channel instances (`[channels.email.<alias>]`).",
          "type": "object"
        },
        "filesystem": {
          "additionalProperties": {
            "$ref": "#/$defs/FilesystemConfig"
          },
          "description": "Filesystem SOP listener instances (`[channels.filesystem.<alias>]`).",
          "type": "object"
        },
        "git": {
          "additionalProperties": {
            "$ref": "#/$defs/GitConfig"
          },
          "description": "Git-forge channel instances (`[channels.git.<alias>]`). GitHub is\nthe first provider; the `provider` field selects the forge.",
          "type": "object"
        },
        "gmail_push": {
          "additionalProperties": {
            "$ref": "#/$defs/GmailPushConfig"
          },
          "description": "Gmail Pub/Sub push notification channel instances (`[channels.gmail_push.<alias>]`).",
          "type": "object"
        },
        "imessage": {
          "additionalProperties": {
            "$ref": "#/$defs/IMessageConfig"
          },
          "description": "iMessage channel instances (`[channels.imessage.<alias>]`, macOS only).",
          "type": "object"
        },
        "irc": {
          "additionalProperties": {
            "$ref": "#/$defs/IrcConfig"
          },
          "description": "IRC channel instances (`[channels.irc.<alias>]`).",
          "type": "object"
        },
        "lark": {
          "additionalProperties": {
            "$ref": "#/$defs/LarkConfig"
          },
          "description": "Lark channel instances (`[channels.lark.<alias>]`).",
          "type": "object"
        },
        "line": {
          "additionalProperties": {
            "$ref": "#/$defs/LineConfig"
          },
          "description": "LINE Messaging API channel instances (`[channels.line.<alias>]`).",
          "type": "object"
        },
        "linq": {
          "additionalProperties": {
            "$ref": "#/$defs/LinqConfig"
          },
          "description": "Linq Partner API channel instances (`[channels.linq.<alias>]`).",
          "type": "object"
        },
        "matrix": {
          "additionalProperties": {
            "$ref": "#/$defs/MatrixConfig"
          },
          "description": "Matrix channel instances (`[channels.matrix.<alias>]`).",
          "type": "object"
        },
        "mattermost": {
          "additionalProperties": {
            "$ref": "#/$defs/MattermostConfig"
          },
          "description": "Mattermost bot channel instances (`[channels.mattermost.<alias>]`).",
          "type": "object"
        },
        "max_concurrent_per_channel": {
          "default": 4,
          "description": "Per-channel multiplier for the global channel message in-flight budget.\nRuntime multiplies this value by the configured channel count, then\napplies its global minimum and maximum bounds to one shared dispatcher\nsemaphore. Default: `4`.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "message_timeout_secs": {
          "default": 300,
          "description": "Base timeout in seconds for processing a single channel message (LLM + tools).\nRuntime uses this as a per-turn budget that scales with tool-loop depth\n(up to 4x, capped) so one slow/retried model call does not consume the\nentire conversation budget.\nDefault: 300s for on-device LLMs (Ollama) which are slower than cloud APIs.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "mochat": {
          "additionalProperties": {
            "$ref": "#/$defs/MochatConfig"
          },
          "description": "Mochat customer service channel instances (`[channels.mochat.<alias>]`).",
          "type": "object"
        },
        "mqtt": {
          "additionalProperties": {
            "$ref": "#/$defs/MqttConfig"
          },
          "description": "MQTT channel instances (`[channels.mqtt.<alias>]`).",
          "type": "object"
        },
        "nextcloud_talk": {
          "additionalProperties": {
            "$ref": "#/$defs/NextcloudTalkConfig"
          },
          "description": "Nextcloud Talk bot channel instances (`[channels.nextcloud_talk.<alias>]`).",
          "type": "object"
        },
        "nostr": {
          "additionalProperties": {
            "$ref": "#/$defs/NostrConfig"
          },
          "type": "object"
        },
        "qq": {
          "additionalProperties": {
            "$ref": "#/$defs/QQConfig"
          },
          "description": "QQ Official Bot channel instances (`[channels.qq.<alias>]`).",
          "type": "object"
        },
        "reddit": {
          "additionalProperties": {
            "$ref": "#/$defs/RedditConfig"
          },
          "description": "Reddit channel instances (`[channels.reddit.<alias>]`).",
          "type": "object"
        },
        "session_backend": {
          "default": "sqlite",
          "description": "Session persistence backend: `\"jsonl\"` (legacy) or `\"sqlite\"` (new default).\nSQLite provides FTS5 search, metadata tracking, and TTL cleanup.",
          "type": "string"
        },
        "session_persistence": {
          "default": true,
          "description": "Persist channel conversation history to JSONL files so sessions survive\ndaemon restarts. Files are stored in `{workspace}/sessions/`. Default: `true`.",
          "type": "boolean"
        },
        "session_ttl_hours": {
          "default": 0,
          "description": "Auto-archive stale sessions older than this many hours. `0` disables. Default: `0`.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "show_tool_calls": {
          "default": false,
          "description": "Whether to send tool-call notification messages (e.g. `🔧 web_search_tool: …`)\nto channel users. When `false`, tool calls are still logged server-side but\nnot forwarded as individual channel messages. Default: `false`.",
          "type": "boolean"
        },
        "signal": {
          "additionalProperties": {
            "$ref": "#/$defs/SignalConfig"
          },
          "description": "Signal channel instances (`[channels.signal.<alias>]`).",
          "type": "object"
        },
        "slack": {
          "additionalProperties": {
            "$ref": "#/$defs/SlackConfig"
          },
          "description": "Slack bot channel instances (`[channels.slack.<alias>]`).",
          "type": "object"
        },
        "telegram": {
          "additionalProperties": {
            "$ref": "#/$defs/TelegramConfig"
          },
          "description": "Telegram bot channel instances (`[channels.telegram.<alias>]`).",
          "type": "object"
        },
        "twitch": {
          "additionalProperties": {
            "$ref": "#/$defs/TwitchConfig"
          },
          "description": "Twitch chat channel instances (`[channels.twitch.<alias>]`).",
          "type": "object"
        },
        "twitter": {
          "additionalProperties": {
            "$ref": "#/$defs/TwitterConfig"
          },
          "description": "X/Twitter channel instances (`[channels.twitter.<alias>]`).",
          "type": "object"
        },
        "voice_call": {
          "additionalProperties": {
            "$ref": "#/$defs/VoiceCallConfig"
          },
          "description": "Voice call channel instances (`[channels.voice_call.<alias>]`).",
          "type": "object"
        },
        "voice_duplex": {
          "additionalProperties": {
            "$ref": "#/$defs/VoiceDuplexConfig"
          },
          "description": "Voice duplex instances (`[channels.voice_duplex.<alias>]`).",
          "type": "object"
        },
        "voice_wake": {
          "additionalProperties": {
            "$ref": "#/$defs/VoiceWakeConfig"
          },
          "description": "Voice wake word detection channel instances (`[channels.voice_wake.<alias>]`).",
          "type": "object"
        },
        "wati": {
          "additionalProperties": {
            "$ref": "#/$defs/WatiConfig"
          },
          "description": "WATI WhatsApp Business API channel instances (`[channels.wati.<alias>]`).",
          "type": "object"
        },
        "webhook": {
          "additionalProperties": {
            "$ref": "#/$defs/WebhookConfig"
          },
          "description": "Webhook channel instances (`[channels.webhook.<alias>]`).",
          "type": "object"
        },
        "wechat": {
          "additionalProperties": {
            "$ref": "#/$defs/WeChatConfig"
          },
          "description": "WeChat personal iLink Bot channel instances (`[channels.wechat.<alias>]`).",
          "type": "object"
        },
        "wecom": {
          "additionalProperties": {
            "$ref": "#/$defs/WeComConfig"
          },
          "description": "WeCom (WeChat Enterprise) Bot Webhook channel instances (`[channels.wecom.<alias>]`).",
          "type": "object"
        },
        "wecom_ws": {
          "additionalProperties": {
            "$ref": "#/$defs/WeComWsConfig"
          },
          "description": "WeCom AI Bot WebSocket channel instances (`[channels.wecom_ws.<alias>]`).",
          "type": "object"
        },
        "whatsapp": {
          "additionalProperties": {
            "$ref": "#/$defs/WhatsAppConfig"
          },
          "description": "WhatsApp channel instances (`[channels.whatsapp.<alias>]`).",
          "type": "object"
        }
      },
      "type": "object"
    },
    "ClassificationRule": {
      "description": "A single classification rule mapping message patterns to a model hint.",
      "properties": {
        "hint": {
          "description": "Must match a `[[model_routes]]` hint value.",
          "type": "string"
        },
        "keywords": {
          "default": [],
          "description": "Case-insensitive substring matches.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_length": {
          "default": null,
          "description": "Only match if message length <= N chars.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "min_length": {
          "default": null,
          "description": "Only match if message length >= N chars.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "patterns": {
          "default": [],
          "description": "Case-sensitive literal matches (for \"```\", \"fn \", etc.).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "priority": {
          "default": 0,
          "description": "Higher priority rules are checked first.",
          "format": "int32",
          "type": "integer"
        }
      },
      "required": [
        "hint"
      ],
      "type": "object"
    },
    "ClaudeCodeConfig": {
      "description": "Claude Code CLI tool configuration (`[claude_code]` section).\n\nDelegates coding tasks to the `claude -p` CLI. Authentication uses the\nbinary's own OAuth session (Max subscription) by default — no API key\nneeded unless `env_passthrough` includes `ANTHROPIC_API_KEY`.",
      "properties": {
        "allowed_tools": {
          "default": [
            "Read",
            "Edit",
            "Bash",
            "Write"
          ],
          "description": "Claude Code tools the subprocess is allowed to use",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "default": false,
          "description": "Enable the `claude_code` tool",
          "type": "boolean"
        },
        "env_passthrough": {
          "default": [],
          "description": "Extra env vars passed to the claude subprocess (e.g. ANTHROPIC_API_KEY for API-key billing)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_output_bytes": {
          "default": 2097152,
          "description": "Maximum output size in bytes (2MB default)",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "system_prompt": {
          "default": null,
          "description": "Optional system prompt appended to Claude Code invocations",
          "type": [
            "string",
            "null"
          ]
        },
        "timeout_secs": {
          "default": 600,
          "description": "Maximum execution time in seconds (coding tasks can be long)",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "ClaudeCodeRunnerConfig": {
      "description": "Claude Code task runner configuration (`[claude_code_runner]` section).\n\nSpawns Claude Code in a tmux session with HTTP hooks that POST tool\nexecution events back to ZeroClaw's gateway, updating a Slack message\nin-place with progress plus an SSH handoff link.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable the `claude_code_runner` tool",
          "type": "boolean"
        },
        "session_ttl": {
          "default": 3600,
          "description": "Session time-to-live in seconds before auto-cleanup (default: 3600)",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "ssh_host": {
          "default": null,
          "description": "SSH host for session handoff links (e.g. \"myhost.example.com\")",
          "type": [
            "string",
            "null"
          ]
        },
        "tmux_prefix": {
          "default": "zc-claude-",
          "description": "Prefix for tmux session names (default: \"zc-claude-\")",
          "type": "string"
        }
      },
      "type": "object"
    },
    "ClawdTalkConfig": {
      "properties": {
        "allowed_destinations": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "api_key": {
          "type": "string"
        },
        "connection_id": {
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "from_number": {
          "type": "string"
        },
        "webhook_secret": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "api_key",
        "connection_id",
        "from_number"
      ],
      "type": "object"
    },
    "CloudOpsConfig": {
      "description": "Controls the read-only cloud transformation analysis tools:\nIaC review, migration assessment, cost analysis, and architecture review.",
      "properties": {
        "cost_threshold_monthly_usd": {
          "default": 100.0,
          "description": "Monthly USD threshold to flag cost items. Default: 100.0.",
          "format": "double",
          "type": "number"
        },
        "default_cloud": {
          "default": "aws",
          "description": "Default cloud model_provider for analysis context. Default: \"aws\".",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Enable cloud operations tools. Default: false.",
          "type": "boolean"
        },
        "iac_tools": {
          "default": [
            "terraform"
          ],
          "description": "Supported IaC tools for review. Default: \\[`terraform`\\].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "supported_clouds": {
          "default": [
            "aws",
            "azure",
            "gcp"
          ],
          "description": "Supported cloud model_providers. Default: [`aws`, `azure`, `gcp`].",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "well_architected_frameworks": {
          "default": [
            "aws-waf"
          ],
          "description": "Well-Architected Frameworks to check against. Default: \\[`aws-waf`\\].",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "CloudflareModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "CloudflareTunnelConfig": {
      "properties": {
        "token": {
          "default": "",
          "description": "Cloudflare Tunnel token (from Zero Trust dashboard)",
          "type": "string",
          "x-secret": true
        }
      },
      "type": "object"
    },
    "CodexCliConfig": {
      "description": "Codex CLI tool configuration (`[codex_cli]` section).\n\nDelegates coding tasks to the `codex exec` CLI. Authentication uses the\nbinary's own session by default — no API key needed unless\n`env_passthrough` includes `OPENAI_API_KEY`.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable the `codex_cli` tool",
          "type": "boolean"
        },
        "env_passthrough": {
          "default": [],
          "description": "Extra env vars passed to the codex subprocess (e.g. OPENAI_API_KEY)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "extra_args": {
          "default": [],
          "description": "Extra CLI arguments appended to `codex exec` before the prompt.\n\nValues come from operator-controlled config (same trust level as\n`env_passthrough`) and are not validated — the operator is responsible\nfor understanding the implications of flags passed here.\n\n**Warning:** `--sandbox=danger-full-access` disables Codex's bubblewrap\nisolation; only use in environments where the container itself provides\nisolation (e.g. Kubernetes pods with restricted PSS).\n\nExample: `[\"--sandbox=danger-full-access\", \"--skip-git-repo-check\"]`",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_output_bytes": {
          "default": 2097152,
          "description": "Maximum output size in bytes (2MB default)",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "timeout_secs": {
          "default": 600,
          "description": "Maximum execution time in seconds (coding tasks can be long)",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "CohereModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "ComposioConfig": {
      "description": "Composio managed OAuth tools integration (`[composio]` section).\n\nProvides access to 1000+ OAuth-connected tools via the Composio platform.",
      "properties": {
        "api_key": {
          "default": null,
          "description": "Composio API key (stored encrypted when secrets.encrypt = true)",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "enabled": {
          "default": false,
          "description": "Enable Composio integration for 1000+ OAuth tools",
          "type": "boolean"
        },
        "entity_id": {
          "default": "default",
          "description": "Default entity ID for multi-user setups",
          "type": "string"
        }
      },
      "type": "object"
    },
    "ContextCompressionConfig": {
      "properties": {
        "enabled": {
          "default": false,
          "description": "The runtime context compressor was removed; no runtime execution path\nconsumes this flag, so setting it to `true` currently has no effect.\nDefaults to `false` to match actual runtime behavior;\n`Config::collect_warnings` reads an explicit `true` only to report\n`context_compression_unsupported`.",
          "type": "boolean"
        },
        "identifier_policy": {
          "default": "strict",
          "type": "string"
        },
        "max_passes": {
          "default": 3,
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "protect_first_n": {
          "default": 3,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "protect_last_n": {
          "default": 4,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "source_max_chars": {
          "default": 50000,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "summary_max_chars": {
          "default": 4000,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "summary_model": {
          "default": null,
          "description": "DEPRECATED bare model id retained as a compatibility fallback.",
          "type": [
            "string",
            "null"
          ]
        },
        "summary_provider": {
          "$ref": "#/$defs/ModelProviderRef",
          "default": "",
          "description": "Summarizer provider as a `<type>.<alias>` reference into `providers.models`."
        },
        "threshold_ratio": {
          "default": 0.5,
          "format": "double",
          "type": "number"
        },
        "timeout_secs": {
          "default": 60,
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "tool_result_retrim_chars": {
          "default": 2000,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "tool_result_trim_exempt": {
          "default": [],
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "ConversationalAiConfig": {
      "description": "Conversational AI agent builder configuration (`[conversational_ai]` section).\n\n**Status: Reserved for future use.** This configuration is parsed but not yet\nconsumed by the runtime. Setting `enabled = true` will produce a startup warning.",
      "properties": {
        "analytics_enabled": {
          "default": false,
          "description": "Enable conversation analytics tracking. Default: false (privacy-by-default).",
          "type": "boolean"
        },
        "auto_detect_language": {
          "default": true,
          "description": "Automatically detect user language from message content. Default: true.",
          "type": "boolean"
        },
        "conversation_timeout_secs": {
          "default": 1800,
          "description": "Conversation timeout in seconds (inactivity). Default: 1800.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "default_language": {
          "default": "en",
          "description": "Default language for conversations (BCP-47 tag). Default: \"en\".",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Enable conversational AI features. Default: false.",
          "type": "boolean"
        },
        "escalation_confidence_threshold": {
          "default": 0.3,
          "description": "Intent confidence below this threshold triggers escalation. Default: 0.3.",
          "format": "double",
          "type": "number"
        },
        "knowledge_base_tool": {
          "default": null,
          "description": "Optional tool name for RAG-based knowledge base lookup during conversations.",
          "type": [
            "string",
            "null"
          ]
        },
        "max_conversation_turns": {
          "default": 50,
          "description": "Maximum conversation turns before auto-ending. Default: 50.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "supported_languages": {
          "default": [
            "en",
            "de",
            "fr",
            "it"
          ],
          "description": "Supported languages for conversations. Default: [`en`, `de`, `fr`, `it`].",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "CopilotModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "CostConfig": {
      "description": "Cost tracking and budget enforcement configuration (`[cost]` section).",
      "properties": {
        "allow_override": {
          "default": false,
          "description": "Allow requests to exceed budget with --override flag (default: false)",
          "type": "boolean"
        },
        "daily_limit_usd": {
          "default": 10.0,
          "description": "Daily spending limit in USD (default: 10.00)",
          "format": "double",
          "type": "number"
        },
        "enabled": {
          "default": true,
          "description": "Enable cost tracking (default: true)",
          "type": "boolean"
        },
        "enforcement": {
          "$ref": "#/$defs/CostEnforcementConfig",
          "default": {
            "mode": "warn",
            "reserve_percent": 10,
            "route_down_model": null
          },
          "description": "Cost enforcement behavior when budget limits are approached or exceeded."
        },
        "monthly_limit_usd": {
          "default": 100.0,
          "description": "Monthly spending limit in USD (default: 100.00)",
          "format": "double",
          "type": "number"
        },
        "rates": {
          "$ref": "#/$defs/CostRatesConfig",
          "default": {
            "providers": {
              "models": {},
              "transcription": {},
              "tts": {}
            },
            "tools": {}
          },
          "description": "Operator-managed rate sheet at `[cost.rates.*]`. Sections mirror\nthe `[providers.*]` dotted-path exactly with the trailing `alias`\nsegment replaced by the resource the rate applies to (model id,\ntool name, …). Layout:\n\n```toml\n[cost.rates.providers.models.anthropic.\"claude-opus-4-7\"]\ninput_per_mtok = 15.0\noutput_per_mtok = 75.0\ncached_input_per_mtok = 1.5\n\n[cost.rates.providers.tts.openai.\"tts-1-hd\"]\nper_mchar = 30.0\n\n[cost.rates.providers.transcription.openai.whisper-1]\nper_minute = 0.006\n\n[cost.rates.tools.web_search]\nper_call = 0.005\n```"
        },
        "track_per_agent": {
          "default": true,
          "description": "Stamp each recorded cost entry with the originating agent alias so\n`/api/cost?agent=<alias>` and CLI rollups can attribute spend to a\nspecific agent. Disable on high-volume deployments if the extra\nHashMap aggregation shows up in profiles (default: true).",
          "type": "boolean"
        },
        "warn_at_percent": {
          "default": 80,
          "description": "Warn when spending reaches this percentage of limit (default: 80)",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "CostEnforcementConfig": {
      "description": "Configuration for cost enforcement behavior when budget limits are reached.",
      "properties": {
        "mode": {
          "default": "warn",
          "description": "Enforcement mode: \"warn\", \"block\", or \"route_down\".",
          "type": "string"
        },
        "reserve_percent": {
          "default": 10,
          "description": "Reserve this percentage of budget for critical operations.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "route_down_model": {
          "default": null,
          "description": "Model hint to route to when budget is exceeded (used with \"route_down\" mode).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "CostRatesConfig": {
      "description": "`[cost.rates]` — top-level rate-sheet namespace. Mirrors the\n`[providers.*]` shape so each subsection here points at the same\nkind of resource its `[providers.*]` counterpart configures.",
      "properties": {
        "providers": {
          "$ref": "#/$defs/ProviderCostRates",
          "default": {
            "models": {},
            "transcription": {},
            "tts": {}
          },
          "description": "`[cost.rates.providers.*]` — rates for everything under\n`[providers.*]` (models, TTS, transcription, …)."
        },
        "tools": {
          "additionalProperties": {
            "$ref": "#/$defs/ToolCostRates"
          },
          "default": {},
          "description": "`[cost.rates.tools.<name>]` — per-call rates for tools that\nhit paid APIs. Keyed by the tool's registered name.",
          "type": "object"
        }
      },
      "type": "object"
    },
    "CronJobDecl": {
      "description": "A declarative cron job definition (`[cron.<alias>]`).\n\nStored alias-keyed on `Config.cron`. The map key serves as the stable job id.\nSynced into the database at scheduler startup with `source = \"declarative\"`,\ndistinguishing them from jobs created imperatively via CLI or API.\nDeclarative config takes precedence on each sync: if the config changes,\nthe DB is updated to match. Imperative jobs are never deleted by sync.",
      "properties": {
        "allowed_tools": {
          "default": null,
          "description": "Optional allowlist of tool names for agent jobs. When omitted, scheduler\ndefaults may still exclude scheduler mutation tools for cron agent jobs.",
          "items": {
            "type": "string"
          },
          "type": [
            "array",
            "null"
          ]
        },
        "command": {
          "default": null,
          "description": "Shell command to run (required when `job_type = \"shell\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "delivery": {
          "anyOf": [
            {
              "$ref": "#/$defs/DeliveryConfigDecl"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Delivery configuration."
        },
        "enabled": {
          "default": true,
          "description": "Whether the job is enabled. Default: `true`.",
          "type": "boolean"
        },
        "job_type": {
          "default": "shell",
          "description": "Job type: `\"shell\"` (default) or `\"agent\"`.",
          "type": "string"
        },
        "model": {
          "default": null,
          "description": "Model override for agent jobs.",
          "type": [
            "string",
            "null"
          ]
        },
        "name": {
          "default": null,
          "description": "Human-readable name.",
          "type": [
            "string",
            "null"
          ]
        },
        "prompt": {
          "default": null,
          "description": "Agent prompt (required when `job_type = \"agent\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "schedule": {
          "$ref": "#/$defs/CronScheduleDecl",
          "default": {
            "expr": "",
            "kind": "cron",
            "tz": null
          },
          "description": "Schedule for the job."
        },
        "session_target": {
          "default": null,
          "description": "Session target: `\"isolated\"` (default) or `\"main\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "shell_output_format": {
          "$ref": "#/$defs/CronShellOutputFormat",
          "default": "wrapped",
          "description": "Output format for shell jobs: `\"wrapped\"` (default) or `\"raw\"`.\n\n- `\"wrapped\"` (default): returns stdout and stderr wrapped in a\n  `status=... / stdout: / stderr:` envelope.\n- `\"raw\"`: returns trimmed stdout when the process exits zero. A\n  non-zero process exit still gets the wrapped status/stdout/stderr\n  envelope for diagnosis. Failures that never reach a process exit —\n  a security-policy denial, a shell setup/spawn failure, or a\n  timeout — return a plain error string in either format."
        },
        "uses_memory": {
          "default": true,
          "description": "Whether to recall and inject memory context before this agent job runs.\nDefaults to `true`; set to `false` for stateless digest jobs.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "CronScheduleDecl": {
      "description": "Schedule variant for declarative cron jobs.",
      "oneOf": [
        {
          "description": "Classic cron expression.",
          "properties": {
            "expr": {
              "type": "string"
            },
            "kind": {
              "const": "cron",
              "type": "string"
            },
            "tz": {
              "default": null,
              "type": [
                "string",
                "null"
              ]
            }
          },
          "required": [
            "kind",
            "expr"
          ],
          "type": "object"
        },
        {
          "description": "Interval in milliseconds.",
          "properties": {
            "every_ms": {
              "format": "uint64",
              "minimum": 0,
              "type": "integer"
            },
            "kind": {
              "const": "every",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "every_ms"
          ],
          "type": "object"
        },
        {
          "description": "One-shot at an RFC 3339 timestamp.",
          "properties": {
            "at": {
              "type": "string"
            },
            "kind": {
              "const": "at",
              "type": "string"
            }
          },
          "required": [
            "kind",
            "at"
          ],
          "type": "object"
        }
      ]
    },
    "CronShellOutputFormat": {
      "description": "Output format for shell cron job stdout.",
      "oneOf": [
        {
          "const": "wrapped",
          "description": "Wrapped format: `status=...\\nstdout:\\n...\\nstderr:\\n...` (default).",
          "type": "string"
        },
        {
          "const": "raw",
          "description": "Raw stdout on a zero process exit; wrapped status/stdout/stderr on a\nnon-zero exit; a plain error string for failures that never reach a\nprocess exit (security denial, spawn/setup failure, timeout).",
          "type": "string"
        }
      ]
    },
    "CustomModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "CustomTunnelConfig": {
      "properties": {
        "health_url": {
          "default": null,
          "description": "Optional URL to check tunnel health",
          "type": [
            "string",
            "null"
          ]
        },
        "start_command": {
          "default": "",
          "description": "Command template to start the tunnel. Use {port} and {host} placeholders.\nExample: \"bore local {port} --to bore.pub\"",
          "type": "string"
        },
        "url_pattern": {
          "default": null,
          "description": "Optional regex to extract public URL from command stdout",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "DataRetentionConfig": {
      "description": "Data retention and purge configuration (`[data_retention]` section).",
      "properties": {
        "categories": {
          "default": [],
          "description": "Limit retention enforcement to specific data categories (empty = all).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dry_run": {
          "default": false,
          "description": "Preview what would be deleted without actually removing anything.",
          "type": "boolean"
        },
        "enabled": {
          "default": false,
          "description": "Enable the `data_management` tool.",
          "type": "boolean"
        },
        "retention_days": {
          "default": 90,
          "description": "Days of data to retain before purge eligibility.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "DeepgramSttConfig": {
      "description": "Deepgram STT model_provider configuration (`[transcription.deepgram]`).",
      "properties": {
        "api_key": {
          "default": null,
          "description": "Deepgram API key.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "model": {
          "default": "nova-2",
          "description": "Deepgram model name (default: \"nova-2\").",
          "type": "string"
        }
      },
      "type": "object"
    },
    "DeepgramTranscriptionProviderConfig": {
      "description": "Shared base for cloud transcription providers. Each cloud family\ncomposes this via `#[serde(flatten)] base: TranscriptionProviderConfig`.",
      "properties": {
        "api_key": {
          "default": null,
          "description": "API key for the transcription provider.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "initial_prompt": {
          "default": null,
          "description": "Whisper-style initial prompt to bias the model toward expected\nvocabulary (proper nouns, technical terms). Provider-specific support;\nsilently ignored where not applicable.",
          "type": [
            "string",
            "null"
          ]
        },
        "language": {
          "default": null,
          "description": "Optional language hint passed to the provider (ISO-639-1 like `\"en\"` /\n`\"ru\"`, or BCP-47 like `\"en-US\"` for Google). Most providers auto-detect\nwhen this is unset.",
          "type": [
            "string",
            "null"
          ]
        },
        "model": {
          "default": null,
          "description": "Deepgram model name (default: `\"nova-2\"`).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "DeepinfraModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "DeepmystModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "DeepseekModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "DelegateExecutionMode": {
      "description": "Execution semantics for one explicit delegate target.",
      "oneOf": [
        {
          "const": "bounded",
          "description": "Parent-bounded delegation: the target is reachable through the caller,\nshares caller budgets, and agentic tools are capped by the caller's\ntool envelope.",
          "type": "string"
        },
        {
          "const": "independent",
          "description": "Independent delegation: the target runs under its own configured\npolicy/tool envelope, like opening a new chat with that agent.",
          "type": "string"
        }
      ]
    },
    "DelegateTargetConfig": {
      "description": "One explicit delegate target listed under `[agents.<alias>].delegates`.\n\nString entries deserialize as `{ agent = \"<string>\", mode = \"bounded\" }`\nfor concise manual editing. Serialization always emits the object form so\nthe config surface is a pure object array.",
      "properties": {
        "agent": {
          "description": "Target agent alias.",
          "type": "string"
        },
        "mode": {
          "$ref": "#/$defs/DelegateExecutionMode",
          "default": "bounded",
          "description": "Whether this target is parent-bounded or independent."
        }
      },
      "required": [
        "agent"
      ],
      "type": "object"
    },
    "DelegateToolConfig": {
      "description": "Global delegate tool configuration for default timeout values.",
      "properties": {
        "agentic_timeout_secs": {
          "default": 300,
          "description": "Default timeout in seconds for agentic sub-agent runs.\nCan be overridden per-agent in `[agents.<name>]` config.\nDefault: 300 seconds.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "timeout_secs": {
          "default": 120,
          "description": "Default timeout in seconds for non-agentic sub-agent model_provider calls.\nCan be overridden per-agent in `[agents.<name>]` config.\nDefault: 120 seconds.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "DelegationMode": {
      "description": "Delegation mode for a risk profile.",
      "oneOf": [
        {
          "const": "forbidden",
          "description": "No delegation permitted.",
          "type": "string"
        },
        {
          "const": "allow",
          "description": "Delegation permitted to the agents named in the allow-list.",
          "type": "string"
        }
      ]
    },
    "DelegationPolicy": {
      "description": "Risk-profile delegation policy for work sent to agents that share it.",
      "properties": {
        "mode": {
          "$ref": "#/$defs/DelegationMode",
          "default": "forbidden"
        }
      },
      "type": "object"
    },
    "DeliveryConfigDecl": {
      "description": "Delivery configuration for declarative cron jobs.",
      "properties": {
        "best_effort": {
          "default": true,
          "description": "Best-effort delivery. Default: `true`.",
          "type": "boolean"
        },
        "channel": {
          "default": null,
          "description": "Channel name (e.g. `\"telegram\"`, `\"discord\"`).",
          "type": [
            "string",
            "null"
          ]
        },
        "mode": {
          "default": "none",
          "description": "Delivery mode: `\"none\"` or `\"announce\"`.",
          "type": "string"
        },
        "thread_id": {
          "description": "Optional thread/conversation identifier carried into the outbound send.\nRequired by channels that route on a separate `thread_id` field (e.g.\nwebhook callbacks bridging into agent-chat platforms).",
          "type": [
            "string",
            "null"
          ]
        },
        "to": {
          "default": null,
          "description": "Target/recipient identifier.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "DingTalkConfig": {
      "description": "DingTalk configuration for Stream Mode messaging",
      "properties": {
        "client_id": {
          "description": "Client ID (AppKey) from DingTalk developer console",
          "type": "string"
        },
        "client_secret": {
          "description": "Client Secret (AppSecret) from DingTalk developer console",
          "type": "string",
          "x-secret": true
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_url": {
          "default": null,
          "description": "Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "client_id",
        "client_secret"
      ],
      "type": "object"
    },
    "DiscordConfig": {
      "description": "Discord bot channel configuration.",
      "properties": {
        "approval_timeout_secs": {
          "default": 300,
          "description": "Seconds to wait for operator approval on `always_ask` tools before auto-denying.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "archive": {
          "default": false,
          "description": "When true, the channel opens a sidecar `discord.db` SQLite memory\nbackend, archives every non-bot message it sees, and registers the\n`discord_search` tool against it. Default: false. Folded in from\nthe legacy `[channels.discord-history]` block.",
          "type": "boolean"
        },
        "bot_token": {
          "default": "",
          "description": "Discord bot token (from Discord Developer Portal). `#[serde(default)]`\nfor the same reason as `TelegramConfig::bot_token`: a missing token\nmust deserialize as an empty string instead of salvage-dropping the\nalias; `validate_bot_token` still requires a real token once\n`enabled = true`.",
          "type": "string",
          "x-secret": true
        },
        "channel_ids": {
          "default": [],
          "description": "Channel IDs to watch. Empty = watch every channel the bot can see.\nUsed by the archive sidecar (when `archive = true`) and by the\nin-channel filter when set.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "draft_update_interval_ms": {
          "default": 1000,
          "description": "Minimum interval (ms) between draft message edits to avoid rate limits.\nOnly used when `stream_mode = \"partial\"`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "guild_ids": {
          "default": [],
          "description": "Guild (server) IDs to restrict the bot to. Empty = listen across all\nguilds the bot is invited to. Migrated from the legacy `guild_id`\nsingular field.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "intents_mask": {
          "default": null,
          "description": "Raw gateway intent mask override. When set, this exact value is sent\nin IDENTIFY instead of the derived mask: an operator escape hatch\nfor downstream deployments that consume gateway events through\ncustom builds or forks. The operator owns the consequences:\nprivileged bits still need their Developer Portal toggles, and\ndropping baseline bits silences message handling (a warning is\nlogged). Unset (default) = derive the mask from the channel's\nfeature config.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "interrupt_on_new_message": {
          "default": false,
          "description": "When true, a newer Discord message from the same sender in the same channel\ncancels the in-flight request and starts a fresh response with preserved history.",
          "type": "boolean"
        },
        "listen_to_bots": {
          "default": false,
          "description": "When true, process messages from other bots (not just humans).\nThe bot still ignores its own messages to prevent feedback loops.",
          "type": "boolean"
        },
        "mention_only": {
          "default": false,
          "description": "When true, only respond to messages that @-mention the bot.\nOther messages in the guild are silently ignored.",
          "type": "boolean"
        },
        "multi_message_delay_ms": {
          "default": 800,
          "description": "Delay (ms) between sending each message chunk in multi-message mode.\nOnly used when `stream_mode = \"multi_message\"`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "proxy_url": {
          "default": null,
          "description": "Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",
          "type": [
            "string",
            "null"
          ]
        },
        "reaction_notifications": {
          "$ref": "#/$defs/DiscordReactionScope",
          "default": "off",
          "description": "Which inbound reactions to record: `off` (default: reaction events\nare not even requested from the gateway), `own` (reactions to the\nbot's messages), or `all` (reactions to any message passing the\nchannel's filters). Recorded reactions are archived to `discord.db`\nwhen `archive` is enabled, and removed again when a user removes\ntheir reaction (bulk clears, remove-all / remove-emoji, are not\nyet swept). A set `intents_mask` overrides the derived mask: if it\nomits the reaction intents, nothing is recorded."
        },
        "reply_min_interval_secs": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "reply_queue_depth_max": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "slash_command_scope": {
          "$ref": "#/$defs/SlashCommandScope",
          "default": "global",
          "description": "Scope for registered slash commands: `global` (default, application-wide,\n~1h propagation) or `guild` (registered to each `guild_ids` entry,\ninstant). Only meaningful when `slash_commands = true`; `guild` with an\nempty `guild_ids` warns and falls back to global. Switching scope reaps\nowned commands from the now-inactive scope; note that *removing* a guild\nfrom `guild_ids` (without switching scope) does not reap that guild's\ncommands - remove the bot from the guild, or switch scope, to clear them."
        },
        "slash_commands": {
          "default": false,
          "description": "When true, register and serve Discord slash commands (e.g. `/ask`)\nover the Gateway WebSocket, in addition to message handling. Default\nfalse. (Prototype: currently registers a single `/ask <prompt>`.)",
          "type": "boolean"
        },
        "stall_timeout_secs": {
          "default": 0,
          "description": "Stall-watchdog timeout in seconds. When non-zero, the bot will abort\nand retry if no progress is made within this duration. 0 = disabled.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "stream_mode": {
          "$ref": "#/$defs/StreamMode",
          "default": "off",
          "description": "Streaming mode for progressive response delivery.\n`off` (default): single message. `partial`: editable draft updates.\n`multi_message`: split response into separate messages at paragraph boundaries."
        }
      },
      "type": "object"
    },
    "DiscordReactionScope": {
      "description": "Scope of inbound Discord reaction events the bot records.\n\nAnything other than `Off` adds the GUILD_MESSAGE_REACTIONS and\nDIRECT_MESSAGE_REACTIONS gateway intents (both unprivileged; no\nDeveloper Portal toggle needed) and archives matching reactions to the\nchannel's `discord.db` sidecar when `archive` is enabled.",
      "oneOf": [
        {
          "const": "off",
          "description": "Ignore inbound reaction events entirely (no reaction intents requested).",
          "type": "string"
        },
        {
          "const": "own",
          "description": "Record reactions to the bot's own messages only.",
          "type": "string"
        },
        {
          "const": "all",
          "description": "Record reactions to any message that passes the channel's filters.",
          "type": "string"
        }
      ]
    },
    "DockerRuntimeConfig": {
      "description": "Docker runtime configuration (`[runtime.docker]` section).",
      "properties": {
        "allowed_workspace_roots": {
          "default": [],
          "description": "Optional workspace root allowlist for Docker mount validation.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cpu_limit": {
          "default": 1.0,
          "description": "Optional CPU limit (`None` = no explicit limit).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "image": {
          "default": "alpine:3.20",
          "description": "Runtime image used to execute shell commands.",
          "type": "string"
        },
        "memory_limit_mb": {
          "default": 512,
          "description": "Optional memory limit in MB (`None` = no explicit limit).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "mount_workspace": {
          "default": true,
          "description": "Mount configured workspace into `/workspace`.",
          "type": "boolean"
        },
        "network": {
          "default": "none",
          "description": "Docker network mode (`none`, `bridge`, etc.).",
          "type": "string"
        },
        "read_only_rootfs": {
          "default": true,
          "description": "Mount root filesystem as read-only.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "DoubaoModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "EdgeTtsProviderConfig": {
      "description": "Per-instance TTS model_provider configuration (`[tts_providers.<type>.<alias>]`).\n\nMirrors `ModelProviderConfig` in shape — one struct holds the union of\nfields across backends. Only the fields relevant to the selected backend\n(determined by the outer `<type>` map key) are read at runtime; others\nare quietly ignored.",
      "properties": {
        "api_key": {
          "default": null,
          "description": "API key (openai, elevenlabs, google).",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "binary_path": {
          "default": null,
          "description": "Path to backend binary (edge-tts subprocess; piper local server).",
          "type": [
            "string",
            "null"
          ]
        },
        "language_code": {
          "default": null,
          "description": "Language code for google (e.g. `en-US`).",
          "type": [
            "string",
            "null"
          ]
        },
        "model": {
          "default": null,
          "description": "Model name. OpenAI uses this for `tts-1`/`tts-1-hd`; elevenlabs uses\nit as the model_id (e.g. `eleven_monolingual_v1`).",
          "type": [
            "string",
            "null"
          ]
        },
        "response_format": {
          "default": null,
          "description": "Audio response format sent to the TTS backend (e.g. `\"opus\"`, `\"mp3\"`,\n`\"wav\"`). Defaults to `\"opus\"` for the OpenAI family. Override to\n`\"wav\"` for Orpheus-class models (e.g. `canopylabs/orpheus-v1-english`\non Groq) or `\"mp3\"` for broader compatibility.",
          "type": [
            "string",
            "null"
          ]
        },
        "similarity_boost": {
          "default": null,
          "description": "Similarity boost for elevenlabs (0.0-1.0; default `0.5`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "speed": {
          "default": null,
          "description": "Playback speed multiplier (openai only; default `1.0`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "stability": {
          "default": null,
          "description": "Voice stability for elevenlabs (0.0-1.0; default `0.5`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "uri": {
          "default": null,
          "description": "Endpoint URI for HTTP-based backends. Overrides the family default\nwhen pointing at a compatible third-party API (Groq, Azure, self-hosted\nproxies). Set to the **full** URL — there is no separate path-suffix\nfield. Renamed from `api_url` for parity with `ModelProviderConfig.uri`.",
          "type": [
            "string",
            "null"
          ]
        },
        "voice": {
          "default": null,
          "description": "Voice override for this instance. When empty, falls back to\n`[tts].default_voice`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ElevenLabsTtsProviderConfig": {
      "description": "Per-instance TTS model_provider configuration (`[tts_providers.<type>.<alias>]`).\n\nMirrors `ModelProviderConfig` in shape — one struct holds the union of\nfields across backends. Only the fields relevant to the selected backend\n(determined by the outer `<type>` map key) are read at runtime; others\nare quietly ignored.",
      "properties": {
        "api_key": {
          "default": null,
          "description": "API key (openai, elevenlabs, google).",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "binary_path": {
          "default": null,
          "description": "Path to backend binary (edge-tts subprocess; piper local server).",
          "type": [
            "string",
            "null"
          ]
        },
        "language_code": {
          "default": null,
          "description": "Language code for google (e.g. `en-US`).",
          "type": [
            "string",
            "null"
          ]
        },
        "model": {
          "default": null,
          "description": "Model name. OpenAI uses this for `tts-1`/`tts-1-hd`; elevenlabs uses\nit as the model_id (e.g. `eleven_monolingual_v1`).",
          "type": [
            "string",
            "null"
          ]
        },
        "response_format": {
          "default": null,
          "description": "Audio response format sent to the TTS backend (e.g. `\"opus\"`, `\"mp3\"`,\n`\"wav\"`). Defaults to `\"opus\"` for the OpenAI family. Override to\n`\"wav\"` for Orpheus-class models (e.g. `canopylabs/orpheus-v1-english`\non Groq) or `\"mp3\"` for broader compatibility.",
          "type": [
            "string",
            "null"
          ]
        },
        "similarity_boost": {
          "default": null,
          "description": "Similarity boost for elevenlabs (0.0-1.0; default `0.5`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "speed": {
          "default": null,
          "description": "Playback speed multiplier (openai only; default `1.0`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "stability": {
          "default": null,
          "description": "Voice stability for elevenlabs (0.0-1.0; default `0.5`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "uri": {
          "default": null,
          "description": "Endpoint URI for HTTP-based backends. Overrides the family default\nwhen pointing at a compatible third-party API (Groq, Azure, self-hosted\nproxies). Set to the **full** URL — there is no separate path-suffix\nfield. Renamed from `api_url` for parity with `ModelProviderConfig.uri`.",
          "type": [
            "string",
            "null"
          ]
        },
        "voice": {
          "default": null,
          "description": "Voice override for this instance. When empty, falls back to\n`[tts].default_voice`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "EmailConfig": {
      "properties": {
        "default_subject": {
          "default": "Re: Message",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "from_address": {
          "type": "string"
        },
        "html_body": {
          "default": true,
          "description": "When `true` (default), outbound emails are rendered as HTML via Markdown conversion.\nSet to `false` to send plain-text emails instead.",
          "type": "boolean"
        },
        "idle_timeout_secs": {
          "default": 1740,
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "imap_folder": {
          "default": "INBOX",
          "type": "string"
        },
        "imap_host": {
          "type": "string"
        },
        "imap_port": {
          "default": 993,
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "max_attachment_bytes": {
          "default": 26214400,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "oauth2": {
          "anyOf": [
            {
              "$ref": "#/$defs/EmailOAuth2Config"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "OAuth2 settings for IMAP authentication. When present, IMAP uses\nXOAUTH2 instead of plain LOGIN. Required for providers like\nOutlook/Hotmail that have deprecated password auth."
        },
        "observer_mode": {
          "default": false,
          "description": "When true, the daemon observes new mail but never modifies any IMAP flag.",
          "type": "boolean"
        },
        "password": {
          "type": "string"
        },
        "poll_interval_secs": {
          "default": 60,
          "description": "Polling interval used when the IMAP server does not advertise the IDLE\ncapability (RFC 2177). Ignored when IDLE is available.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "smtp_host": {
          "type": "string"
        },
        "smtp_password": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "smtp_port": {
          "default": 465,
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "smtp_tls": {
          "default": true,
          "type": "boolean"
        },
        "smtp_username": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "username": {
          "type": "string"
        }
      },
      "required": [
        "imap_host",
        "smtp_host",
        "username",
        "password",
        "from_address"
      ],
      "type": "object"
    },
    "EmailOAuth2Config": {
      "description": "OAuth2 settings for IMAP email authentication (XOAUTH2 / RFC 8628).\nPopulate these to use OAuth2 bearer tokens instead of plain-password LOGIN.\nAll major providers (Microsoft, Google) support this flow via device code.",
      "properties": {
        "client_id": {
          "description": "OAuth2 application client ID (public client; no secret required for device flow).",
          "type": "string"
        },
        "device_code_url": {
          "description": "Device-code initiation endpoint\n(e.g. `https://login.microsoftonline.com/consumers/oauth2/v2.0/devicecode`).",
          "type": "string"
        },
        "scopes": {
          "description": "OAuth2 scopes to request (must include `offline_access` for refresh tokens).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "token_url": {
          "description": "Token endpoint for refresh and device-code polling\n(e.g. `https://login.microsoftonline.com/consumers/oauth2/v2.0/token`).",
          "type": "string"
        }
      },
      "required": [
        "client_id",
        "token_url",
        "device_code_url",
        "scopes"
      ],
      "type": "object"
    },
    "EmbeddingRouteConfig": {
      "description": "Route an embedding hint to a specific model_provider + model.\n\n```toml\n[[embedding_routes]]\nhint = \"semantic\"\nmodel_provider = \"openai.embeddings\"\nmodel = \"text-embedding-3-small\"\ndimensions = 1536\n\n[memory]\nembedding_model = \"hint:semantic\"\n```",
      "properties": {
        "api_key": {
          "default": null,
          "description": "Optional API key override for this route's model_provider",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "dimensions": {
          "default": null,
          "description": "Optional embedding dimension override for this route",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "hint": {
          "default": "",
          "description": "Route hint name (e.g. \"semantic\", \"archive\", \"faq\")\n`#[serde(default)]` lets the `create_map_key` macro default-construct\nfrom `{}` before the hint gets injected via `set_prop`. Empty strings\nare rejected by `Config::validate()`.",
          "type": "string"
        },
        "model": {
          "default": "",
          "description": "Provider-local embedding model identifier to use with that provider profile\n`#[serde(default)]` is required for `Default` + `create_map_key` construction.\nEmpty strings are rejected by `Config::validate()`.",
          "type": "string"
        },
        "model_provider": {
          "default": "",
          "description": "Dotted embedding-capable provider profile ref\n`#[serde(default)]` is required for `Default` + `create_map_key` construction.\nEmpty strings are rejected by `Config::validate()`.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "EscalationConfig": {
      "description": "Escalation routing configuration (`[escalation]` section).\n\nControls which channels receive alert notifications when\n`escalate_to_human` is called with high or critical urgency.\nChannels are identified by name (e.g. `\"telegram\"`, `\"slack\"`).\nAlerts are sent best-effort and do not block the escalation.",
      "properties": {
        "alert_channels": {
          "default": [],
          "description": "Channel names to alert on high/critical escalations (default: empty).\n\nEach name must match a configured channel. Unrecognised names are\nlogged at WARN level and skipped.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "EstopConfig": {
      "additionalProperties": false,
      "description": "Emergency stop configuration.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable emergency stop controls.",
          "type": "boolean"
        },
        "require_otp_to_resume": {
          "default": true,
          "description": "Require a valid OTP before resume operations.",
          "type": "boolean"
        },
        "state_file": {
          "default": "/home/jeremy/.zeroclaw/estop-state.json",
          "description": "File path used to persist estop state.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "EvalConfig": {
      "properties": {
        "enabled": {
          "default": false,
          "type": "boolean"
        },
        "max_retries": {
          "default": 1,
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "min_quality_score": {
          "default": 0.5,
          "format": "double",
          "type": "number"
        }
      },
      "type": "object"
    },
    "EvalHarnessConfig": {
      "description": "Configuration for the agent evaluation harness (`[eval]`), surfaced via the\n`zeroclaw eval` command. Distinct from `[agent.eval]`, which is the in-loop\nresponse-quality scorer.",
      "properties": {
        "mode": {
          "default": "replay",
          "description": "Default execution mode (`replay` or `live`) used when `--mode` is omitted.",
          "type": "string"
        },
        "suite_dir": {
          "default": "evals",
          "description": "Default directory of `*.json` trace fixtures used when `--suite` is omitted.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "ExternalRegistry": {
      "properties": {
        "enabled": {
          "default": true,
          "description": "Whether this registry is eligible for installs. Default: `true`.",
          "type": "boolean"
        },
        "kind": {
          "default": "git",
          "description": "Registry protocol. Only `\"git\"` is supported today; other protocols\nare reserved for a future additive release.",
          "type": "string"
        },
        "name": {
          "description": "Short alias used in `registry:<name>/<skill>` install specs.",
          "type": "string"
        },
        "url": {
          "description": "Git repository URL of the registry (must expose a top-level `skills/` dir).",
          "type": "string"
        }
      },
      "required": [
        "name",
        "url"
      ],
      "type": "object"
    },
    "FeatherlessModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "FileDownloadConfig": {
      "description": "Standalone file download tool configuration (`[file_download]`).\n\nWhen `url` is set to a non-empty value, registers a `file_download` tool\nthat GETs a file from the configured endpoint and writes it to the agent's\nworkspace filesystem. The LLM supplies only a document identifier and a\nworkspace-relative destination path; the endpoint URL comes solely from this\nconfig and is never model-controlled. Response bytes are streamed to disk\nand never loaded into model context.\n\nWhen `url` is `None` or empty, the tool is not registered.",
      "properties": {
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "description": "Static HTTP headers attached to every download request — typically an\n`Authorization: Bearer …` token for the upstream endpoint. Same shape as\n`[mcp.servers.*.headers]`.",
          "type": "object",
          "x-secret": true
        },
        "max_file_size_bytes": {
          "default": 26214400,
          "description": "Maximum download size in bytes. Enforced while streaming: the transfer\nis aborted and the partial file removed once this ceiling is exceeded,\nso an oversized or unbounded body never fully buffers in memory or lands\non disk. Default: 25 MiB.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "timeout_secs": {
          "default": 120,
          "description": "Request timeout in seconds. Default: 120.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "url": {
          "default": null,
          "description": "Download endpoint URL. Tool is disabled when this is `None` or empty.\nThe file to fetch is selected by the `document_id` query parameter.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "FileUploadBundleConfig": {
      "description": "Standalone multi-file bundle upload tool configuration\n(`[file_upload_bundle]`).\n\nWhen `url` is set to a non-empty value, registers a `file_upload_bundle`\ntool that POSTs N files from the agent's local filesystem to the\nconfigured endpoint as a single `multipart/form-data` request. The LLM\nprovides only file paths; the host reads the bytes.\n\nWhen `url` is `None` or empty, the tool is not registered.",
      "properties": {
        "field_name": {
          "default": "file",
          "description": "Multipart form-field name reused across every file part. Default: `file`.",
          "type": "string"
        },
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "description": "Static HTTP headers attached to every upload request.",
          "type": "object",
          "x-secret": true
        },
        "max_file_size_bytes": {
          "default": 10485760,
          "description": "Maximum per-file size in bytes. Default: 10 MiB.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "max_files": {
          "default": 16,
          "description": "Maximum number of files per call. Default: 16.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "max_response_body_bytes": {
          "default": 4096,
          "description": "Maximum response body bytes to read from the upload endpoint.\nPrevents unbounded memory use from a malicious or verbose receiver.\nDefault: 4096 (4 KiB).",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_total_size_bytes": {
          "default": 33554432,
          "description": "Maximum cumulative size across every file in one call. Default: 32 MiB.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "method": {
          "default": "POST",
          "description": "HTTP method. Only `POST` (default) and `PUT` are accepted.",
          "type": "string"
        },
        "timeout_secs": {
          "default": 120,
          "description": "Request timeout in seconds. Default: 120.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "url": {
          "default": null,
          "description": "Upload endpoint URL. Tool is disabled when this is `None` or empty.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "FileUploadConfig": {
      "description": "Standalone file upload tool configuration (`[file_upload]`).\n\nWhen `url` is set to a non-empty value, registers a `file_upload` tool that\nPOSTs files from the agent's local filesystem to the configured endpoint\nusing `multipart/form-data`. The LLM provides only a file path; the host\nreads the bytes and uploads them without ever including file content in\nthe model context.\n\nWhen `url` is `None` or empty, the tool is not registered.",
      "properties": {
        "field_name": {
          "default": "file",
          "description": "Multipart form-field name for the file part. Default: `file`.",
          "type": "string"
        },
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "description": "Static HTTP headers attached to every upload request. Same shape as\n`[mcp.servers.*.headers]`.",
          "type": "object",
          "x-secret": true
        },
        "max_file_size_bytes": {
          "default": 26214400,
          "description": "Maximum file size in bytes. Larger files are rejected before any\nbytes hit the network. Default: 25 MiB.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "method": {
          "default": "POST",
          "description": "HTTP method. Only `POST` (default) and `PUT` are accepted.",
          "type": "string"
        },
        "timeout_secs": {
          "default": 60,
          "description": "Request timeout in seconds. Default: 60.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "url": {
          "default": null,
          "description": "Upload endpoint URL. Tool is disabled when this is `None` or empty.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "FilesystemConfig": {
      "description": "Filesystem SOP listener configuration.\n\nWatches configured paths and dispatches file create/modify/delete/rename\nevents to the SOP engine. This is a fan-in listener, not a chat channel:\nits `Channel::send` has no outbound surface.",
      "properties": {
        "allow_broad_roots": {
          "default": false,
          "description": "Watch broad system roots (`/`, `/home`, `/etc`, `/var`, `/proc`,\n`/sys`, `/dev`, `/tmp`) despite the deny-broad-roots default. The\npseudo-filesystems `/proc` and `/sys` would surface kernel object\npaths in SOP payloads and flood the watcher with events. Off unless\nexplicitly enabled.",
          "type": "boolean"
        },
        "debounce_ms": {
          "default": 500,
          "description": "Collapse rapid repeated events per path/kind within this window.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "events": {
          "default": [
            "created",
            "modified",
            "deleted",
            "renamed"
          ],
          "description": "Event kinds to emit (`created`, `modified`, `deleted`, `renamed`).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "exclude": {
          "default": [],
          "description": "Glob patterns to exclude. Applied after `include`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "follow_symlinks": {
          "default": false,
          "description": "Follow symlinks when reading event-path metadata, hash, and content.\nOff by default: a symlink under a watched root is rejected before any\nmetadata/hash/content handling so its target cannot escape the root. When\non, the canonical target must still resolve inside a configured root.",
          "type": "boolean"
        },
        "include": {
          "default": [],
          "description": "Glob patterns to include. Empty matches all paths under the roots.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_content_bytes": {
          "default": 65536,
          "description": "Maximum file content bytes admitted into a payload when `read_content`,\nand the size ceiling for hashing. `Some(n)` caps reads and hashing at\n`n` bytes; oversize files are skipped rather than truncated. `None`\nremoves the ceiling entirely (unbounded reads). Defaults to\n`Some(65536)` (64 KiB), so the cap applies by default whenever\n`read_content` or hashing runs.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "paths": {
          "default": [],
          "description": "Root paths to watch. At least one is required.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "read_content": {
          "default": false,
          "description": "Read file content into the event payload. Opt-in.",
          "type": "boolean"
        },
        "recursive": {
          "default": true,
          "description": "Watch nested paths beneath each root.",
          "type": "boolean"
        },
        "settle_ms": {
          "default": 250,
          "description": "Wait this long after the last event before reading metadata or content,\ngiving atomic writes and slow copies time to finish.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "FirecrawlConfig": {
      "description": "Firecrawl fallback configuration for JS-heavy and bot-blocked sites.\n\nWhen enabled, if the standard web fetch fails (HTTP error, empty body, or\nbody shorter than 100 characters suggesting a JS-only page), the tool\nfalls back to the Firecrawl API for stealth content extraction.",
      "properties": {
        "api_key_env": {
          "default": "FIRECRAWL_API_KEY",
          "description": "Environment variable name for the Firecrawl API key",
          "type": "string"
        },
        "api_url": {
          "default": "https://api.firecrawl.dev/v1",
          "description": "Firecrawl API base URL",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Enable Firecrawl fallback",
          "type": "boolean"
        },
        "mode": {
          "$ref": "#/$defs/FirecrawlMode",
          "default": "scrape",
          "description": "Firecrawl extraction mode"
        }
      },
      "type": "object"
    },
    "FirecrawlMode": {
      "description": "Firecrawl fallback mode: scrape a single page or crawl linked pages.",
      "oneOf": [
        {
          "enum": [
            "scrape"
          ],
          "type": "string"
        },
        {
          "const": "crawl",
          "description": "Reserved for future multi-page crawl support. Accepted in config\ndeserialization to avoid breaking existing files, but not yet\nimplemented — `fetch_via_firecrawl` always uses the `/scrape` endpoint.",
          "type": "string"
        }
      ]
    },
    "FireworksModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "FriendliModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "GatewayClientAuthConfig": {
      "description": "Client certificate authentication (mTLS) configuration (`[gateway.tls.client_auth]`).",
      "properties": {
        "ca_cert_path": {
          "default": "",
          "description": "Path to the PEM-encoded CA certificate used to verify client certs.",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Enable client certificate verification (default: false).",
          "type": "boolean"
        },
        "pinned_certs": {
          "default": [],
          "description": "Optional SHA-256 fingerprints for certificate pinning.\nWhen non-empty, only client certs matching one of these fingerprints are accepted.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "require_client_cert": {
          "default": true,
          "description": "Reject connections that do not present a valid client certificate (default: true).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "GatewayConfig": {
      "description": "Gateway server configuration (`[gateway]` section).\n\nControls the HTTP gateway for webhook and pairing endpoints.",
      "properties": {
        "allow_public_bind": {
          "default": false,
          "description": "Allow binding to non-localhost without a tunnel (default: false)",
          "type": "boolean"
        },
        "allow_remote_admin": {
          "default": false,
          "description": "Allow authenticated remote callers to use admin endpoints that are\notherwise localhost-only. Currently this gates `POST /admin/reload`.\nWhen false (default), those endpoints reject any non-loopback peer.\nWhen true, a non-loopback request is accepted only if it also passes\npairing authentication — which requires `require_pairing = true`; with\npairing off a remote caller cannot be authenticated and is rejected, so\nthis flag never exposes an anonymous remote reload. `/admin/shutdown`\nand the pairing-code endpoints stay localhost-only regardless.\n(default: false)",
          "type": "boolean"
        },
        "allow_self_upgrade": {
          "default": false,
          "description": "Allow triggering a self-upgrade (binary swap via `zeroclaw update`) from\nthe dashboard. This is a remote-code-execution-adjacent surface: any\nauthenticated dashboard user could replace the running binary. Keep off\nunless you trust every paired client. (default: false)",
          "type": "boolean"
        },
        "check_updates": {
          "default": true,
          "description": "Poll GitHub for newer releases and show an \"update available\" indicator\non the dashboard version tag. Read-only; does not install anything.\n(default: true)",
          "type": "boolean"
        },
        "host": {
          "default": "127.0.0.1",
          "description": "Gateway host (default: 127.0.0.1)",
          "type": "string"
        },
        "idempotency_max_keys": {
          "default": 10000,
          "description": "Maximum distinct idempotency keys retained in memory.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "idempotency_ttl_secs": {
          "default": 300,
          "description": "TTL for webhook idempotency keys.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "long_running_request_timeout_secs": {
          "default": 600,
          "description": "HTTP request timeout (seconds) for `POST /api/cron/{id}/run`, which\nruns jobs synchronously and routinely exceeds the 30s default.\nDefault: 600s (10 minutes).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "pair_rate_limit_per_minute": {
          "default": 10,
          "description": "Max `/pair` requests per minute per client key.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "paired_tokens": {
          "default": [],
          "description": "Paired bearer tokens (managed automatically, not user-edited)",
          "items": {
            "type": "string"
          },
          "type": "array",
          "x-secret": true
        },
        "pairing_dashboard": {
          "$ref": "#/$defs/PairingDashboardConfig",
          "default": {
            "code_length": 8,
            "code_ttl_secs": 3600,
            "lockout_secs": 300,
            "max_failed_attempts": 5,
            "max_pending_codes": 3
          },
          "description": "Pairing dashboard configuration"
        },
        "path_prefix": {
          "default": null,
          "description": "Optional URL path prefix for reverse-proxy deployments.\nWhen set, all gateway routes are served under this prefix.\nMust start with `/` and must not end with `/`.",
          "type": [
            "string",
            "null"
          ]
        },
        "port": {
          "default": 42617,
          "description": "Gateway port (default: 42617)",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "rate_limit_max_keys": {
          "default": 10000,
          "description": "Maximum distinct client keys tracked by gateway rate limiter maps.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "request_timeout_secs": {
          "default": 30,
          "description": "HTTP request timeout (seconds) for gateway routes other than the\nlong-running cron-trigger endpoint. Default: 30s.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "require_pairing": {
          "default": true,
          "description": "Require pairing before accepting requests (default: true)",
          "type": "boolean"
        },
        "session_persistence": {
          "default": true,
          "description": "Persist gateway WebSocket chat sessions to SQLite. Default: true.",
          "type": "boolean"
        },
        "session_ttl_hours": {
          "default": 0,
          "description": "Auto-archive stale gateway sessions older than N hours. 0 = disabled. Default: 0.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "tls": {
          "anyOf": [
            {
              "$ref": "#/$defs/GatewayTlsConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "TLS configuration for the gateway server (`[gateway.tls]`)."
        },
        "trust_forwarded_headers": {
          "default": false,
          "description": "Trust proxy-forwarded client IP headers (`X-Forwarded-For`, `X-Real-IP`).\nDisabled by default; enable only behind a trusted reverse proxy.",
          "type": "boolean"
        },
        "web_dist_dir": {
          "default": null,
          "description": "Path to the web dashboard `dist` directory. When set, the gateway\nserves the compiled frontend from the filesystem instead of requiring\nit to be embedded in the binary. Accepts absolute paths or paths\nrelative to the working directory. When omitted the gateway runs in\nAPI-only mode (no web dashboard) unless auto-detection finds it.",
          "type": [
            "string",
            "null"
          ]
        },
        "webhook_rate_limit_per_minute": {
          "default": 60,
          "description": "Max `/webhook` requests per minute per client key.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "GatewayTlsConfig": {
      "description": "TLS configuration for the gateway server (`[gateway.tls]`).",
      "properties": {
        "cert_path": {
          "description": "Path to the PEM-encoded server certificate file.",
          "type": "string"
        },
        "client_auth": {
          "anyOf": [
            {
              "$ref": "#/$defs/GatewayClientAuthConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Client certificate authentication (mutual TLS) settings."
        },
        "enabled": {
          "default": false,
          "description": "Enable TLS for the gateway (default: false).",
          "type": "boolean"
        },
        "key_path": {
          "description": "Path to the PEM-encoded server private key file.",
          "type": "string"
        }
      },
      "required": [
        "cert_path",
        "key_path"
      ],
      "type": "object"
    },
    "GeminiCliConfig": {
      "description": "Gemini CLI tool configuration (`[gemini_cli]` section).\n\nDelegates coding tasks to the `gemini -p` CLI. Authentication uses the\nbinary's own session by default — no API key needed unless\n`env_passthrough` includes `GOOGLE_API_KEY`.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable the `gemini_cli` tool",
          "type": "boolean"
        },
        "env_passthrough": {
          "default": [],
          "description": "Extra env vars passed to the gemini subprocess (e.g. GOOGLE_API_KEY)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_output_bytes": {
          "default": 2097152,
          "description": "Maximum output size in bytes (2MB default)",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "timeout_secs": {
          "default": 600,
          "description": "Maximum execution time in seconds (coding tasks can be long)",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "GeminiCliModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "binary_path": {
          "description": "Path to the `gemini` CLI binary. Falls back to `gemini` (PATH lookup).",
          "type": [
            "string",
            "null"
          ]
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "GeminiModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "auth_mode": {
          "anyOf": [
            {
              "$ref": "#/$defs/AuthMode"
            },
            {
              "type": "null"
            }
          ],
          "description": "Auth flow. Defaults to `api_key`; `oauth` uses GeminiModelProvider's\nOAuth-cache integration instead of the `api_key` field."
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "oauth_client_id": {
          "description": "Google OAuth app `client_id`, used when this alias drives ZeroClaw's\nown browser/device-code login flow (`zeroclaw auth login\n--model-provider gemini --profile <alias>`). Operators relying on\nthe upstream `gemini login` tool don't need this; that tool writes\nits own client_id / client_secret into `~/.gemini/oauth_creds.json`.",
          "type": [
            "string",
            "null"
          ]
        },
        "oauth_client_secret": {
          "description": "Google OAuth app `client_secret`. Set alongside `oauth_client_id`.",
          "type": [
            "string",
            "null"
          ]
        },
        "oauth_project": {
          "description": "Pin a specific GCP project ID for the OAuth `loadCodeAssist`\ndiscovery call. When unset, the discovery probes for an\nalready-onboarded project on the credential's account. Replaces\n`GOOGLE_CLOUD_PROJECT` / `GOOGLE_CLOUD_PROJECT_ID` env vars.",
          "type": [
            "string",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "GitConfig": {
      "description": "Git-forge channel configuration (polling-based; no webhook required).\n\nA `provider` selects the forge. GitHub authenticates as a GitHub App:\na short-lived RS256 JWT signed with the app's private key is exchanged\nfor per-installation access tokens. Gitea and Forgejo use a personal\naccess token against the instance's `/api/v1` endpoint, named by the\nrequired `api_base_url`. Inbound issue/PR comments are polled from the\nforge REST API, so the daemon needs no inbound network exposure.",
      "properties": {
        "access_token": {
          "default": "",
          "description": "Personal access token for Gitea/Forgejo API requests. The token needs\nrepository read access plus issue/PR comment write access for replies\nand reactions. Gitea/Forgejo provider only.",
          "type": "string",
          "x-secret": true
        },
        "api_base_url": {
          "description": "Gitea/Forgejo API base URL, including `/api/v1`, for example\n`https://git.example.org/api/v1` (for the public Gitea service:\n`https://gitea.com/api/v1`). Required when `provider` is `\"gitea\"`\nor `\"forgejo\"` - there is no default host, because every API\nrequest carries `access_token`; the channel fails closed at\nstartup rather than send the token to an endpoint the operator\nnever named. Gitea/Forgejo provider only.",
          "type": [
            "string",
            "null"
          ]
        },
        "app_id": {
          "default": 0,
          "description": "GitHub App ID (shown on the app's settings page). GitHub provider only.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "events": {
          "additionalProperties": {
            "$ref": "#/$defs/GitEventRoute"
          },
          "default": {},
          "description": "Per-event routing table, keyed by normalized event type\n(`\"issue_comment.created\"`, `\"pull_request.opened\"`,\n`\"workflow_run.failed\"`, …). Event types absent from the table fall\nback to the conversational default: `issue_comment.created`,\n`issues.opened`, and `pull_request.opened` are delivered as\nmessages (mention-gated); everything else is ignored. Which API\nendpoints are polled is derived from this table; routing an event\ntype is also subscribing to it.",
          "type": "object"
        },
        "events_backbone": {
          "default": false,
          "description": "Also poll the repository Events API (`/repos/{owner}/{repo}/events`)\nas a broad backbone transport: one conditional (ETag) request per\nrepository per tick, so an idle repo costs almost nothing. Caveats:\nevents arrive with a delay of up to ~5 minutes and the feed carries\nno Actions/check events (workflow runs always use their dedicated\nendpoint). Items also surfaced by a targeted endpoint are\nde-duplicated. Default: `false`.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "installation_id": {
          "description": "Installation ID to act as. When unset, the app's installations are\nlisted on first use and a sole installation is auto-selected;\nstartup fails if the app has zero or multiple installations.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "listen_to_bots": {
          "default": false,
          "description": "Process comments authored by other bot accounts. The app's own\ncomments are always ignored. Default: `false`.",
          "type": "boolean"
        },
        "mention_only": {
          "default": true,
          "description": "Only respond to comments that @-mention the app's bot login.\nDefault: `true`.",
          "type": "boolean"
        },
        "poll_interval_secs": {
          "default": 30,
          "description": "Poll interval in seconds for new issues and comments. Values below\n15 are clamped to 15. Default: `30`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "private_key": {
          "description": "RS256 private key PEM, the contents of the `.pem` file GitHub\ngenerates on the app's settings page, inline and encrypted at rest.\nInclude the BEGIN/END lines. GitHub provider only.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "private_key_path": {
          "description": "Filesystem path to the RS256 private key `.pem` file, read at startup\nwhen `private_key` (inline PEM) is unset. Backward-compatible fallback\nfor configs that predate the inline-PEM field. GitHub provider only.",
          "type": [
            "string",
            "null"
          ]
        },
        "provider": {
          "default": "github",
          "description": "Git forge provider. Supported: `\"github\"`, `\"gitea\"`, and\n`\"forgejo\"` (Forgejo uses the Gitea-compatible REST provider).\nDefault: `\"github\"`.",
          "type": "string"
        },
        "proxy_url": {
          "description": "Per-channel proxy override for GitHub API requests.",
          "type": [
            "string",
            "null"
          ]
        },
        "repos": {
          "default": [],
          "description": "Repositories to poll, as `owner/repo`. Empty = every repository\nvisible to the installation.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "GitEventRoute": {
      "description": "Routing for one normalized git-forge event type, keyed in\n`[channels.git.<alias>.events]` by the event type string\n(`\"pull_request.opened\" = { sop = \"pr-triage\" }`).\n\nAn entry with neither `message = true` nor a `sop` explicitly ignores the\nevent type (overriding the conversational default).",
      "properties": {
        "message": {
          "default": false,
          "description": "Deliver the event into the agent loop as a normal channel message.",
          "type": "boolean"
        },
        "sop": {
          "description": "Route the event to the named SOP through channel-sourced SOP ingress.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "GithubModelsModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "GlmEndpoint": {
      "enum": [
        "cn",
        "global"
      ],
      "type": "string"
    },
    "GlmModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "endpoint": {
          "$ref": "#/$defs/GlmEndpoint",
          "default": "global"
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "GmailPushConfig": {
      "properties": {
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "label_filter": {
          "default": [
            "INBOX"
          ],
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "oauth_token": {
          "default": "",
          "type": "string"
        },
        "topic": {
          "type": "string"
        },
        "webhook_secret": {
          "default": "",
          "type": "string"
        },
        "webhook_url": {
          "default": "",
          "type": "string"
        }
      },
      "required": [
        "topic"
      ],
      "type": "object"
    },
    "GoogleSttConfig": {
      "description": "Google Cloud Speech-to-Text model_provider configuration (`[transcription.google]`).",
      "properties": {
        "api_key": {
          "default": null,
          "description": "Google Cloud API key.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "language_code": {
          "default": "en-US",
          "description": "BCP-47 language code (default: \"en-US\").",
          "type": "string"
        }
      },
      "type": "object"
    },
    "GoogleTranscriptionProviderConfig": {
      "description": "Shared base for cloud transcription providers. Each cloud family\ncomposes this via `#[serde(flatten)] base: TranscriptionProviderConfig`.",
      "properties": {
        "api_key": {
          "default": null,
          "description": "API key for the transcription provider.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "initial_prompt": {
          "default": null,
          "description": "Whisper-style initial prompt to bias the model toward expected\nvocabulary (proper nouns, technical terms). Provider-specific support;\nsilently ignored where not applicable.",
          "type": [
            "string",
            "null"
          ]
        },
        "language": {
          "default": null,
          "description": "Optional language hint passed to the provider (ISO-639-1 like `\"en\"` /\n`\"ru\"`, or BCP-47 like `\"en-US\"` for Google). Most providers auto-detect\nwhen this is unset.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "GoogleTtsProviderConfig": {
      "description": "Per-instance TTS model_provider configuration (`[tts_providers.<type>.<alias>]`).\n\nMirrors `ModelProviderConfig` in shape — one struct holds the union of\nfields across backends. Only the fields relevant to the selected backend\n(determined by the outer `<type>` map key) are read at runtime; others\nare quietly ignored.",
      "properties": {
        "api_key": {
          "default": null,
          "description": "API key (openai, elevenlabs, google).",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "binary_path": {
          "default": null,
          "description": "Path to backend binary (edge-tts subprocess; piper local server).",
          "type": [
            "string",
            "null"
          ]
        },
        "language_code": {
          "default": null,
          "description": "Language code for google (e.g. `en-US`).",
          "type": [
            "string",
            "null"
          ]
        },
        "model": {
          "default": null,
          "description": "Model name. OpenAI uses this for `tts-1`/`tts-1-hd`; elevenlabs uses\nit as the model_id (e.g. `eleven_monolingual_v1`).",
          "type": [
            "string",
            "null"
          ]
        },
        "response_format": {
          "default": null,
          "description": "Audio response format sent to the TTS backend (e.g. `\"opus\"`, `\"mp3\"`,\n`\"wav\"`). Defaults to `\"opus\"` for the OpenAI family. Override to\n`\"wav\"` for Orpheus-class models (e.g. `canopylabs/orpheus-v1-english`\non Groq) or `\"mp3\"` for broader compatibility.",
          "type": [
            "string",
            "null"
          ]
        },
        "similarity_boost": {
          "default": null,
          "description": "Similarity boost for elevenlabs (0.0-1.0; default `0.5`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "speed": {
          "default": null,
          "description": "Playback speed multiplier (openai only; default `1.0`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "stability": {
          "default": null,
          "description": "Voice stability for elevenlabs (0.0-1.0; default `0.5`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "uri": {
          "default": null,
          "description": "Endpoint URI for HTTP-based backends. Overrides the family default\nwhen pointing at a compatible third-party API (Groq, Azure, self-hosted\nproxies). Set to the **full** URL — there is no separate path-suffix\nfield. Renamed from `api_url` for parity with `ModelProviderConfig.uri`.",
          "type": [
            "string",
            "null"
          ]
        },
        "voice": {
          "default": null,
          "description": "Voice override for this instance. When empty, falls back to\n`[tts].default_voice`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "GoogleWorkspaceAllowedOperation": {
      "description": "Google Workspace CLI (`gws`) tool configuration (`[google_workspace]` section).\n\n## Defaults\n- `enabled`: `false` (tool is not registered unless explicitly opted-in).\n- `allowed_services`: empty vector, which grants access to the full default\n  service set: `drive`, `sheets`, `gmail`, `calendar`, `docs`, `slides`,\n  `tasks`, `people`, `chat`, `classroom`, `forms`, `keep`, `meet`, `events`.\n- `credentials_path`: `None` (uses default `gws` credential discovery).\n- `default_account`: `None` (uses the `gws` active account).\n- `rate_limit_per_minute`: `60`.\n- `timeout_secs`: `30`.\n- `audit_log`: `false`.\n- `credentials_path`: `None` (uses default `gws` credential discovery).\n- `default_account`: `None` (uses the `gws` active account).\n- `rate_limit_per_minute`: `60`.\n- `timeout_secs`: `30`.\n- `audit_log`: `false`.\n\n## Compatibility\nConfigs that omit the `[google_workspace]` section entirely are treated as\n`GoogleWorkspaceConfig::default()` (disabled, all defaults allowed). Adding\nthe section is purely opt-in and does not affect other config sections.\n\n## Rollback / Migration\nTo revert, remove the `[google_workspace]` section from the config file (or\nset `enabled = false`). No data migration is required; the tool simply stops\nbeing registered.",
      "properties": {
        "methods": {
          "default": [],
          "description": "Allowed methods for the service/resource/sub_resource combination.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "resource": {
          "description": "Top-level resource name for the service (for example `users` for Gmail or `files` for Drive).",
          "type": "string"
        },
        "service": {
          "description": "Google Workspace service ID (for example `gmail` or `drive`).",
          "type": "string"
        },
        "sub_resource": {
          "default": null,
          "description": "Optional sub-resource for 4-segment gws commands\n(for example `messages` or `drafts` under `gmail users`).\nWhen present, the entry only matches calls that include this exact sub_resource.\nWhen absent, the entry only matches calls with no sub_resource.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "service",
        "resource"
      ],
      "type": "object"
    },
    "GoogleWorkspaceConfig": {
      "description": "Google Workspace CLI (`gws`) tool configuration (`[google_workspace]` section).\n\n## Defaults\n- `enabled`: `false` (tool is not registered unless explicitly opted-in).\n- `allowed_services`: empty vector, which grants access to the full default\n  service set: `drive`, `sheets`, `gmail`, `calendar`, `docs`, `slides`,\n  `tasks`, `people`, `chat`, `classroom`, `forms`, `keep`, `meet`, `events`.\n- `allowed_operations`: empty vector, which preserves the legacy behavior of\n  allowing any resource/method under the allowed service set.\n- `credentials_path`: `None` (uses default `gws` credential discovery).\n- `default_account`: `None` (uses the `gws` active account).\n- `rate_limit_per_minute`: `60`.\n- `timeout_secs`: `30`.\n- `audit_log`: `false`.\n\n## Compatibility\nConfigs that omit the `[google_workspace]` section entirely are treated as\n`GoogleWorkspaceConfig::default()` (disabled, all defaults allowed). Adding\nthe section is purely opt-in and does not affect other config sections.\n\n## Rollback / Migration\nTo revert, remove the `[google_workspace]` section from the config file (or\nset `enabled = false`). No data migration is required; the tool simply stops\nbeing registered.",
      "properties": {
        "allowed_operations": {
          "default": [],
          "description": "Restrict which resource/method combinations the agent can access.\n\nWhen empty (the default), all methods under `allowed_services` remain\navailable for backward compatibility. When non-empty, the runtime denies\nany `(service, resource, sub_resource, method)` combination that is not\nexplicitly listed. `sub_resource` is optional per entry: an entry without\nit matches only 3-segment `gws` calls; an entry with it matches only calls\nthat supply that exact sub_resource value.\n\nEach entry's `service` must appear in `allowed_services` when that list is\nnon-empty; config validation rejects entries that would never match at\nruntime.",
          "items": {
            "$ref": "#/$defs/GoogleWorkspaceAllowedOperation"
          },
          "type": "array"
        },
        "allowed_services": {
          "default": [],
          "description": "Restrict which Google Workspace services the agent can access.\n\nWhen empty (the default), the full default service set is allowed (see\nstruct-level docs). When non-empty, only the listed service IDs are\npermitted. Each entry must be non-empty, lowercase alphanumeric with\noptional underscores/hyphens, and unique.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "audit_log": {
          "default": false,
          "description": "Enable audit logging of every `gws` invocation (service, resource,\nmethod, timestamp). Default: `false`.",
          "type": "boolean"
        },
        "credentials_path": {
          "default": null,
          "description": "Path to service account JSON or OAuth client credentials file.\n\nWhen `None`, the tool relies on the default `gws` credential discovery\n(`gws auth login`). Set this to point at a service-account key or an\nOAuth client-secrets JSON for headless / CI environments.",
          "type": [
            "string",
            "null"
          ]
        },
        "default_account": {
          "default": null,
          "description": "Default Google account email to pass to `gws --account`.\n\nWhen `None`, the currently active `gws` account is used.",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "default": false,
          "description": "Enable the `google_workspace` tool. Default: `false`.",
          "type": "boolean"
        },
        "rate_limit_per_minute": {
          "default": 60,
          "description": "Maximum number of `gws` API calls allowed per minute. Default: `60`.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "timeout_secs": {
          "default": 30,
          "description": "Command execution timeout in seconds. Default: `30`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "GroqModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "GroqTranscriptionProviderConfig": {
      "description": "Shared base for cloud transcription providers. Each cloud family\ncomposes this via `#[serde(flatten)] base: TranscriptionProviderConfig`.",
      "properties": {
        "api_key": {
          "default": null,
          "description": "API key for the transcription provider.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "initial_prompt": {
          "default": null,
          "description": "Whisper-style initial prompt to bias the model toward expected\nvocabulary (proper nouns, technical terms). Provider-specific support;\nsilently ignored where not applicable.",
          "type": [
            "string",
            "null"
          ]
        },
        "language": {
          "default": null,
          "description": "Optional language hint passed to the provider (ISO-639-1 like `\"en\"` /\n`\"ru\"`, or BCP-47 like `\"en-US\"` for Google). Most providers auto-detect\nwhen this is unset.",
          "type": [
            "string",
            "null"
          ]
        },
        "model": {
          "default": null,
          "description": "Whisper model name (default: `\"whisper-large-v3-turbo\"`).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "HardwareConfig": {
      "description": "Wizard-driven hardware configuration for physical world interaction.",
      "properties": {
        "baud_rate": {
          "default": 115200,
          "description": "Baud rate negotiated on the serial link. 115200 matches the common Arduino / ESP32 bootloader default; bump to 230400+ when your firmware explicitly supports faster rates and you need the throughput.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "enabled": {
          "default": false,
          "description": "Opt in to direct physical-hardware control — GPIO pins, USB-tethered microcontrollers (Arduino, ESP32, Nucleo), or SWD/JTAG debug probes. Leave off for software-only use; turning it on without the right transport configured does nothing.",
          "type": "boolean"
        },
        "probe_target": {
          "default": null,
          "description": "Target chip identifier for `transport = probe` (e.g. `STM32F401RE`, `nRF52840_xxAA`). Passed straight to probe-rs for flash/debug operations; must match a chip probe-rs recognizes.",
          "type": [
            "string",
            "null"
          ]
        },
        "serial_port": {
          "default": null,
          "description": "TTY path for the `serial` transport — e.g. `/dev/ttyACM0` on Linux, `/dev/tty.usbmodem1` on macOS, `COM3` on Windows. Ignored for other transports.",
          "type": [
            "string",
            "null"
          ]
        },
        "transport": {
          "$ref": "#/$defs/HardwareTransport",
          "default": "None",
          "description": "How ZeroClaw reaches the hardware: `native` = Linux SBC with direct GPIO access (Raspberry Pi, Orange Pi); `serial` = USB-tethered microcontroller speaking over a TTY; `probe` = SWD/JTAG debug probe driving a target chip via probe-rs; `none` = disabled."
        },
        "workspace_datasheets": {
          "default": false,
          "description": "Index pre-converted `.md` and `.txt` datasheets from the workspace into\na local RAG store, so the agent can look up pin assignments and\nelectrical specs inline when you ask hardware questions. PDFs are not\nparsed as text; download or convert them to `.md`/`.txt` first. Off by\ndefault — turn on once the workspace has relevant text datasheets.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "HardwareTransport": {
      "description": "Hardware transport mode.",
      "enum": [
        "None",
        "Native",
        "Serial",
        "Probe"
      ],
      "type": "string"
    },
    "HeartbeatConfig": {
      "description": "Heartbeat configuration for periodic health pings (`[heartbeat]` section).",
      "properties": {
        "adaptive": {
          "default": false,
          "description": "Enable adaptive intervals that back off on failures and speed up for\nhigh-priority tasks. Default: `false`.",
          "type": "boolean"
        },
        "agent": {
          "default": "",
          "description": "Configured agent alias the heartbeat worker runs as. Required\nwhen `enabled = true`; refers to a `[agents.<alias>]` entry.",
          "type": "string"
        },
        "deadman_channel": {
          "default": null,
          "description": "Channel for dead-man's switch alerts (e.g. `telegram`). Falls back to\nthe heartbeat delivery channel.",
          "type": [
            "string",
            "null"
          ]
        },
        "deadman_timeout_minutes": {
          "default": 0,
          "description": "Dead-man's switch timeout in minutes. If the heartbeat has not ticked\nwithin this window, an alert is sent. `0` disables. Default: `0`.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "deadman_to": {
          "default": null,
          "description": "Recipient for dead-man's switch alerts. Falls back to `to`.",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "default": false,
          "description": "Enable periodic heartbeat pings. Default: `false`. When enabled,\n`agent` must name a configured agent — there is no default agent\nfor heartbeat to fall through to.",
          "type": "boolean"
        },
        "interval_minutes": {
          "default": 30,
          "description": "Interval in minutes between heartbeat pings. Minimum: `1`. Default: `30`.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "load_session_context": {
          "default": false,
          "description": "Load the channel session history before each heartbeat task execution so\nthe LLM has conversational context. Default: `false`.\n\nWhen `true`, the session file for the configured `target`/`to` is passed\nto the agent as `session_state_file`, giving it access to the recent\nconversation history — just as if the user had sent a message.",
          "type": "boolean"
        },
        "max_interval_minutes": {
          "default": 120,
          "description": "Maximum interval in minutes when adaptive mode backs off. Default: `120`.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "max_run_history": {
          "default": 100,
          "description": "Maximum number of heartbeat run history records to retain. Default: `100`.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "message": {
          "default": null,
          "description": "Optional fallback task text when `HEARTBEAT.md` has no task entries.",
          "type": [
            "string",
            "null"
          ]
        },
        "min_interval_minutes": {
          "default": 5,
          "description": "Minimum interval in minutes when adaptive mode is enabled. Default: `5`.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "target": {
          "default": null,
          "description": "Optional delivery channel for heartbeat output (for example: `telegram`).\nWhen omitted, auto-selects the first configured channel.",
          "type": [
            "string",
            "null"
          ]
        },
        "task_timeout_secs": {
          "default": 600,
          "description": "Maximum wall-clock seconds allowed for a single agent invocation\n(Phase 1 decision or Phase 2 task execution). `0` disables.\nDefault: `600` (10 minutes).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "to": {
          "default": null,
          "description": "Optional delivery recipient/chat identifier (required when `target` is\nexplicitly set).",
          "type": [
            "string",
            "null"
          ]
        },
        "two_phase": {
          "default": true,
          "description": "Enable two-phase heartbeat: Phase 1 asks LLM whether to run, Phase 2\nexecutes only when the LLM decides there is work to do. Saves API cost\nduring quiet periods. Default: `true`.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "HistoryPrunerConfig": {
      "properties": {
        "collapse_tool_results": {
          "default": true,
          "type": "boolean"
        },
        "enabled": {
          "default": false,
          "type": "boolean"
        },
        "keep_recent": {
          "default": 4,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_tokens": {
          "default": 8192,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "HooksConfig": {
      "properties": {
        "builtin": {
          "$ref": "#/$defs/BuiltinHooksConfig",
          "default": {
            "command_logger": false,
            "webhook_audit": {
              "enabled": false,
              "include_args": false,
              "max_args_bytes": 4096,
              "tool_patterns": [],
              "url": ""
            }
          }
        },
        "enabled": {
          "default": true,
          "description": "Enable lifecycle hook execution.\n\nHooks run in-process with the same privileges as the main runtime.\nKeep enabled hook handlers narrowly scoped and auditable.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "HttpRequestConfig": {
      "description": "HTTP request tool configuration (`[http_request]` section).\n\nDomain filtering: `allowed_domains` controls which hosts are reachable (use `[\"*\"]`\nfor all public hosts, which is the default). If `allowed_domains` is empty, all\nrequests are rejected.",
      "properties": {
        "allow_private_hosts": {
          "default": false,
          "description": "Allow requests to private/LAN hosts (RFC 1918, loopback, link-local, .local).\nDefault: false (deny private hosts for SSRF protection).",
          "type": "boolean"
        },
        "allowed_domains": {
          "default": [
            "*"
          ],
          "description": "Allowed domains for HTTP requests (exact or subdomain match)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "allowed_private_hosts": {
          "default": [],
          "description": "Private/internal hosts explicitly allowed to bypass SSRF protection.\nExact and subdomain matches are supported; `*` permits all private/local hosts.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "default": true,
          "description": "Enable `http_request` tool for API interactions",
          "type": "boolean"
        },
        "max_response_size": {
          "default": 1000000,
          "description": "Maximum response size in bytes (default: 1MB, 0 = unlimited)",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "secrets": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "description": "Named authorization secrets for `auth_secret` requests.",
          "type": "object",
          "x-secret": true
        },
        "timeout_secs": {
          "default": 30,
          "description": "Request timeout in seconds (default: 30)",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "HuggingfaceModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "HunyuanModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "HyperbolicModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "IMessageConfig": {
      "description": "iMessage channel configuration (macOS only).",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "reply_min_interval_secs": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "reply_queue_depth_max": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "IdentityConfig": {
      "description": "Identity format configuration (`[identity]` section).\n\nSupports `\"openclaw\"` (default) or `\"aieos\"` identity documents.",
      "properties": {
        "aieos_inline": {
          "default": null,
          "description": "Inline AIEOS JSON (alternative to file path)",
          "type": [
            "string",
            "null"
          ]
        },
        "aieos_path": {
          "default": null,
          "description": "Path to AIEOS JSON file (relative to workspace)",
          "type": [
            "string",
            "null"
          ]
        },
        "format": {
          "default": "openclaw",
          "description": "Identity format: \"openclaw\" (default) or \"aieos\"",
          "type": "string"
        }
      },
      "type": "object"
    },
    "ImageGenConfig": {
      "description": "Standalone image generation tool configuration (`[image_gen]`).\n\nWhen enabled, registers an `image_gen` tool that generates images via\nfal.ai's synchronous API (Flux / Nano Banana models) and saves them\nto the workspace `images/` directory.",
      "properties": {
        "api_key_env": {
          "default": "FAL_API_KEY",
          "description": "Environment variable name holding the fal.ai API key.",
          "type": "string"
        },
        "default_model": {
          "default": "fal-ai/flux/schnell",
          "description": "Default fal.ai model identifier.",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Enable the standalone image generation tool. Default: false.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "ImageProviderDalleConfig": {
      "description": "OpenAI DALL-E settings (`[linkedin.image.dalle]`).",
      "properties": {
        "api_key_env": {
          "default": "OPENAI_API_KEY",
          "description": "Environment variable name holding the OpenAI API key.",
          "type": "string"
        },
        "model": {
          "default": "dall-e-3",
          "description": "DALL-E model identifier.",
          "type": "string"
        },
        "size": {
          "default": "1024x1024",
          "description": "Image dimensions.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "ImageProviderFluxConfig": {
      "description": "Flux (fal.ai) image generation settings (`[linkedin.image.flux]`).",
      "properties": {
        "api_key_env": {
          "default": "FAL_API_KEY",
          "description": "Environment variable name holding the fal.ai API key.",
          "type": "string"
        },
        "model": {
          "default": "fal-ai/flux/schnell",
          "description": "Flux model identifier.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "ImageProviderImagenConfig": {
      "description": "Google Imagen (Vertex AI) settings (`[linkedin.image.imagen]`).",
      "properties": {
        "api_key_env": {
          "default": "GOOGLE_VERTEX_API_KEY",
          "description": "Environment variable name holding the API key.",
          "type": "string"
        },
        "project_id_env": {
          "default": "GOOGLE_CLOUD_PROJECT",
          "description": "Environment variable for the Google Cloud project ID.",
          "type": "string"
        },
        "region": {
          "default": "us-central1",
          "description": "Vertex AI region.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "ImageProviderStabilityConfig": {
      "description": "Stability AI image generation settings (`[linkedin.image.stability]`).",
      "properties": {
        "api_key_env": {
          "default": "STABILITY_API_KEY",
          "description": "Environment variable name holding the API key.",
          "type": "string"
        },
        "model": {
          "default": "stable-diffusion-xl-1024-v1-0",
          "description": "Stability model identifier.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "InceptionModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "IrcConfig": {
      "description": "IRC channel configuration.",
      "properties": {
        "channels": {
          "default": [],
          "description": "Channels to join on connect",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "mention_only": {
          "default": false,
          "description": "When true, only respond to messages that mention the bot.\nOther messages in the channel are silently ignored.",
          "type": "boolean"
        },
        "nickname": {
          "description": "Bot nickname",
          "type": "string"
        },
        "nickserv_password": {
          "description": "NickServ IDENTIFY password",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "port": {
          "default": 6697,
          "description": "IRC server port (default: 6697 for TLS)",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "sasl_password": {
          "description": "SASL PLAIN password (IRCv3)",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "server": {
          "description": "IRC server hostname",
          "type": "string"
        },
        "server_password": {
          "description": "Server password (for bouncers like ZNC)",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "username": {
          "description": "Username (defaults to nickname if not set)",
          "type": [
            "string",
            "null"
          ]
        },
        "verify_tls": {
          "description": "Verify TLS certificate (default: true)",
          "type": [
            "boolean",
            "null"
          ]
        }
      },
      "required": [
        "server",
        "nickname"
      ],
      "type": "object"
    },
    "JiraConfig": {
      "description": "Jira integration configuration (`[jira]`).\n\nWhen `enabled = true`, registers the `jira` tool which can get tickets,\nsearch with JQL, and add comments. Requires `base_url` and `api_token`\n(or the `JIRA_API_TOKEN` env var).\n\n## Defaults\n- `enabled`: `false`\n- `allowed_actions`: `[\"get_ticket\"]` — read-only by default.\n  Add `\"search_tickets\"` or `\"comment_ticket\"` to unlock them.\n- `timeout_secs`: `30`\n\n## Auth\nJira Cloud uses HTTP Basic auth: `email` + `api_token`.\nJira Server/Data Center uses Bearer token auth: omit `email` and set\n`api_token` to a personal access token.\n`api_token` is stored encrypted at rest; set it here or via `JIRA_API_TOKEN`.",
      "properties": {
        "allowed_actions": {
          "default": [
            "get_ticket"
          ],
          "description": "Actions the agent is permitted to call.\nValid values: `\"get_ticket\"`, `\"search_tickets\"`, `\"comment_ticket\"`,\n`\"list_projects\"`, `\"myself\"`, `\"list_transitions\"`,\n`\"transition_ticket\"`, `\"create_ticket\"`.\nDefaults to `[\"get_ticket\"]` (read-only).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "api_token": {
          "default": "",
          "description": "Jira API token. Encrypted at rest. Falls back to `JIRA_API_TOKEN` env var.",
          "type": "string",
          "x-secret": true
        },
        "base_url": {
          "default": "",
          "description": "Atlassian instance base URL, e.g. `https://yourco.atlassian.net`.",
          "type": "string"
        },
        "email": {
          "description": "Jira account email used for Basic auth (Cloud).\nOmit for Server/DC deployments using Bearer token auth.\nAn empty string (`email = \"\"`) deserializes as `None`. Configs\nthat round-tripped the empty default to disk would otherwise\nsilently regress to Basic auth with empty username, since the\nemail-required validation was dropped when Server/DC Bearer-token\nsupport landed.",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "default": false,
          "description": "Enable the `jira` tool. Default: `false`.",
          "type": "boolean"
        },
        "timeout_secs": {
          "default": 30,
          "description": "Request timeout in seconds. Default: `30`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "KiloCliModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "binary_path": {
          "description": "Path to the `kilo` CLI binary. Falls back to `kilo` (PATH lookup).",
          "type": [
            "string",
            "null"
          ]
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "KiloEndpoint": {
      "description": "Kilo AI Gateway endpoint. Single canonical endpoint at kilo.ai.",
      "enum": [
        "gateway"
      ],
      "type": "string"
    },
    "KiloModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "endpoint": {
          "$ref": "#/$defs/KiloEndpoint",
          "description": "Kilo endpoint variant. Defaults to the canonical Kilo AI Gateway."
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "KnowledgeBundleConfig": {
      "description": "Named knowledge bundle (`[knowledge_bundles.<alias>]`).\n\nA reusable set of knowledge sources (documents, URLs, or RAG corpus paths)\nthat can be attached to an agent by alias.",
      "properties": {
        "sources": {
          "default": [],
          "description": "Paths or URLs to include in this knowledge bundle.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tags": {
          "default": [],
          "description": "Tags for filtering or categorising sources within the bundle.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "KnowledgeConfig": {
      "description": "Knowledge graph configuration for capturing and reusing expertise.",
      "properties": {
        "auto_capture": {
          "default": false,
          "description": "Automatically capture knowledge from conversations. Default: false.",
          "type": "boolean"
        },
        "db_path": {
          "default": "/home/jeremy/.zeroclaw/knowledge.db",
          "description": "Path to the knowledge graph SQLite database.",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Enable the knowledge graph tool. Default: false.",
          "type": "boolean"
        },
        "max_nodes": {
          "default": 100000,
          "description": "Maximum number of knowledge nodes. Default: 100000.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "suggest_on_query": {
          "default": true,
          "description": "Proactively suggest relevant knowledge on queries. Default: true.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "LambdaAiModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "LarkConfig": {
      "description": "Lark/Feishu configuration for messaging integration.\nLark is the international version; Feishu is the Chinese version.",
      "properties": {
        "ack_reactions": {
          "default": null,
          "description": "Override for the top-level `ack_reactions` setting. When `None`, the\nchannel falls back to `[channels].ack_reactions`. When set\nexplicitly, it takes precedence.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "app_id": {
          "description": "App ID from Lark/Feishu developer console",
          "type": "string"
        },
        "app_secret": {
          "description": "App Secret from Lark/Feishu developer console",
          "type": "string",
          "x-secret": true
        },
        "approval_timeout_secs": {
          "default": 300,
          "description": "Time in seconds an approval card waits for user response before\nthe runtime auto-denies. Default: 300 (5 minutes).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "draft_update_interval_ms": {
          "default": 1000,
          "description": "Minimum interval between consecutive `update_draft` PATCH calls in\nmilliseconds. Default 1000 ms tunes to Feishu's 5 QPS-per-message\nedit cap; raise on enterprise plans with higher quotas.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "encrypt_key": {
          "default": null,
          "description": "Encrypt key for webhook message decryption (optional)",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "mention_only": {
          "default": false,
          "description": "When true, only respond to messages that @-mention the bot in groups.\nDirect messages are always processed.",
          "type": "boolean"
        },
        "per_user_session": {
          "default": false,
          "description": "When `true`, group-chat sessions key on the sender's open_id, so\ndistinct members of the same group chat don't share conversation\ncontext. When `false` (default), all members of a group share one\nsession keyed on chat_id (matches the existing behavior). 1-on-1\nchats are unaffected (chat_id is already unique per user-bot pair).",
          "type": "boolean"
        },
        "port": {
          "default": null,
          "description": "HTTP port for webhook mode only. Must be set when receive_mode = \"webhook\".\nNot required (and ignored) for websocket mode.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "proxy_url": {
          "default": null,
          "description": "Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",
          "type": [
            "string",
            "null"
          ]
        },
        "receive_mode": {
          "$ref": "#/$defs/LarkReceiveMode",
          "default": "websocket",
          "description": "Event receive mode: \"websocket\" (default) or \"webhook\""
        },
        "stream_mode": {
          "$ref": "#/$defs/StreamMode",
          "default": "off",
          "description": "Streaming mode for the LLM response: `off` (default) routes every\nresponse through `send()`; `partial` opens a Feishu interactive\ncard and edits it incrementally via `update_draft` /\n`finalize_draft`; `multi_message` is rejected for Lark (Feishu has\nno equivalent surface — falls back to `off` with a warning)."
        },
        "use_feishu": {
          "default": false,
          "description": "Whether to use the Feishu (Chinese) endpoint instead of Lark (International)",
          "type": "boolean"
        },
        "verification_token": {
          "default": null,
          "description": "Verification token for webhook validation (optional)",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        }
      },
      "required": [
        "app_id",
        "app_secret"
      ],
      "type": "object"
    },
    "LarkReceiveMode": {
      "description": "How ZeroClaw receives events from Feishu / Lark.\n\n- `websocket` (default) — persistent WSS long-connection; no public URL required.\n- `webhook` — HTTP callback server; requires a public HTTPS endpoint.",
      "enum": [
        "websocket",
        "webhook"
      ],
      "type": "string"
    },
    "LeakDetectionConfig": {
      "description": "Outbound credential leak detection configuration.\n\nThese settings control the final guardrail pass over outbound channel\nresponses before they are delivered. Deterministic credential patterns\ninclude API keys, private keys, database URLs, bot tokens, and related\ntoken syntax. The high-entropy pass is a separate heuristic for standalone\nopaque tokens.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Enable outbound credential leak detection and redaction.",
          "type": "boolean"
        },
        "high_entropy_tokens": {
          "default": true,
          "description": "Enable high-entropy token redaction; deterministic patterns still run when false.",
          "type": "boolean"
        },
        "sensitivity": {
          "default": 0.7,
          "description": "Detection sensitivity from 0.0 to 1.0; higher is more aggressive.",
          "format": "double",
          "type": "number"
        }
      },
      "type": "object"
    },
    "LeptonModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "LineConfig": {
      "description": "LINE Messaging API channel configuration.",
      "properties": {
        "channel_access_token": {
          "default": "",
          "description": "Long-lived channel access token (from LINE Developers Console).\nUsed for both the Reply API and the Push API fallback.\nFalls back to the `LINE_CHANNEL_ACCESS_TOKEN` environment variable if empty.",
          "type": "string",
          "x-secret": true
        },
        "channel_secret": {
          "default": "",
          "description": "Channel secret (from LINE Developers Console).\nUsed to verify the `X-Line-Signature` header on incoming webhooks.\nFalls back to the `LINE_CHANNEL_SECRET` environment variable if empty.",
          "type": "string",
          "x-secret": true
        },
        "dm_policy": {
          "$ref": "#/$defs/LineDmPolicy",
          "default": "pairing",
          "description": "DM (1:1 chat) access policy. Default: `pairing`.\n\n- `open`: respond to everyone\n- `pairing`: require one-time `/bind <code>` handshake on first contact\n- `allowlist`: respond only to user IDs listed in `allowed_users`"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "group_policy": {
          "$ref": "#/$defs/LineGroupPolicy",
          "default": "mention",
          "description": "Group / multi-person chat policy. Default: `mention`.\n\n- `open`: respond to every message\n- `mention`: respond only when @mentioned\n- `disabled`: ignore all group messages"
        },
        "proxy_url": {
          "default": null,
          "description": "Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",
          "type": [
            "string",
            "null"
          ]
        },
        "sender_name": {
          "default": null,
          "description": "Display name shown as the sender in LINE chat bubbles.\nDefaults to `\"AI\"` when unset or empty.",
          "type": [
            "string",
            "null"
          ]
        },
        "webhook_port": {
          "default": 8443,
          "description": "TCP port the embedded webhook server listens on. Default: `8443`.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "LineDmPolicy": {
      "description": "DM (1:1 chat) access policy for the LINE channel.",
      "oneOf": [
        {
          "const": "open",
          "description": "Respond to every DM regardless of who sent it.",
          "type": "string"
        },
        {
          "const": "pairing",
          "description": "Require a one-time `/bind <code>` handshake before responding (default).\nZeroClaw prints the bind code on startup; send it once to unlock access.",
          "type": "string"
        },
        {
          "const": "allowlist",
          "description": "Respond only to LINE user IDs listed in `allowed_users`.",
          "type": "string"
        }
      ]
    },
    "LineGroupPolicy": {
      "description": "Group / multi-person chat policy for the LINE channel.",
      "oneOf": [
        {
          "const": "open",
          "description": "Respond to every message in group/room chats.",
          "type": "string"
        },
        {
          "const": "mention",
          "description": "Respond only when the bot is @mentioned (default).",
          "type": "string"
        },
        {
          "const": "disabled",
          "description": "Ignore all messages in group/room chats.",
          "type": "string"
        }
      ]
    },
    "LinkEnricherConfig": {
      "description": "Automatic link understanding for inbound channel messages (`[link_enricher]`).\n\nWhen enabled, URLs in incoming messages are automatically fetched and\nsummarised. The summary is prepended to the message before the agent\nprocesses it, giving the LLM context about linked pages without an\nexplicit tool call.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable the link enricher pipeline stage (default: false)",
          "type": "boolean"
        },
        "max_links": {
          "default": 3,
          "description": "Maximum number of links to fetch per message (default: 3)",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "timeout_secs": {
          "default": 10,
          "description": "Per-link fetch timeout in seconds (default: 10)",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "LinkedInConfig": {
      "description": "LinkedIn integration configuration (`[linkedin]` section).\n\nWhen enabled, the `linkedin` tool is registered in the agent tool surface.\nRequires `LINKEDIN_*` credentials in the workspace `.env` file.",
      "properties": {
        "api_version": {
          "default": "202602",
          "description": "LinkedIn REST API version header (YYYYMM format).",
          "type": "string"
        },
        "content": {
          "$ref": "#/$defs/LinkedInContentConfig",
          "default": {
            "github_repos": [],
            "github_users": [],
            "instructions": "",
            "persona": "",
            "rss_feeds": [],
            "topics": []
          },
          "description": "Content strategy for automated posting."
        },
        "enabled": {
          "default": false,
          "description": "Enable the LinkedIn tool.",
          "type": "boolean"
        },
        "image": {
          "$ref": "#/$defs/LinkedInImageConfig",
          "default": {
            "card_accent_color": "#0A66C2",
            "dalle": {
              "api_key_env": "OPENAI_API_KEY",
              "model": "dall-e-3",
              "size": "1024x1024"
            },
            "enabled": false,
            "fallback_card": true,
            "flux": {
              "api_key_env": "FAL_API_KEY",
              "model": "fal-ai/flux/schnell"
            },
            "imagen": {
              "api_key_env": "GOOGLE_VERTEX_API_KEY",
              "project_id_env": "GOOGLE_CLOUD_PROJECT",
              "region": "us-central1"
            },
            "providers": [
              "stability",
              "imagen",
              "dalle",
              "flux"
            ],
            "stability": {
              "api_key_env": "STABILITY_API_KEY",
              "model": "stable-diffusion-xl-1024-v1-0"
            },
            "temp_dir": "linkedin/images"
          },
          "description": "Image generation for posts (`[linkedin.image]`)."
        }
      },
      "type": "object"
    },
    "LinkedInContentConfig": {
      "description": "Content strategy configuration for LinkedIn auto-posting (`[linkedin.content]`).\n\nThe agent reads this via the `linkedin get_content_strategy` action to know\nwhat feeds to check, which repos to highlight, and how to write posts.",
      "properties": {
        "github_repos": {
          "default": [],
          "description": "GitHub repositories to highlight (format: `owner/repo`).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "github_users": {
          "default": [],
          "description": "GitHub usernames whose public activity to reference.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "instructions": {
          "default": "",
          "description": "Freeform posting instructions for the AI agent.",
          "type": "string"
        },
        "persona": {
          "default": "",
          "description": "Professional persona description (name, role, expertise).",
          "type": "string"
        },
        "rss_feeds": {
          "default": [],
          "description": "RSS feed URLs to monitor for topic inspiration (titles only).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "topics": {
          "default": [],
          "description": "Topics of expertise and interest for post themes.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "LinkedInImageConfig": {
      "description": "Image generation configuration for LinkedIn posts (`[linkedin.image]`).",
      "properties": {
        "card_accent_color": {
          "default": "#0A66C2",
          "description": "Accent color for the fallback card (CSS hex).",
          "type": "string"
        },
        "dalle": {
          "$ref": "#/$defs/ImageProviderDalleConfig",
          "default": {
            "api_key_env": "OPENAI_API_KEY",
            "model": "dall-e-3",
            "size": "1024x1024"
          },
          "description": "OpenAI DALL-E model_provider settings."
        },
        "enabled": {
          "default": false,
          "description": "Enable image generation for posts.",
          "type": "boolean"
        },
        "fallback_card": {
          "default": true,
          "description": "Generate a branded SVG text card when all AI model_providers fail.",
          "type": "boolean"
        },
        "flux": {
          "$ref": "#/$defs/ImageProviderFluxConfig",
          "default": {
            "api_key_env": "FAL_API_KEY",
            "model": "fal-ai/flux/schnell"
          },
          "description": "Flux (fal.ai) model_provider settings."
        },
        "imagen": {
          "$ref": "#/$defs/ImageProviderImagenConfig",
          "default": {
            "api_key_env": "GOOGLE_VERTEX_API_KEY",
            "project_id_env": "GOOGLE_CLOUD_PROJECT",
            "region": "us-central1"
          },
          "description": "Google Imagen (Vertex AI) model_provider settings."
        },
        "providers": {
          "default": [
            "stability",
            "imagen",
            "dalle",
            "flux"
          ],
          "description": "ModelProvider priority order. Tried in sequence; first success wins.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "stability": {
          "$ref": "#/$defs/ImageProviderStabilityConfig",
          "default": {
            "api_key_env": "STABILITY_API_KEY",
            "model": "stable-diffusion-xl-1024-v1-0"
          },
          "description": "Stability AI model_provider settings."
        },
        "temp_dir": {
          "default": "linkedin/images",
          "description": "Temp directory for generated images, relative to workspace.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "LinqConfig": {
      "properties": {
        "api_token": {
          "description": "Linq Partner API token (Bearer auth)",
          "type": "string",
          "x-secret": true
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "from_phone": {
          "description": "Phone number to send from (E.164 format)",
          "type": "string"
        },
        "signing_secret": {
          "default": null,
          "description": "Webhook signing secret for signature verification",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        }
      },
      "required": [
        "api_token",
        "from_phone"
      ],
      "type": "object"
    },
    "LitellmModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "LlamacppModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "LmstudioModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "LocalWhisperConfig": {
      "description": "Local/self-hosted Whisper-compatible STT endpoint (`[transcription.local_whisper]`).\n\nConfigures a self-hosted STT endpoint. Can be on localhost, a private network host, or any reachable URL.",
      "properties": {
        "bearer_token": {
          "default": null,
          "description": "Bearer token for endpoint authentication.\nOmit for unauthenticated local endpoints.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "max_audio_bytes": {
          "default": 26214400,
          "description": "Maximum audio file size in bytes accepted by this endpoint.\nDefaults to 25 MB — matching the cloud API cap for a safe out-of-the-box\nexperience. Self-hosted endpoints can accept much larger files; raise this\nas needed, but note that each transcription call clones the audio buffer\ninto a multipart payload, so peak memory per request is ~2× this value.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "timeout_secs": {
          "default": 300,
          "description": "Request timeout in seconds. Defaults to 300 (large files on local GPU).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "url": {
          "description": "HTTP or HTTPS endpoint URL, e.g. `\"http://10.10.0.1:8001/v1/transcribe\"`.",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    },
    "LocalWhisperTranscriptionProviderConfig": {
      "description": "Local / self-hosted Whisper-compatible transcription endpoint. Skips the\nshared `TranscriptionProviderConfig` base because it uses a bearer-token\nscheme and a per-instance URL rather than a vendor API key.",
      "properties": {
        "bearer_token": {
          "default": null,
          "description": "Bearer token for endpoint authentication. Omit for unauthenticated\nlocal endpoints.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "language": {
          "default": null,
          "description": "Optional language hint (passed through to the local endpoint).",
          "type": [
            "string",
            "null"
          ]
        },
        "max_audio_bytes": {
          "default": 26214400,
          "description": "Maximum audio file size in bytes accepted by this endpoint.\nDefaults to 25 MB to match the cloud cap; raise as needed.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "timeout_secs": {
          "default": 300,
          "description": "Request timeout in seconds.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "uri": {
          "description": "Endpoint URL, e.g. `\"http://10.10.0.1:8001/v1/transcribe\"`.",
          "type": "string"
        }
      },
      "required": [
        "uri"
      ],
      "type": "object"
    },
    "LogLlmRequestPayload": {
      "description": "LLM request payload capture policy. Mirrors [`LogToolIo`] but gates the\nprompt/messages sent on each `llm_request`. Defaults to `Off` because the\nfull payload is the entire system prompt plus conversation every turn.",
      "enum": [
        "off",
        "redacted",
        "full"
      ],
      "type": "string"
    },
    "LogPersistence": {
      "description": "JSONL log persistence mode.",
      "oneOf": [
        {
          "enum": [
            "none",
            "rolling",
            "full"
          ],
          "type": "string"
        },
        {
          "const": "rotating",
          "description": "Persist all events, but with ZeroClaw-managed archive rotation: the\nactive file is rotated to a timestamped archive on a size and/or daily\nboundary, and old archives are pruned by count and/or age. Unlike\n`rolling` (entry-count trim of the active file), rotated events are\npreserved in archive files for later diagnostics. Rotation is tuned via\nthe `log_persistence_max_bytes`, `log_persistence_rotate_daily`,\n`log_persistence_retention_max_files`, and\n`log_persistence_retention_max_age_days` settings.",
          "type": "string"
        }
      ]
    },
    "LogToolIo": {
      "description": "Tool I/O capture policy.",
      "enum": [
        "off",
        "redacted",
        "full"
      ],
      "type": "string"
    },
    "LucidStorageConfig": {
      "description": "Lucid CLI sync backend (`[storage.lucid.<alias>]`).",
      "properties": {
        "binary_path": {
          "default": null,
          "description": "Optional path to the lucid-memory binary. When unset, the bare `lucid`\nexecutable is resolved on `PATH`. A blank or whitespace-only value is\nrejected at config validation and remains invalid if startup continues,\nso an invalid explicit selector never falls through to `PATH`.",
          "type": [
            "string",
            "null"
          ]
        },
        "recall_timeout_ms": {
          "default": null,
          "description": "Recall (context) timeout override, in milliseconds. Lucid CLI cold\nstarts (loading the local embedding model) can exceed 1.5s on ARM\nhosts; raise this if recalls still time out on your hardware.\nUnset falls back to the built-in default (3000ms). `0` is rejected\nat config validation; because startup warns and continues after\nvalidation errors, a `0` that reaches the runtime is treated as\nunset and falls back to the default.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "store_timeout_ms": {
          "default": null,
          "description": "Store timeout override, in milliseconds. Same cold-start\nconsideration as `recall_timeout_ms`. Unset falls back to the\nbuilt-in default (3000ms); `0` is rejected at validation and, if\nstartup continues after the warning, is likewise treated as unset.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ManifestModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "MarkdownStorageConfig": {
      "description": "Markdown directory storage (`[storage.markdown.<alias>]`).",
      "properties": {
        "directory": {
          "default": null,
          "description": "Optional override for the markdown root directory.\nWhen unset, defaults to `<workspace_dir>/memory/`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "MatrixConfig": {
      "description": "Matrix channel configuration.",
      "properties": {
        "access_token": {
          "default": null,
          "description": "Matrix access token for the bot account. When unset, the channel\nfalls back to password login using `user_id` + `password`.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "ack_reactions": {
          "default": null,
          "description": "Override for the top-level `[channels].ack_reactions`. When\n`None`, falls back to the channels-wide default. When set\nexplicitly (`true`/`false`), takes precedence for this Matrix\ninstance only.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "allowed_rooms": {
          "default": [],
          "description": "Allowed Matrix room IDs. Empty = allow all rooms the bot has joined.\nEntries are matched literally against the canonical room ID\n(`!abc:server`) of each incoming message; `#room:server` aliases are\nnot resolved for this allowlist (they are resolved only for outbound\ndelivery targets such as cron `delivery.to`).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "approval_timeout_secs": {
          "default": 300,
          "description": "Seconds to wait for operator approval on `always_ask` tools before auto-denying.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "device_id": {
          "default": null,
          "description": "Optional Matrix device ID.",
          "type": [
            "string",
            "null"
          ]
        },
        "draft_update_interval_ms": {
          "default": 1500,
          "description": "Minimum interval (ms) between draft message edits in Partial mode.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "homeserver": {
          "description": "Matrix homeserver URL (e.g. `\"https://matrix.org\"`).",
          "type": "string"
        },
        "interrupt_on_new_message": {
          "default": false,
          "description": "Whether to interrupt an in-flight agent response when a new message arrives.",
          "type": "boolean"
        },
        "mention_only": {
          "default": false,
          "description": "When true, only respond to messages that @-mention the bot in groups.\nDirect messages are always processed.",
          "type": "boolean"
        },
        "multi_message_delay_ms": {
          "default": 800,
          "description": "Delay (ms) between sending each paragraph in MultiMessage mode.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "password": {
          "default": null,
          "description": "Optional login password for Matrix account (used for initial login flow).",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "recovery_key": {
          "default": null,
          "description": "Optional Matrix recovery key for automatic E2EE key backup restore.\nWhen set, ZeroClaw recovers room keys and cross-signing secrets on startup.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "reply_in_thread": {
          "default": true,
          "description": "When true (default), replies are sent as thread replies. Starts a new thread from the\nincoming message when none exists. When false, only continues existing threads.",
          "type": "boolean"
        },
        "reply_min_interval_secs": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "reply_queue_depth_max": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "stream_mode": {
          "$ref": "#/$defs/StreamMode",
          "default": "off",
          "description": "Streaming mode for progressive response delivery.\n`\"off\"` (default): single message. `\"partial\"`: edit-in-place draft.\n`\"multi_message\"`: paragraph-split delivery."
        },
        "user_id": {
          "default": null,
          "description": "Optional Matrix user ID (e.g. `\"@bot:matrix.org\"`).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "homeserver"
      ],
      "type": "object"
    },
    "MattermostConfig": {
      "description": "Mattermost bot channel configuration.",
      "properties": {
        "bot_token": {
          "default": null,
          "description": "Mattermost bot access token. When unset, the channel falls back to\nthe login flow using `login_id` + `password`.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "channel_ids": {
          "default": [],
          "description": "Channel IDs to restrict the bot to. Empty or `[\"*\"]` = auto-discover\nevery channel the bot can read (public, private, DMs, group DMs) and\npoll them all. Explicit IDs disable discovery and pin the bot to the\nlisted channels only. Migrated from the legacy `channel_id` singular\nfield.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "discover_dms": {
          "default": null,
          "description": "When true (default), auto-discovery includes DM (`type=D`) and group\nDM (`type=G`) channels. Set false to restrict the bot to public and\nprivate team channels only. Has no effect when `channel_ids` lists\nexplicit IDs. Defaults to `true` at the call site via\n`discover_dms.unwrap_or(true)`.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "interrupt_on_new_message": {
          "default": false,
          "description": "When true, a newer Mattermost message from the same sender in the same channel\ncancels the in-flight request and starts a fresh response with preserved history.",
          "type": "boolean"
        },
        "listen_mode": {
          "$ref": "#/$defs/MattermostListenMode",
          "default": "polling",
          "description": "Listen mode: `\"polling\"` (REST API every 3s, default) or `\"websocket\"`\n(persistent WebSocket connection to `/api/v4/websocket` for near-real-time\nevent delivery). WebSocket mode reduces server load and delivers events\nfaster but requires a WebSocket-capable Mattermost server (v4.0+)."
        },
        "login_id": {
          "default": null,
          "description": "Login ID (email or username) for the password login flow. Used only\nwhen `bot_token` is unset; both `login_id` and `password` must be\nset together.",
          "type": [
            "string",
            "null"
          ]
        },
        "mention_only": {
          "default": null,
          "description": "When true, only respond to messages that @-mention the bot. Other\nmessages in the channel are silently ignored. DM and group DM\nchannels always bypass this filter: a 1:1 (or small-group) direct\nconversation has no ambient noise to gate against, so every message\nis treated as addressed to the bot.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "password": {
          "default": null,
          "description": "Account password for the login flow. Used only when `bot_token` is\nunset; both `login_id` and `password` must be set together.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "proxy_url": {
          "default": null,
          "description": "Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",
          "type": [
            "string",
            "null"
          ]
        },
        "reply_min_interval_secs": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "reply_queue_depth_max": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "team_ids": {
          "default": [],
          "description": "Team IDs to restrict auto-discovery to. Empty = discover across every\nteam the bot belongs to. Non-empty = only discover public/private\nchannels whose `team_id` is in this list. DMs and group DMs (which\nhave no team) are governed by `discover_dms` instead.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "thread_replies": {
          "default": null,
          "description": "When true (default), replies thread on the original post.\nWhen false, replies go to the channel root.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "url": {
          "description": "Mattermost server URL (e.g. `\"https://mattermost.example.com\"`).",
          "type": "string"
        }
      },
      "required": [
        "url"
      ],
      "type": "object"
    },
    "MattermostListenMode": {
      "description": "How the Mattermost channel receives inbound messages.",
      "oneOf": [
        {
          "const": "polling",
          "description": "Poll REST API every 3 seconds. The default — all existing configs\ncontinue to work without changes.",
          "type": "string"
        },
        {
          "const": "websocket",
          "description": "Connect to `/api/v4/websocket` for near-real-time event delivery.",
          "type": "string"
        }
      ]
    },
    "McpBundleConfig": {
      "description": "Named MCP server bundle (`[mcp_bundles.<alias>]`).\n\nA reusable group of MCP servers granted to an agent that references the\nbundle by alias in `agents.<alias>.mcp_bundles`. Server IDs are matched\nagainst `[mcp.servers]` by `name`. Resolution is secure by default (see\n`Config::mcp_servers_for_bundles`): an ID with no matching server grants\nnothing, and `exclude` wins over `servers` across every bundle an agent\nreferences.",
      "properties": {
        "exclude": {
          "default": [],
          "description": "MCP server IDs removed from the grant. Deny wins: a name listed here is\nexcluded even if another referenced bundle includes it.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "servers": {
          "default": [],
          "description": "MCP server IDs (`[mcp.servers].name`) granted by this bundle.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "McpConfig": {
      "description": "External MCP client configuration (`[mcp]` section).",
      "properties": {
        "deferred_loading": {
          "default": false,
          "description": "Load MCP tool schemas on-demand via `tool_search` instead of eagerly\nincluding them in the LLM context window. When enabled, only tool names\nare listed in the system prompt; the LLM must call `tool_search` to fetch\nfull schemas before invoking a deferred tool.",
          "type": "boolean"
        },
        "enabled": {
          "default": true,
          "description": "Enable MCP tool loading.",
          "type": "boolean"
        },
        "servers": {
          "default": [],
          "description": "Configured MCP servers. The `#[nested]` annotation makes the macro\nexpose this as a List section in `map_key_sections()`, so the\ndashboard's `+ Add MCP server` affordance and the `POST\n/api/config/map-key?path=mcp.servers&key=<name>` endpoint pick it\nup automatically (no hand-table on the gateway side).\n\n`#[natural_key = \"name\"]` opts the Vec into per-element property\nrouting (see `route_vec_path` and the `Configurable` derive's\n`#[natural_key]` arm). With it, `set_prop(\"mcp.servers.<name>.url\",\n...)` and `get_prop(\"mcp.servers.<name>.transport\")` resolve to\nthe matching element's own `set_prop` / `get_prop`, and the\n`mcp.servers` section behaves like a `HashMap<String,\nMcpServerConfig>` from the dashboard / TUI's point of view.\n`name` itself becomes read-only via `set_prop`; rename goes\nthrough `rename_map_key` which mutates the `name` field in place.",
          "items": {
            "$ref": "#/$defs/McpServerConfig"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "McpServerConfig": {
      "description": "Configuration for a single external MCP server.",
      "properties": {
        "args": {
          "default": [],
          "description": "Command arguments for stdio transport.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "command": {
          "default": "",
          "description": "Executable to spawn for stdio transport.",
          "type": "string"
        },
        "env": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "description": "Optional environment variables for stdio transport.",
          "type": "object",
          "x-secret": true
        },
        "headers": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "description": "Optional HTTP headers for HTTP/SSE transports. Treated as secret:\nthe values commonly carry Bearer tokens for the upstream MCP server.",
          "type": "object",
          "x-secret": true
        },
        "name": {
          "default": "",
          "description": "Display name used as a tool prefix (`<server>__<tool>`). Filled in\nfrom the supplied `map_key` when the entry is created via\n`create_map_key(\"mcp.servers\", \"<name>\")`; `#[serde(default)]` lets\nthe macro default-construct from `{}` before the name gets injected.",
          "type": "string"
        },
        "pinned_resources": {
          "default": [],
          "description": "Resource URIs to read once at agent startup and inject into the system\nprompt as untrusted, server-origin context. Each is read via\n`resources/read` on this server; pins on a server that does not advertise\nresources, or that the agent's tool policy denies, are skipped with a\nwarning. Read once per run (not refreshed; no subscriptions).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tool_timeout_secs": {
          "default": null,
          "description": "Optional per-call timeout in seconds (hard capped in validation).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "transport": {
          "$ref": "#/$defs/McpTransport",
          "default": "stdio",
          "description": "Transport type (default: stdio)."
        },
        "url": {
          "default": null,
          "description": "URL for HTTP/SSE transports.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object",
      "x-required-by-transport": {
        "http": "url",
        "sse": "url",
        "stdio": "command"
      }
    },
    "McpTransport": {
      "description": "Transport type for MCP server connections.",
      "oneOf": [
        {
          "const": "stdio",
          "description": "Spawn a local process and communicate over stdin/stdout.",
          "type": "string"
        },
        {
          "const": "http",
          "description": "Connect via HTTP POST.",
          "type": "string"
        },
        {
          "const": "sse",
          "description": "Connect via HTTP + Server-Sent Events.",
          "type": "string"
        }
      ]
    },
    "MdnsConfig": {
      "description": "Configuration for LAN-local mDNS peer discovery (`[nodes.mdns]`).\n\nThis config controls only discovery behavior. The advertised gateway\nendpoint is derived from the running gateway's actual host, port, and path\nprefix at startup so `[nodes.mdns]` does not duplicate gateway listen state.",
      "properties": {
        "announce_interval_secs": {
          "default": 30,
          "description": "How often this node re-broadcasts its presence, in seconds.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "enabled": {
          "default": false,
          "description": "Enable mDNS local peer discovery.",
          "type": "boolean"
        },
        "max_peers": {
          "default": 16,
          "description": "Maximum number of unauthenticated LAN peer hints retained in memory.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "node_name": {
          "default": null,
          "description": "Human-readable node name advertised to LAN peers. Defaults to a stable\nlocal fallback when unset.",
          "type": [
            "string",
            "null"
          ]
        },
        "peer_ttl_secs": {
          "default": 90,
          "description": "Seconds after the last announcement before a peer is evicted.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "MediaPipelineConfig": {
      "description": "Automatic media understanding pipeline configuration (`[media_pipeline]`).\n\nWhen enabled, inbound channel messages with media attachments are\npre-processed before reaching the agent: audio is transcribed, images are\nannotated, and videos are summarised.",
      "properties": {
        "describe_images": {
          "default": true,
          "description": "Add image descriptions when a vision-capable model is active.",
          "type": "boolean"
        },
        "enabled": {
          "default": false,
          "description": "Master toggle for the media pipeline (default: false).",
          "type": "boolean"
        },
        "summarize_video": {
          "default": true,
          "description": "Summarize video attachments (placeholder — requires external API).",
          "type": "boolean"
        },
        "transcribe_audio": {
          "default": true,
          "description": "Transcribe audio attachments using the configured transcription model_provider.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "MemoryBackendKind": {
      "description": "Selects the memory backend used by an agent.",
      "oneOf": [
        {
          "const": "none",
          "description": "No memory backend. Recall returns empty; stores are no-ops.",
          "type": "string"
        },
        {
          "const": "sqlite",
          "description": "Embedded SQLite (`crates/zeroclaw-memory/src/sqlite.rs`). Default for\nnew installs because every supported platform can run it without\nextra services.",
          "type": "string"
        },
        {
          "const": "postgres",
          "description": "PostgreSQL with optional pgvector\n(`crates/zeroclaw-memory/src/postgres.rs`, feature `memory-postgres`).",
          "type": "string"
        },
        {
          "const": "qdrant",
          "description": "Qdrant vector store (`crates/zeroclaw-memory/src/qdrant.rs`).",
          "type": "string"
        },
        {
          "const": "markdown",
          "description": "Markdown files in the agent's workspace\n(`crates/zeroclaw-memory/src/markdown.rs`).",
          "type": "string"
        },
        {
          "const": "lucid",
          "description": "Hybrid local SQLite + external Lucid CLI\n(`crates/zeroclaw-memory/src/lucid.rs`).",
          "type": "string"
        }
      ]
    },
    "MemoryConfig": {
      "description": "Memory backend configuration (`[memory]` section).\n\nControls conversation memory storage, embeddings, hybrid search, response\ncaching, and memory snapshot/hydration. Backend-specific connection settings\nlive under `[storage.<backend>.<alias>]`; this section selects which storage\ninstance to use via the `backend` dotted reference.",
      "properties": {
        "archive_after_days": {
          "default": 7,
          "description": "Move daily/session files to the archive directory after this many days. Keeps the hot working set small without deleting history.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "audit_enabled": {
          "default": false,
          "description": "Enable audit logging of memory operations.",
          "type": "boolean"
        },
        "audit_retention_days": {
          "default": 30,
          "description": "Retention period for audit entries in days (default: 30).",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "auto_hydrate": {
          "default": true,
          "description": "Auto-hydrate from MEMORY_SNAPSHOT.md when brain.db is missing",
          "type": "boolean"
        },
        "auto_reindex_on_identity_change": {
          "default": false,
          "description": "Automatically re-embed all memories in the background when a change of embedding provider/model/dimensions is detected at startup (after the stale vectors have been cleared). Costs one embedding API call per memory, so it's off by default — leave it off for large stores and run `zeroclaw memory reindex` explicitly instead.",
          "type": "boolean"
        },
        "auto_save": {
          "default": true,
          "description": "Auto-save what *you* tell ZeroClaw into memory as conversation history — the agent's own replies are not saved. Turn off if you want memory to only hold things you explicitly record via the memory tool.",
          "type": "boolean"
        },
        "backend": {
          "default": "sqlite",
          "description": "Dotted reference to the active storage instance: `<backend>.<alias>`\n(e.g. `\"sqlite.default\"`, `\"postgres.work\"`). Resolves through\n`Config.storage.<backend>.<alias>` at runtime. Bare backend names\n(`\"sqlite\"`) are treated as `\"<backend>.default\"`. Set to `\"none\"` to\ndisable persistence entirely.",
          "type": "string"
        },
        "candidate_multiplier": {
          "default": 4,
          "description": "Candidate pool multiplier over the final recall limit before blend/rerank trimming.\nValues must be in 1..=20; runtime also enforces a bounded candidate pool.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "chunk_max_tokens": {
          "default": 512,
          "description": "Max tokens per chunk for document splitting",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "conflict_supersede_enabled": {
          "default": true,
          "description": "Enable reversible supersede soft-hide machinery when wired.",
          "type": "boolean"
        },
        "conflict_threshold": {
          "default": 0.85,
          "description": "Cosine similarity threshold for conflict detection (0.0–1.0).",
          "format": "double",
          "type": "number"
        },
        "consolidation_extract_facts": {
          "default": false,
          "description": "Also extract atomic durable facts from each consolidated turn and store\nthem as individual Core memories. Default off; the flip is sequenced in\na later phase. SQLite-only: enabling this requires the sqlite memory\nbackend globally and on every agent (validated at config load).",
          "type": "boolean"
        },
        "conversation_retention_days": {
          "default": 30,
          "description": "Delete conversation rows older than this many days from the DB (sqlite backend only). Age is measured by `updated_at` (last write time). 0 = keep forever.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "core_max_bytes": {
          "default": 0,
          "description": "Maximum Core bytes before budget compaction. 0 = unbounded.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "core_max_rows": {
          "default": 0,
          "description": "Maximum Core rows before budget compaction. 0 = unbounded.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "core_retention_days": {
          "default": 0,
          "description": "Delete core memory rows older than this many days from the DB. Age is measured by `created_at` (first-write time). Neither recall nor ordinary rewrites refresh `created_at` under the current SQLite upsert, so core retention is an absolute age limit from first write. Set this to a generously large window for durable core memories, or keep 0 = keep forever.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "daily_max_rows": {
          "default": 0,
          "description": "Maximum Daily rows before budget compaction. 0 = unbounded.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "daily_retention_days": {
          "default": 0,
          "description": "Delete daily memory rows older than this many days from the DB. Age is measured by `updated_at` (last write time). 0 = keep forever.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "dedup_action": {
          "$ref": "#/$defs/MemoryDedupAction",
          "default": "reject",
          "description": "Action to take when a duplicate is detected."
        },
        "dedup_jaccard_threshold": {
          "default": 0.8,
          "description": "Jaccard threshold for text-only duplicate detection.",
          "format": "double",
          "type": "number"
        },
        "dedup_on_write": {
          "default": false,
          "description": "Enable write-time near-duplicate detection.",
          "type": "boolean"
        },
        "default_namespace": {
          "default": "default",
          "description": "Default namespace for memory entries.",
          "type": "string"
        },
        "embedding_api_key": {
          "description": "Optional API key for the embedding endpoint. When set, embedding calls use this key instead of inheriting one from the seed model provider — decoupling embeddings from the chat model. Use it when the chat model runs on a provider that carries no usable embedding credential (e.g. an OAuth-only provider) while embeddings keep hitting an `openai`/`custom:` endpoint with their own key. Leave unset to inherit the seed provider's key (backward-compatible default).",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "embedding_cache_size": {
          "default": 10000,
          "description": "Max embedding cache entries before LRU eviction",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "embedding_dimensions": {
          "default": 1536,
          "description": "Vector width produced by the embedding model — must match the model's native dimension or vectors won't store correctly. Look up the number on the model_provider's model page.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "embedding_model": {
          "default": "text-embedding-3-small",
          "description": "Embedding model identifier — must match a model your chosen embedding model_provider serves (e.g. `text-embedding-3-small` for OpenAI). Changing this invalidates existing embeddings: the change is detected at startup and stale vectors are cleared automatically; run `zeroclaw memory reindex` to re-embed (or set `auto_reindex_on_identity_change`).",
          "type": "string"
        },
        "embedding_provider": {
          "default": "none",
          "description": "Source of embedding vectors for semantic search. `none` = keyword-only retrieval (no API calls, no vector cost); `openai` = OpenAI's embedding API; `custom:URL` = any OpenAI-compatible embedding endpoint (LiteLLM, local gateway, etc.).",
          "type": "string"
        },
        "evict_order": {
          "$ref": "#/$defs/MemoryEvictOrder",
          "default": "value",
          "description": "Eviction ordering for budget compaction."
        },
        "fts_early_return_score": {
          "default": 0.85,
          "description": "Reserved (0.0-1.0): the FTS score above which recall would skip the\nvector stage. Inert until the backend exposes distinct FTS and vector\noperations; recall is a single hybrid call today, so this has no effect.",
          "format": "double",
          "type": "number"
        },
        "hygiene_enabled": {
          "default": true,
          "description": "Run the periodic hygiene pass that archives stale daily/session files and enforces retention windows. Leave on unless you want to manage cleanup yourself.",
          "type": "boolean"
        },
        "importance_weight": {
          "default": 0.2,
          "description": "Importance weight used by the recall blend.",
          "format": "double",
          "type": "number"
        },
        "keyword_weight": {
          "default": 0.3,
          "description": "How heavily BM25 (keyword) overlap counts when `search_mode = hybrid`. Raise toward 1.0 for exact-term matching; lower it when paraphrases should still score well.",
          "format": "double",
          "type": "number"
        },
        "min_relevance_score": {
          "default": 0.4,
          "description": "Minimum hybrid score (0.0–1.0) for a memory to be included in context.\nMemories scoring below this threshold are dropped to prevent irrelevant\ncontext from bleeding into conversations. Default: 0.4",
          "format": "double",
          "type": "number"
        },
        "mmr_lambda": {
          "default": 0.7,
          "description": "MMR relevance-vs-diversity weight, where 1.0 means relevance-only.",
          "format": "double",
          "type": "number"
        },
        "pin_min_importance": {
          "default": 1.01,
          "description": "Pin entries at or above this importance. >1.0 means disabled.",
          "format": "double",
          "type": "number"
        },
        "pin_namespaces": {
          "default": [],
          "description": "Namespaces protected from budget eviction.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "policy": {
          "$ref": "#/$defs/MemoryPolicyConfig",
          "default": {
            "max_entries_per_category": 0,
            "max_entries_per_namespace": 0,
            "read_only_namespaces": [],
            "redact_categories": [
              "secret",
              "api_key",
              "private_key",
              "email",
              "phone"
            ],
            "redact_on_write": false,
            "retention_days_by_category": {},
            "threat_scan": "on",
            "threat_scan_load_time": true,
            "threat_scan_on_hit": "reject"
          },
          "description": "Memory policy configuration."
        },
        "purge_after_days": {
          "default": 30,
          "description": "Delete archived files permanently after this many days. Set high if you need long-term history; set low for privacy / disk-space reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "recency_weight": {
          "default": 0.1,
          "description": "Recency weight used by the recall blend.",
          "format": "double",
          "type": "number"
        },
        "rerank_enabled": {
          "default": false,
          "description": "Enable the recall rerank stage: blend retrieval score with importance\nand recency, collapse near-duplicate entries, then trim back to the\nrecall limit. The advanced strategy below runs when the candidate\ncount reaches `rerank_threshold`.",
          "type": "boolean"
        },
        "rerank_strategy": {
          "default": "none",
          "description": "Advanced rerank strategy. Valid: \"none\", \"mmr\".",
          "type": "string"
        },
        "rerank_threshold": {
          "default": 5,
          "description": "Minimum candidate count to trigger the advanced rerank strategy.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "response_cache_enabled": {
          "default": false,
          "description": "Enable LLM response caching to avoid paying for duplicate prompts",
          "type": "boolean"
        },
        "response_cache_hot_entries": {
          "default": 256,
          "description": "Max in-memory hot cache entries for the two-tier response cache (default: 256)",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "response_cache_max_entries": {
          "default": 5000,
          "description": "Max number of cached responses before LRU eviction (default: 5000)",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "response_cache_ttl_minutes": {
          "default": 60,
          "description": "TTL in minutes for cached responses (default: 60)",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "retrieval_stages": {
          "default": [
            "fts",
            "vector"
          ],
          "description": "Retrieval stages for per-agent recall. Only `\"cache\"` is active: it\nenables an in-process, per-handle hot cache over recall results and is\nomitted from the default so recall stays coherent across handles.\n`\"fts\"` and `\"vector\"` are reserved for when the backend exposes\ndistinct FTS and vector operations; recall is a single hybrid backend\ncall today, so those names have no effect (kept for forward compat).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "search_mode": {
          "$ref": "#/$defs/SearchMode",
          "default": "hybrid",
          "description": "How memories are retrieved: `bm25` = keyword-only (no embeddings, cheapest); `embedding` = vector similarity only (needs an embedding model_provider); `hybrid` = blended keyword + vector score using the weights above (most robust)."
        },
        "snapshot_enabled": {
          "default": false,
          "description": "Enable periodic export of core memories to MEMORY_SNAPSHOT.md",
          "type": "boolean"
        },
        "snapshot_on_hygiene": {
          "default": false,
          "description": "Run snapshot during hygiene passes (heartbeat-driven)",
          "type": "boolean"
        },
        "types": {
          "$ref": "#/$defs/MemoryTypesConfig",
          "default": {
            "enabled": false
          },
          "description": "Typed memory configuration (`[memory.types]` section)."
        },
        "vector_weight": {
          "default": 0.7,
          "description": "How heavily vector (semantic) similarity counts when `search_mode = hybrid`. Raise toward 1.0 to favor meaning-based matches; lower it to lean on keyword overlap instead.",
          "format": "double",
          "type": "number"
        }
      },
      "type": "object"
    },
    "MemoryDedupAction": {
      "description": "Write-time duplicate handling policy for memory entries.",
      "oneOf": [
        {
          "const": "reject",
          "description": "Reject an incoming near-duplicate.",
          "type": "string"
        },
        {
          "const": "merge",
          "description": "Merge an incoming near-duplicate into a survivor.",
          "type": "string"
        }
      ]
    },
    "MemoryEvictOrder": {
      "description": "Memory budget eviction order.",
      "oneOf": [
        {
          "const": "value",
          "description": "Evict the lowest-value rows first.",
          "type": "string"
        },
        {
          "const": "oldest",
          "description": "Evict the oldest rows first.",
          "type": "string"
        }
      ]
    },
    "MemoryPolicyConfig": {
      "description": "Memory policy configuration (`[memory.policy]` section).",
      "properties": {
        "max_entries_per_category": {
          "default": 0,
          "description": "Maximum entries per category (0 = unlimited).",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_entries_per_namespace": {
          "default": 0,
          "description": "Maximum entries per namespace (0 = unlimited).",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "read_only_namespaces": {
          "default": [],
          "description": "Namespaces that are read-only (writes are rejected).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "redact_categories": {
          "default": [
            "secret",
            "api_key",
            "private_key",
            "email",
            "phone"
          ],
          "description": "Redaction categories applied when `redact_on_write` is true.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "redact_on_write": {
          "default": false,
          "description": "Redact configured secret/PII categories before persistence.",
          "type": "boolean"
        },
        "retention_days_by_category": {
          "additionalProperties": {
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "default": {},
          "description": "Retention days by category (overrides global). Keys: \"core\", \"daily\", \"conversation\".",
          "type": "object"
        },
        "threat_scan": {
          "default": "on",
          "description": "Content scan mode for durable memory writes: \"off\", \"on\", or \"strict\".",
          "type": "string"
        },
        "threat_scan_load_time": {
          "default": true,
          "description": "Re-scan stored entries at recall/read time and withhold flagged entries.",
          "type": "boolean"
        },
        "threat_scan_on_hit": {
          "default": "reject",
          "description": "Behavior when a write-time content scan matches: \"reject\" or\n\"block-on-read\".",
          "type": "string"
        }
      },
      "type": "object"
    },
    "MemoryTypesConfig": {
      "description": "Typed memory configuration (`[memory.types]` section).\n\nBehaviour-neutral by default: `enabled` gates MemoryKind assignment on new\nconsolidation writes and defaults off; the flip is sequenced in a later\nphase. SQLite-only: enabling requires the sqlite memory backend globally\nand on every agent (validated at config load).",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Assign a first-class MemoryKind to new consolidation writes.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "Microsoft365Config": {
      "description": "Microsoft 365 integration via Microsoft Graph API (`[microsoft365]` section).\n\nProvides access to Outlook mail, Teams messages, Calendar events,\nOneDrive files, and SharePoint search.",
      "properties": {
        "auth_flow": {
          "default": "client_credentials",
          "description": "Authentication flow: \"client_credentials\" or \"device_code\"",
          "type": "string"
        },
        "client_id": {
          "default": null,
          "description": "Azure AD application (client) ID",
          "type": [
            "string",
            "null"
          ]
        },
        "client_secret": {
          "default": null,
          "description": "Azure AD client secret (stored encrypted when secrets.encrypt = true)",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "enabled": {
          "default": false,
          "description": "Enable Microsoft 365 integration",
          "type": "boolean"
        },
        "scopes": {
          "default": [
            "https://graph.microsoft.com/.default"
          ],
          "description": "OAuth scopes to request",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tenant_id": {
          "default": null,
          "description": "Azure AD tenant ID",
          "type": [
            "string",
            "null"
          ]
        },
        "token_cache_encrypted": {
          "default": true,
          "description": "Encrypt the token cache file on disk",
          "type": "boolean"
        },
        "user_id": {
          "default": null,
          "description": "User principal name or \"me\" (for delegated flows)",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "MinimaxEndpoint": {
      "enum": [
        "cn",
        "intl"
      ],
      "type": "string"
    },
    "MinimaxModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "auth_mode": {
          "anyOf": [
            {
              "$ref": "#/$defs/AuthMode"
            },
            {
              "type": "null"
            }
          ]
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "endpoint": {
          "$ref": "#/$defs/MinimaxEndpoint",
          "default": "intl"
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "oauth_client_id": {
          "description": "Override of MiniMax's published OAuth client_id. Most operators\nshould leave this unset; the runtime defaults to the\nvendor-published client_id (same one MiniMax's own portal uses).",
          "type": [
            "string",
            "null"
          ]
        },
        "oauth_refresh_token": {
          "description": "Long-lived OAuth refresh token issued by MiniMax. When set, the\nruntime exchanges it for a short-lived access token at provider\nconstruction time and uses that as the API credential. Operators\nwho prefer dashboard-generated long-lived API keys can leave this\nunset and populate `api_key` directly.",
          "type": [
            "string",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "MistralModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "MochatConfig": {
      "description": "Mochat channel configuration (Mochat customer service API)",
      "properties": {
        "api_token": {
          "description": "Mochat API token",
          "type": "string",
          "x-secret": true
        },
        "api_url": {
          "description": "Mochat API base URL",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "poll_interval_secs": {
          "default": 5,
          "description": "Poll interval in seconds for new messages. Default: 5",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "api_url",
        "api_token"
      ],
      "type": "object"
    },
    "ModelCostRates": {
      "description": "Token-cost rates for a single chat / completion model, in USD per\n1M tokens. Every field optional so partial sheets work without\nceremony (an operator who only knows the input rate can record it).",
      "properties": {
        "cached_input_per_mtok": {
          "description": "Cached input tokens (USD per 1M). Optional — leave unset on\nproviders that don't charge separately for prompt cache hits.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "input_per_mtok": {
          "description": "Input tokens (USD per 1M).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "output_per_mtok": {
          "description": "Output tokens (USD per 1M).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ModelCostRatesByProvider": {
      "description": "`[cost.rates.providers.models.<type>.<model>]` — token-cost rates\nper (provider type, model). One slot per provider type; each\nslot is a `HashMap<model_id, ModelCostRates>`. The slot list\nmatches `ModelProviders` byte-for-byte (same source macro).",
      "properties": {
        "ai21": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "aihubmix": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "anthropic": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "anyscale": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "arcee": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "astrai": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "atomic_chat": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "avian": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "azure": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "baichuan": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "baseten": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "bedrock": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "cerebras": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "cloudflare": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "cohere": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "copilot": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "custom": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "deepinfra": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "deepmyst": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "deepseek": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "doubao": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "featherless": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "fireworks": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "friendli": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "gemini": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "gemini_cli": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "github_models": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "glm": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "groq": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "huggingface": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "hunyuan": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "hyperbolic": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "inception": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "kilo": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "kilocli": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "lambda_ai": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "lepton": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "litellm": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "llamacpp": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "lmstudio": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "manifest": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "minimax": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "mistral": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "moonshot": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "morph": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "nearai": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "nebius": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "novita": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "nscale": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "nvidia": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "ollama": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "openai": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "opencode": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "openrouter": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "osaurus": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "ovh": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "perplexity": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "qianfan": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "qwen": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "reka": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "sambanova": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "sglang": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "siliconflow": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "stepfun": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "synthetic": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "telnyx": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "together": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "upstage": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "venice": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "vercel": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "vllm": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "xai": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "yi": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        },
        "zai": {
          "additionalProperties": {
            "$ref": "#/$defs/ModelCostRates"
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "ModelProviderRef": {
      "description": "Reference to a configured `[providers.models.<type>.<alias>]` entry.\nEmpty value means \"no preference\" (opt-out). Non-empty values must\nresolve to a configured alias; `Config::validate()` enforces this.",
      "type": "string"
    },
    "ModelProviders": {
      "description": "Typed model provider container with one alias map per provider family.",
      "properties": {
        "ai21": {
          "additionalProperties": {
            "$ref": "#/$defs/Ai21ModelProviderConfig"
          },
          "type": "object"
        },
        "aihubmix": {
          "additionalProperties": {
            "$ref": "#/$defs/AihubmixModelProviderConfig"
          },
          "type": "object"
        },
        "anthropic": {
          "additionalProperties": {
            "$ref": "#/$defs/AnthropicModelProviderConfig"
          },
          "type": "object"
        },
        "anyscale": {
          "additionalProperties": {
            "$ref": "#/$defs/AnyscaleModelProviderConfig"
          },
          "type": "object"
        },
        "arcee": {
          "additionalProperties": {
            "$ref": "#/$defs/ArceeModelProviderConfig"
          },
          "type": "object"
        },
        "astrai": {
          "additionalProperties": {
            "$ref": "#/$defs/AstraiModelProviderConfig"
          },
          "type": "object"
        },
        "atomic_chat": {
          "additionalProperties": {
            "$ref": "#/$defs/AtomicChatModelProviderConfig"
          },
          "type": "object"
        },
        "avian": {
          "additionalProperties": {
            "$ref": "#/$defs/AvianModelProviderConfig"
          },
          "type": "object"
        },
        "azure": {
          "additionalProperties": {
            "$ref": "#/$defs/AzureModelProviderConfig"
          },
          "type": "object"
        },
        "baichuan": {
          "additionalProperties": {
            "$ref": "#/$defs/BaichuanModelProviderConfig"
          },
          "type": "object"
        },
        "baseten": {
          "additionalProperties": {
            "$ref": "#/$defs/BasetenModelProviderConfig"
          },
          "type": "object"
        },
        "bedrock": {
          "additionalProperties": {
            "$ref": "#/$defs/BedrockModelProviderConfig"
          },
          "type": "object"
        },
        "cerebras": {
          "additionalProperties": {
            "$ref": "#/$defs/CerebrasModelProviderConfig"
          },
          "type": "object"
        },
        "cloudflare": {
          "additionalProperties": {
            "$ref": "#/$defs/CloudflareModelProviderConfig"
          },
          "type": "object"
        },
        "cohere": {
          "additionalProperties": {
            "$ref": "#/$defs/CohereModelProviderConfig"
          },
          "type": "object"
        },
        "copilot": {
          "additionalProperties": {
            "$ref": "#/$defs/CopilotModelProviderConfig"
          },
          "type": "object"
        },
        "custom": {
          "additionalProperties": {
            "$ref": "#/$defs/CustomModelProviderConfig"
          },
          "type": "object"
        },
        "deepinfra": {
          "additionalProperties": {
            "$ref": "#/$defs/DeepinfraModelProviderConfig"
          },
          "type": "object"
        },
        "deepmyst": {
          "additionalProperties": {
            "$ref": "#/$defs/DeepmystModelProviderConfig"
          },
          "type": "object"
        },
        "deepseek": {
          "additionalProperties": {
            "$ref": "#/$defs/DeepseekModelProviderConfig"
          },
          "type": "object"
        },
        "doubao": {
          "additionalProperties": {
            "$ref": "#/$defs/DoubaoModelProviderConfig"
          },
          "type": "object"
        },
        "featherless": {
          "additionalProperties": {
            "$ref": "#/$defs/FeatherlessModelProviderConfig"
          },
          "type": "object"
        },
        "fireworks": {
          "additionalProperties": {
            "$ref": "#/$defs/FireworksModelProviderConfig"
          },
          "type": "object"
        },
        "friendli": {
          "additionalProperties": {
            "$ref": "#/$defs/FriendliModelProviderConfig"
          },
          "type": "object"
        },
        "gemini": {
          "additionalProperties": {
            "$ref": "#/$defs/GeminiModelProviderConfig"
          },
          "type": "object"
        },
        "gemini_cli": {
          "additionalProperties": {
            "$ref": "#/$defs/GeminiCliModelProviderConfig"
          },
          "type": "object"
        },
        "github_models": {
          "additionalProperties": {
            "$ref": "#/$defs/GithubModelsModelProviderConfig"
          },
          "type": "object"
        },
        "glm": {
          "additionalProperties": {
            "$ref": "#/$defs/GlmModelProviderConfig"
          },
          "type": "object"
        },
        "groq": {
          "additionalProperties": {
            "$ref": "#/$defs/GroqModelProviderConfig"
          },
          "type": "object"
        },
        "huggingface": {
          "additionalProperties": {
            "$ref": "#/$defs/HuggingfaceModelProviderConfig"
          },
          "type": "object"
        },
        "hunyuan": {
          "additionalProperties": {
            "$ref": "#/$defs/HunyuanModelProviderConfig"
          },
          "type": "object"
        },
        "hyperbolic": {
          "additionalProperties": {
            "$ref": "#/$defs/HyperbolicModelProviderConfig"
          },
          "type": "object"
        },
        "inception": {
          "additionalProperties": {
            "$ref": "#/$defs/InceptionModelProviderConfig"
          },
          "type": "object"
        },
        "kilo": {
          "additionalProperties": {
            "$ref": "#/$defs/KiloModelProviderConfig"
          },
          "type": "object"
        },
        "kilocli": {
          "additionalProperties": {
            "$ref": "#/$defs/KiloCliModelProviderConfig"
          },
          "type": "object"
        },
        "lambda_ai": {
          "additionalProperties": {
            "$ref": "#/$defs/LambdaAiModelProviderConfig"
          },
          "type": "object"
        },
        "lepton": {
          "additionalProperties": {
            "$ref": "#/$defs/LeptonModelProviderConfig"
          },
          "type": "object"
        },
        "litellm": {
          "additionalProperties": {
            "$ref": "#/$defs/LitellmModelProviderConfig"
          },
          "type": "object"
        },
        "llamacpp": {
          "additionalProperties": {
            "$ref": "#/$defs/LlamacppModelProviderConfig"
          },
          "type": "object"
        },
        "lmstudio": {
          "additionalProperties": {
            "$ref": "#/$defs/LmstudioModelProviderConfig"
          },
          "type": "object"
        },
        "manifest": {
          "additionalProperties": {
            "$ref": "#/$defs/ManifestModelProviderConfig"
          },
          "type": "object"
        },
        "minimax": {
          "additionalProperties": {
            "$ref": "#/$defs/MinimaxModelProviderConfig"
          },
          "type": "object"
        },
        "mistral": {
          "additionalProperties": {
            "$ref": "#/$defs/MistralModelProviderConfig"
          },
          "type": "object"
        },
        "moonshot": {
          "additionalProperties": {
            "$ref": "#/$defs/MoonshotModelProviderConfig"
          },
          "type": "object"
        },
        "morph": {
          "additionalProperties": {
            "$ref": "#/$defs/MorphModelProviderConfig"
          },
          "type": "object"
        },
        "nearai": {
          "additionalProperties": {
            "$ref": "#/$defs/NearaiModelProviderConfig"
          },
          "type": "object"
        },
        "nebius": {
          "additionalProperties": {
            "$ref": "#/$defs/NebiusModelProviderConfig"
          },
          "type": "object"
        },
        "novita": {
          "additionalProperties": {
            "$ref": "#/$defs/NovitaModelProviderConfig"
          },
          "type": "object"
        },
        "nscale": {
          "additionalProperties": {
            "$ref": "#/$defs/NscaleModelProviderConfig"
          },
          "type": "object"
        },
        "nvidia": {
          "additionalProperties": {
            "$ref": "#/$defs/NvidiaModelProviderConfig"
          },
          "type": "object"
        },
        "ollama": {
          "additionalProperties": {
            "$ref": "#/$defs/OllamaModelProviderConfig"
          },
          "type": "object"
        },
        "openai": {
          "additionalProperties": {
            "$ref": "#/$defs/OpenAIModelProviderConfig"
          },
          "type": "object"
        },
        "opencode": {
          "additionalProperties": {
            "$ref": "#/$defs/OpencodeModelProviderConfig"
          },
          "type": "object"
        },
        "openrouter": {
          "additionalProperties": {
            "$ref": "#/$defs/OpenRouterModelProviderConfig"
          },
          "type": "object"
        },
        "osaurus": {
          "additionalProperties": {
            "$ref": "#/$defs/OsaurusModelProviderConfig"
          },
          "type": "object"
        },
        "ovh": {
          "additionalProperties": {
            "$ref": "#/$defs/OvhModelProviderConfig"
          },
          "type": "object"
        },
        "perplexity": {
          "additionalProperties": {
            "$ref": "#/$defs/PerplexityModelProviderConfig"
          },
          "type": "object"
        },
        "qianfan": {
          "additionalProperties": {
            "$ref": "#/$defs/QianfanModelProviderConfig"
          },
          "type": "object"
        },
        "qwen": {
          "additionalProperties": {
            "$ref": "#/$defs/QwenModelProviderConfig"
          },
          "type": "object"
        },
        "reka": {
          "additionalProperties": {
            "$ref": "#/$defs/RekaModelProviderConfig"
          },
          "type": "object"
        },
        "sambanova": {
          "additionalProperties": {
            "$ref": "#/$defs/SambanovaModelProviderConfig"
          },
          "type": "object"
        },
        "sglang": {
          "additionalProperties": {
            "$ref": "#/$defs/SglangModelProviderConfig"
          },
          "type": "object"
        },
        "siliconflow": {
          "additionalProperties": {
            "$ref": "#/$defs/SiliconflowModelProviderConfig"
          },
          "type": "object"
        },
        "stepfun": {
          "additionalProperties": {
            "$ref": "#/$defs/StepfunModelProviderConfig"
          },
          "type": "object"
        },
        "synthetic": {
          "additionalProperties": {
            "$ref": "#/$defs/SyntheticModelProviderConfig"
          },
          "type": "object"
        },
        "telnyx": {
          "additionalProperties": {
            "$ref": "#/$defs/TelnyxModelProviderConfig"
          },
          "type": "object"
        },
        "together": {
          "additionalProperties": {
            "$ref": "#/$defs/TogetherModelProviderConfig"
          },
          "type": "object"
        },
        "upstage": {
          "additionalProperties": {
            "$ref": "#/$defs/UpstageModelProviderConfig"
          },
          "type": "object"
        },
        "venice": {
          "additionalProperties": {
            "$ref": "#/$defs/VeniceModelProviderConfig"
          },
          "type": "object"
        },
        "vercel": {
          "additionalProperties": {
            "$ref": "#/$defs/VercelModelProviderConfig"
          },
          "type": "object"
        },
        "vllm": {
          "additionalProperties": {
            "$ref": "#/$defs/VllmModelProviderConfig"
          },
          "type": "object"
        },
        "xai": {
          "additionalProperties": {
            "$ref": "#/$defs/XaiModelProviderConfig"
          },
          "type": "object"
        },
        "yi": {
          "additionalProperties": {
            "$ref": "#/$defs/YiModelProviderConfig"
          },
          "type": "object"
        },
        "zai": {
          "additionalProperties": {
            "$ref": "#/$defs/ZaiModelProviderConfig"
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "ModelRouteConfig": {
      "description": "Route a task hint to a specific model_provider + model.\n\n```toml\n[[model_routes]]\nhint = \"reasoning\"\nmodel_provider = \"openrouter.default\"\nmodel = \"anthropic/claude-opus-4-20250514\"\n\n[[model_routes]]\nhint = \"fast\"\nmodel_provider = \"groq.low-latency\"\nmodel = \"llama-3.3-70b-versatile\"\n```\n\nUsage: pass `hint:reasoning` as the model parameter to route the request.",
      "properties": {
        "api_key": {
          "default": null,
          "description": "Optional API key override for this route's model provider",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "hint": {
          "default": "",
          "description": "Task hint name (e.g. \"reasoning\", \"fast\", \"code\", \"summarize\")\n`#[serde(default)]` lets the `create_map_key` macro default-construct\nfrom `{}` before the hint gets injected via `set_prop`. Empty strings\nare rejected by `Config::validate()`.",
          "type": "string"
        },
        "model": {
          "default": "",
          "description": "Provider-local model identifier to use with that provider profile\n`#[serde(default)]` is required for `Default` + `create_map_key` construction.\nEmpty strings are rejected by `Config::validate()`.",
          "type": "string"
        },
        "model_provider": {
          "default": "",
          "description": "Dotted provider profile ref to route to (must resolve to `providers.models.<type>.<alias>`)\n`#[serde(default)]` is required for `Default` + `create_map_key` construction.\nEmpty strings are rejected by `Config::validate()`.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "MoonshotEndpoint": {
      "description": "Moonshot endpoint variants. Operators pick the region that matches their\naccount; the runtime resolves the URI from the chosen variant unless\noverridden by `base.uri`. Code variant is intl-only.",
      "oneOf": [
        {
          "const": "cn",
          "description": "Mainland China endpoint.",
          "type": "string"
        },
        {
          "const": "intl",
          "description": "International endpoint.",
          "type": "string"
        },
        {
          "const": "code",
          "description": "Code-specialist endpoint (intl).",
          "type": "string"
        }
      ]
    },
    "MoonshotModelProviderConfig": {
      "description": "Moonshot model model_provider config. The `endpoint` field is required (no\nimplicit default) — operators must pick a region explicitly. Migration\nfills it in from collapsed `moonshot-cn` / `moonshot-intl` outer keys.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "endpoint": {
          "$ref": "#/$defs/MoonshotEndpoint",
          "default": "intl",
          "description": "Required: pick `cn`, `intl`, or `code`. Defaults to `intl` when omitted\nto ease transition; operators on the China endpoint should set\n`endpoint = \"cn\"` explicitly."
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "MorphModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "MqttConfig": {
      "description": "MQTT channel configuration (SOP listener).\n\nSubscribes to MQTT topics and dispatches incoming messages\nto the SOP engine for processing.",
      "properties": {
        "broker_url": {
          "description": "MQTT broker URL (e.g., `mqtt://localhost:1883` or `mqtts://broker.example.com:8883`).\nUse `mqtt://` for plain connections or `mqtts://` for TLS.",
          "type": "string"
        },
        "client_id": {
          "description": "MQTT client ID (must be unique per broker).",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "keep_alive_secs": {
          "default": 30,
          "description": "Keep-alive interval in seconds (default: 30). Prevents broker disconnect on idle.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "password": {
          "description": "Password for authentication (optional).",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "qos": {
          "default": 1,
          "description": "MQTT QoS level (0 = at-most-once, 1 = at-least-once, 2 = exactly-once). Default: 1.",
          "format": "uint8",
          "maximum": 255,
          "minimum": 0,
          "type": "integer"
        },
        "topics": {
          "default": [],
          "description": "Topics to subscribe to (e.g., `sensors/#`, `alerts/+/critical`).\nAt least one topic is required.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "use_tls": {
          "default": false,
          "description": "Enable TLS encryption. Must match the broker_url scheme:\n- `mqtt://` → `use_tls: false`\n- `mqtts://` → `use_tls: true`",
          "type": "boolean"
        },
        "username": {
          "description": "Username for authentication (optional).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "broker_url",
        "client_id"
      ],
      "type": "object"
    },
    "MultimodalConfig": {
      "description": "Multimodal (image) handling configuration (`[multimodal]` section).\n\n# Privacy and cost note\n\nTool results that print real local image paths (e.g. shell tools doing\n`ls /pictures` or `find . -name '*.png'`) are canonicalized into\n`[IMAGE:...]` markers and base64-inlined into the next provider request.\nThis means image bytes that previously stayed local will be uploaded to\nthe configured provider when surfaced by a tool.\n\n`max_images` (and the `trim_old_images` LRU policy) bounds the per-request\nimage budget, but operators running shell-style tools over directories of\npersonal or sensitive images should be aware of the upload semantics. See\n`docs/book/src/contributing/privacy.md` for the project's privacy stance.",
      "properties": {
        "allow_remote_fetch": {
          "default": false,
          "description": "Allow fetching remote image URLs (http/https). Disabled by default.",
          "type": "boolean"
        },
        "max_image_size_mb": {
          "default": 5,
          "description": "Maximum image payload size in MiB before base64 encoding.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_image_turns": {
          "default": 0,
          "description": "Maximum age of images in conversation turns.\n\nWhen non-zero, images in user messages that are more than this many\nturns back from the end of history are stripped before the request is\nsent to the provider. This prevents a single screenshot from being\nre-encoded and re-uploaded on every subsequent turn indefinitely.\nTool-result images are already managed by the stale-tool-result\nmechanism and are not affected by this setting.\n\n`0` (the default) disables age-based trimming entirely — images are\nonly evicted by the `max_images` count cap.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_images": {
          "default": 4,
          "description": "Maximum number of image attachments accepted per request.\n\nCaps the total number of `[IMAGE:...]` markers that survive into the\nprovider request after multimodal preprocessing. Older images are\ndropped first when the cumulative count exceeds this limit. Acts as\nthe upper bound on per-turn upload cost when tool outputs surface\nlocal image paths.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "vision_model": {
          "default": null,
          "description": "Model to use when routing to the vision model_provider (e.g. `\"llava:7b\"`).\nOnly used when `vision_model_provider` is set.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision_model_provider": {
          "default": null,
          "description": "ModelProvider name to use for vision/image messages (e.g. `\"ollama\"`).\nWhen set, messages containing `[IMAGE:]` markers are routed to this\nmodel_provider instead of the default text model_provider.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "NearaiModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "NebiusModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "NevisConfig": {
      "additionalProperties": false,
      "description": "Nevis IAM integration configuration.\n\nWhen `enabled` is true, ZeroClaw validates incoming requests against a Nevis\nSecurity Suite instance and maps Nevis roles to tool/workspace permissions.",
      "properties": {
        "client_id": {
          "default": "",
          "description": "OAuth2 client ID registered in Nevis.",
          "type": "string"
        },
        "client_secret": {
          "default": null,
          "description": "OAuth2 client secret. Encrypted via SecretStore when stored on disk.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "enabled": {
          "default": false,
          "description": "Enable Nevis IAM integration. Defaults to false for backward compatibility.",
          "type": "boolean"
        },
        "instance_url": {
          "default": "",
          "description": "Base URL of the Nevis instance (e.g. `https://nevis.example.com`).",
          "type": "string"
        },
        "jwks_url": {
          "default": null,
          "description": "JWKS endpoint URL for local token validation.",
          "type": [
            "string",
            "null"
          ]
        },
        "realm": {
          "default": "master",
          "description": "Nevis realm to authenticate against.",
          "type": "string"
        },
        "require_mfa": {
          "default": false,
          "description": "Require MFA verification for all Nevis-authenticated requests.",
          "type": "boolean"
        },
        "role_mapping": {
          "default": [],
          "description": "Nevis role to ZeroClaw permission mappings.",
          "items": {
            "$ref": "#/$defs/NevisRoleMappingConfig"
          },
          "type": "array"
        },
        "session_timeout_secs": {
          "default": 3600,
          "description": "Session timeout in seconds.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "token_validation": {
          "default": "local",
          "description": "Token validation strategy: `\"local\"` (JWKS) or `\"remote\"` (introspection).",
          "type": "string"
        }
      },
      "type": "object"
    },
    "NevisRoleMappingConfig": {
      "additionalProperties": false,
      "description": "Maps a Nevis role to ZeroClaw tool permissions and workspace access.",
      "properties": {
        "nevis_role": {
          "description": "Nevis role name (case-insensitive).",
          "type": "string"
        },
        "workspace_access": {
          "default": [],
          "description": "Workspace names this role can access. Use `\"all\"` for unrestricted.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "zeroclaw_permissions": {
          "default": [],
          "description": "Tool names this role can access. Use `\"all\"` for unrestricted tool access.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "nevis_role"
      ],
      "type": "object"
    },
    "NextcloudTalkConfig": {
      "description": "Nextcloud Talk bot configuration (webhook receive + signed bot send API).",
      "properties": {
        "app_token": {
          "default": null,
          "description": "Deprecated, unused. Nextcloud Talk sends do not authenticate via\nOCS bearer auth (see `webhook_secret`); this field is only accepted so\nexisting configs that set it don't fail to parse. Safe to remove\nfrom your config.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "base_url": {
          "description": "Nextcloud base URL (e.g. `\"https://cloud.example.com\"`).",
          "type": "string"
        },
        "bot_name": {
          "default": null,
          "description": "Display name of the bot in Nextcloud Talk (e.g. \"zeroclaw\").\nUsed to filter out the bot's own messages and prevent feedback loops.\nIf not set, defaults to an empty string (no self-message filtering by name).",
          "type": [
            "string",
            "null"
          ]
        },
        "bot_token": {
          "description": "DEPRECATED alias for `webhook_secret`, kept for migration.\n\nNextcloud issues ONE secret per installed bot and uses it for both\ndirections, so this cannot hold a different outbound secret. When both\nare set to different non-empty values the channel logs the conflict and\nfails closed as unconfigured: inbound `401` and no outbound send.\nPrefer `webhook_secret`; this alias will be removed.\n\nUpgrade from a `bot_token`-only configuration: copy the installed bot\nsecret to `webhook_secret`, verify replies still send, then delete\n`bot_token`.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "draft_update_interval_ms": {
          "default": 1000,
          "description": "Retained for configuration compatibility. Currently inert while draft\nupdates are disabled for this channel. Default: 1000 ms.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_url": {
          "default": null,
          "description": "Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",
          "type": [
            "string",
            "null"
          ]
        },
        "stream_mode": {
          "$ref": "#/$defs/StreamMode",
          "default": "off",
          "description": "Retained for configuration compatibility. Nextcloud Talk's bot API does\nnot provide message IDs or edit/delete operations, so draft updates are\ndisabled and responses are currently sent as one final message for every\nvalue."
        },
        "webhook_secret": {
          "default": null,
          "description": "The bot secret Nextcloud installed for this bot. Canonical field.\n\nUsed for BOTH directions: verifying inbound webhook signatures and\nsigning outbound bot-API requests. When it is unset, inbound webhooks\nare rejected and no outbound request is sent (fail closed).\n\nCan also be set via `ZEROCLAW_NEXTCLOUD_TALK_WEBHOOK_SECRET`.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        }
      },
      "required": [
        "base_url"
      ],
      "type": "object"
    },
    "NgrokTunnelConfig": {
      "properties": {
        "auth_token": {
          "default": "",
          "description": "ngrok auth token",
          "type": "string",
          "x-secret": true
        },
        "domain": {
          "default": null,
          "description": "Optional custom domain",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "NodeTransportConfig": {
      "description": "Secure transport configuration for inter-node communication (`[node_transport]`).",
      "properties": {
        "allowed_peers": {
          "default": [],
          "description": "Allow specific node IPs/CIDRs.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "connection_pool_size": {
          "default": 4,
          "description": "Maximum number of connections per peer.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "enabled": {
          "default": true,
          "description": "Enable the secure transport layer.",
          "type": "boolean"
        },
        "max_request_age_secs": {
          "default": 300,
          "description": "Maximum age of signed requests in seconds (replay protection).",
          "format": "int64",
          "type": "integer"
        },
        "mutual_tls": {
          "default": false,
          "description": "Require client certificates (mutual TLS).",
          "type": "boolean"
        },
        "require_https": {
          "default": true,
          "description": "Require HTTPS for all node communication.",
          "type": "boolean"
        },
        "shared_secret": {
          "default": "",
          "description": "Shared secret for HMAC authentication between nodes.",
          "type": "string",
          "x-secret": true
        },
        "tls_cert_path": {
          "default": null,
          "description": "Path to TLS certificate file.",
          "type": [
            "string",
            "null"
          ]
        },
        "tls_key_path": {
          "default": null,
          "description": "Path to TLS private key file.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "NodesConfig": {
      "description": "Configuration for the dynamic node discovery system (`[nodes]`).\n\nWhen enabled, external processes/devices can connect via WebSocket\nat `/ws/nodes` and advertise their capabilities at runtime.",
      "properties": {
        "auth_token": {
          "default": null,
          "description": "Optional bearer token for node authentication.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "enabled": {
          "default": false,
          "description": "Enable dynamic node discovery endpoint.",
          "type": "boolean"
        },
        "max_nodes": {
          "default": 16,
          "description": "Maximum number of concurrent node connections.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "mdns": {
          "$ref": "#/$defs/MdnsConfig",
          "default": {
            "announce_interval_secs": 30,
            "enabled": false,
            "max_peers": 16,
            "node_name": null,
            "peer_ttl_secs": 90
          },
          "description": "LAN-local mDNS peer discovery."
        }
      },
      "type": "object"
    },
    "NostrConfig": {
      "description": "Nostr channel configuration (NIP-04 + NIP-17 private messages)",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "private_key": {
          "description": "Private key in hex or nsec bech32 format",
          "type": "string",
          "x-secret": true
        },
        "relays": {
          "default": [
            "wss://relay.damus.io",
            "wss://nos.lol",
            "wss://relay.primal.net",
            "wss://relay.snort.social"
          ],
          "description": "Relay URLs (wss://). Defaults to popular public relays if omitted.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "private_key"
      ],
      "type": "object"
    },
    "NotionConfig": {
      "description": "Notion integration configuration (`[notion]`).\n\nWhen `enabled = true`, the agent polls a Notion database for pending tasks\nand exposes a `notion` tool for querying, reading, creating, and updating pages.\nRequires `api_key` (or the `NOTION_API_KEY` env var) and `database_id`.",
      "properties": {
        "api_key": {
          "default": "",
          "type": "string",
          "x-secret": true
        },
        "database_id": {
          "default": "",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "type": "boolean"
        },
        "input_property": {
          "default": "Input",
          "type": "string"
        },
        "max_concurrent": {
          "default": 4,
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "poll_interval_secs": {
          "default": 5,
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "recover_stale": {
          "default": true,
          "type": "boolean"
        },
        "result_property": {
          "default": "Result",
          "type": "string"
        },
        "status_property": {
          "default": "Status",
          "type": "string"
        }
      },
      "type": "object"
    },
    "NovitaModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "NscaleModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "NvidiaModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "ObservabilityBackend": {
      "description": "Observability sink backend.",
      "enum": [
        "none",
        "log",
        "verbose",
        "prometheus",
        "otel"
      ],
      "type": "string"
    },
    "ObservabilityConfig": {
      "description": "Observability backend configuration (`[observability]` section).",
      "properties": {
        "backend": {
          "$ref": "#/$defs/ObservabilityBackend",
          "default": "none",
          "description": "Observability sink: none | log | verbose | prometheus | otel."
        },
        "log_llm_request_payload": {
          "$ref": "#/$defs/LogLlmRequestPayload",
          "default": "off",
          "description": "LLM request payload capture: \"off\" | \"redacted\" | \"full\".\n- `off` (default): only `messages_count` lands on the `llm_request`\n  event. No prompt or conversation content is persisted.\n- `redacted`: the full message history (role + content) is leak-scanned\n  and truncated at `log_tool_io_truncate_bytes` before persisting.\n- `full`: full message history, still leak-scanned, untruncated. For\n  operators who need replay fidelity and accept the disk cost.\n\nOpt-in (default off) because `full`/`redacted` capture the entire system\nprompt and conversation on every turn."
        },
        "log_persistence": {
          "$ref": "#/$defs/LogPersistence",
          "default": "rolling",
          "description": "Log persistence mode: \"none\" | \"rolling\" | \"full\".\nControls whether every event passing through `zeroclaw_log::record!`\nis appended to the on-disk JSONL log."
        },
        "log_persistence_max_bytes": {
          "default": 0,
          "description": "Size threshold in bytes that triggers an archive rotation when\n`log_persistence = \"rotating\"`. The active file is rotated once an\nappend leaves it at or above this size. `0` disables size-based\nrotation. Ignored unless `log_persistence = \"rotating\"`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "log_persistence_max_entries": {
          "default": 200,
          "description": "Maximum entries retained when `log_persistence = \"rolling\"`.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "log_persistence_path": {
          "default": "state/runtime-trace.jsonl",
          "description": "Log persistence file path. Relative paths resolve under workspace_dir.",
          "type": "string"
        },
        "log_persistence_retention_max_age_days": {
          "default": 0,
          "description": "Retention cap on the age (in days) of rotated archive files when\n`log_persistence = \"rotating\"`. After a rotation, archives older than\nthis many days are deleted. `0` disables age-based cleanup. Ignored\nunless `log_persistence = \"rotating\"`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "log_persistence_retention_max_files": {
          "default": 7,
          "description": "Retention cap on the number of rotated archive files kept alongside the\nactive file when `log_persistence = \"rotating\"`. After a rotation the\noldest archives beyond this count are deleted. `0` keeps all archives.\nIgnored unless `log_persistence = \"rotating\"`.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "log_persistence_rotate_daily": {
          "default": true,
          "description": "Rotate the active file to an archive on a UTC day boundary when\n`log_persistence = \"rotating\"`: before the first event of a new day is\nappended, a file whose last write fell on an earlier day is archived.\nIgnored unless `log_persistence = \"rotating\"`.",
          "type": "boolean"
        },
        "log_tool_io": {
          "$ref": "#/$defs/LogToolIo",
          "default": "redacted",
          "description": "Tool I/O capture policy: \"off\" | \"redacted\" | \"full\".\n- `off`: only tool name + outcome + duration land in the log.\n- `redacted` (default): tool input + output are leak-scanned and\n  truncated at `log_tool_io_truncate_bytes` before persisting.\n- `full`: full input + output, still leak-scanned. For operators\n  who need replay fidelity and accept the disk cost."
        },
        "log_tool_io_denylist": {
          "default": [],
          "description": "Tool names whose I/O is never logged beyond name + outcome + duration\nregardless of `log_tool_io`. Use for tools whose I/O is intrinsically\nsensitive (e.g. memory recall against personal namespaces, agent\nsecret reads). Empty by default.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "log_tool_io_truncate_bytes": {
          "default": 40960,
          "description": "Truncate the captured tool input and output at this many bytes when\n`log_tool_io = \"redacted\"`. Truncated events carry an explicit\n`tool_output_truncated: true` flag plus `tool_output_original_bytes`.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "otel_endpoint": {
          "default": null,
          "description": "OTLP endpoint (e.g. `\"http://localhost:4318\"`). Only used when backend = `\"otel\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "otel_genai_content": {
          "$ref": "#/$defs/OtelContentPolicy",
          "default": "off",
          "description": "OTel GenAI content capture: \"off\" | \"redacted\" | \"full\".\nControls whether `gen_ai.system_instructions`, `gen_ai.input.messages`,\nand `gen_ai.output.messages` are emitted on OTel spans.\n- `off` (default): no content attributes, only metadata.\n- `redacted`: content is leak-scanned and truncated at `otel_genai_content_max_chars`.\n- `full`: content is leak-scanned but not truncated."
        },
        "otel_genai_content_max_chars": {
          "default": 1000,
          "description": "Per-field character truncation limit for OTel GenAI content when\n`otel_genai_content = \"redacted\"`. Each string field is truncated\nindependently. `0` is treated as `off`.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "otel_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "default": null,
          "description": "Optional HTTP headers sent with every OTLP export request (e.g. authorization).\nSpecified as key-value pairs in TOML:\n```toml\n[observability.otel_headers]\nAuthorization = \"Bearer sk-...\"\n```",
          "type": [
            "object",
            "null"
          ],
          "x-secret": true
        },
        "otel_service_name": {
          "default": null,
          "description": "Service name reported to the OTel collector. Defaults to \"zeroclaw\".",
          "type": [
            "string",
            "null"
          ]
        },
        "otel_tool_io": {
          "$ref": "#/$defs/OtelContentPolicy",
          "default": "off",
          "description": "OTel tool I/O capture: \"off\" | \"redacted\" | \"full\".\nControls whether `gen_ai.tool.arguments`, `input.value`,\n`gen_ai.tool.result`, and `output.value` are emitted on OTel spans.\n- `off` (default): no content attributes, only tool name + outcome.\n- `redacted`: content is leak-scanned and truncated at `otel_tool_io_max_chars`.\n- `full`: content is leak-scanned but not truncated."
        },
        "otel_tool_io_max_chars": {
          "default": 1000,
          "description": "Per-field character truncation limit for OTel tool I/O when\n`otel_tool_io = \"redacted\"`. Each string field is truncated\nindependently. `0` is treated as `off`.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "OllamaModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "num_ctx": {
          "description": "Override the Ollama `num_ctx` (context window, in tokens) sent on\nevery `/api/chat` request. Defaults to the framework constant\n(`OLLAMA_DEFAULT_NUM_CTX`) when unset.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "num_predict": {
          "description": "Override the Ollama `num_predict` (max output tokens) sent on every\n`/api/chat` request. Defaults to the framework constant\n(`OLLAMA_DEFAULT_NUM_PREDICT`) when unset.",
          "format": "int32",
          "type": [
            "integer",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "temperature_override": {
          "description": "Force every Ollama `/api/chat` request to use this temperature,\noverriding the per-call value passed through\n`ModelProvider::chat_with_system(.., temperature)`. When unset\n(`None`, the default), the per-call temperature wins: full\nbackward compatibility.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "OnNoApprover": {
      "description": "What to do when a configured approver cannot be reached. Default FAIL-CLOSED.",
      "oneOf": [
        {
          "const": "deny",
          "description": "Fail-closed: deny when the approver is unreachable / declines / times out.",
          "type": "string"
        },
        {
          "const": "inherit-originator",
          "description": "Explicit opt-in: fall back to the originating channel (today's behavior).",
          "type": "string"
        }
      ]
    },
    "OnboardStateConfig": {
      "description": "Multi-client workspace isolation configuration.\n\nWhen enabled, each client engagement gets an isolated workspace with\nseparate memory, audit, secrets, and tool restrictions.\nOpaque state the Quickstart flow writes so it can tell, on a\nre-run, which sections the user has already walked through at least\nonce — which lets it offer \"Reconfigure? [y/N]\" skip gates instead of\nforcing users through every field again.\n\nThis is meta-state about the Quickstart flow, not user-facing config.",
      "properties": {
        "completed_sections": {
          "default": [],
          "description": "Section keys the user has completed at least once.\nValues are the lowercased Section variant names\n(`\"workspace\"`, `\"model_providers\"`, …).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "quickstart_completed": {
          "default": false,
          "description": "`true` once the Quickstart has applied a `BuilderSubmission`\nsuccessfully on this install. Web gateway and TUI auto-launch\nthe Quickstart on startup iff this is `false` **and** no\n`agents.*` entries exist (the implicit-completion rule covers\nupgrades). The flag is flipped in the same atomic write that\nlands the Quickstart submission; re-entering the Quickstart\nlater to add another agent does not flip it back to `false`.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "OpenAIModelProviderConfig": {
      "description": "OpenAI model model_provider config. The OpenAI-family extras (`wire_api`,\n`requires_openai_auth`) live on the shared `ModelProviderConfig` base\nbecause they're consumed by validation and runtime helpers that operate\non the base struct without family awareness; this wrapper is a thin\ntyped slot, no extra fields.\n\nNew OpenAI provider entries **persisted via `create_map_key` / `ensure`**\n(quickstart, gateway/config UI, programmatic slot creation) default to\n`wire_api = \"responses\"` because OpenAI has moved recent GPT models onto\n`POST /v1/responses` as the primary wire. OpenAI-compatible families\n(`custom`, `llamacpp`, branded vendors, …) keep the shared\n`ModelProviderConfig` default of unset / chat-completions.\n\nThis default governs **persisted slot creation only**. Two paths keep the\nlegacy chat-completions wire for backward compatibility:\n- Existing persisted configs that omit `wire_api` deserialize as `None`, and\n  the factory falls back to chat-completions.\n- Implicit dispatch with no config entry at all — a bare\n  `model_provider = \"openai\"` reference or a dotted ref to a nonexistent\n  alias — is built from a chat-anchored fallback config (see\n  `openai_missing_entry_fallback_config` in `zeroclaw-providers`), not this\n  `Default`, and stays on the chat wire so existing bare-ref installs don't\n  flip wire + tool-calling mode on upgrade.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "OpenAITtsProviderConfig": {
      "description": "Per-instance TTS model_provider configuration (`[tts_providers.<type>.<alias>]`).\n\nMirrors `ModelProviderConfig` in shape — one struct holds the union of\nfields across backends. Only the fields relevant to the selected backend\n(determined by the outer `<type>` map key) are read at runtime; others\nare quietly ignored.",
      "properties": {
        "api_key": {
          "default": null,
          "description": "API key (openai, elevenlabs, google).",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "binary_path": {
          "default": null,
          "description": "Path to backend binary (edge-tts subprocess; piper local server).",
          "type": [
            "string",
            "null"
          ]
        },
        "language_code": {
          "default": null,
          "description": "Language code for google (e.g. `en-US`).",
          "type": [
            "string",
            "null"
          ]
        },
        "model": {
          "default": null,
          "description": "Model name. OpenAI uses this for `tts-1`/`tts-1-hd`; elevenlabs uses\nit as the model_id (e.g. `eleven_monolingual_v1`).",
          "type": [
            "string",
            "null"
          ]
        },
        "response_format": {
          "default": null,
          "description": "Audio response format sent to the TTS backend (e.g. `\"opus\"`, `\"mp3\"`,\n`\"wav\"`). Defaults to `\"opus\"` for the OpenAI family. Override to\n`\"wav\"` for Orpheus-class models (e.g. `canopylabs/orpheus-v1-english`\non Groq) or `\"mp3\"` for broader compatibility.",
          "type": [
            "string",
            "null"
          ]
        },
        "similarity_boost": {
          "default": null,
          "description": "Similarity boost for elevenlabs (0.0-1.0; default `0.5`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "speed": {
          "default": null,
          "description": "Playback speed multiplier (openai only; default `1.0`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "stability": {
          "default": null,
          "description": "Voice stability for elevenlabs (0.0-1.0; default `0.5`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "uri": {
          "default": null,
          "description": "Endpoint URI for HTTP-based backends. Overrides the family default\nwhen pointing at a compatible third-party API (Groq, Azure, self-hosted\nproxies). Set to the **full** URL — there is no separate path-suffix\nfield. Renamed from `api_url` for parity with `ModelProviderConfig.uri`.",
          "type": [
            "string",
            "null"
          ]
        },
        "voice": {
          "default": null,
          "description": "Voice override for this instance. When empty, falls back to\n`[tts].default_voice`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "OpenAiSttConfig": {
      "description": "OpenAI Whisper STT model_provider configuration (`[transcription.openai]`).",
      "properties": {
        "api_key": {
          "default": null,
          "description": "OpenAI API key for Whisper transcription.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "model": {
          "default": "whisper-1",
          "description": "Whisper model name (default: \"whisper-1\").",
          "type": "string"
        }
      },
      "type": "object"
    },
    "OpenAiTranscriptionProviderConfig": {
      "description": "Shared base for cloud transcription providers. Each cloud family\ncomposes this via `#[serde(flatten)] base: TranscriptionProviderConfig`.",
      "properties": {
        "api_key": {
          "default": null,
          "description": "API key for the transcription provider.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "initial_prompt": {
          "default": null,
          "description": "Whisper-style initial prompt to bias the model toward expected\nvocabulary (proper nouns, technical terms). Provider-specific support;\nsilently ignored where not applicable.",
          "type": [
            "string",
            "null"
          ]
        },
        "language": {
          "default": null,
          "description": "Optional language hint passed to the provider (ISO-639-1 like `\"en\"` /\n`\"ru\"`, or BCP-47 like `\"en-US\"` for Google). Most providers auto-detect\nwhen this is unset.",
          "type": [
            "string",
            "null"
          ]
        },
        "model": {
          "default": null,
          "description": "Whisper model name (default: `\"whisper-1\"`).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "OpenCodeCliConfig": {
      "description": "OpenCode CLI tool configuration (`[opencode_cli]` section).\n\nDelegates coding tasks to the `opencode run` CLI. Authentication uses the\nbinary's own session by default — no API key needed unless\n`env_passthrough` includes provider-specific keys.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable the `opencode_cli` tool",
          "type": "boolean"
        },
        "env_passthrough": {
          "default": [],
          "description": "Extra env vars passed to the opencode subprocess",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_output_bytes": {
          "default": 2097152,
          "description": "Maximum output size in bytes (2MB default)",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "timeout_secs": {
          "default": 600,
          "description": "Maximum execution time in seconds (coding tasks can be long)",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "OpenRouterModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "OpenVpnTunnelConfig": {
      "description": "OpenVPN tunnel configuration (`[tunnel.openvpn]`).\n\nRequired when `tunnel.tunnel_provider = \"openvpn\"`. Omitting this section entirely\npreserves previous behavior. Setting `tunnel.tunnel_provider = \"none\"` (or removing\nthe `[tunnel.openvpn]` block) cleanly reverts to no-tunnel mode.\n\nDefaults: `connect_timeout_secs = 30`.",
      "properties": {
        "advertise_address": {
          "default": null,
          "description": "Advertised address once VPN is connected (e.g., `\"10.8.0.2:42617\"`).\nWhen omitted the tunnel falls back to `http://{local_host}:{local_port}`.",
          "type": [
            "string",
            "null"
          ]
        },
        "auth_file": {
          "default": null,
          "description": "Optional path to auth credentials file (`--auth-user-pass`).",
          "type": [
            "string",
            "null"
          ]
        },
        "config_file": {
          "description": "Path to `.ovpn` configuration file (must not be empty).",
          "type": "string"
        },
        "connect_timeout_secs": {
          "default": 30,
          "description": "Connection timeout in seconds (default: 30, must be > 0).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "extra_args": {
          "default": [],
          "description": "Extra openvpn CLI arguments forwarded verbatim.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "config_file"
      ],
      "type": "object"
    },
    "OpencodeModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "OsaurusModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "OtelContentPolicy": {
      "description": "OTel content capture policy. Mirrors [`LogToolIo`] but gates OTel span\nattribute emission, not log persistence. Defaults to `Off` for privacy.",
      "enum": [
        "off",
        "redacted",
        "full"
      ],
      "type": "string"
    },
    "OtpConfig": {
      "additionalProperties": false,
      "description": "Security OTP configuration.",
      "properties": {
        "cache_valid_secs": {
          "default": 300,
          "description": "Reuse window for recently validated OTP codes.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "challenge_max_attempts": {
          "default": 3,
          "description": "Maximum number of OTP challenge attempts before lockout.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "enabled": {
          "default": false,
          "description": "Enable OTP gating. Defaults to disabled for backward compatibility.",
          "type": "boolean"
        },
        "gated_actions": {
          "default": [
            "shell",
            "file_write",
            "browser_open",
            "browser",
            "memory_forget"
          ],
          "description": "Tool/action names gated by OTP. Empty or malformed entries are rejected\nat config load; an entry that does not match a known gated action is\naccepted but logged at WARN, since it cannot be enforced.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "gated_domain_categories": {
          "default": [],
          "description": "Domain-category presets expanded into `gated_domains`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "gated_domains": {
          "default": [],
          "description": "Explicit domain patterns gated by OTP.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "method": {
          "$ref": "#/$defs/OtpMethod",
          "default": "totp",
          "description": "OTP method."
        },
        "token_ttl_secs": {
          "default": 30,
          "description": "TOTP time-step in seconds.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "OtpMethod": {
      "description": "OTP validation strategy.",
      "oneOf": [
        {
          "const": "totp",
          "description": "Time-based one-time password (RFC 6238).",
          "type": "string"
        },
        {
          "const": "pairing",
          "description": "Future method for paired-device confirmations.",
          "type": "string"
        },
        {
          "const": "cli-prompt",
          "description": "Future method for local CLI challenge prompts.",
          "type": "string"
        }
      ]
    },
    "OutputModality": {
      "description": "Preferred output modality for a peer group.",
      "oneOf": [
        {
          "const": "mirror",
          "description": "Always reply in kind — voice note if user sent voice, text otherwise.",
          "type": "string"
        },
        {
          "const": "voice",
          "description": "Always deliver via TTS as a voice note, regardless of input modality.\nApplies to proactive messages (cron, announces) as well as replies.",
          "type": "string"
        },
        {
          "const": "text",
          "description": "Always deliver as text, even if user sent a voice note.",
          "type": "string"
        }
      ]
    },
    "OvhModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "PacingConfig": {
      "description": "Pacing controls for slow/local LLM workloads (`[pacing]` section).\n\nAll fields are optional and default to values that preserve existing\nbehavior. When set, they extend — not replace — the existing timeout\nand loop-detection subsystems.",
      "properties": {
        "loop_detection_enabled": {
          "default": true,
          "description": "Enable pattern-based loop detection (exact repeat, ping-pong,\nno-progress). Defaults to `true`.",
          "type": "boolean"
        },
        "loop_detection_max_repeats": {
          "default": 3,
          "description": "Number of consecutive identical tool+args calls before the first\nescalation (Warning). Defaults to 3.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "loop_detection_min_elapsed_secs": {
          "default": null,
          "description": "Minimum elapsed seconds before loop detection activates.\nTasks completing under this threshold get aggressive loop protection;\nlonger-running tasks receive a grace period before the detector starts\ncounting. `None` means loop detection is always active (existing behavior).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "loop_detection_window_size": {
          "default": 20,
          "description": "Sliding window size for the pattern-based loop detector.\nDefaults to 20.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "loop_ignore_tools": {
          "default": [],
          "description": "Tool names excluded from identical-output / alternating-pattern loop\ndetection. Useful for browser workflows where `browser_screenshot`\nstructurally resembles a loop even when making progress.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "message_timeout_scale_max": {
          "default": null,
          "description": "Override for the hardcoded timeout scaling cap (default: 4).\nThe channel message timeout budget is computed as:\n  `message_timeout_secs * min(max_tool_iterations, message_timeout_scale_max)`\nRaising this value lets long multi-step tasks with slow local models\nreceive a proportionally larger budget without inflating the base timeout.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "step_timeout_secs": {
          "default": null,
          "description": "Per-step timeout in seconds: the maximum time allowed for a single\nLLM inference turn, independent of the total message budget.\n`None` means no per-step timeout (existing behavior).",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "PairingDashboardConfig": {
      "description": "Pairing dashboard configuration (`[gateway.pairing_dashboard]`).",
      "properties": {
        "code_length": {
          "default": 8,
          "description": "Length of pairing codes (default: 8)",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "code_ttl_secs": {
          "default": 3600,
          "description": "Time-to-live for pending pairing codes in seconds (default: 3600)",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "lockout_secs": {
          "default": 300,
          "description": "Lockout duration in seconds after max attempts (default: 300)",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "max_failed_attempts": {
          "default": 5,
          "description": "Maximum failed pairing attempts before lockout (default: 5)",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "max_pending_codes": {
          "default": 3,
          "description": "Maximum concurrent pending pairing codes (default: 3)",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "PeerGroupConfig": {
      "description": "`[peer_groups.<name>]` — mutual-opt-in peer group on a channel type.",
      "properties": {
        "admin_for_agent_scope": {
          "default": false,
          "description": "When `true`, members of this peer group are authorized to issue\n`/model --agent <model>` on the agent this group is bound to.\nDefault `false` (deny-by-default). The runtime resolves this live\nfrom `Config::peer_groups` at command-dispatch time via\n`Config::channel_agent_scope_admins`; no cache, no per-channel\nduplicate sender list.",
          "type": "boolean"
        },
        "agents": {
          "default": [],
          "description": "Member agents by alias.",
          "items": {
            "$ref": "#/$defs/AgentAlias"
          },
          "type": "array"
        },
        "channel": {
          "$ref": "#/$defs/ChannelRef",
          "default": "",
          "description": "Either a channel type (`\"telegram\"`) or a dotted channel alias\n(`\"telegram.work\"`). A bare type applies to every alias of that\ntype; a dotted form scopes the group to that single instance."
        },
        "external_peers": {
          "default": [],
          "description": "Non-agent members by channel-native username.",
          "items": {
            "$ref": "#/$defs/PeerUsername"
          },
          "type": "array"
        },
        "ignore": {
          "default": [],
          "description": "Per-group blocklist; subtracts from the resolved peer set.",
          "items": {
            "$ref": "#/$defs/PeerUsername"
          },
          "type": "array"
        },
        "output_modality": {
          "$ref": "#/$defs/OutputModality",
          "default": "mirror",
          "description": "Preferred output modality for all peers in this group.\nDefaults to `mirror` (input-driven). Set to `voice` to have the\nagent always reply and deliver proactive messages (cron, announces)\nas TTS voice notes on channels that support audio output."
        }
      },
      "type": "object"
    },
    "PeerUsername": {
      "description": "Reference to a configured `[channels.peers.<type>.<alias>]` entry.\nEmpty value means \"no preference\" (opt-out). Non-empty values must\nresolve to a configured alias; `Config::validate()` enforces this.",
      "type": "string"
    },
    "PeripheralBoardConfig": {
      "description": "Configuration for a single peripheral board (e.g. STM32, RPi GPIO).",
      "properties": {
        "baud": {
          "default": 115200,
          "description": "Baud rate for serial (default: 115200)",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "board": {
          "description": "Board type: \"nucleo-f401re\", \"rpi-gpio\", \"esp32\", etc.",
          "type": "string"
        },
        "path": {
          "default": null,
          "description": "Path for serial: \"/dev/ttyACM0\", \"/dev/ttyUSB0\"",
          "type": [
            "string",
            "null"
          ]
        },
        "transport": {
          "default": "serial",
          "description": "Transport: \"serial\", \"native\", \"websocket\"",
          "type": "string"
        }
      },
      "required": [
        "board"
      ],
      "type": "object"
    },
    "PeripheralsConfig": {
      "description": "Peripheral board integration configuration (`[peripherals]` section).\n\nBoards become agent tools when enabled.",
      "properties": {
        "boards": {
          "default": [],
          "description": "Board configurations (nucleo-f401re, rpi-gpio, etc.)",
          "items": {
            "$ref": "#/$defs/PeripheralBoardConfig"
          },
          "type": "array"
        },
        "datasheet_dir": {
          "default": null,
          "description": "Path to datasheet docs (relative to workspace) for RAG retrieval.\nPlace .md/.txt files named by board (e.g. nucleo-f401re.md, rpi-gpio.md).",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "default": false,
          "description": "Enable peripheral support (boards become agent tools)",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "PerplexityModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "PinggyTunnelConfig": {
      "properties": {
        "region": {
          "default": null,
          "description": "Server region: `\"us\"` (USA), `\"eu\"` (Europe), `\"ap\"` (Asia), `\"br\"` (South America), `\"au\"` (Australia), or omit for auto.",
          "type": [
            "string",
            "null"
          ]
        },
        "token": {
          "default": null,
          "description": "Pinggy access token (optional — free tier works without one).",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        }
      },
      "type": "object"
    },
    "PipelineConfig": {
      "description": "Pipeline tool configuration (`[pipeline]` section).",
      "properties": {
        "allowed_tools": {
          "default": [],
          "description": "Tools allowed in pipeline steps. Steps referencing tools not on this\nlist are rejected before execution.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "default": false,
          "description": "Enable the `execute_pipeline` meta-tool.\nDefault: `false`.",
          "type": "boolean"
        },
        "max_steps": {
          "default": 20,
          "description": "Maximum number of steps allowed in a single pipeline invocation.\nDefault: `20`.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "PiperTtsProviderConfig": {
      "description": "Per-instance TTS model_provider configuration (`[tts_providers.<type>.<alias>]`).\n\nMirrors `ModelProviderConfig` in shape — one struct holds the union of\nfields across backends. Only the fields relevant to the selected backend\n(determined by the outer `<type>` map key) are read at runtime; others\nare quietly ignored.",
      "properties": {
        "api_key": {
          "default": null,
          "description": "API key (openai, elevenlabs, google).",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "binary_path": {
          "default": null,
          "description": "Path to backend binary (edge-tts subprocess; piper local server).",
          "type": [
            "string",
            "null"
          ]
        },
        "language_code": {
          "default": null,
          "description": "Language code for google (e.g. `en-US`).",
          "type": [
            "string",
            "null"
          ]
        },
        "model": {
          "default": null,
          "description": "Model name. OpenAI uses this for `tts-1`/`tts-1-hd`; elevenlabs uses\nit as the model_id (e.g. `eleven_monolingual_v1`).",
          "type": [
            "string",
            "null"
          ]
        },
        "response_format": {
          "default": null,
          "description": "Audio response format sent to the TTS backend (e.g. `\"opus\"`, `\"mp3\"`,\n`\"wav\"`). Defaults to `\"opus\"` for the OpenAI family. Override to\n`\"wav\"` for Orpheus-class models (e.g. `canopylabs/orpheus-v1-english`\non Groq) or `\"mp3\"` for broader compatibility.",
          "type": [
            "string",
            "null"
          ]
        },
        "similarity_boost": {
          "default": null,
          "description": "Similarity boost for elevenlabs (0.0-1.0; default `0.5`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "speed": {
          "default": null,
          "description": "Playback speed multiplier (openai only; default `1.0`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "stability": {
          "default": null,
          "description": "Voice stability for elevenlabs (0.0-1.0; default `0.5`).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "uri": {
          "default": null,
          "description": "Endpoint URI for HTTP-based backends. Overrides the family default\nwhen pointing at a compatible third-party API (Groq, Azure, self-hosted\nproxies). Set to the **full** URL — there is no separate path-suffix\nfield. Renamed from `api_url` for parity with `ModelProviderConfig.uri`.",
          "type": [
            "string",
            "null"
          ]
        },
        "voice": {
          "default": null,
          "description": "Voice override for this instance. When empty, falls back to\n`[tts].default_voice`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "PluginEntryConfig": {
      "description": "Per-plugin config section keyed by plugin alias; values are secret so they\nencrypt at rest under the same adjacent `.secret_key` as every other secret.",
      "properties": {
        "config": {
          "additionalProperties": {
            "type": "string"
          },
          "default": {},
          "type": "object",
          "x-secret": true
        },
        "name": {
          "default": "",
          "type": "string"
        }
      },
      "type": "object"
    },
    "PluginLimitsConfig": {
      "description": "Per-call WASM execution limits (`[plugins.limits]`).\n\nBounds a single plugin call so a runaway or malicious component traps\ninstead of hanging the host or exhausting memory. `call_fuel` caps\ninstructions per call; the memory, table, and instance ceilings bound a\nstore's growth. Every value is operator-tunable and validated as non-zero.",
      "properties": {
        "call_fuel": {
          "default": 1000000000,
          "description": "Fuel budget per plugin call (wasmtime instruction units).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "max_instances": {
          "default": 64,
          "description": "Maximum component instances a plugin store may create.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_memory_mb": {
          "default": 256,
          "description": "Maximum linear memory a plugin store may grow to, in megabytes.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_table_elements": {
          "default": 100000,
          "description": "Maximum table elements a plugin store may allocate.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "PluginSecurityConfig": {
      "description": "Plugin signature verification configuration (`[plugins.security]`).\n\nControls Ed25519 signature verification for plugin manifests.\nIn `strict` mode, only plugins signed by a trusted publisher key are loaded.\nIn `permissive` mode, unsigned or untrusted plugins produce warnings but are\nstill loaded. In `disabled` mode (the default), no signature checking occurs.",
      "properties": {
        "signature_mode": {
          "default": "disabled",
          "description": "Signature enforcement mode: \"disabled\", \"permissive\", or \"strict\".",
          "type": "string"
        },
        "trusted_publisher_keys": {
          "default": [],
          "description": "Hex-encoded Ed25519 public keys of trusted plugin publishers.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "PluginsConfig": {
      "description": "Plugin system configuration.",
      "properties": {
        "auto_discover": {
          "default": false,
          "description": "Auto-discover and load plugins on startup",
          "type": "boolean"
        },
        "enabled": {
          "default": false,
          "description": "Enable the plugin system (default: false)",
          "type": "boolean"
        },
        "entries": {
          "default": [],
          "items": {
            "$ref": "#/$defs/PluginEntryConfig"
          },
          "type": "array"
        },
        "limits": {
          "$ref": "#/$defs/PluginLimitsConfig",
          "default": {
            "call_fuel": 1000000000,
            "max_instances": 64,
            "max_memory_mb": 256,
            "max_table_elements": 100000
          },
          "description": "Per-call WASM execution limits"
        },
        "max_plugins": {
          "default": 50,
          "description": "Maximum number of plugins that can be loaded",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "plugins_dir": {
          "default": "/home/jeremy/.zeroclaw/plugins",
          "description": "Directory where plugins are stored",
          "type": "string"
        },
        "security": {
          "$ref": "#/$defs/PluginSecurityConfig",
          "default": {
            "signature_mode": "disabled",
            "trusted_publisher_keys": []
          },
          "description": "Plugin signature verification security settings"
        }
      },
      "type": "object"
    },
    "PostgresStorageConfig": {
      "description": "PostgreSQL storage backend (`[storage.postgres.<alias>]`).\n\nHolds connection parameters AND pgvector settings on one alias-keyed\nentry; previously these lived in two separate sections.",
      "properties": {
        "connect_timeout_secs": {
          "default": null,
          "description": "Optional connection timeout in seconds.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "db_url": {
          "default": null,
          "description": "Connection URL (e.g. `\"postgres://user:pass@host/db\"`).\nAccepts legacy aliases: dbURL, database_url, databaseUrl.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "schema": {
          "default": "public",
          "description": "Database schema for the memory table.",
          "type": "string"
        },
        "table": {
          "default": "memories",
          "description": "Table name for memory entries.",
          "type": "string"
        },
        "vector_dimensions": {
          "default": 1536,
          "description": "Vector dimensions for pgvector embeddings.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "vector_enabled": {
          "default": false,
          "description": "Enable pgvector extension for hybrid vector+keyword recall.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "ProjectIntelConfig": {
      "description": "Project delivery intelligence configuration (`[project_intel]` section).",
      "properties": {
        "default_language": {
          "default": "en",
          "description": "Default report language (en, de, fr, it). Default: \"en\".",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Enable the project_intel tool. Default: false.",
          "type": "boolean"
        },
        "include_git_data": {
          "default": true,
          "description": "Include git log data in reports. Default: true.",
          "type": "boolean"
        },
        "include_jira_data": {
          "default": false,
          "description": "Include Jira data in reports. Default: false.",
          "type": "boolean"
        },
        "jira_base_url": {
          "default": null,
          "description": "Jira instance base URL (required if include_jira_data is true).",
          "type": [
            "string",
            "null"
          ]
        },
        "report_output_dir": {
          "default": "/home/jeremy/.zeroclaw/project-reports",
          "description": "Output directory for generated reports.",
          "type": "string"
        },
        "risk_sensitivity": {
          "default": "medium",
          "description": "Risk detection sensitivity: low, medium, high. Default: \"medium\".",
          "type": "string"
        },
        "templates_dir": {
          "default": null,
          "description": "Optional custom templates directory.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ProviderCostRates": {
      "description": "`[cost.rates.providers.*]` — provider-shaped rate sheets. Each field\nhere mirrors a corresponding field on `[providers.*]` with the\ntrailing alias segment replaced by the resource the rate prices.\nThe inner typed wrappers carry the per-provider-type slot layout\nand own dispatch (their slot list is the single source of truth,\nshared with their providers counterpart via the `for_each_*_provider_slot!`\nmacros in [`crate::providers`]).",
      "properties": {
        "models": {
          "$ref": "#/$defs/ModelCostRatesByProvider",
          "default": {},
          "description": "`[cost.rates.providers.models.<type>.<model>]`."
        },
        "transcription": {
          "$ref": "#/$defs/TranscriptionCostRatesByProvider",
          "default": {},
          "description": "`[cost.rates.providers.transcription.<type>.<model>]`."
        },
        "tts": {
          "$ref": "#/$defs/TtsCostRatesByProvider",
          "default": {},
          "description": "`[cost.rates.providers.tts.<type>.<voice>]`."
        }
      },
      "type": "object"
    },
    "Providers": {
      "description": "Top-level wrapper for every configured provider category.",
      "properties": {
        "models": {
          "$ref": "#/$defs/ModelProviders",
          "default": {},
          "description": "Model providers — `[providers.models.<type>.<alias>]`."
        },
        "transcription": {
          "$ref": "#/$defs/TranscriptionProviders",
          "default": {},
          "description": "Transcription / speech-to-text providers — `[providers.transcription.<type>.<alias>]`."
        },
        "tts": {
          "$ref": "#/$defs/TtsProviders",
          "default": {},
          "description": "Text-to-speech providers — `[providers.tts.<type>.<alias>]`."
        }
      },
      "type": "object"
    },
    "ProxyConfig": {
      "description": "Proxy configuration for outbound HTTP/HTTPS/SOCKS5 traffic (`[proxy]` section).",
      "properties": {
        "all_proxy": {
          "default": null,
          "description": "Fallback proxy URL for all schemes.",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "default": false,
          "description": "Enable proxy support for selected scope.",
          "type": "boolean"
        },
        "http_proxy": {
          "default": null,
          "description": "Proxy URL for HTTP requests (supports http, https, socks5, socks5h).",
          "type": [
            "string",
            "null"
          ]
        },
        "https_proxy": {
          "default": null,
          "description": "Proxy URL for HTTPS requests (supports http, https, socks5, socks5h).",
          "type": [
            "string",
            "null"
          ]
        },
        "no_proxy": {
          "default": [],
          "description": "No-proxy bypass list. Same format as NO_PROXY.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "scope": {
          "$ref": "#/$defs/ProxyScope",
          "default": "zeroclaw",
          "description": "Proxy application scope."
        },
        "services": {
          "default": [],
          "description": "Service selectors used when scope = \"services\".",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "ProxyScope": {
      "description": "Proxy application scope — determines which outbound traffic uses the proxy.",
      "oneOf": [
        {
          "const": "environment",
          "description": "Use system environment proxy variables only.",
          "type": "string"
        },
        {
          "const": "zeroclaw",
          "description": "Apply proxy to all ZeroClaw-managed HTTP traffic (default).",
          "type": "string"
        },
        {
          "const": "services",
          "description": "Apply proxy only to explicitly listed service selectors.",
          "type": "string"
        }
      ]
    },
    "QQConfig": {
      "description": "QQ Official Bot configuration (Tencent QQ Bot SDK)",
      "properties": {
        "app_id": {
          "description": "App ID from QQ Bot developer console",
          "type": "string"
        },
        "app_secret": {
          "description": "App Secret from QQ Bot developer console",
          "type": "string",
          "x-secret": true
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_url": {
          "default": null,
          "description": "Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "app_id",
        "app_secret"
      ],
      "type": "object"
    },
    "QdrantStorageConfig": {
      "description": "Qdrant vector database backend (`[storage.qdrant.<alias>]`).\n\nURL, collection, and API key are typed config values. Use schema-mirror env\noverrides such as `ZEROCLAW_storage__qdrant__<alias>__url=...` for runtime\nenv injection.",
      "properties": {
        "api_key": {
          "default": null,
          "description": "API key for Qdrant Cloud or secured instances.\nUse `ZEROCLAW_storage__qdrant__<alias>__api_key=...` for env injection.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "collection": {
          "default": "zeroclaw_memories",
          "description": "Collection name for storing memories.\nUse `ZEROCLAW_storage__qdrant__<alias>__collection=...` for env injection.",
          "type": "string"
        },
        "url": {
          "default": null,
          "description": "Qdrant server URL (e.g. `\"http://localhost:6333\"`).\nUse `ZEROCLAW_storage__qdrant__<alias>__url=...` for env injection.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "QianfanModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "QueryClassificationConfig": {
      "description": "Automatic query classification — classifies user messages by keyword/pattern\nand routes to the appropriate model hint. Disabled by default.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable automatic query classification. Default: `false`.",
          "type": "boolean"
        },
        "rules": {
          "default": [],
          "description": "Classification rules evaluated in priority order.",
          "items": {
            "$ref": "#/$defs/ClassificationRule"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "QwenEndpoint": {
      "description": "Qwen endpoint variants. Operators pick the region matching their account.",
      "oneOf": [
        {
          "const": "cn",
          "description": "Mainland China (DashScope).",
          "type": "string"
        },
        {
          "const": "intl",
          "description": "International (alicloud international).",
          "type": "string"
        },
        {
          "const": "us",
          "description": "United States (DashScope US).",
          "type": "string"
        },
        {
          "const": "code",
          "description": "Code-specialist endpoint.",
          "type": "string"
        }
      ]
    },
    "QwenModelProviderConfig": {
      "description": "Qwen model model_provider config. Multi-region (`endpoint` required) and\nsupports both API key and OAuth flows (`auth_mode` chooses which).",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "auth_mode": {
          "anyOf": [
            {
              "$ref": "#/$defs/AuthMode"
            },
            {
              "type": "null"
            }
          ],
          "description": "Auth flow. Defaults to `api_key`; set to `oauth` to use the vendor's\nOAuth-cache integration instead of the `api_key` field."
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "endpoint": {
          "$ref": "#/$defs/QwenEndpoint",
          "default": "intl"
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "oauth_client_id": {
          "description": "Override of Qwen's published OAuth client_id. Most operators\nshould leave this unset.",
          "type": [
            "string",
            "null"
          ]
        },
        "oauth_refresh_token": {
          "description": "Long-lived Qwen OAuth refresh token. When set, the runtime\nexchanges it for a short-lived access token at provider\nconstruction time. Operators relying on the upstream `qwen login`\ntool (which writes `~/.qwen/oauth_creds.json`) leave this unset;\nthe file-cache integration takes over.",
          "type": [
            "string",
            "null"
          ]
        },
        "oauth_resource_url": {
          "description": "Operator override of the resource URL the refreshed access token\nis paired with. When unset, the runtime falls back to the\n`endpoint`-derived URL (or the cached `resource_url` when reading\nfrom `~/.qwen/oauth_creds.json`).",
          "type": [
            "string",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "RedditConfig": {
      "description": "Reddit channel configuration (OAuth2 bot).",
      "properties": {
        "client_id": {
          "description": "Reddit OAuth2 client ID.",
          "type": "string"
        },
        "client_secret": {
          "description": "Reddit OAuth2 client secret.",
          "type": "string",
          "x-secret": true
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "refresh_token": {
          "description": "Reddit OAuth2 refresh token for persistent access.",
          "type": "string",
          "x-secret": true
        },
        "subreddits": {
          "default": [],
          "description": "Subreddits to filter messages (without `r/` prefix). Empty = accept\nfrom any subreddit the bot has access to. Migrated from the legacy\n`subreddit` singular field.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "username": {
          "description": "Reddit bot username (without `u/` prefix).",
          "type": "string"
        }
      },
      "required": [
        "client_id",
        "client_secret",
        "refresh_token",
        "username"
      ],
      "type": "object"
    },
    "RekaModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "ReliabilityConfig": {
      "description": "Reliability and supervision configuration (`[reliability]` section).\n\nControls model_provider retries, API key rotation, and channel restart backoff.",
      "properties": {
        "api_keys": {
          "default": [],
          "description": "Additional API keys for round-robin rotation on rate-limit (429) errors.\nThe primary `api_key` is always tried first; these are extras.",
          "items": {
            "type": "string"
          },
          "type": "array",
          "x-secret": true
        },
        "channel_initial_backoff_secs": {
          "default": 2,
          "description": "Initial backoff for channel/daemon restarts.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "channel_max_backoff_secs": {
          "default": 60,
          "description": "Max backoff for channel/daemon restarts.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "provider_backoff_ms": {
          "default": 500,
          "description": "Base backoff (ms) for model_provider retry delay.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "provider_retries": {
          "default": 2,
          "description": "Retries per model_provider before bailing.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "scheduler_poll_secs": {
          "default": 15,
          "description": "Scheduler polling cadence in seconds.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "scheduler_retries": {
          "default": 2,
          "description": "Max retries for cron job execution attempts.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "RiskProfileConfig": {
      "description": "Named risk/autonomy profile (`[risk_profiles.<alias>]`).\n\nUnified policy surface. Agents reference a profile by alias and the\nruntime resolves through it for shell command allowlists, approval gates,\nsandbox/resource limits, and delegation guardrails. The conventional\n`risk_profiles[\"default\"]` is the resolution target for non-agent\ncontexts (orchestrator init, cron worker startup); the `Default` impl\nbelow mirrors the legacy safety-first defaults so a fresh install\nbehaves the same as a config from before the per-profile split.",
      "properties": {
        "allowed_commands": {
          "default": [
            "git",
            "npm",
            "cargo",
            "ls",
            "cat",
            "grep",
            "find",
            "echo",
            "pwd",
            "wc",
            "head",
            "tail",
            "date",
            "df",
            "du",
            "uname",
            "uptime",
            "hostname",
            "python",
            "python3",
            "pip",
            "node",
            "free"
          ],
          "description": "Allowlist of executable names for shell execution.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "allowed_roots": {
          "default": [],
          "description": "Extra directory roots the agent may access.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "allowed_tools": {
          "default": [],
          "description": "Tools the agent may call in agentic mode. Empty = inherit / no\nauthorization constraint. Authorization decision: which tools is\nthe agent permitted to invoke at all. See `excluded_tools` for\nthe inverse denylist scoped to non-CLI channels.\n\nThe TOML config does not distinguish an omitted field from\n`allowed_tools = []`; both deserialize to `Vec::new()` and\n`SecurityPolicy::from_profiles` maps that to \"no authorization\nconstraint\" at this layer. If you need an explicit deny-all gate,\napply it on the caller-supplied per-run `allowed_tools` (cron\njobs and other narrowers pass that list in directly to\n`ToolAccessPolicy`, which honors `Some(vec![])` as deny-all) or\nvia `excluded_tools` covering the specific tools you want blocked.\n\nMCP exception: when the list is non-empty, runtime-discovered MCP\ntools (any name containing `__`, which is the `<server>__<tool>`\nconvention used by the MCP wrapper) are auto-admitted into the\neffective allow-list without needing to be listed here individually.\nThis keeps the post-change eager-MCP default usable for agents with an\nexplicit allow-list. Block individual MCP tools via `excluded_tools`.\n\nScope of the exception: the `__` auto-admit applies only to this\nrisk-profile allow-list, **not** to caller-supplied per-run\n`allowed_tools` (cron job `allowed_tools`, narrowed delegate\ninvocations, etc.). Per-run lists are still strict explicit-list\nintersections, so a job that narrows `allowed_tools = [\"cron_add\"]`\nwill not see runtime-discovered MCP tools unless it names them.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "always_ask": {
          "default": [],
          "description": "Tools that always require approval in this profile.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "approval_route": {
          "anyOf": [
            {
              "$ref": "#/$defs/ApprovalRoute"
            },
            {
              "type": "null"
            }
          ],
          "description": "Route this profile's tool approvals to a DISTINCT approver channel instead of the\nchannel that triggered the run (closes the cross-channel-HITL gap). Absent ⇒ the\noriginating channel approves (today's behavior). See [`crate::autonomy::ApprovalRoute`].\n\nHonored on both the interactive channel-driven path and the non-interactive turn path\n(gateway chat/webhook dispatch and agent-to-agent peer messages). On the non-interactive\npath the approver is resolved from the live daemon channel registry; if no registry is\navailable or the approver is not live, the gate keeps the non-interactive default\n(fail-closed deny under the default `on_no_approver`)."
        },
        "auto_approve": {
          "default": [
            "file_read",
            "memory_recall",
            "web_search_tool",
            "web_fetch",
            "calculator",
            "glob_search",
            "content_search",
            "image_info",
            "weather",
            "tool_search",
            "browser",
            "browser_open"
          ],
          "description": "Tools that never require approval in this profile.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "block_high_risk_commands": {
          "default": true,
          "description": "Block high-risk commands even when allowlisted.",
          "type": "boolean"
        },
        "delegation_policy": {
          "$ref": "#/$defs/DelegationPolicy",
          "default": {
            "mode": "forbidden"
          },
          "description": "Whether agents using this profile may initiate delegation. Defaults to\n`Forbidden`. Reachable delegate targets and bounded/independent mode\nare resolved from each caller agent's config."
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from non-CLI channels under this profile.\n\nAlso subtracts from the agentic-delegate allow-list resolved at\nruntime, which is the only way to block individual\n`<server>__<tool>` MCP names that would otherwise be auto-admitted\nby the `allowed_tools` MCP exception described above.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "firejail_args": {
          "default": [],
          "description": "Extra arguments forwarded to firejail when sandbox_backend = \"firejail\".",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "forbidden_paths": {
          "default": [
            "/etc",
            "/root",
            "/home",
            "/usr",
            "/bin",
            "/sbin",
            "/lib",
            "/opt",
            "/boot",
            "/dev",
            "/proc",
            "/sys",
            "/var",
            "/tmp",
            "~/.ssh",
            "~/.gnupg",
            "~/.aws",
            "~/.config"
          ],
          "description": "Explicit path denylist.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "level": {
          "$ref": "#/$defs/AutonomyLevel",
          "default": "supervised",
          "description": "Autonomy level applied to this profile. Default: `supervised`."
        },
        "require_approval_for_medium_risk": {
          "default": true,
          "description": "Require approval for medium-risk operations.",
          "type": "boolean"
        },
        "sandbox_backend": {
          "default": null,
          "description": "Sandbox backend identifier (e.g. `\"firejail\"`, `\"landlock\"`). `None` inherits.",
          "type": [
            "string",
            "null"
          ]
        },
        "sandbox_enabled": {
          "default": null,
          "description": "Whether the sandbox is enabled for this profile. `None` inherits global.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "shell_env_passthrough": {
          "default": [],
          "description": "Environment variable names passed through to shell subprocesses.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "workspace_only": {
          "default": true,
          "description": "Restrict filesystem access to workspace-relative paths. Default: `false`.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "RiskProfileRef": {
      "description": "Reference to a configured `[risk_profiles.<type>.<alias>]` entry.\nEmpty value means \"no preference\" (opt-out). Non-empty values must\nresolve to a configured alias; `Config::validate()` enforces this.",
      "type": "string"
    },
    "RuntimeConfig": {
      "description": "Runtime adapter configuration (`[runtime]` section).",
      "properties": {
        "docker": {
          "$ref": "#/$defs/DockerRuntimeConfig",
          "default": {
            "allowed_workspace_roots": [],
            "cpu_limit": 1.0,
            "image": "alpine:3.20",
            "memory_limit_mb": 512,
            "mount_workspace": true,
            "network": "none",
            "read_only_rootfs": true
          },
          "description": "Docker runtime settings (used when `kind = \"docker\"`)."
        },
        "kind": {
          "$ref": "#/$defs/RuntimeKind",
          "default": "native",
          "description": "Runtime kind: native | docker | cloudflare."
        },
        "reasoning_effort": {
          "default": null,
          "description": "Optional reasoning effort for model_providers that expose a level control.",
          "type": [
            "string",
            "null"
          ]
        },
        "reasoning_enabled": {
          "default": null,
          "description": "Global reasoning override for model_providers that expose explicit controls.\n- `None`: model_provider default behavior\n- `Some(true)`: request reasoning/thinking when supported\n- `Some(false)`: disable reasoning/thinking when supported",
          "type": [
            "boolean",
            "null"
          ]
        },
        "shell": {
          "default": null,
          "description": "Shell binary the native runtime uses for command execution.\n\nApplies only to `runtime.kind = \"native\"`; other runtimes ignore it.\nWhen unset or `null`, the system default `sh` is used. The shell is\ninvoked as `<shell> -c \"<command>\"`, so it must be a POSIX-compatible\nshell binary.\n\nAccepted forms (Unix):\n- a bare command name resolved via `PATH` (e.g. `\"bash\"`), or\n- an absolute path (e.g. `\"/bin/bash\"`, `\"/usr/bin/zsh\"`).\n\nThe value is validated when the native runtime is constructed, so a bad\nvalue is reported up front rather than failing on the first shell\ncommand. Rejected: empty/whitespace; a relative path with separators\n(e.g. `\"./sh\"`, `\"bin/sh\"` — use a bare `PATH` name or an absolute path\ninstead); a bare name not found on `PATH`; and a path that does not\nexist or is not executable.\n\n**Ignored on Windows and Android** (and not validated there): Windows\nalways uses `cmd.exe`, and Android always uses `/system/bin/sh`\n(its shell is not on `PATH` for spawned processes).\n\n**Examples:**\n```toml\n[runtime]\nshell = \"bash\" # resolves via PATH\nshell = \"/bin/zsh\" # absolute path\n```",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "RuntimeKind": {
      "description": "Runtime adapter kind.",
      "enum": [
        "native",
        "docker",
        "cloudflare"
      ],
      "type": "string"
    },
    "RuntimeProfileConfig": {
      "description": "Named runtime/LLM execution profile (`[runtime_profiles.<alias>]`).\n\nReusable operational tuning: agentic mode, iteration caps, context\nbudget, parallel dispatch, resource ceilings, recursion depth, and\nthe budget knobs that `SecurityPolicy` enforces with subagent\nparent-subset discipline. Anything authorization-shaped (allowed\ncommands/tools/paths, approval gates, sandbox) lives on\n`[risk_profiles.<alias>]`. Anything model-provider shaped (model,\ntemperature, max_tokens, timeout_secs) lives on\n`[providers.models.<type>.<alias>]`.",
      "properties": {
        "agentic": {
          "default": false,
          "description": "Enable agentic (multi-turn tool-call loop) mode.",
          "type": "boolean"
        },
        "agentic_timeout_secs": {
          "default": null,
          "description": "Agentic delegate run timeout in seconds. `None` inherits global.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "auto_classify": {
          "anyOf": [
            {
              "$ref": "#/$defs/AutoClassifyConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "compact_context": {
          "default": null,
          "description": "Use compact bootstrap (6000 chars / 2 RAG chunks). `None` inherits.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "context_compression": {
          "$ref": "#/$defs/ContextCompressionConfig",
          "default": {
            "enabled": false,
            "identifier_policy": "strict",
            "max_passes": 3,
            "protect_first_n": 3,
            "protect_last_n": 4,
            "source_max_chars": 50000,
            "summary_max_chars": 4000,
            "summary_model": null,
            "summary_provider": "",
            "threshold_ratio": 0.5,
            "timeout_secs": 60,
            "tool_result_retrim_chars": 2000,
            "tool_result_trim_exempt": []
          }
        },
        "delegation_timeout_secs": {
          "default": null,
          "description": "Delegate call timeout in seconds. `None` inherits global delegate timeout.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "eval": {
          "$ref": "#/$defs/EvalConfig",
          "default": {
            "enabled": false,
            "max_retries": 1,
            "min_quality_score": 0.5
          }
        },
        "history_pruning": {
          "$ref": "#/$defs/HistoryPrunerConfig",
          "default": {
            "collapse_tool_results": true,
            "enabled": false,
            "keep_recent": 4,
            "max_tokens": 8192
          }
        },
        "keep_tool_context_turns": {
          "default": null,
          "description": "Number of recent turns whose full tool context is preserved. `None` inherits.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_actions_per_hour": {
          "default": 20,
          "description": "Maximum actions allowed per hour. `0` is a hard zero budget — the\nper-sender rate tracker treats a max of 0 as always exhausted\n(`PerSenderTracker::is_exhausted`), blocking every action. For an\neffectively-unlimited budget use a high value (e.g. `u32::MAX`), not 0.\n`SecurityPolicy::ensure_no_escalation_beyond` rejects subagents\nthat try to raise this above the parent's value.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "max_context_tokens": {
          "default": null,
          "description": "Maximum estimated tokens for context before compaction. `None` inherits.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_cost_per_day_cents": {
          "default": 500,
          "description": "Maximum cost per day in cents. `0` inherits the global limit.\nParent-subset enforced for subagents.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "max_delegation_depth": {
          "default": 0,
          "description": "Maximum delegation recursion depth. `0` inherits the default.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "max_history_messages": {
          "default": null,
          "description": "Maximum conversation history messages retained per session. `None` inherits.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_system_prompt_chars": {
          "default": null,
          "description": "Maximum characters for the assembled system prompt. `None` inherits.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_tool_iterations": {
          "default": 0,
          "description": "Maximum tool-call iterations in agentic mode. `0` inherits the global default.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_tool_result_chars": {
          "default": null,
          "description": "Maximum characters for a single tool result. `None` inherits.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "memory_recall_limit": {
          "default": null,
          "description": "Maximum memory entries injected per turn. `None` inherits global default (5).\nSet to `0` for unlimited.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "parallel_tools": {
          "default": null,
          "description": "Enable parallel tool execution per iteration. `None` inherits.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "prompt_injection_mode": {
          "anyOf": [
            {
              "$ref": "#/$defs/SkillsPromptInjectionMode"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "How skills are injected into the system prompt for agents on this\nprofile. `None` inherits the global `[skills] prompt_injection_mode`;\n`compact` inlines only compact skill metadata and registers the\n`read_skill` tool, `full` inlines full skill instructions. Resolved\nthrough [`Config::effective_skills_prompt_mode`], the single point both\nthe system-prompt builder and the `read_skill` tool-registration gate\nconsult so they always agree on the effective mode."
        },
        "shell_timeout_secs": {
          "default": 60,
          "description": "Shell subprocess timeout in seconds. `0` inherits the global timeout.\nParent-subset enforced for subagents.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "strict_tool_parsing": {
          "default": false,
          "type": "boolean"
        },
        "thinking": {
          "$ref": "#/$defs/ThinkingConfig",
          "default": {
            "budget_tokens": {},
            "default_level": "medium",
            "native_thinking": false
          }
        },
        "tool_call_dedup_exempt": {
          "default": [],
          "description": "Tools exempt from within-turn dedup check.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "tool_dispatcher": {
          "default": null,
          "description": "Tool dispatch strategy (e.g. `\"auto\"`). `None` inherits.",
          "type": [
            "string",
            "null"
          ]
        },
        "tool_filter_groups": {
          "default": [],
          "items": {
            "$ref": "#/$defs/ToolFilterGroup"
          },
          "type": "array"
        },
        "tool_receipts": {
          "$ref": "#/$defs/ToolReceiptsConfig",
          "default": {
            "enabled": false,
            "inject_system_prompt": true,
            "show_in_response": false
          }
        }
      },
      "type": "object"
    },
    "RuntimeProfileRef": {
      "description": "Reference to a configured `[runtime_profiles.<type>.<alias>]` entry.\nEmpty value means \"no preference\" (opt-out). Non-empty values must\nresolve to a configured alias; `Config::validate()` enforces this.",
      "type": "string"
    },
    "SambanovaModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "SchedulerConfig": {
      "description": "Scheduler configuration for periodic task execution (`[scheduler]` section).\n\nOwns the cron-runtime knobs: per-job declarations live on\n`Config.cron: HashMap<String, CronJobDecl>` (alias-keyed), while the\nscheduler loop's runtime behavior (`enabled`, polling cap, catch-up) lives here.",
      "properties": {
        "catch_up_on_startup": {
          "default": true,
          "description": "Run all overdue jobs at scheduler startup. Default: `true`.\n\nWhen the daemon restarts late, jobs whose `next_run` is in the past\nfire once before normal polling resumes. Disable to wait for the\nnext scheduled occurrence instead.",
          "type": "boolean"
        },
        "enabled": {
          "default": true,
          "description": "Enable the built-in scheduler loop. When false, no cron jobs run.",
          "type": "boolean"
        },
        "max_concurrent": {
          "default": 4,
          "description": "Maximum tasks executed in parallel within a single polling cycle.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_run_history": {
          "default": 50,
          "description": "Maximum number of historical cron run records to retain. Default: `50`.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "max_tasks": {
          "default": 64,
          "description": "Maximum number of persisted scheduled tasks per polling cycle.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "SearchMode": {
      "description": "Search strategy for memory recall.",
      "oneOf": [
        {
          "const": "bm25",
          "description": "Pure keyword search (FTS5 BM25)",
          "type": "string"
        },
        {
          "const": "embedding",
          "description": "Pure vector/semantic search",
          "type": "string"
        },
        {
          "const": "hybrid",
          "description": "Weighted combination of keyword + vector (default)",
          "type": "string"
        }
      ]
    },
    "SecretsConfig": {
      "description": "Secrets encryption configuration (`[secrets]` section).",
      "properties": {
        "encrypt": {
          "default": true,
          "description": "Enable encryption for API keys and tokens at rest",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "SecurityConfig": {
      "description": "Security configuration for audit logging, OTP, e-stop, IAM/SSO, and WebAuthn.\n\nSandbox backend and resource limits live on per-agent risk profiles\n(see `RiskProfileConfig::sandbox_*` and `RiskProfileConfig::max_*`); the\nruntime resolves them via `Config::active_risk_profile(agent_alias)`.",
      "properties": {
        "audit": {
          "$ref": "#/$defs/AuditConfig",
          "default": {
            "enabled": true,
            "log_path": "audit.log",
            "max_size_mb": 100,
            "sign_events": false
          },
          "description": "Audit logging configuration"
        },
        "estop": {
          "$ref": "#/$defs/EstopConfig",
          "default": {
            "enabled": false,
            "require_otp_to_resume": true,
            "state_file": "/home/jeremy/.zeroclaw/estop-state.json"
          },
          "description": "Emergency-stop state machine configuration."
        },
        "leak_detection": {
          "$ref": "#/$defs/LeakDetectionConfig",
          "default": {
            "enabled": true,
            "high_entropy_tokens": true,
            "sensitivity": 0.7
          },
          "description": "Outbound credential leak detection and redaction configuration. See\n`[security.leak_detection]` for the detector controls."
        },
        "nevis": {
          "$ref": "#/$defs/NevisConfig",
          "default": {
            "client_id": "",
            "client_secret": null,
            "enabled": false,
            "instance_url": "",
            "jwks_url": null,
            "realm": "master",
            "require_mfa": false,
            "role_mapping": [],
            "session_timeout_secs": 3600,
            "token_validation": "local"
          },
          "description": "Nevis IAM integration for SSO/MFA authentication and role-based access."
        },
        "otp": {
          "$ref": "#/$defs/OtpConfig",
          "default": {
            "cache_valid_secs": 300,
            "challenge_max_attempts": 3,
            "enabled": false,
            "gated_actions": [
              "shell",
              "file_write",
              "browser_open",
              "browser",
              "memory_forget"
            ],
            "gated_domain_categories": [],
            "gated_domains": [],
            "method": "totp",
            "token_ttl_secs": 30
          },
          "description": "OTP gating configuration for sensitive actions/domains."
        },
        "webauthn": {
          "$ref": "#/$defs/WebAuthnConfig",
          "default": {
            "enabled": false,
            "rp_id": "localhost",
            "rp_name": "ZeroClaw",
            "rp_origin": "http://localhost:42617"
          },
          "description": "WebAuthn / FIDO2 hardware key authentication configuration."
        }
      },
      "type": "object"
    },
    "SecurityOpsConfig": {
      "description": "Managed Cybersecurity Service (MCSS) dashboard agent configuration (`[security_ops]`).",
      "properties": {
        "auto_triage": {
          "default": false,
          "description": "Automatically triage incoming alerts without user prompt.",
          "type": "boolean"
        },
        "enabled": {
          "default": false,
          "description": "Enable security operations tools.",
          "type": "boolean"
        },
        "max_auto_severity": {
          "default": "low",
          "description": "Maximum severity level that can be auto-remediated without approval.\nOne of: \"low\", \"medium\", \"high\", \"critical\". Default: \"low\".",
          "type": "string"
        },
        "playbooks_dir": {
          "default": "/home/jeremy/.zeroclaw/playbooks",
          "description": "Directory containing incident response playbook definitions (JSON).",
          "type": "string"
        },
        "report_output_dir": {
          "default": "/home/jeremy/.zeroclaw/security-reports",
          "description": "Directory for generated security reports.",
          "type": "string"
        },
        "require_approval_for_actions": {
          "default": true,
          "description": "Require human approval before executing playbook actions.",
          "type": "boolean"
        },
        "siem_integration": {
          "default": null,
          "description": "Optional SIEM webhook URL for alert ingestion.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "SglangModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "ShellToolConfig": {
      "description": "Shell tool configuration (`[shell_tool]` section).\n\nControls the behaviour of the `shell` execution tool. The main\ntunable is `timeout_secs` — the maximum wall-clock time a single\nshell command may run before it is killed.",
      "properties": {
        "timeout_secs": {
          "default": 60,
          "description": "Maximum shell command execution time in seconds (default: 60).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "SignalConfig": {
      "properties": {
        "account": {
          "description": "E.164 phone number of the signal-cli account (e.g. \"+1234567890\").",
          "type": "string"
        },
        "approval_timeout_secs": {
          "default": 300,
          "description": "Seconds to wait for operator approval on `always_ask` tools before auto-denying.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "dm_only": {
          "default": false,
          "description": "When true, only accept direct messages and ignore all group traffic.\nMutually exclusive with `group_ids` (which is ignored when this is\nset). Migrated from the legacy `group_id = \"dm\"` sentinel.",
          "type": "boolean"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "group_ids": {
          "default": [],
          "description": "Group IDs to filter messages. Empty = accept all messages (DMs and\ngroups). When non-empty, only messages from listed groups are\naccepted (DMs are still accepted unless `dm_only` flips the policy\nto DMs-only). Migrated from the legacy `group_id` singular field.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "http_url": {
          "description": "Base URL for the signal-cli HTTP daemon (e.g. `\"http://127.0.0.1:8686\"`).",
          "type": "string"
        },
        "ignore_attachments": {
          "default": false,
          "description": "Skip messages that are attachment-only (no text body).",
          "type": "boolean"
        },
        "ignore_stories": {
          "default": false,
          "description": "Skip incoming story messages.",
          "type": "boolean"
        },
        "proxy_url": {
          "default": null,
          "description": "Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",
          "type": [
            "string",
            "null"
          ]
        },
        "reply_min_interval_secs": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "reply_queue_depth_max": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "http_url",
        "account"
      ],
      "type": "object"
    },
    "SiliconflowModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "SkillBundleConfig": {
      "description": "Named skill bundle (`[skill_bundles.<alias>]`).\n\nA reusable group of skills that can be attached to an agent or channel\nby alias, controlling which skills are loaded and from where.",
      "properties": {
        "directory": {
          "default": null,
          "description": "Directory path (relative to workspace root) to load skills from.",
          "type": [
            "string",
            "null"
          ]
        },
        "exclude": {
          "default": [],
          "description": "Skill names to exclude from this bundle.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "include": {
          "default": [],
          "description": "Skill names to include. Empty means include all skills in `directory`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "SkillCreationConfig": {
      "description": "Autonomous skill creation configuration (`[skills.skill_creation]` section).",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable automatic skill creation after successful multi-step tasks.\nDefault: `false`.",
          "type": "boolean"
        },
        "max_final_answer_chars": {
          "default": 2000,
          "description": "Maximum characters of the final assistant answer fed into the\nreflection prompt. Bounds prompt size so a long answer cannot blow up\nthe reflection request. Default: `2000`.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_skills": {
          "default": 500,
          "description": "Maximum number of auto-generated skills to keep.\nWhen exceeded, the oldest auto-generated skill is removed (LRU eviction).",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_task_chars": {
          "default": 1000,
          "description": "Maximum characters of the task description fed into the reflection\nprompt. Default: `1000`.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_tool_trace_chars": {
          "default": 4000,
          "description": "Maximum characters of the rendered tool-call trace fed into the\nreflection prompt. Default: `4000`.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "reflection_enabled": {
          "default": false,
          "description": "Synthesize a canonical `SKILL.md` from the execution trace via a\nbounded model-provider reflection call instead of the deterministic\n`SKILL.toml` generator. Requires `enabled = true`. Falls back to\n`SKILL.toml` whenever the reflection call or its output is invalid, so\nturning this on never leaves a skill un-created. This is distinct from\nthe `[skills.skill_improvement]` background review fork, which patches\nexisting skills after use rather than creating new ones from a trace.\nDefault: `false`.",
          "type": "boolean"
        },
        "similarity_threshold": {
          "default": 0.85,
          "description": "Embedding similarity threshold for deduplication.\nSkills with descriptions more similar than this value are skipped.",
          "format": "double",
          "type": "number"
        }
      },
      "type": "object"
    },
    "SkillImprovementConfig": {
      "description": "Skill self-improvement configuration (`[skills.skill-improvement]` section).\n\nControls the post-turn background review fork that may patch, expand, or\narchive skills based on what the conversation revealed. The fork runs in a\nrestricted toolset (only `skills_list`, `skill_view`, `skill_manage`) and\nnever touches the user-visible conversation.",
      "properties": {
        "cooldown_secs": {
          "default": 3600,
          "description": "Minimum interval (in seconds) between reviews for the same skill.\nActs as a durable rate limit on patches to any one skill. Default: `3600`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "enabled": {
          "default": false,
          "description": "Enable the background skill-review fork. Default: `false`.\nOpt-in: users must explicitly enable this to allow post-turn skill\nmutations.",
          "type": "boolean"
        },
        "max_review_iterations": {
          "default": 8,
          "description": "Maximum tool-call iterations the review fork itself is allowed to make.\nCaps the fork's LLM cost. Default: `8`.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "nudge_interval_iterations": {
          "default": 10,
          "description": "Spawn a review fork once at least this many tool-call iterations have\naccumulated in the current run. `0` disables iteration-based triggering.\nDefault: `10`.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "SkillInstallSuggestionsConfig": {
      "description": "Prompt-triggered skill install suggestions (`[skills.install_suggestions]` section).",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable suggestions for installable skills before normal agent turns.\nDefault: `false`.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "SkillsConfig": {
      "description": "Skills loading configuration (`[skills]` section).",
      "properties": {
        "allow_scripts": {
          "default": false,
          "description": "Allow script-like files in skills (`.sh`, `.bash`, `.ps1`, shebang shell files).\nDefault: `false` (secure by default).",
          "type": "boolean"
        },
        "extra_registries": {
          "description": "Additional user-configured skill registries, installed via\n`registry:<name>/<skill>`. Each reuses the git-clone registry path and\nis cloned to its own `extra-registry-<name>/` workspace directory.",
          "items": {
            "$ref": "#/$defs/ExternalRegistry"
          },
          "type": "array"
        },
        "install_suggestions": {
          "$ref": "#/$defs/SkillInstallSuggestionsConfig",
          "default": {
            "enabled": false
          },
          "description": "Prompt-triggered install suggestions for missing skills."
        },
        "open_skills_dir": {
          "default": null,
          "description": "Optional path to a local open-skills repository.\nIf unset, defaults to `$HOME/open-skills` when enabled.",
          "type": [
            "string",
            "null"
          ]
        },
        "open_skills_enabled": {
          "default": false,
          "description": "Enable loading and syncing the community open-skills repository.\nDefault: `false` (opt-in).",
          "type": "boolean"
        },
        "prompt_injection_mode": {
          "$ref": "#/$defs/SkillsPromptInjectionMode",
          "default": "full",
          "description": "Controls how skills are injected into the system prompt.\n`full` preserves legacy behavior. `compact` keeps context small and loads skills on demand."
        },
        "registry_url": {
          "default": null,
          "description": "URL of the skills registry repository for bare-name installs.\nDefault: `https://github.com/zeroclaw-labs/zeroclaw-skills`",
          "type": [
            "string",
            "null"
          ]
        },
        "skill_creation": {
          "$ref": "#/$defs/SkillCreationConfig",
          "default": {
            "enabled": false,
            "max_final_answer_chars": 2000,
            "max_skills": 500,
            "max_task_chars": 1000,
            "max_tool_trace_chars": 4000,
            "reflection_enabled": false,
            "similarity_threshold": 0.85
          },
          "description": "Autonomous skill creation from successful multi-step task executions."
        },
        "skill_improvement": {
          "$ref": "#/$defs/SkillImprovementConfig",
          "default": {
            "cooldown_secs": 3600,
            "enabled": false,
            "max_review_iterations": 8,
            "nudge_interval_iterations": 10
          },
          "description": "Automatic skill self-improvement after successful skill usage."
        }
      },
      "type": "object"
    },
    "SkillsPromptInjectionMode": {
      "description": "Skills loading configuration (`[skills]` section).",
      "oneOf": [
        {
          "const": "full",
          "description": "Inline full skill instructions and tool metadata into the system prompt.",
          "type": "string"
        },
        {
          "const": "compact",
          "description": "Inline only compact skill metadata (name/description/location) and load details on demand.",
          "type": "string"
        }
      ]
    },
    "SlackConfig": {
      "description": "Slack bot channel configuration.",
      "properties": {
        "app_token": {
          "default": null,
          "description": "Slack app-level token for Socket Mode (xapp-...). When unset or empty,\nresolved at channel construction from `ZEROCLAW_SLACK_APP_TOKEN`, then\n`SLACK_APP_TOKEN`. `#[serde(default)]` makes omission explicit (an\n`Option` field is already omittable, but this keeps it consistent with\n`bot_token`).",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "approval_timeout_secs": {
          "default": 300,
          "description": "Seconds to wait for operator approval on `always_ask` tools before auto-denying.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "bot_token": {
          "default": null,
          "description": "Slack bot OAuth token (xoxb-...). Optional in config: when unset or\nempty it is resolved at channel construction from\n`ZEROCLAW_SLACK_BOT_TOKEN`, then `SLACK_BOT_TOKEN`. `#[serde(default)]`\nso a config that omits it still deserializes - the env fallback then\nsupplies it - instead of failing with `missing field 'bot_token'`.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "cancel_reaction": {
          "default": null,
          "description": "Emoji reaction name (without colons) that cancels an in-flight request.\nFor example, `\"x\"` means reacting with `:x:` cancels the task.\nLeave unset to disable reaction-based cancellation.",
          "type": [
            "string",
            "null"
          ]
        },
        "channel_ids": {
          "default": [],
          "description": "Explicit list of channel IDs to watch.\nEmpty = listen across all accessible channels.\nMigrated from the legacy `channel_id` singular field.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "draft_update_interval_ms": {
          "default": 1200,
          "description": "Minimum interval (ms) between draft message edits to avoid Slack rate limits.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "interrupt_on_new_message": {
          "default": false,
          "description": "When true, a newer Slack message from the same sender in the same channel\ncancels the in-flight request and starts a fresh response with preserved history.",
          "type": "boolean"
        },
        "mention_only": {
          "default": false,
          "description": "When true, only respond to messages that @-mention the bot in groups.\nDirect messages remain allowed.",
          "type": "boolean"
        },
        "proxy_url": {
          "default": null,
          "description": "Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",
          "type": [
            "string",
            "null"
          ]
        },
        "reply_min_interval_secs": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "reply_queue_depth_max": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "stream_drafts": {
          "default": false,
          "description": "Enable progressive draft message streaming via `chat.update`.",
          "type": "boolean"
        },
        "strict_mention_in_thread": {
          "default": false,
          "description": "When true (and `mention_only` is also true), messages inside a Slack\nthread must also @-mention the bot to trigger a response. By default,\nthread replies are allowed through without a mention so the bot can\nkeep a back-and-forth going without the user repeating @-mentions.\nSet this to true in channels shared with human discussion where the\nbot should stay silent unless explicitly addressed.",
          "type": "boolean"
        },
        "thread_replies": {
          "default": null,
          "description": "When true (default), replies stay in the originating Slack thread.\nWhen false, replies go to the channel root instead.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "use_markdown_blocks": {
          "default": false,
          "description": "Use the newer Slack `markdown` block type (12 000 char limit, richer formatting).\nDefaults to false (uses universally supported `section` blocks with `mrkdwn`).\nEnable this only if your Slack workspace supports the `markdown` block type.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "SlashCommandScope": {
      "description": "Where a channel registers its skill slash commands. `global` (default)\nregisters application-wide - the commands work everywhere the bot is, but\nDiscord takes up to ~1h to propagate changes. `guild` registers to each\nguild in `guild_ids`, which propagates instantly (the right choice for fast\niteration and single-server bots). When `guild` is set with an empty\n`guild_ids`, the channel logs a warning and falls back to global.",
      "oneOf": [
        {
          "const": "global",
          "description": "Application-wide commands (propagate in up to ~1h). Default.",
          "type": "string"
        },
        {
          "const": "guild",
          "description": "Commands registered to each guild in `guild_ids` (instant propagation).",
          "type": "string"
        }
      ]
    },
    "SopApprovalConfig": {
      "description": "`[sop.approval]` - approval broker policy config. A permanent identity source\nfor channel-provided approvers (not a stopgap): the approval broker consumes it\nfor group-membership and quorum checks. Empty = no broker policy applies.",
      "properties": {
        "groups": {
          "additionalProperties": {
            "$ref": "#/$defs/ApprovalGroupConfig"
          },
          "description": "Named approver groups: `group name -> members`. A member is matched against\nthe transport-derived (channel-authenticated) `ApprovalPrincipal` identity.\nA member may be source-qualified (`<source>:<identity>`, e.g. `http:ZeroClawOperator`,\n`ws:<subject>`, `agent:<alias>`) to grant rights on one transport only, or a\nbare identity (`alice`) to grant from any non-channel source. Channel members\nmust include the channel namespace (`channel:<channel-key>:<sender>`) so sender\nids from different channel aliases cannot collide. A future auth system adds a\nsecond resolver alongside this one; it does not replace channel identities.",
          "type": "object"
        },
        "policies": {
          "additionalProperties": {
            "$ref": "#/$defs/ApprovalPolicyConfig"
          },
          "description": "Named approval policies a SOP step may reference by name.",
          "type": "object"
        }
      },
      "type": "object"
    },
    "SopConfig": {
      "description": "Standard Operating Procedures engine configuration (`[sop]`).\n\nThe `default_execution_mode` field uses the `SopExecutionMode` type from\n`sop::types` (re-exported via `sop::SopExecutionMode`). To avoid circular\nmodule references, config stores it using the same enum definition.",
      "properties": {
        "approval": {
          "$ref": "#/$defs/SopApprovalConfig",
          "default": {},
          "description": "Approval broker policy config (`[sop.approval]`): named approver groups and\nper-name approval policies (required group + quorum + escalation route) the\napproval broker consumes for group-membership and quorum checks. Members are\nchannel-provided identities (a gateway user, a forge login), so this is a\npermanent identity source, not a stopgap; a future auth system adds another\nresolver alongside it rather than replacing it. An empty block means no broker\npolicy applies (`approval_mode` alone governs a gate, unchanged behavior)."
        },
        "approval_mode": {
          "$ref": "#/$defs/ApprovalMode",
          "default": "both",
          "description": "WHO may clear a SOP approval gate. Layered with execution_mode / priority /\nrequires_confirmation (those still apply). Default `both` keeps today's\nbehavior (the agent tool OR an out-of-band principal can clear a gate)."
        },
        "approval_timeout_action": {
          "$ref": "#/$defs/ApprovalTimeoutAction",
          "default": "escalate",
          "description": "What happens to a SOP gate that times out (after `approval_timeout_secs`).\nDefault `escalate` is fail-closed: re-surface to the out-of-band approver and\nkeep waiting, never self-approve. (The SOP-gate analog of the channel\napproval-routing fail-closed default; reconcile with that model if both land.)"
        },
        "approval_timeout_secs": {
          "default": 300,
          "description": "Approval timeout in seconds. When a run waits for approval longer than\nthis, the configured `approval_timeout_action` is applied (default\n`escalate`: re-surface the gate to the out-of-band approver and never\nself-approve). Set to 0 to disable the timeout sweep.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "default_execution_mode": {
          "default": "supervised",
          "description": "Default execution mode for SOPs that omit `execution_mode`.\nValues: `auto`, `supervised` (default), `step_by_step`,\n`priority_based`, `deterministic`.",
          "type": "string"
        },
        "maintenance_interval_secs": {
          "default": 60,
          "description": "How often (seconds) the daemon runs the SOP maintenance tick: fire\nfail-closed approval timeouts (per `approval_timeout_secs` /\n`approval_timeout_action`), reap expired concurrency-claim leases, and\nprune terminal runs past `max_finished_runs`. Default `60`; set to `0` to\ndisable the tick entirely. The tick itself self-approves nothing - timeout\nhandling is governed by `approval_timeout_action` (default `escalate`).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "max_concurrent_total": {
          "default": 4,
          "description": "Maximum total concurrent SOP runs across all SOPs.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_finished_runs": {
          "default": 100,
          "description": "Maximum number of finished runs kept in memory for status queries.\nOldest runs are evicted when over capacity. 0 = unlimited.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "max_step_retries": {
          "default": 2,
          "description": "Maximum retries allowed by a step failure policy.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "max_step_visits": {
          "default": 256,
          "description": "Maximum times a routed SOP run can visit one step.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "persist_runs": {
          "default": true,
          "description": "Persist run state durably across restarts. Default `true`: `build_sop_engine`\nselects the configured backend (`sqlite`) and in-flight runs - including runs\nparked at a HITL approval - survive a restart. This is the durable substrate\nthe HITL admission model relies on so a pending approval is not lost when the\ndaemon restarts. Set to `false` to opt back into ephemeral in-memory state.",
          "type": "boolean"
        },
        "procedural_memory_enabled": {
          "default": false,
          "description": "Enable SOP procedural-memory proposal tooling. Default false keeps\nself-modifying SOP write-back opt-in while the SOP subsystem is\nExperimental.",
          "type": "boolean"
        },
        "run_state_dir": {
          "default": null,
          "description": "Directory for the durable run store (created mode-0700). When omitted,\n`<data_dir>/sop`. Never OS-temp.",
          "type": [
            "string",
            "null"
          ]
        },
        "run_store_backend": {
          "$ref": "#/$defs/SopRunStoreBackend",
          "default": "sqlite",
          "description": "Durable run-state backend when `persist_runs` is true: `sqlite` (default,\ndurable) or `memory` (explicitly non-durable, for tests/degraded)."
        },
        "sops_dir": {
          "default": null,
          "description": "Directory containing SOP definitions (subdirs with SOP.toml + SOP.md).\nOptional override. When omitted, the runtime and CLI both resolve the\ndefault `<workspace>/sops`; SOPs load from there whenever it exists.",
          "type": [
            "string",
            "null"
          ]
        },
        "step_mandatory_tools": {
          "default": [
            "sop_advance",
            "sop_approve",
            "sop_status"
          ],
          "description": "Tool names that remain available while step scope is enforced.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "step_schema_enforce": {
          "default": true,
          "description": "Enforce per-step input/output schemas when a step declares them.",
          "type": "boolean"
        },
        "step_scope_enforce": {
          "default": false,
          "description": "Enforce per-step tool scope. Default false keeps `tools:` advisory.",
          "type": "boolean"
        },
        "untrusted_frame_warning": {
          "default": true,
          "description": "Include the explanatory warning text inside untrusted-content frames.\nBoundary framing itself is always on once wired.",
          "type": "boolean"
        },
        "untrusted_guard_sensitivity": {
          "default": 0.7,
          "description": "Prompt-guard and outbound-redaction sensitivity for untrusted SOP content.",
          "format": "double",
          "type": "number"
        },
        "untrusted_input_guard": {
          "default": "warn",
          "description": "Prompt-guard action for untrusted SOP trigger input: warn, block, or sanitize.",
          "type": "string"
        },
        "untrusted_outbound_redact": {
          "default": true,
          "description": "Redact outbound SOP content before persistence/audit consumers write it.\nIntended to converge with the shared B/F redaction switch once those consumers land.",
          "type": "boolean"
        },
        "untrusted_payload_max_bytes": {
          "default": 8192,
          "description": "Maximum bytes accepted from untrusted SOP trigger payload/topic content\nbefore char-boundary truncation. 0 disables the cap.",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "SopDispatch": {
      "description": "Generic AMQP 0-9-1 channel configuration (RabbitMQ, Fedora Messaging, etc.).\n\nSubscribes to an exchange via routing keys and lifts each delivery into an\ninbound `ChannelMessage`. The mapping from a JSON delivery body to message\nfields is config-driven (`content_template`, `thread_id_field`) so a new\nsource — Anitya, an internal bus, anything publishing JSON — is onboarded by\nconfiguration rather than code.\nWhere a fan-in delivery is routed once consumed. Used by AMQP, which carries\nthis field to choose between the agent loop, the SOP engine, or both.\nAgent-loop channels (Telegram, Discord, Slack, ...) do not carry a `dispatch`\nfield: their fan-in is trigger-driven, opting in via a SOP `channel` trigger\nwhile the normal agent turn always runs. The mode is source-agnostic.",
      "oneOf": [
        {
          "const": "agent_loop",
          "description": "Drive a normal agent turn: the delivery becomes a `ChannelMessage`.\nThis is the default and preserves each channel's original behavior.",
          "type": "string"
        },
        {
          "const": "sop",
          "description": "Dispatch the delivery to the SOP engine as a `SopEvent` (`topic` =\nsource identifier, `payload` = body), matching SOPs whose trigger for\nthis source matches. No agent turn is started.",
          "type": "string"
        },
        {
          "const": "sop_and_agent_loop",
          "description": "Do both: dispatch to the SOP engine and drive an agent turn from the\nsame delivery.",
          "type": "string"
        }
      ]
    },
    "SopRunStoreBackend": {
      "description": "Durable SOP run-state backend selector. A closed, compile-time-known set, so it\nis a serde enum rather than free text (mirrors `SandboxBackend` /\n`ObservabilityBackend` in this file); unknown values are rejected at parse time.",
      "oneOf": [
        {
          "const": "sqlite",
          "description": "Durable WAL-mode SQLite store (default).",
          "type": "string"
        },
        {
          "const": "memory",
          "description": "Ephemeral in-memory store: explicitly non-durable, for tests / degraded boot.",
          "type": "string"
        }
      ]
    },
    "SqliteStorageConfig": {
      "description": "SQLite storage backend (`[storage.sqlite.<alias>]`).",
      "properties": {
        "open_timeout_secs": {
          "default": null,
          "description": "Maximum seconds to wait when opening the DB if it's locked.\n`None` waits indefinitely. Recommended max: 300.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "path": {
          "default": null,
          "description": "Optional override for the SQLite database path.\nWhen unset, defaults to `<workspace_dir>/brain.db`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "StepfunEndpoint": {
      "oneOf": [
        {
          "const": "cn",
          "description": "Mainland China endpoint.",
          "type": "string"
        },
        {
          "const": "intl",
          "description": "International endpoint.",
          "type": "string"
        }
      ]
    },
    "StepfunModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "endpoint": {
          "$ref": "#/$defs/StepfunEndpoint",
          "default": "intl"
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "StorageConfig": {
      "description": "Persistent storage configuration (`[storage]` section).\n\nStorage is a two-tier alias-keyed map: `[storage.<backend>.<alias>]`,\nparallel to `[providers.models.<type>.<alias>]`. Each backend has its own typed\nconfig struct. `MemoryConfig.backend` carries a dotted reference (`\"sqlite.default\"`,\n`\"postgres.work\"`) that resolves to one of these entries via\n[`Config::resolve_active_storage`].",
      "properties": {
        "lucid": {
          "additionalProperties": {
            "$ref": "#/$defs/LucidStorageConfig"
          },
          "description": "Lucid CLI sync instances (`[storage.lucid.<alias>]`).",
          "type": "object"
        },
        "markdown": {
          "additionalProperties": {
            "$ref": "#/$defs/MarkdownStorageConfig"
          },
          "description": "Markdown storage instances (`[storage.markdown.<alias>]`).",
          "type": "object"
        },
        "postgres": {
          "additionalProperties": {
            "$ref": "#/$defs/PostgresStorageConfig"
          },
          "description": "PostgreSQL storage instances (`[storage.postgres.<alias>]`).",
          "type": "object"
        },
        "qdrant": {
          "additionalProperties": {
            "$ref": "#/$defs/QdrantStorageConfig"
          },
          "description": "Qdrant storage instances (`[storage.qdrant.<alias>]`).",
          "type": "object"
        },
        "sqlite": {
          "additionalProperties": {
            "$ref": "#/$defs/SqliteStorageConfig"
          },
          "description": "SQLite storage instances (`[storage.sqlite.<alias>]`).",
          "type": "object"
        }
      },
      "type": "object"
    },
    "StreamMode": {
      "description": "Streaming mode for channels that support progressive message updates.",
      "oneOf": [
        {
          "const": "off",
          "description": "No streaming -- send the complete response as a single message (default).",
          "type": "string"
        },
        {
          "const": "partial",
          "description": "Update a draft message with every flush interval.",
          "type": "string"
        },
        {
          "const": "multi_message",
          "description": "Send the response as multiple separate messages at paragraph boundaries.",
          "type": "string"
        }
      ]
    },
    "SyntheticModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "TailscaleTunnelConfig": {
      "properties": {
        "funnel": {
          "default": false,
          "description": "Use Tailscale Funnel (public internet) vs Serve (tailnet only)",
          "type": "boolean"
        },
        "hostname": {
          "default": null,
          "description": "Optional hostname override",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "TelegramConfig": {
      "description": "Telegram bot channel configuration.",
      "properties": {
        "ack_reactions": {
          "default": null,
          "description": "Override for the top-level `ack_reactions` setting. When `None`, the\nchannel falls back to `[channels].ack_reactions`. When set\nexplicitly, it takes precedence.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "api_base_url": {
          "default": "https://api.telegram.org",
          "description": "Telegram Bot API base URL. Defaults to the official Telegram endpoint;\nset to a local Bot API server URL when self-hosting Telegram's bot API.",
          "type": "string"
        },
        "approval_timeout_secs": {
          "default": 120,
          "description": "How long (seconds) to wait for the operator to tap an inline-keyboard\nbutton on a tool approval prompt before auto-denying. Default: 120.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "bot_token": {
          "default": "",
          "description": "Telegram Bot API token (from @BotFather). `#[serde(default)]` so a\nconfig that omits or later has it pruned (e.g. a freshly created\nalias with an empty token, stripped by `prune_empty_leaves` before\nwrite) still deserializes as an empty string - instead of failing\nwith `missing field 'bot_token'` and getting dropped by the resilient\nsalvage pass. `validate_bot_token` below still requires a real token\nonce `enabled = true`.",
          "type": "string",
          "x-secret": true
        },
        "debounce_ms": {
          "default": null,
          "description": "Inbound message debounce window in milliseconds for this Telegram alias.\nWhen set, overrides the global `[channels].debounce_ms` for this channel\nonly. `0` or unset falls back to the global value.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "draft_update_interval_ms": {
          "default": 1000,
          "description": "Minimum interval (ms) between draft message edits to avoid rate limits.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "interrupt_on_new_message": {
          "default": false,
          "description": "When true, a newer Telegram message from the same sender in the same chat\ncancels the in-flight request and starts a fresh response with preserved history.",
          "type": "boolean"
        },
        "mention_only": {
          "default": false,
          "description": "When true, only respond to messages that @-mention the bot in groups.\nDirect messages are always processed.",
          "type": "boolean"
        },
        "proxy_url": {
          "default": null,
          "description": "Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",
          "type": [
            "string",
            "null"
          ]
        },
        "reply_min_interval_secs": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "reply_queue_depth_max": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "stream_mode": {
          "$ref": "#/$defs/StreamMode",
          "default": "off",
          "description": "Streaming mode for progressive response delivery via message edits."
        }
      },
      "type": "object"
    },
    "TelnyxModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "TextBrowserConfig": {
      "description": "Text browser tool configuration (`[text_browser]` section).\n\nUses text-based browsers (lynx, links, w3m) to render web pages as plain\ntext. Designed for headless/SSH environments without graphical browsers.",
      "properties": {
        "allowed_private_hosts": {
          "default": [],
          "description": "Private/internal hosts allowed to bypass SSRF protection.\nExact and subdomain matches are supported; `[\"*\"]` permits **all** private/local\nhosts (RFC 1918, loopback, link-local, `.local`). Default: empty (deny).\nWarning: `[\"*\"]` also reaches link-local addresses, including the cloud metadata\nendpoint (`169.254.169.254`) — list specific hosts unless you accept that exposure.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "default": false,
          "description": "Enable `text_browser` tool",
          "type": "boolean"
        },
        "preferred_browser": {
          "default": null,
          "description": "Preferred text browser (\"lynx\", \"links\", or \"w3m\"). If unset, auto-detects.",
          "type": [
            "string",
            "null"
          ]
        },
        "timeout_secs": {
          "default": 30,
          "description": "Request timeout in seconds (default: 30)",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "ThinkingConfig": {
      "description": "Configuration for thinking/reasoning level control.",
      "properties": {
        "budget_tokens": {
          "additionalProperties": {
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          },
          "default": {},
          "type": "object"
        },
        "default_level": {
          "$ref": "#/$defs/ThinkingLevel",
          "default": "medium"
        },
        "native_thinking": {
          "default": false,
          "description": "Enables provider-native thinking parameters when the selected level has a budget.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "ThinkingLevel": {
      "description": "How deeply the model should reason for a given message.",
      "enum": [
        "off",
        "minimal",
        "low",
        "medium",
        "high",
        "max"
      ],
      "type": "string"
    },
    "TodoTrackerConfig": {
      "description": "ZeroCode live task tracker configuration (`[todotracker]` section).\n\nRead-only visual tracker driven by the model's `TodoWrite` tool.",
      "properties": {
        "enabled": {
          "default": true,
          "description": "Master switch. When `false` the tracker never renders and never\nauto-pops. Default: `true`.",
          "type": "boolean"
        },
        "enabled_at_start": {
          "default": false,
          "description": "Whether the panel is visible at launch (when `enabled`). When\n`false` it stays hidden until toggled or auto-popped by the first\nplan. Default: `false`.",
          "type": "boolean"
        },
        "location": {
          "$ref": "#/$defs/TodoTrackerLocation",
          "default": "right",
          "description": "Panel location: `bottom` (between transcript and input), `left`,\nor `right` (default)."
        },
        "max_height": {
          "default": 5,
          "description": "Bottom-strip maximum height in rows (grows up to this). Ignored for\nleft/right. Default: `5`.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "width": {
          "default": 32,
          "description": "Side-panel target column width (left/right). Runtime-clamped to at\nmost half the terminal width. Ignored for `bottom`. Default: `32`.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "TodoTrackerLocation": {
      "description": "Location of the ZeroCode TodoWrite tracker panel.",
      "oneOf": [
        {
          "const": "bottom",
          "description": "Horizontal strip between the transcript and the input bar (Claude Code style).",
          "type": "string"
        },
        {
          "const": "left",
          "description": "Vertical side panel on the left.",
          "type": "string"
        },
        {
          "const": "right",
          "description": "Vertical side panel on the right (OpenCode style). Default.",
          "type": "string"
        }
      ]
    },
    "TogetherModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "ToolCostRates": {
      "description": "Rates for a tool that hits a paid external API. Keyed in\n`[cost.rates.tools.<name>]` by the tool's registered name.",
      "properties": {
        "per_call": {
          "description": "Per-call cost (USD).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "ToolFilterGroup": {
      "description": "A named group of MCP tool patterns with an activation mode.\n\nEach group lists glob patterns for MCP tool names and an optional set of\nkeywords that trigger inclusion in `dynamic` mode. MCP tools are named\n`<server>__<tool>` (double-underscore separator, e.g. `filesystem__read_file`).\n\nClassification is by origin, not name shape: only tools that actually came\nfrom the MCP registry are subject to these groups. Built-in tools AND skill\ntools — which share the `<x>__<y>` naming convention (`{skill}__{tool}`) —\nalways pass through and are never affected by `tool_filter_groups`.\n\nUnder `[mcp] deferred_loading = true`, `mode = \"always\"` entries are\npre-activated at assembly time so matching tools are live from the first\nturn without a `tool_search` round-trip.\n\n# Example\n```toml\n[[runtime_profiles.default.tool_filter_groups]]\nmode = \"always\"\ntools = [\"filesystem__*\"]\nkeywords = []\n\n[[runtime_profiles.default.tool_filter_groups]]\nmode = \"dynamic\"\ntools = [\"browser__*\"]\nkeywords = [\"browse\", \"website\", \"url\", \"search\"]\n```",
      "properties": {
        "keywords": {
          "default": [],
          "description": "Keywords that activate this group in `dynamic` mode (case-insensitive substring).\nIgnored when `mode = \"always\"`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "mode": {
          "$ref": "#/$defs/ToolFilterGroupMode",
          "default": "dynamic",
          "description": "Activation mode: `\"always\"` or `\"dynamic\"`."
        },
        "tools": {
          "default": [],
          "description": "Glob patterns matching MCP tool names (single `*` wildcard supported).",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "ToolFilterGroupMode": {
      "description": "Determines when a `ToolFilterGroup` is active.",
      "oneOf": [
        {
          "const": "always",
          "description": "Tools in this group are always included in every turn.",
          "type": "string"
        },
        {
          "const": "dynamic",
          "description": "Tools in this group are included only when the user message contains\nat least one of the configured `keywords` (case-insensitive substring match).",
          "type": "string"
        }
      ]
    },
    "ToolReceiptsConfig": {
      "description": "HMAC tool execution receipt configuration, per agent\n(`[agents.<alias>.tool_receipts]`).\n\nReceipts are short HMAC-SHA256 tags appended to tool results so the model\ncannot claim it ran a tool that never actually executed. See\n`docs/book/src/security/tool-receipts.md`.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Generate HMAC receipts on every tool execution. Default: `false`.\nWhen false, the entire receipt subsystem is inert (no key, no\ngeneration, no append, no system-prompt addendum).",
          "type": "boolean"
        },
        "inject_system_prompt": {
          "default": true,
          "description": "Inject the receipt-echo instruction into the system prompt so the\nmodel carries receipts verbatim into its response. Default: `true`.\nNo effect when `enabled = false`.",
          "type": "boolean"
        },
        "show_in_response": {
          "default": false,
          "description": "Append a trailing `Tool receipts:` block to user-visible replies so\nreceipts are auditable from the channel surface, not just the\ninternal history. Default: `false`.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "TranscriptionConfig": {
      "description": "Voice transcription configuration with multi-provider support.\n\nThe top-level `api_url`, `model`, and `api_key` fields remain for backward\ncompatibility with existing Groq-based configurations.",
      "properties": {
        "api_key": {
          "default": null,
          "description": "API key used for transcription requests (Groq transcription provider).\n\nUse the schema-mirror env grammar (`ZEROCLAW_transcription__api_key=...`)\nfor runtime env injection. Legacy `GROQ_API_KEY` constructor fallback was\nremoved in v0.8.0.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "api_url": {
          "default": "https://api.groq.com/openai/v1/audio/transcriptions",
          "description": "Whisper API endpoint URL (Groq transcription provider).",
          "type": "string"
        },
        "assemblyai": {
          "anyOf": [
            {
              "$ref": "#/$defs/AssemblyAiSttConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "AssemblyAI STT model_provider configuration."
        },
        "deepgram": {
          "anyOf": [
            {
              "$ref": "#/$defs/DeepgramSttConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Deepgram STT model_provider configuration."
        },
        "enabled": {
          "default": false,
          "description": "Enable voice transcription for channels that support it.",
          "type": "boolean"
        },
        "google": {
          "anyOf": [
            {
              "$ref": "#/$defs/GoogleSttConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Google Cloud Speech-to-Text model_provider configuration."
        },
        "initial_prompt": {
          "default": null,
          "description": "Optional initial prompt to bias transcription toward expected vocabulary\n(proper nouns, technical terms, etc.). Sent as the `prompt` field in the\nWhisper API request.",
          "type": [
            "string",
            "null"
          ]
        },
        "language": {
          "default": null,
          "description": "Optional language hint (ISO-639-1, e.g. \"en\", \"ru\") for Groq transcription provider.",
          "type": [
            "string",
            "null"
          ]
        },
        "local_whisper": {
          "anyOf": [
            {
              "$ref": "#/$defs/LocalWhisperConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Local/self-hosted Whisper-compatible STT model_provider."
        },
        "max_audio_bytes": {
          "default": null,
          "description": "Optional global audio size upper bound in bytes, enforced before\ndispatching to any transcription provider. Provider-specific caps still\napply.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "max_duration_secs": {
          "default": 120,
          "description": "Maximum voice duration in seconds (messages longer than this are skipped).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "model": {
          "default": "whisper-large-v3-turbo",
          "description": "Whisper model name (Groq transcription provider).",
          "type": "string"
        },
        "openai": {
          "anyOf": [
            {
              "$ref": "#/$defs/OpenAiSttConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "OpenAI Whisper STT model_provider configuration."
        },
        "transcribe_non_ptt_audio": {
          "default": false,
          "description": "Also transcribe non-PTT (forwarded/regular) audio messages on WhatsApp,\nnot just voice notes. Default: `false` (preserves legacy behavior).",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "TranscriptionCostRates": {
      "description": "Rates for a transcription model, in USD per minute of audio.",
      "properties": {
        "per_minute": {
          "description": "Audio transcribed (USD per minute).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "TranscriptionCostRatesByProvider": {
      "description": "`cost.rates.providers.transcription.<type>.<model>`\n — per-(provider type, resource) cost rates.",
      "properties": {
        "assemblyai": {
          "additionalProperties": {
            "$ref": "#/$defs/TranscriptionCostRates"
          },
          "type": "object"
        },
        "deepgram": {
          "additionalProperties": {
            "$ref": "#/$defs/TranscriptionCostRates"
          },
          "type": "object"
        },
        "google": {
          "additionalProperties": {
            "$ref": "#/$defs/TranscriptionCostRates"
          },
          "type": "object"
        },
        "groq": {
          "additionalProperties": {
            "$ref": "#/$defs/TranscriptionCostRates"
          },
          "type": "object"
        },
        "local_whisper": {
          "additionalProperties": {
            "$ref": "#/$defs/TranscriptionCostRates"
          },
          "type": "object"
        },
        "openai": {
          "additionalProperties": {
            "$ref": "#/$defs/TranscriptionCostRates"
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "TranscriptionProviderRef": {
      "description": "Reference to a configured `[providers.transcription.<type>.<alias>]` entry.\nEmpty value means \"no preference\" (opt-out). Non-empty values must\nresolve to a configured alias; `Config::validate()` enforces this.",
      "type": "string"
    },
    "TranscriptionProviders": {
      "description": "Typed transcription-provider container — one slot per STT family.\nMirrors `ModelProviders` / `TtsProviders`. Closed set of 6 families:\ngroq, openai, deepgram, assemblyai, google, local_whisper.",
      "properties": {
        "assemblyai": {
          "additionalProperties": {
            "$ref": "#/$defs/AssemblyAiTranscriptionProviderConfig"
          },
          "type": "object"
        },
        "deepgram": {
          "additionalProperties": {
            "$ref": "#/$defs/DeepgramTranscriptionProviderConfig"
          },
          "type": "object"
        },
        "google": {
          "additionalProperties": {
            "$ref": "#/$defs/GoogleTranscriptionProviderConfig"
          },
          "type": "object"
        },
        "groq": {
          "additionalProperties": {
            "$ref": "#/$defs/GroqTranscriptionProviderConfig"
          },
          "type": "object"
        },
        "local_whisper": {
          "additionalProperties": {
            "$ref": "#/$defs/LocalWhisperTranscriptionProviderConfig"
          },
          "type": "object"
        },
        "openai": {
          "additionalProperties": {
            "$ref": "#/$defs/OpenAiTranscriptionProviderConfig"
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "TrustConfig": {
      "properties": {
        "correction_penalty": {
          "default": 0.05,
          "format": "double",
          "type": "number"
        },
        "decay_half_life_days": {
          "default": 30.0,
          "format": "double",
          "type": "number"
        },
        "initial_score": {
          "default": 0.8,
          "format": "double",
          "type": "number"
        },
        "regression_threshold": {
          "default": 0.5,
          "format": "double",
          "type": "number"
        },
        "success_boost": {
          "default": 0.01,
          "format": "double",
          "type": "number"
        }
      },
      "type": "object"
    },
    "TtsConfig": {
      "description": "Text-to-Speech subsystem configuration (`[tts]`).\n\nPer-instance TTS configs live under `[tts_providers.<type>.<alias>]`\n(parallel to `providers.models`). What remains here are the global\nruntime knobs that apply to every model_provider invocation.",
      "properties": {
        "default_format": {
          "default": "mp3",
          "description": "Default audio output format (`\"mp3\"`, `\"opus\"`, `\"wav\"`).",
          "type": "string"
        },
        "default_voice": {
          "default": "alloy",
          "description": "Default voice ID passed to the selected tts provider.",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Enable TTS synthesis.",
          "type": "boolean"
        },
        "max_text_length": {
          "default": 4096,
          "description": "Maximum input text length in characters (default 4096).",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "TtsCostRates": {
      "description": "Rates for a TTS model, in USD per 1M characters.",
      "properties": {
        "per_mchar": {
          "description": "Characters synthesised (USD per 1M).",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "TtsCostRatesByProvider": {
      "description": "`cost.rates.providers.tts.<type>.<voice>`\n — per-(provider type, resource) cost rates.",
      "properties": {
        "edge": {
          "additionalProperties": {
            "$ref": "#/$defs/TtsCostRates"
          },
          "type": "object"
        },
        "elevenlabs": {
          "additionalProperties": {
            "$ref": "#/$defs/TtsCostRates"
          },
          "type": "object"
        },
        "google": {
          "additionalProperties": {
            "$ref": "#/$defs/TtsCostRates"
          },
          "type": "object"
        },
        "openai": {
          "additionalProperties": {
            "$ref": "#/$defs/TtsCostRates"
          },
          "type": "object"
        },
        "piper": {
          "additionalProperties": {
            "$ref": "#/$defs/TtsCostRates"
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "TtsProviderRef": {
      "description": "Reference to a configured `[providers.tts.<type>.<alias>]` entry.\nEmpty value means \"no preference\" (opt-out). Non-empty values must\nresolve to a configured alias; `Config::validate()` enforces this.",
      "type": "string"
    },
    "TtsProviders": {
      "description": "Typed TTS-provider container — one slot per TTS family. Mirrors\n`ModelProviders` but smaller (TTS has a closed set of 5 families:\nopenai, elevenlabs, google, edge, piper). No catch-all needed.",
      "properties": {
        "edge": {
          "additionalProperties": {
            "$ref": "#/$defs/EdgeTtsProviderConfig"
          },
          "type": "object"
        },
        "elevenlabs": {
          "additionalProperties": {
            "$ref": "#/$defs/ElevenLabsTtsProviderConfig"
          },
          "type": "object"
        },
        "google": {
          "additionalProperties": {
            "$ref": "#/$defs/GoogleTtsProviderConfig"
          },
          "type": "object"
        },
        "openai": {
          "additionalProperties": {
            "$ref": "#/$defs/OpenAITtsProviderConfig"
          },
          "type": "object"
        },
        "piper": {
          "additionalProperties": {
            "$ref": "#/$defs/PiperTtsProviderConfig"
          },
          "type": "object"
        }
      },
      "type": "object"
    },
    "TunnelConfig": {
      "description": "Tunnel configuration for exposing the gateway publicly (`[tunnel]` section).\n\nSupported model_providers: `\"none\"` (default), `\"cloudflare\"`, `\"tailscale\"`, `\"ngrok\"`, `\"openvpn\"`, `\"pinggy\"`, `\"custom\"`.",
      "properties": {
        "cloudflare": {
          "anyOf": [
            {
              "$ref": "#/$defs/CloudflareTunnelConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Cloudflare Tunnel configuration (used when `tunnel_provider = \"cloudflare\"`)."
        },
        "custom": {
          "anyOf": [
            {
              "$ref": "#/$defs/CustomTunnelConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Custom tunnel command configuration (used when `tunnel_provider = \"custom\"`)."
        },
        "ngrok": {
          "anyOf": [
            {
              "$ref": "#/$defs/NgrokTunnelConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "ngrok tunnel configuration (used when `tunnel_provider = \"ngrok\"`)."
        },
        "openvpn": {
          "anyOf": [
            {
              "$ref": "#/$defs/OpenVpnTunnelConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "OpenVPN tunnel configuration (used when `tunnel_provider = \"openvpn\"`)."
        },
        "pinggy": {
          "anyOf": [
            {
              "$ref": "#/$defs/PinggyTunnelConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Pinggy tunnel configuration (used when `tunnel_provider = \"pinggy\"`)."
        },
        "tailscale": {
          "anyOf": [
            {
              "$ref": "#/$defs/TailscaleTunnelConfig"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "description": "Tailscale Funnel/Serve configuration (used when `tunnel_provider = \"tailscale\"`)."
        },
        "tunnel_provider": {
          "default": "none",
          "description": "How the gateway gets exposed to the public internet so webhooks (Telegram, Slack, etc.) can reach it. `none` = keep it local, no tunnel; `cloudflare` = Cloudflare Tunnel via cloudflared (needs a Zero Trust account and token); `tailscale` = Tailscale Funnel/Serve (tailnet-only or public, no account beyond tailscale); `ngrok` = ngrok agent with auth token; `openvpn` = bring-your-own OpenVPN egress; `pinggy` = Pinggy SSH tunnels (quick one-shot URLs); `custom` = run an arbitrary command you define under `[tunnel.custom]`.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "TwitchConfig": {
      "description": "Twitch chat channel configuration. A thin adapter over IRC\n(`irc.chat.twitch.tv:6697` over TLS); see the `twitch` channel module.",
      "properties": {
        "bot_username": {
          "description": "Twitch login name of the bot account (case-insensitive — lowercased\nbefore send).",
          "type": "string"
        },
        "channels": {
          "default": [],
          "description": "Twitch channels to join. Each entry receives a `#` prefix if missing\nand is lowercased before send (Twitch channel names are\ncase-insensitive). E.g. `[\"mychannel\", \"#anotherchannel\"]`.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false`.",
          "type": "boolean"
        },
        "mention_only": {
          "default": false,
          "description": "When true, only respond to messages that mention the bot's login\nname. Default: `false`.",
          "type": "boolean"
        },
        "oauth_token": {
          "description": "Twitch OAuth user-access token. The `oauth:` prefix is added\nautomatically if missing, so both `\"oauth:abcdef\"` and `\"abcdef\"`\nwork. Mint via <https://twitchapps.com/tmi/> or the Twitch CLI.",
          "type": "string",
          "x-secret": true
        }
      },
      "required": [
        "bot_username",
        "oauth_token"
      ],
      "type": "object"
    },
    "TwitterConfig": {
      "description": "X/Twitter channel configuration (Twitter API v2)",
      "properties": {
        "bearer_token": {
          "description": "Twitter API v2 Bearer Token (OAuth 2.0)",
          "type": "string",
          "x-secret": true
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "required": [
        "bearer_token"
      ],
      "type": "object"
    },
    "UpstageModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "VeniceModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "VercelModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "VerifiableIntentConfig": {
      "description": "Verifiable Intent (VI) credential verification and issuance (`[verifiable_intent]` section).",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable VI credential verification on commerce tool calls (default: false).",
          "type": "boolean"
        },
        "strictness": {
          "default": "strict",
          "description": "Strictness mode for constraint evaluation: \"strict\" (fail-closed on unknown\nconstraint types) or \"permissive\" (skip unknown types with a warning).\nDefault: \"strict\".",
          "type": "string"
        }
      },
      "type": "object"
    },
    "VllmModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "VoiceCallConfig": {
      "properties": {
        "account_id": {
          "type": "string"
        },
        "auth_token": {
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "from_number": {
          "type": "string"
        },
        "max_call_duration_secs": {
          "default": 3600,
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "model_provider": {
          "$ref": "#/$defs/VoiceProvider",
          "default": "twilio"
        },
        "require_outbound_approval": {
          "default": true,
          "type": "boolean"
        },
        "transcription_logging": {
          "default": true,
          "type": "boolean"
        },
        "tts_voice": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "webhook_base_url": {
          "default": null,
          "type": [
            "string",
            "null"
          ]
        },
        "webhook_port": {
          "default": 8090,
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        }
      },
      "required": [
        "account_id",
        "auth_token",
        "from_number"
      ],
      "type": "object"
    },
    "VoiceDuplexConfig": {
      "description": "Voice duplex configuration (`[channels.voice_duplex]`).\n\nEnables full-duplex voice event handling over WebSocket.\nWhen disabled (default), voice events are rejected as unknown types.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        }
      },
      "type": "object"
    },
    "VoiceProvider": {
      "description": "Which telephony model_provider to use.",
      "enum": [
        "twilio",
        "telnyx",
        "plivo"
      ],
      "type": "string"
    },
    "VoiceWakeConfig": {
      "description": "Voice wake word detection channel configuration.\n\nListens on the default microphone for a configurable wake word,\nthen captures the following utterance and transcribes it via the\nexisting transcription API.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "energy_threshold": {
          "default": 0.009999999776482582,
          "description": "RMS energy threshold for voice activity detection. Samples below\nthis level are treated as silence. Default: `0.01`.",
          "format": "float",
          "type": "number"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "max_capture_secs": {
          "default": 30,
          "description": "Maximum capture duration in seconds before forcing transcription.\nDefault: `30`.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "silence_timeout_ms": {
          "default": 2000,
          "description": "Silence timeout in milliseconds: how long to wait after the last\nenergy spike before finalizing a capture window. Default: `2000`.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "wake_word": {
          "default": "hey zeroclaw",
          "description": "Wake word phrase to listen for (case-insensitive substring match).\nDefault: `\"hey zeroclaw\"`.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "WatiConfig": {
      "description": "WATI WhatsApp Business API channel configuration.",
      "properties": {
        "api_token": {
          "description": "WATI API token (Bearer auth).",
          "type": "string",
          "x-secret": true
        },
        "api_url": {
          "default": "https://live-mt-server.wati.io",
          "description": "WATI API base URL (default: <https://live-mt-server.wati.io>).",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "proxy_url": {
          "default": null,
          "description": "Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",
          "type": [
            "string",
            "null"
          ]
        },
        "tenant_id": {
          "default": null,
          "description": "Tenant ID for multi-channel setups (optional).",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "api_token"
      ],
      "type": "object"
    },
    "WeChatConfig": {
      "description": "WeChat personal iLink Bot channel configuration.\n\nUses the iLink Bot API (`ilinkai.weixin.qq.com`) with QR-code login.\nThe bot token is obtained by scanning a QR code and persisted to disk\nso subsequent restarts do not require re-scanning.",
      "properties": {
        "api_base_url": {
          "default": null,
          "description": "Override the iLink API base URL. Default: `https://ilinkai.weixin.qq.com`.",
          "type": [
            "string",
            "null"
          ]
        },
        "cdn_base_url": {
          "default": null,
          "description": "Override the CDN base URL. Default: `https://novac2c.cdn.weixin.qq.com/c2c`.",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "state_dir": {
          "default": null,
          "description": "Directory to persist bot token and sync cursor.\nDefault: `~/.zeroclaw/wechat/`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "WeComConfig": {
      "description": "WeCom (WeChat Enterprise) Bot Webhook configuration",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "webhook_key": {
          "description": "Webhook key from WeCom Bot configuration",
          "type": "string",
          "x-secret": true
        }
      },
      "required": [
        "webhook_key"
      ],
      "type": "object"
    },
    "WeComWsConfig": {
      "description": "WeCom AI Bot WebSocket configuration.\n\nThis is distinct from webhook-based [`WeComConfig`] and uses the WeCom AI\nBot long-connection API for inbound messages and active-session replies.",
      "properties": {
        "allowed_groups": {
          "default": [],
          "description": "Allowed WeCom group chat IDs. Empty = deny all groups, \"*\" = allow all groups.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "allowed_users": {
          "default": [],
          "description": "Allowed WeCom user IDs. Empty = deny all, \"*\" = allow all users.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "bot_id": {
          "description": "Bot ID for WeCom WebSocket subscription.",
          "type": "string"
        },
        "bot_name": {
          "default": null,
          "description": "Display name or mention alias of the WeCom AI bot, for example `danya`.\n\nWeCom group text often arrives as plain text such as `@danya say hi`;\npassing this name through lets the generic reply-intent precheck\nrecognize that a group message was addressed to the bot.",
          "type": [
            "string",
            "null"
          ]
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "file_retention_days": {
          "default": 7,
          "description": "File retention days for downloaded WeCom attachments under the workspace cache.",
          "format": "uint32",
          "minimum": 0,
          "type": "integer"
        },
        "max_file_size_mb": {
          "default": 20,
          "description": "Maximum accepted file size in MiB for WeCom attachment download attempts.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "proxy_url": {
          "default": null,
          "description": "Optional per-channel proxy override. Falls back to the global proxy config when empty.",
          "type": [
            "string",
            "null"
          ]
        },
        "secret": {
          "description": "Secret for WeCom WebSocket subscription authentication.",
          "type": "string",
          "x-secret": true
        },
        "stream_mode": {
          "$ref": "#/$defs/StreamMode",
          "default": "partial",
          "description": "Streaming mode for progressive draft delivery over the WeCom long connection."
        }
      },
      "required": [
        "bot_id",
        "secret"
      ],
      "type": "object"
    },
    "WebAuthnConfig": {
      "description": "WebAuthn / FIDO2 hardware key authentication configuration (`[security.webauthn]`).\n\nEnables registration and authentication via hardware security keys\n(YubiKey, SoloKey, etc.) and platform authenticators (Touch ID, Windows Hello).",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable WebAuthn authentication. Default: false.",
          "type": "boolean"
        },
        "rp_id": {
          "default": "localhost",
          "description": "Relying Party ID (domain name, e.g. \"example.com\"). Default: \"localhost\".",
          "type": "string"
        },
        "rp_name": {
          "default": "ZeroClaw",
          "description": "Relying Party display name. Default: \"ZeroClaw\".",
          "type": "string"
        },
        "rp_origin": {
          "default": "http://localhost:42617",
          "description": "Relying Party origin URL (e.g. `\"https://example.com\"`). Default: `\"http://localhost:42617\"`.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "WebFetchConfig": {
      "description": "Web fetch tool configuration (`[web_fetch]` section).\n\nFetches web pages and converts HTML to plain text for LLM consumption.\nDomain filtering: `allowed_domains` controls which hosts are reachable (use `[\"*\"]`\nfor all public hosts). `blocked_domains` takes priority over `allowed_domains`.\nIf `allowed_domains` is empty, all requests are rejected (deny-by-default).",
      "properties": {
        "allowed_domains": {
          "default": [
            "*"
          ],
          "description": "Allowed domains for web fetch (exact or subdomain match; `[\"*\"]` = all public hosts)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "allowed_private_hosts": {
          "default": [],
          "description": "Private/internal hosts allowed to bypass SSRF protection (e.g. `[\"192.168.1.10\", \"internal.local\"]`).\nExact and subdomain matches are supported. Listing a host skips the\nresolved-IP SSRF check for it; a *literal* private/local host (IP literal,\nlocalhost, .local) listed here also bypasses `allowed_domains`. The `*`\nwildcard permits any literal private/local host and lets a name already in\n`allowed_domains` resolve to a private IP, but it does NOT widen\n`allowed_domains` — a non-private host (matched explicitly or via `*`) still\nneeds `allowed_domains`, so `*` cannot reach an arbitrary public host.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "blocked_domains": {
          "default": [],
          "description": "Blocked domains (exact or subdomain match; always takes priority over allowed_domains)",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "enabled": {
          "default": true,
          "description": "Enable `web_fetch` tool for fetching web page content",
          "type": "boolean"
        },
        "firecrawl": {
          "$ref": "#/$defs/FirecrawlConfig",
          "default": {
            "api_key_env": "FIRECRAWL_API_KEY",
            "api_url": "https://api.firecrawl.dev/v1",
            "enabled": false,
            "mode": "scrape"
          },
          "description": "Firecrawl fallback configuration (`[web_fetch.firecrawl]`)"
        },
        "max_response_size": {
          "default": 500000,
          "description": "Maximum response size in bytes (default: 500KB, plain text is much smaller than raw HTML)",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "timeout_secs": {
          "default": 30,
          "description": "Request timeout in seconds (default: 30)",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "WebSearchConfig": {
      "description": "Web search tool configuration (`[web_search]` section).",
      "properties": {
        "bocha_api_key": {
          "default": null,
          "description": "Bocha AI Web Search API key (required if search_provider is `\"bocha\"`). Obtain at <https://open.bochaai.com>.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "brave_api_key": {
          "default": null,
          "description": "Brave Search API key (required if search_provider is \"brave\")",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "enabled": {
          "default": true,
          "description": "Enable `web_search_tool` for web searches",
          "type": "boolean"
        },
        "jina_api_key": {
          "default": null,
          "description": "Jina AI API key (required if search_provider is \"jina\")",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "max_results": {
          "default": 5,
          "description": "Maximum results per search (1-10)",
          "format": "uint",
          "minimum": 0,
          "type": "integer"
        },
        "search_provider": {
          "default": "duckduckgo",
          "description": "Search provider: \"duckduckgo\" (free), \"brave\" (requires API key), \"tavily\" (requires API key), \"searxng\" (self-hosted), \"jina\" (requires API key), or \"bocha\" (Bocha AI, requires API key — Chinese-friendly, <https://open.bochaai.com>)",
          "type": "string"
        },
        "searxng_instance_url": {
          "default": null,
          "description": "SearXNG instance URL (required if search_provider is `\"searxng\"`), e.g. `\"https://searx.example.com\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "tavily_api_key": {
          "default": null,
          "description": "Tavily Search API key (required if search_provider is \"tavily\")",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "timeout_secs": {
          "default": 15,
          "description": "Request timeout in seconds",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "WebhookAuditConfig": {
      "description": "Configuration for the webhook-audit builtin hook.\n\nSends an HTTP POST with a JSON body to an external endpoint each time\na tool call matches one of the configured patterns. Useful for\ncentralised audit logging, SIEM ingestion, or compliance pipelines.",
      "properties": {
        "enabled": {
          "default": false,
          "description": "Enable the webhook-audit hook. Default: `false`.",
          "type": "boolean"
        },
        "include_args": {
          "default": false,
          "description": "Include tool call arguments in the audit payload. Default: `false`.\n\nBe mindful of sensitive data — arguments may contain secrets or PII.",
          "type": "boolean"
        },
        "max_args_bytes": {
          "default": 4096,
          "description": "Maximum size (in bytes) of serialised arguments included in a single\naudit payload. Arguments exceeding this limit are truncated.\nDefault: `4096`.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "tool_patterns": {
          "default": [],
          "description": "Glob patterns for tool names to audit (e.g. `[\"Bash\", \"Write\"]`).\nAn empty list means **no** tools are audited.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "url": {
          "default": "",
          "description": "Target URL that will receive the audit POST requests.",
          "type": "string"
        }
      },
      "type": "object"
    },
    "WebhookConfig": {
      "description": "Webhook channel configuration.\n\nReceives messages via HTTP POST and sends replies to a configurable outbound URL.\nThis is the \"universal adapter\" for any system that supports webhooks.",
      "properties": {
        "auth_header": {
          "default": null,
          "description": "Optional `Authorization` header value for outbound requests.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "listen_path": {
          "default": null,
          "description": "URL path to listen on (default: `/webhook`).",
          "type": [
            "string",
            "null"
          ]
        },
        "max_retries": {
          "default": null,
          "description": "Maximum number of retry attempts for outbound sends on transient failures\n(network errors, 429, 5xx). Set to `0` to disable retries. Default: `3`.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "port": {
          "default": 8090,
          "description": "Port to listen on for incoming webhooks.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "reply_min_interval_secs": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "reply_queue_depth_max": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "retry_base_delay_ms": {
          "default": null,
          "description": "Base delay in milliseconds for exponential backoff between retries. Default: `500`.\nValues below `1` are clamped to `1ms` at runtime to avoid busy-retry loops.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "retry_max_delay_ms": {
          "default": null,
          "description": "Maximum delay cap in milliseconds for any single retry wait. Default: `30000` (30s).\nValues below `1` are clamped to `1ms` at runtime to avoid busy-retry loops.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "secret": {
          "description": "Shared secret for webhook signature verification (HMAC-SHA256).\nThe channel will refuse to start without one. Set\n`[channels.webhook.<alias>].secret` in config.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "send_method": {
          "default": null,
          "description": "HTTP method for outbound messages (`POST` or `PUT`). Default: `POST`.",
          "type": [
            "string",
            "null"
          ]
        },
        "send_url": {
          "default": null,
          "description": "URL to POST/PUT outbound messages to.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "WhatsAppChatPolicy": {
      "description": "Policy for a particular WhatsApp chat type (DMs or groups) when\n`mode = \"personal\"`.",
      "oneOf": [
        {
          "const": "allowlist",
          "description": "Only respond to senders on the `allowed_numbers` list (default).",
          "type": "string"
        },
        {
          "const": "ignore",
          "description": "Ignore all messages in this chat type.",
          "type": "string"
        },
        {
          "const": "all",
          "description": "Respond to every message regardless of allowlist.",
          "type": "string"
        }
      ]
    },
    "WhatsAppConfig": {
      "description": "WhatsApp channel configuration (Cloud API or Web mode).\n\nSet `phone_number_id` for Cloud API mode, or `session_path` for Web mode.",
      "properties": {
        "access_token": {
          "default": null,
          "description": "Access token from Meta Business Suite (Cloud API mode)",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "allowed_groups": {
          "default": [],
          "description": "Allowed group chats by JID (Web mode). An empty list (the default)\npermits all groups; a non-empty list drops every group message whose\nchat JID matches no entry. Each entry matches either the full group\nJID (`123456789012345@g.us`) or the JID user part - the segment before\n`@` (`123456789012345`) - compared exactly, not as a string prefix.\nDirect messages bypass this filter regardless of list contents.\nModeled on the Matrix channel's `allowed_rooms`; it gates group\nidentity, which `dm_policy`/`group_policy` (chat type) and the\nsender allowlist (sender) do not.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "app_secret": {
          "default": null,
          "description": "App secret from Meta Business Suite (for webhook signature verification)\nCan also be set via `ZEROCLAW_WHATSAPP_APP_SECRET` environment variable\nOnly used in Cloud API mode",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "approval_timeout_secs": {
          "default": 300,
          "description": "Seconds to wait for operator approval on `always_ask` tools before auto-denying.",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "dm_mention_patterns": {
          "default": [],
          "description": "Regex patterns for DM mention gating (case-insensitive).\nWhen non-empty, only direct messages matching at least one pattern are\nprocessed; matched fragments are stripped from the forwarded content.\nExample: `[\"@?ZeroClaw\", \"\\\\+?15555550123\"]`",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "dm_policy": {
          "$ref": "#/$defs/WhatsAppChatPolicy",
          "default": "allowlist",
          "description": "Policy for direct messages when mode = \"personal\".\n\"allowlist\" (default) | \"ignore\" | \"all\"."
        },
        "enabled": {
          "default": false,
          "description": "Whether this channel is active. The runtime only loads channels whose\n`enabled = true`. Default: `false` so an operator who pastes a partial\n`[channels.<type>.<alias>]` block doesn't accidentally bring a channel\nlive before the rest of its config is filled in.",
          "type": "boolean"
        },
        "excluded_tools": {
          "default": [],
          "description": "Tools excluded from this channel's tool spec. When set, these tools\nare not exposed to the model when responding via this channel.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "group_mention_patterns": {
          "default": [],
          "description": "Regex patterns for group-chat mention gating (case-insensitive).\nWhen non-empty, only group messages matching at least one pattern are\nprocessed; matched fragments are stripped from the forwarded content.\nExample: `[\"@?ZeroClaw\", \"\\\\+?15555550123\"]`",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "group_policy": {
          "$ref": "#/$defs/WhatsAppChatPolicy",
          "default": "allowlist",
          "description": "Policy for group chats when mode = \"personal\".\n\"allowlist\" (default) | \"ignore\" | \"all\"."
        },
        "interrupt_on_new_message": {
          "default": false,
          "description": "Cancel an in-flight response from this channel sender when a newer\nWhatsApp message arrives. Default: `false`.",
          "type": "boolean"
        },
        "mention_only": {
          "default": false,
          "description": "When true, only respond to messages that @-mention the bot in groups (Web mode only).\nDirect messages are always processed.\nBot identity is resolved from the wa-rs device at runtime; `pair_phone` seeds it on first connect.",
          "type": "boolean"
        },
        "mode": {
          "$ref": "#/$defs/WhatsAppWebMode",
          "default": "business",
          "description": "Usage mode for WhatsApp Web: \"business\" (default) or \"personal\".\nIn personal mode the bot applies dm_policy, group_policy, and\nself_chat_mode to decide which chats to respond in."
        },
        "pair_code": {
          "default": null,
          "description": "Custom pair code for linking (Web mode, optional)\nLeave empty to let WhatsApp generate one",
          "type": [
            "string",
            "null"
          ]
        },
        "pair_phone": {
          "default": null,
          "description": "Phone number for pair code linking (Web mode, optional)\nFormat: country code + number (e.g., \"15551234567\")\nIf not set, QR code pairing will be used",
          "type": [
            "string",
            "null"
          ]
        },
        "passive_group_context": {
          "default": false,
          "description": "When true in WhatsApp Web group chats, unaddressed messages that pass\nsender/chat authorization are recorded as passive conversation context\nwithout starting an agent turn. Default: `false`.",
          "type": "boolean"
        },
        "phone_number_id": {
          "default": null,
          "description": "Phone number ID from Meta Business API (Cloud API mode)",
          "type": [
            "string",
            "null"
          ]
        },
        "proxy_url": {
          "default": null,
          "description": "Per-channel proxy URL (http, https, socks5, socks5h).\nOverrides the global `[proxy]` setting for this channel only.",
          "type": [
            "string",
            "null"
          ]
        },
        "reply_min_interval_secs": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing floor in seconds.\nRange: `0..=REPLY_MIN_INTERVAL_MAX_SECS` (0 disables).",
          "format": "uint64",
          "minimum": 0,
          "type": "integer"
        },
        "reply_queue_depth_max": {
          "default": 0,
          "description": "Per-(channel, recipient) outbound pacing queue depth.\nRange: `0..=REPLY_QUEUE_DEPTH_CEILING`. When `reply_min_interval_secs > 0`\nand this value is `0`, the pacing wrapper substitutes\n`DEFAULT_REPLY_QUEUE_DEPTH` (16). When the queue is full, the\nnewest send is dropped and a `WARN` is logged.",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        },
        "self_chat_mode": {
          "default": false,
          "description": "When true and mode = \"personal\", always respond to messages in the\nuser's own self-chat (Notes to Self). Defaults to false.",
          "type": "boolean"
        },
        "session_path": {
          "default": null,
          "description": "Session database path for WhatsApp Web client (Web mode)\nWhen set, enables native WhatsApp Web mode with wa-rs",
          "type": [
            "string",
            "null"
          ]
        },
        "verify_token": {
          "default": null,
          "description": "Webhook verify token (you define this, Meta sends it back for verification)\nOnly used in Cloud API mode",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "ws_url": {
          "default": null,
          "description": "Override the WhatsApp Web WebSocket URL (Web mode, optional). Used\nby integration tests and proxy setups; leave unset to use the\ndefault endpoint that ships with `wa-rs`.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "type": "object"
    },
    "WhatsAppWebMode": {
      "description": "WhatsApp Web usage mode.\n\n`Personal` treats the account as a personal phone — the bot only responds to\nincoming messages that pass the DM/group/self-chat policy filters.\n`Business` (default) responds to all incoming messages, subject only to the\n`allowed_numbers` allowlist.",
      "oneOf": [
        {
          "const": "business",
          "description": "Respond to all messages passing the allowlist (default).",
          "type": "string"
        },
        {
          "const": "personal",
          "description": "Apply per-chat-type policies (dm_policy, group_policy, self_chat_mode).",
          "type": "string"
        }
      ]
    },
    "WireApi": {
      "description": "Wire protocol flavor for the model_provider client. `responses` routes\nthrough OpenAI's Responses API (`POST /v1/responses`);\n`chat_completions` routes through the legacy `/v1/chat/completions` (or\nthe family's chat-completions-compatible endpoint). New OpenAI provider\nslots default to `responses`; other families default to chat-completions\n(or ignore the field) when unset.",
      "enum": [
        "responses",
        "chat_completions"
      ],
      "type": "string"
    },
    "WssConfig": {
      "description": "WebSocket Secure (WSS) transport for remote TUI-to-daemon connections (`[wss]`).\n\nWhen enabled, the daemon listens for TLS-encrypted WebSocket connections\non the configured bind address and port. TUI clients connect via\n`--connect wss://host:port`.",
      "properties": {
        "bind": {
          "default": "0.0.0.0",
          "description": "Bind address for the WSS listener (default: \"0.0.0.0\").",
          "type": "string"
        },
        "cert_path": {
          "default": "",
          "description": "Path to the PEM-encoded server certificate file.",
          "type": "string"
        },
        "enabled": {
          "default": false,
          "description": "Enable the WSS listener (default: false).",
          "type": "boolean"
        },
        "key_path": {
          "default": "",
          "description": "Path to the PEM-encoded server private key file.",
          "type": "string"
        },
        "port": {
          "default": 9781,
          "description": "Port for the WSS listener (default: 9781).",
          "format": "uint16",
          "maximum": 65535,
          "minimum": 0,
          "type": "integer"
        }
      },
      "type": "object"
    },
    "XaiModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "YiModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    },
    "ZaiEndpoint": {
      "enum": [
        "cn",
        "global"
      ],
      "type": "string"
    },
    "ZaiModelProviderConfig": {
      "description": "Named model_provider profile definition.",
      "properties": {
        "api_key": {
          "description": "Secret API token for this model_provider. Grab it from the model_provider's dashboard (OpenAI platform, Anthropic console, OpenRouter keys page, etc.). Stored via the OS keyring when possible; never commit it to config.toml directly.",
          "type": [
            "string",
            "null"
          ],
          "x-secret": true
        },
        "chat_template_kwargs": {
          "description": "Arbitrary key/value pairs forwarded verbatim as `chat_template_kwargs`\nin the request body (llama.cpp-specific). Use this to pass model-family\ntemplate variables that control behaviour not exposed by other fields.\nExample (Qwen3 thinking suppression):\n  `chat_template_kwargs = { enable_thinking = false }`"
        },
        "context_window": {
          "description": "Context window size (max input tokens) for this model.\nAuto-populated on setup from provider's /models endpoint if available.\nOverride manually for custom endpoints or when auto-detection fails.",
          "format": "uint",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "endpoint": {
          "$ref": "#/$defs/ZaiEndpoint",
          "default": "global"
        },
        "extra_headers": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Extra HTTP headers sent with every request. Niche: used for auth bridges, corporate proxies, or custom gateways that demand a tracing header. Most users never touch this; edit `config.toml` directly if you need it.",
          "type": "object",
          "x-secret": true
        },
        "fallback": {
          "description": "Ordered list of other provider aliases to try when every model on this\nalias has failed. Each entry is a dotted `<type>.<alias>` reference into\n`providers.models` and resolves with its own credentials, endpoint, and\nmodel. A fallback never inherits this alias's key. The walk is\ndepth-first: this alias's models are exhausted first, then each fallback\nalias is descended in turn (applying its own `fallback_models` and\n`fallback`). Empty means no provider-level fallback.",
          "items": {
            "$ref": "#/$defs/ModelProviderRef"
          },
          "type": "array"
        },
        "fallback_models": {
          "description": "Ordered alternate models to try on THIS provider before falling over to\nthe `fallback` aliases. Same endpoint, key, and headers as the primary\n`model`. Only the model identifier changes. Use this when a provider\nserves a backup model (e.g. a smaller or older variant) that should be\ntried before leaving the provider entirely. Empty means only `model` is\ntried.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "kind": {
          "description": "Provider implementation to instantiate for this profile. Use this\nwhen a canonical typed slot should run through a compatible\nimplementation, e.g. `[providers.models.openai.proxy] kind =\n\"openai-compatible\"`.",
          "type": [
            "string",
            "null"
          ]
        },
        "live_pricing": {
          "description": "Pull live token prices for this provider's models from its own\nOpenAI-compatible `/models` listing (the gateway is the source of truth\nfor its prices), filling cost-tracking rates for models the operator\nhas NOT priced under `[cost.rates]` / `pricing`. Models the gateway does\nnot price (or providers with no HTTP `/models` listing at all, such as\na subprocess gateway like `kilocli`) fall back to the public models.dev\ncatalog. Configured rates always win; live prices only fill gaps. A\nbackground task refreshes the price snapshot hourly; the cost-recording\npath reads the cached snapshot and never blocks on the network. Default\n`false`: off means no fetching and behavior identical to a build\nwithout the feature.",
          "type": "boolean"
        },
        "max_tokens": {
          "description": "Hard cap on response length in tokens. Most models enforce sensible built-in limits already; leave unset unless you specifically need to clip long outputs for cost or latency reasons.",
          "format": "uint32",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "merge_system_into_user": {
          "description": "ModelProvider-specific quirk: fold the system prompt into the first user message instead of sending a separate system role. Only needed for models that reject (or mishandle) a standalone system role, e.g. certain older Mistral variants.",
          "type": "boolean"
        },
        "model": {
          "description": "Model identifier to send with each request: the ID string from the model_provider's catalog (e.g. `gpt-4o`, `claude-sonnet-4-5`, `llama-3.3-70b`). Must match a model the model_provider actually serves on this account.",
          "type": [
            "string",
            "null"
          ]
        },
        "native_tools": {
          "description": "Override the provider's default for native tool calling.\n`None` (default) honors the provider's built-in choice. `Some(true)`\nforces native tool calls on, `Some(false)` forces text-fallback.\nCurrently consulted only by the Groq factory, which defaults to\ntext-fallback because llama-family Groq models reject native tool\ncalls with HTTP 400. Setting `native_tools = true` re-enables native\ntool calling for Groq models that support it.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "pricing": {
          "additionalProperties": {
            "format": "double",
            "type": "number"
          },
          "description": "Per-model pricing for cost tracking, USD per 1M tokens.\n\nFree-form key/value map. Keys are user-defined model identifiers; an\noptional `.input` / `.output` suffix encodes pricing dimension when\nthe operator wants to split rates. A bare key without a suffix is\nused as a flat per-token rate when neither dimension is specified.\nDefault is empty: cost tracking falls back to \"unknown\" rates and\nonly token usage is recorded.\n\nExample: `pricing = { opus = 15.0, sonnet = 3.0 }`\nOr split: `pricing = { \"opus.input\" = 15.0, \"opus.output\" = 75.0 }`",
          "type": "object"
        },
        "provider_extra": {
          "description": "Extra JSON parameters to include in API requests.\nMerged at the top level of the request body, allowing provider-specific\nfeatures (routing, transforms, etc.) without code changes.\nExample: `provider_extra = { model_provider = { only = [\"Anthropic\"] } }`"
        },
        "replay_assistant_reasoning": {
          "description": "Whether stored assistant reasoning should be replayed on outbound\nassistant history messages. `Some(false)` strips `reasoning_content`\nand `reasoning` before sending. `None` (default) honours the provider's\nbuilt-in default (true for most compat providers, false for Groq).",
          "type": [
            "boolean",
            "null"
          ]
        },
        "requires_openai_auth": {
          "description": "When true, the client pulls credentials from ZeroClaw's stored `openai-codex` auth profile instead of the `api_key` field above. Import an existing Codex CLI login with `zeroclaw auth login --model-provider openai-codex --import ~/.codex/auth.json`, or run `zeroclaw auth login --model-provider openai-codex`. Turn on only for the OpenAI Codex model_provider; leave off for standard API-key model_providers.",
          "type": "boolean"
        },
        "temperature": {
          "description": "Sampling temperature passed to the model. Lower values (0.0–0.3) give\ndeterministic, near-verbatim output, which fits code, routing, summarization.\nHigher values (0.7–1.2) give more varied output, which fits open-ended chat.",
          "format": "double",
          "type": [
            "number",
            "null"
          ]
        },
        "think": {
          "description": "Enable or disable chain-of-thought thinking for models that support it\n(e.g. Qwen3, GLM-4). `true` turns thinking on, `false` turns it off.\n`None` (default) lets the model decide. Forwarded as `enable_thinking`\nin the request body; mirrors the Ollama provider's `think` field.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "timeout_secs": {
          "description": "HTTP request timeout in seconds. Bump this for slow local model_providers (Ollama on CPU, big local models) or high-latency networks; leave unset otherwise.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "tls_ca_cert_path": {
          "description": "Path to a PEM-encoded CA certificate for TLS connections to this provider.\nMust be an absolute path; shell expansion (e.g. `~`) is not performed.\nLeave unset to use the system's default trust store.",
          "type": [
            "string",
            "null"
          ]
        },
        "uri": {
          "description": "Endpoint URI the client hits. Override the family's default endpoint when pointing at a self-hosted gateway (LiteLLM, vLLM, Ollama), a custom proxy, or any non-standard URL. Leave unset to use the family's default URI from its `ModelEndpoint` impl. Set this to the FULL endpoint URL; there is no separate path-suffix field.",
          "type": [
            "string",
            "null"
          ]
        },
        "vision": {
          "description": "Override the provider's vision (image input) capability.\n`None` (default) uses the provider family's built-in default. Several\nfamilies (llama.cpp, the generic OpenAI-compatible endpoint, etc.)\nassume vision-capable because they can serve multimodal models. Set\n`vision = false` for a text-only model served by such a family (e.g. a\ntext LLM behind llama.cpp) so image messages are routed to a configured\n`[multimodal] vision_model_provider` instead of being sent to a model\nthat rejects them. `Some(true)` forces vision on.",
          "type": [
            "boolean",
            "null"
          ]
        },
        "wire_api": {
          "anyOf": [
            {
              "$ref": "#/$defs/WireApi"
            },
            {
              "type": "null"
            }
          ],
          "description": "Wire protocol flavor: `responses` for OpenAI's Responses API (`POST /v1/responses`), `chat_completions` for the legacy chat wire and most OpenAI-compatible gateways. New OpenAI provider slots default to `responses`; other families default to chat-completions (or ignore the field). Only override if you're forcing an unusual combination."
        }
      },
      "type": "object"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Top-level ZeroClaw configuration, loaded from `config.toml`.\n\nResolution order: `ZEROCLAW_CONFIG_DIR` env → `ZEROCLAW_WORKSPACE` env → `~/.zeroclaw/config.toml`.",
  "properties": {
    "a2a": {
      "$ref": "#/$defs/A2aServerSection",
      "default": {
        "server": {
          "enabled": false,
          "public_base_url": ""
        }
      },
      "description": "Inbound A2A discovery server (`[a2a.server]`). Default-closed:\nserves the well-known catalog card and per-alias agent cards only\nwhen `enabled = true`. See `crate::multi_agent::A2aServerConfig`."
    },
    "acp": {
      "$ref": "#/$defs/AcpConfig",
      "default": {
        "max_sessions": 10,
        "session_timeout_secs": 3600
      },
      "description": "ACP (Agent Client Protocol) server configuration (`[acp]`)."
    },
    "agents": {
      "additionalProperties": {
        "$ref": "#/$defs/AliasedAgentConfig"
      },
      "default": {},
      "description": "Aliased agents in this install. Each entry under `[agents.<alias>]`\nis one user-facing agent with its own identity, channels, model\nprovider, risk profile, workspace, and memory scope.\n`DelegateTool` consults this map when one agent delegates a\nsubtask to another.",
      "type": "object"
    },
    "backup": {
      "$ref": "#/$defs/BackupConfig",
      "default": {
        "compress": true,
        "destination_dir": "state/backups",
        "enabled": true,
        "encrypt": false,
        "include_dirs": [
          "config",
          "memory",
          "audit",
          "knowledge"
        ],
        "max_keep": 10,
        "schedule_cron": null,
        "schedule_timezone": null
      },
      "description": "Backup tool configuration (`[backup]`)."
    },
    "browser": {
      "$ref": "#/$defs/BrowserConfig",
      "default": {
        "allowed_domains": [
          "*"
        ],
        "allowed_private_hosts": [],
        "backend": "agent_browser",
        "computer_use": {
          "allow_remote_endpoint": false,
          "api_key": null,
          "endpoint": "http://127.0.0.1:8787/v1/actions",
          "max_coordinate_x": null,
          "max_coordinate_y": null,
          "timeout_ms": 15000,
          "window_allowlist": []
        },
        "enabled": true,
        "headed": null,
        "native_chrome_path": null,
        "native_headless": true,
        "native_webdriver_url": "http://127.0.0.1:9515",
        "session_name": null
      },
      "description": "Browser automation configuration (`[browser]`)."
    },
    "browser_delegate": {
      "$ref": "#/$defs/BrowserDelegateConfig",
      "default": {
        "allowed_domains": [],
        "blocked_domains": [],
        "chrome_profile_dir": "",
        "cli_binary": "claude",
        "enabled": false,
        "task_timeout_secs": 120
      },
      "description": "Browser delegation configuration (`[browser_delegate]`).\n\nDelegates browser-based tasks to a browser-capable CLI subprocess (e.g.\nClaude Code with `claude-in-chrome` MCP tools). Useful for interacting\nwith corporate web apps (Teams, Outlook, Jira, Confluence) that lack\ndirect API access. A persistent Chrome profile can be configured so SSO\nsessions survive across invocations.\n\nFields:\n- `enabled` (`bool`, default `false`) — enable the browser delegation tool.\n- `cli_binary` (`String`, default `\"claude\"`) — CLI binary to spawn for browser tasks.\n- `chrome_profile_dir` (`String`, default `\"\"`) — Chrome user-data directory for\n  persistent SSO sessions. When empty, a fresh profile is used each invocation.\n- `allowed_domains` (`Vec<String>`, default `[]`) — allowlist of domains the browser\n  may navigate to. Empty means all non-blocked domains are permitted.\n- `blocked_domains` (`Vec<String>`, default `[]`) — denylist of domains. Blocked\n  domains take precedence over allowed domains.\n- `task_timeout_secs` (`u64`, default `120`) — per-task timeout in seconds.\n\nCompatibility: additive and disabled by default; existing configs remain valid when omitted.\nRollback/migration: remove `[browser_delegate]` or keep `enabled = false` to disable."
    },
    "channels": {
      "$ref": "#/$defs/ChannelsConfig",
      "default": {
        "ack_reactions": true,
        "cli": true,
        "debounce_ms": 0,
        "max_concurrent_per_channel": 4,
        "message_timeout_secs": 300,
        "session_backend": "sqlite",
        "session_persistence": true,
        "session_ttl_hours": 0,
        "show_tool_calls": false
      },
      "description": "Channel configurations: Telegram, Discord, Slack, etc. (`[channels]`)."
    },
    "claude_code": {
      "$ref": "#/$defs/ClaudeCodeConfig",
      "default": {
        "allowed_tools": [
          "Read",
          "Edit",
          "Bash",
          "Write"
        ],
        "enabled": false,
        "env_passthrough": [],
        "max_output_bytes": 2097152,
        "system_prompt": null,
        "timeout_secs": 600
      },
      "description": "Claude Code tool configuration (`[claude_code]`)."
    },
    "claude_code_runner": {
      "$ref": "#/$defs/ClaudeCodeRunnerConfig",
      "default": {
        "enabled": false,
        "session_ttl": 3600,
        "ssh_host": null,
        "tmux_prefix": "zc-claude-"
      },
      "description": "Claude Code task runner with Slack progress and SSH session handoff (`[claude_code_runner]`)."
    },
    "cloud_ops": {
      "$ref": "#/$defs/CloudOpsConfig",
      "default": {
        "cost_threshold_monthly_usd": 100.0,
        "default_cloud": "aws",
        "enabled": false,
        "iac_tools": [
          "terraform"
        ],
        "supported_clouds": [
          "aws",
          "azure",
          "gcp"
        ],
        "well_architected_frameworks": [
          "aws-waf"
        ]
      },
      "description": "Cloud transformation accelerator configuration (`[cloud_ops]`)."
    },
    "codex_cli": {
      "$ref": "#/$defs/CodexCliConfig",
      "default": {
        "enabled": false,
        "env_passthrough": [],
        "extra_args": [],
        "max_output_bytes": 2097152,
        "timeout_secs": 600
      },
      "description": "Codex CLI tool configuration (`[codex_cli]`)."
    },
    "composio": {
      "$ref": "#/$defs/ComposioConfig",
      "default": {
        "api_key": null,
        "enabled": false,
        "entity_id": "default"
      },
      "description": "Composio managed OAuth tools integration (`[composio]`)."
    },
    "conversational_ai": {
      "$ref": "#/$defs/ConversationalAiConfig",
      "description": "Conversational AI agent builder configuration (`[conversational_ai]`).\n\nExperimental / future feature — not yet wired into the agent runtime.\nOmitted from generated config files when disabled (the default).\nExisting configs that already contain this section will continue to\ndeserialize correctly thanks to `#[serde(default)]`."
    },
    "cost": {
      "$ref": "#/$defs/CostConfig",
      "default": {
        "allow_override": false,
        "daily_limit_usd": 10.0,
        "enabled": true,
        "enforcement": {
          "mode": "warn",
          "reserve_percent": 10,
          "route_down_model": null
        },
        "monthly_limit_usd": 100.0,
        "rates": {
          "providers": {
            "models": {},
            "transcription": {},
            "tts": {}
          },
          "tools": {}
        },
        "track_per_agent": true,
        "warn_at_percent": 80
      },
      "description": "Cost tracking and budget enforcement configuration (`[cost]`).\nAlso hosts the operator-managed rate sheet at\n`[cost.rates.<type>.<model>]`."
    },
    "cron": {
      "additionalProperties": {
        "$ref": "#/$defs/CronJobDecl"
      },
      "description": "Declarative cron jobs (`[cron.<alias>]`), alias-keyed.\n\nEach entry is a named scheduled job synced into the database at\nscheduler startup. Subsystem runtime knobs (enable/disable, catch-up,\nrun-history retention) live on `[scheduler]`.",
      "type": "object"
    },
    "data_retention": {
      "$ref": "#/$defs/DataRetentionConfig",
      "default": {
        "categories": [],
        "dry_run": false,
        "enabled": false,
        "retention_days": 90
      },
      "description": "Data retention and purge configuration (`[data_retention]`)."
    },
    "delegate": {
      "$ref": "#/$defs/DelegateToolConfig",
      "default": {
        "agentic_timeout_secs": 300,
        "timeout_secs": 120
      },
      "description": "Delegate tool global default configuration (`[delegate]`)."
    },
    "embedding_routes": {
      "description": "Embedding-routing rules — route `hint:<name>` to specific\nmodel_provider + model combos for embedding requests.",
      "items": {
        "$ref": "#/$defs/EmbeddingRouteConfig"
      },
      "type": "array"
    },
    "escalation": {
      "$ref": "#/$defs/EscalationConfig",
      "default": {
        "alert_channels": []
      },
      "description": "Escalation routing configuration (`[escalation]`)."
    },
    "eval": {
      "$ref": "#/$defs/EvalHarnessConfig",
      "default": {
        "mode": "replay",
        "suite_dir": "evals"
      },
      "description": "Agent evaluation harness (`[eval]`) — surfaced via `zeroclaw eval`.\nDistinct from `[agent.eval]`, which is the in-loop response-quality scorer."
    },
    "file_download": {
      "$ref": "#/$defs/FileDownloadConfig",
      "default": {
        "headers": {},
        "max_file_size_bytes": 26214400,
        "timeout_secs": 120,
        "url": null
      },
      "description": "Standalone file download tool configuration (`[file_download]`)."
    },
    "file_upload": {
      "$ref": "#/$defs/FileUploadConfig",
      "default": {
        "field_name": "file",
        "headers": {},
        "max_file_size_bytes": 26214400,
        "method": "POST",
        "timeout_secs": 60,
        "url": null
      },
      "description": "Standalone file upload tool configuration (`[file_upload]`)."
    },
    "file_upload_bundle": {
      "$ref": "#/$defs/FileUploadBundleConfig",
      "default": {
        "field_name": "file",
        "headers": {},
        "max_file_size_bytes": 10485760,
        "max_files": 16,
        "max_response_body_bytes": 4096,
        "max_total_size_bytes": 33554432,
        "method": "POST",
        "timeout_secs": 120,
        "url": null
      },
      "description": "Standalone multi-file bundle upload tool configuration\n(`[file_upload_bundle]`)."
    },
    "gateway": {
      "$ref": "#/$defs/GatewayConfig",
      "default": {
        "allow_public_bind": false,
        "allow_remote_admin": false,
        "allow_self_upgrade": false,
        "check_updates": true,
        "host": "127.0.0.1",
        "idempotency_max_keys": 10000,
        "idempotency_ttl_secs": 300,
        "long_running_request_timeout_secs": 600,
        "pair_rate_limit_per_minute": 10,
        "paired_tokens": [],
        "pairing_dashboard": {
          "code_length": 8,
          "code_ttl_secs": 3600,
          "lockout_secs": 300,
          "max_failed_attempts": 5,
          "max_pending_codes": 3
        },
        "path_prefix": null,
        "port": 42617,
        "rate_limit_max_keys": 10000,
        "request_timeout_secs": 30,
        "require_pairing": true,
        "session_persistence": true,
        "session_ttl_hours": 0,
        "tls": null,
        "trust_forwarded_headers": false,
        "web_dist_dir": null,
        "webhook_rate_limit_per_minute": 60
      },
      "description": "Gateway server configuration: host, port, pairing, rate limits (`[gateway]`)."
    },
    "gemini_cli": {
      "$ref": "#/$defs/GeminiCliConfig",
      "default": {
        "enabled": false,
        "env_passthrough": [],
        "max_output_bytes": 2097152,
        "timeout_secs": 600
      },
      "description": "Gemini CLI tool configuration (`[gemini_cli]`)."
    },
    "google_workspace": {
      "$ref": "#/$defs/GoogleWorkspaceConfig",
      "default": {
        "allowed_operations": [],
        "allowed_services": [],
        "audit_log": false,
        "credentials_path": null,
        "default_account": null,
        "enabled": false,
        "rate_limit_per_minute": 60,
        "timeout_secs": 30
      },
      "description": "Google Workspace CLI (`gws`) tool configuration (`[google_workspace]`)."
    },
    "hardware": {
      "$ref": "#/$defs/HardwareConfig",
      "default": {
        "baud_rate": 115200,
        "enabled": false,
        "probe_target": null,
        "serial_port": null,
        "transport": "None",
        "workspace_datasheets": false
      },
      "description": "Hardware configuration (wizard-driven physical world setup)."
    },
    "heartbeat": {
      "$ref": "#/$defs/HeartbeatConfig",
      "default": {
        "adaptive": false,
        "agent": "",
        "deadman_channel": null,
        "deadman_timeout_minutes": 0,
        "deadman_to": null,
        "enabled": false,
        "interval_minutes": 30,
        "load_session_context": false,
        "max_interval_minutes": 120,
        "max_run_history": 100,
        "message": null,
        "min_interval_minutes": 5,
        "target": null,
        "task_timeout_secs": 600,
        "to": null,
        "two_phase": true
      },
      "description": "Heartbeat configuration for periodic health pings (`[heartbeat]`)."
    },
    "hooks": {
      "$ref": "#/$defs/HooksConfig",
      "default": {
        "builtin": {
          "command_logger": false,
          "webhook_audit": {
            "enabled": false,
            "include_args": false,
            "max_args_bytes": 4096,
            "tool_patterns": [],
            "url": ""
          }
        },
        "enabled": true
      },
      "description": "Hooks configuration (lifecycle hooks and built-in hook toggles)."
    },
    "http_request": {
      "$ref": "#/$defs/HttpRequestConfig",
      "default": {
        "allow_private_hosts": false,
        "allowed_domains": [
          "*"
        ],
        "allowed_private_hosts": [],
        "enabled": true,
        "max_response_size": 1000000,
        "secrets": {},
        "timeout_secs": 30
      },
      "description": "HTTP request tool configuration (`[http_request]`)."
    },
    "image_gen": {
      "$ref": "#/$defs/ImageGenConfig",
      "default": {
        "api_key_env": "FAL_API_KEY",
        "default_model": "fal-ai/flux/schnell",
        "enabled": false
      },
      "description": "Standalone image generation tool configuration (`[image_gen]`)."
    },
    "jira": {
      "$ref": "#/$defs/JiraConfig",
      "default": {
        "allowed_actions": [
          "get_ticket"
        ],
        "api_token": "",
        "base_url": "",
        "enabled": false,
        "timeout_secs": 30
      },
      "description": "Jira integration configuration (`[jira]`)."
    },
    "knowledge": {
      "$ref": "#/$defs/KnowledgeConfig",
      "default": {
        "auto_capture": false,
        "db_path": "/home/jeremy/.zeroclaw/knowledge.db",
        "enabled": false,
        "max_nodes": 100000,
        "suggest_on_query": true
      },
      "description": "Knowledge graph configuration (`[knowledge]`)."
    },
    "knowledge_bundles": {
      "additionalProperties": {
        "$ref": "#/$defs/KnowledgeBundleConfig"
      },
      "description": "Named knowledge bundles (`[knowledge_bundles.<alias>]`).",
      "type": "object"
    },
    "link_enricher": {
      "$ref": "#/$defs/LinkEnricherConfig",
      "default": {
        "enabled": false,
        "max_links": 3,
        "timeout_secs": 10
      },
      "description": "Link enricher configuration (`[link_enricher]`)."
    },
    "linkedin": {
      "$ref": "#/$defs/LinkedInConfig",
      "default": {
        "api_version": "202602",
        "content": {
          "github_repos": [],
          "github_users": [],
          "instructions": "",
          "persona": "",
          "rss_feeds": [],
          "topics": []
        },
        "enabled": false,
        "image": {
          "card_accent_color": "#0A66C2",
          "dalle": {
            "api_key_env": "OPENAI_API_KEY",
            "model": "dall-e-3",
            "size": "1024x1024"
          },
          "enabled": false,
          "fallback_card": true,
          "flux": {
            "api_key_env": "FAL_API_KEY",
            "model": "fal-ai/flux/schnell"
          },
          "imagen": {
            "api_key_env": "GOOGLE_VERTEX_API_KEY",
            "project_id_env": "GOOGLE_CLOUD_PROJECT",
            "region": "us-central1"
          },
          "providers": [
            "stability",
            "imagen",
            "dalle",
            "flux"
          ],
          "stability": {
            "api_key_env": "STABILITY_API_KEY",
            "model": "stable-diffusion-xl-1024-v1-0"
          },
          "temp_dir": "linkedin/images"
        }
      },
      "description": "LinkedIn integration configuration (`[linkedin]`)."
    },
    "locale": {
      "default": null,
      "description": "Locale for tool descriptions (e.g. `\"en\"`, `\"zh-CN\"`).\n\nWhen set, tool descriptions shown in system prompts are loaded from\nFluent `.ftl` locale files. Falls back to embedded English, then to\nhardcoded descriptions.\n\nIf omitted or empty, the locale is auto-detected from the host\nsystem's locale (defaulting to `\"en\"` if that can't be determined).",
      "type": [
        "string",
        "null"
      ]
    },
    "mcp": {
      "$ref": "#/$defs/McpConfig",
      "default": {
        "deferred_loading": false,
        "enabled": true,
        "servers": []
      },
      "description": "External MCP server connections (`[mcp]`)."
    },
    "mcp_bundles": {
      "additionalProperties": {
        "$ref": "#/$defs/McpBundleConfig"
      },
      "description": "Named MCP server bundles (`[mcp_bundles.<alias>]`).",
      "type": "object"
    },
    "media_pipeline": {
      "$ref": "#/$defs/MediaPipelineConfig",
      "default": {
        "describe_images": true,
        "enabled": false,
        "summarize_video": true,
        "transcribe_audio": true
      },
      "description": "Automatic media understanding pipeline (`[media_pipeline]`)."
    },
    "memory": {
      "$ref": "#/$defs/MemoryConfig",
      "default": {
        "archive_after_days": 7,
        "audit_enabled": false,
        "audit_retention_days": 30,
        "auto_hydrate": true,
        "auto_reindex_on_identity_change": false,
        "auto_save": true,
        "backend": "sqlite",
        "candidate_multiplier": 4,
        "chunk_max_tokens": 512,
        "conflict_supersede_enabled": true,
        "conflict_threshold": 0.85,
        "consolidation_extract_facts": false,
        "conversation_retention_days": 30,
        "core_max_bytes": 0,
        "core_max_rows": 0,
        "core_retention_days": 0,
        "daily_max_rows": 0,
        "daily_retention_days": 0,
        "dedup_action": "reject",
        "dedup_jaccard_threshold": 0.8,
        "dedup_on_write": false,
        "default_namespace": "default",
        "embedding_cache_size": 10000,
        "embedding_dimensions": 1536,
        "embedding_model": "text-embedding-3-small",
        "embedding_provider": "none",
        "evict_order": "value",
        "fts_early_return_score": 0.85,
        "hygiene_enabled": true,
        "importance_weight": 0.2,
        "keyword_weight": 0.3,
        "min_relevance_score": 0.4,
        "mmr_lambda": 0.7,
        "pin_min_importance": 1.01,
        "pin_namespaces": [],
        "policy": {
          "max_entries_per_category": 0,
          "max_entries_per_namespace": 0,
          "read_only_namespaces": [],
          "redact_categories": [
            "secret",
            "api_key",
            "private_key",
            "email",
            "phone"
          ],
          "redact_on_write": false,
          "retention_days_by_category": {},
          "threat_scan": "on",
          "threat_scan_load_time": true,
          "threat_scan_on_hit": "reject"
        },
        "purge_after_days": 30,
        "recency_weight": 0.1,
        "rerank_enabled": false,
        "rerank_strategy": "none",
        "rerank_threshold": 5,
        "response_cache_enabled": false,
        "response_cache_hot_entries": 256,
        "response_cache_max_entries": 5000,
        "response_cache_ttl_minutes": 60,
        "retrieval_stages": [
          "fts",
          "vector"
        ],
        "search_mode": "hybrid",
        "snapshot_enabled": false,
        "snapshot_on_hygiene": false,
        "types": {
          "enabled": false
        },
        "vector_weight": 0.7
      },
      "description": "Memory backend configuration: sqlite, markdown, embeddings (`[memory]`)."
    },
    "microsoft365": {
      "$ref": "#/$defs/Microsoft365Config",
      "default": {
        "auth_flow": "client_credentials",
        "client_id": null,
        "client_secret": null,
        "enabled": false,
        "scopes": [
          "https://graph.microsoft.com/.default"
        ],
        "tenant_id": null,
        "token_cache_encrypted": true,
        "user_id": null
      },
      "description": "Microsoft 365 Graph API integration (`[microsoft365]`)."
    },
    "model_routes": {
      "description": "Model-routing rules — route `hint:<name>` to specific\nmodel_provider + model combos.",
      "items": {
        "$ref": "#/$defs/ModelRouteConfig"
      },
      "type": "array"
    },
    "multimodal": {
      "$ref": "#/$defs/MultimodalConfig",
      "default": {
        "allow_remote_fetch": false,
        "max_image_size_mb": 5,
        "max_image_turns": 0,
        "max_images": 4,
        "vision_model": null,
        "vision_model_provider": null
      },
      "description": "Multimodal (image) handling configuration (`[multimodal]`)."
    },
    "node_transport": {
      "$ref": "#/$defs/NodeTransportConfig",
      "default": {
        "allowed_peers": [],
        "connection_pool_size": 4,
        "enabled": true,
        "max_request_age_secs": 300,
        "mutual_tls": false,
        "require_https": true,
        "shared_secret": "",
        "tls_cert_path": null,
        "tls_key_path": null
      },
      "description": "Secure inter-node transport configuration (`[node_transport]`)."
    },
    "nodes": {
      "$ref": "#/$defs/NodesConfig",
      "default": {
        "auth_token": null,
        "enabled": false,
        "max_nodes": 16,
        "mdns": {
          "announce_interval_secs": 30,
          "enabled": false,
          "max_peers": 16,
          "node_name": null,
          "peer_ttl_secs": 90
        }
      },
      "description": "Dynamic node discovery configuration (`[nodes]`)."
    },
    "notion": {
      "$ref": "#/$defs/NotionConfig",
      "default": {
        "api_key": "",
        "database_id": "",
        "enabled": false,
        "input_property": "Input",
        "max_concurrent": 4,
        "poll_interval_secs": 5,
        "recover_stale": true,
        "result_property": "Result",
        "status_property": "Status"
      },
      "description": "Notion integration configuration (`[notion]`)."
    },
    "observability": {
      "$ref": "#/$defs/ObservabilityConfig",
      "default": {
        "backend": "none",
        "log_llm_request_payload": "off",
        "log_persistence": "rolling",
        "log_persistence_max_bytes": 0,
        "log_persistence_max_entries": 200,
        "log_persistence_path": "state/runtime-trace.jsonl",
        "log_persistence_retention_max_age_days": 0,
        "log_persistence_retention_max_files": 7,
        "log_persistence_rotate_daily": true,
        "log_tool_io": "redacted",
        "log_tool_io_denylist": [],
        "log_tool_io_truncate_bytes": 40960,
        "otel_endpoint": null,
        "otel_genai_content": "off",
        "otel_genai_content_max_chars": 1000,
        "otel_headers": null,
        "otel_service_name": null,
        "otel_tool_io": "off",
        "otel_tool_io_max_chars": 1000
      },
      "description": "Observability backend configuration (`[observability]`)."
    },
    "onboard_state": {
      "$ref": "#/$defs/OnboardStateConfig",
      "default": {
        "completed_sections": [],
        "quickstart_completed": false
      },
      "description": "Meta-state for the Quickstart flow (which sections the user has\nalready walked through). Not user-facing config (`[onboard_state]`)."
    },
    "opencode_cli": {
      "$ref": "#/$defs/OpenCodeCliConfig",
      "default": {
        "enabled": false,
        "env_passthrough": [],
        "max_output_bytes": 2097152,
        "timeout_secs": 600
      },
      "description": "OpenCode CLI tool configuration (`[opencode_cli]`)."
    },
    "pacing": {
      "$ref": "#/$defs/PacingConfig",
      "default": {
        "loop_detection_enabled": true,
        "loop_detection_max_repeats": 3,
        "loop_detection_min_elapsed_secs": null,
        "loop_detection_window_size": 20,
        "loop_ignore_tools": [],
        "message_timeout_scale_max": null,
        "step_timeout_secs": null
      },
      "description": "Pacing controls for slow/local LLM workloads (`[pacing]`)."
    },
    "peer_groups": {
      "additionalProperties": {
        "$ref": "#/$defs/PeerGroupConfig"
      },
      "description": "Named peer groups (`[peer_groups.<name>]`). Each entry binds a\nchannel, a list of member agents, and optional non-agent\n(external) members and a per-group blocklist. Mutual opt-in:\ntwo agents become peers only when both appear in the same\ngroup's `agents`. Empty by default for single-agent installs.\nSee `crate::multi_agent::PeerGroupConfig`.",
      "type": "object"
    },
    "peripherals": {
      "$ref": "#/$defs/PeripheralsConfig",
      "default": {
        "boards": [],
        "datasheet_dir": null,
        "enabled": false
      },
      "description": "Peripheral board configuration for hardware integration (`[peripherals]`)."
    },
    "pipeline": {
      "$ref": "#/$defs/PipelineConfig",
      "default": {
        "allowed_tools": [],
        "enabled": false,
        "max_steps": 20
      },
      "description": "Pipeline tool configuration (`[pipeline]`)."
    },
    "plugins": {
      "$ref": "#/$defs/PluginsConfig",
      "default": {
        "auto_discover": false,
        "enabled": false,
        "entries": [],
        "limits": {
          "call_fuel": 1000000000,
          "max_instances": 64,
          "max_memory_mb": 256,
          "max_table_elements": 100000
        },
        "max_plugins": 50,
        "plugins_dir": "/home/jeremy/.zeroclaw/plugins",
        "security": {
          "signature_mode": "disabled",
          "trusted_publisher_keys": []
        }
      },
      "description": "Plugin system configuration (`[plugins]`)."
    },
    "project_intel": {
      "$ref": "#/$defs/ProjectIntelConfig",
      "default": {
        "default_language": "en",
        "enabled": false,
        "include_git_data": true,
        "include_jira_data": false,
        "jira_base_url": null,
        "report_output_dir": "/home/jeremy/.zeroclaw/project-reports",
        "risk_sensitivity": "medium",
        "templates_dir": null
      },
      "description": "Project delivery intelligence configuration (`[project_intel]`)."
    },
    "providers": {
      "$ref": "#/$defs/Providers",
      "default": {
        "models": {},
        "transcription": {},
        "tts": {}
      },
      "description": "All configured provider profiles, grouped by category under a\nsingle `[providers]` root. Categories today: `models`, `tts`,\n`transcription`. Shape: `[providers.<category>.<type>.<alias>]`,\ne.g. `[providers.models.anthropic.default]`,\n`[providers.tts.openai.default]`,\n`[providers.transcription.groq.default]`."
    },
    "proxy": {
      "$ref": "#/$defs/ProxyConfig",
      "default": {
        "all_proxy": null,
        "enabled": false,
        "http_proxy": null,
        "https_proxy": null,
        "no_proxy": [],
        "scope": "zeroclaw",
        "services": []
      },
      "description": "Proxy configuration for outbound HTTP/HTTPS/SOCKS5 traffic (`[proxy]`)."
    },
    "query_classification": {
      "$ref": "#/$defs/QueryClassificationConfig",
      "default": {
        "enabled": false,
        "rules": []
      },
      "description": "Automatic query classification — maps user messages to model hints."
    },
    "reliability": {
      "$ref": "#/$defs/ReliabilityConfig",
      "default": {
        "api_keys": [],
        "channel_initial_backoff_secs": 2,
        "channel_max_backoff_secs": 60,
        "provider_backoff_ms": 500,
        "provider_retries": 2,
        "scheduler_poll_secs": 15,
        "scheduler_retries": 2
      },
      "description": "Reliability settings: retries, backoff, key rotation (`[reliability]`)."
    },
    "risk_profiles": {
      "additionalProperties": {
        "$ref": "#/$defs/RiskProfileConfig"
      },
      "description": "Named risk/autonomy profiles (`[risk_profiles.<alias>]`).",
      "type": "object"
    },
    "runtime": {
      "$ref": "#/$defs/RuntimeConfig",
      "default": {
        "docker": {
          "allowed_workspace_roots": [],
          "cpu_limit": 1.0,
          "image": "alpine:3.20",
          "memory_limit_mb": 512,
          "mount_workspace": true,
          "network": "none",
          "read_only_rootfs": true
        },
        "kind": "native",
        "reasoning_effort": null,
        "reasoning_enabled": null,
        "shell": null
      },
      "description": "Runtime adapter configuration (`[runtime]`). Controls native vs Docker execution."
    },
    "runtime_profiles": {
      "additionalProperties": {
        "$ref": "#/$defs/RuntimeProfileConfig"
      },
      "description": "Named runtime/LLM execution profiles (`[runtime_profiles.<alias>]`).",
      "type": "object"
    },
    "scheduler": {
      "$ref": "#/$defs/SchedulerConfig",
      "default": {
        "catch_up_on_startup": true,
        "enabled": true,
        "max_concurrent": 4,
        "max_run_history": 50,
        "max_tasks": 64
      },
      "description": "Scheduler configuration for periodic task execution (`[scheduler]`)."
    },
    "schema_version": {
      "default": 0,
      "description": "Config file schema version.",
      "format": "uint32",
      "minimum": 0,
      "type": "integer"
    },
    "secrets": {
      "$ref": "#/$defs/SecretsConfig",
      "default": {
        "encrypt": true
      },
      "description": "Secrets encryption configuration (`[secrets]`)."
    },
    "security": {
      "$ref": "#/$defs/SecurityConfig",
      "default": {
        "audit": {
          "enabled": true,
          "log_path": "audit.log",
          "max_size_mb": 100,
          "sign_events": false
        },
        "estop": {
          "enabled": false,
          "require_otp_to_resume": true,
          "state_file": "/home/jeremy/.zeroclaw/estop-state.json"
        },
        "leak_detection": {
          "enabled": true,
          "high_entropy_tokens": true,
          "sensitivity": 0.7
        },
        "nevis": {
          "client_id": "",
          "client_secret": null,
          "enabled": false,
          "instance_url": "",
          "jwks_url": null,
          "realm": "master",
          "require_mfa": false,
          "role_mapping": [],
          "session_timeout_secs": 3600,
          "token_validation": "local"
        },
        "otp": {
          "cache_valid_secs": 300,
          "challenge_max_attempts": 3,
          "enabled": false,
          "gated_actions": [
            "shell",
            "file_write",
            "browser_open",
            "browser",
            "memory_forget"
          ],
          "gated_domain_categories": [],
          "gated_domains": [],
          "method": "totp",
          "token_ttl_secs": 30
        },
        "webauthn": {
          "enabled": false,
          "rp_id": "localhost",
          "rp_name": "ZeroClaw",
          "rp_origin": "http://localhost:42617"
        }
      },
      "description": "Security subsystem configuration (`[security]`)."
    },
    "security_ops": {
      "$ref": "#/$defs/SecurityOpsConfig",
      "default": {
        "auto_triage": false,
        "enabled": false,
        "max_auto_severity": "low",
        "playbooks_dir": "/home/jeremy/.zeroclaw/playbooks",
        "report_output_dir": "/home/jeremy/.zeroclaw/security-reports",
        "require_approval_for_actions": true,
        "siem_integration": null
      },
      "description": "Managed cybersecurity service configuration (`[security_ops]`)."
    },
    "shell_tool": {
      "$ref": "#/$defs/ShellToolConfig",
      "default": {
        "timeout_secs": 60
      },
      "description": "Shell tool configuration (`[shell_tool]`)."
    },
    "skill_bundles": {
      "additionalProperties": {
        "$ref": "#/$defs/SkillBundleConfig"
      },
      "description": "Named skill bundles (`[skill_bundles.<alias>]`).",
      "type": "object"
    },
    "skills": {
      "$ref": "#/$defs/SkillsConfig",
      "default": {
        "allow_scripts": false,
        "install_suggestions": {
          "enabled": false
        },
        "open_skills_dir": null,
        "open_skills_enabled": false,
        "prompt_injection_mode": "full",
        "registry_url": null,
        "skill_creation": {
          "enabled": false,
          "max_final_answer_chars": 2000,
          "max_skills": 500,
          "max_task_chars": 1000,
          "max_tool_trace_chars": 4000,
          "reflection_enabled": false,
          "similarity_threshold": 0.85
        },
        "skill_improvement": {
          "cooldown_secs": 3600,
          "enabled": false,
          "max_review_iterations": 8,
          "nudge_interval_iterations": 10
        }
      },
      "description": "Skills loading and community repository behavior (`[skills]`)."
    },
    "sop": {
      "$ref": "#/$defs/SopConfig",
      "default": {
        "approval": {},
        "approval_mode": "both",
        "approval_timeout_action": "escalate",
        "approval_timeout_secs": 300,
        "default_execution_mode": "supervised",
        "maintenance_interval_secs": 60,
        "max_concurrent_total": 4,
        "max_finished_runs": 100,
        "max_step_retries": 2,
        "max_step_visits": 256,
        "persist_runs": true,
        "procedural_memory_enabled": false,
        "run_state_dir": null,
        "run_store_backend": "sqlite",
        "sops_dir": null,
        "step_mandatory_tools": [
          "sop_advance",
          "sop_approve",
          "sop_status"
        ],
        "step_schema_enforce": true,
        "step_scope_enforce": false,
        "untrusted_frame_warning": true,
        "untrusted_guard_sensitivity": 0.7,
        "untrusted_input_guard": "warn",
        "untrusted_outbound_redact": true,
        "untrusted_payload_max_bytes": 8192
      },
      "description": "Standard Operating Procedures engine configuration (`[sop]`)."
    },
    "storage": {
      "$ref": "#/$defs/StorageConfig",
      "default": {},
      "description": "Persistent storage model_provider configuration (`[storage]`)."
    },
    "text_browser": {
      "$ref": "#/$defs/TextBrowserConfig",
      "default": {
        "allowed_private_hosts": [],
        "enabled": false,
        "preferred_browser": null,
        "timeout_secs": 30
      },
      "description": "Text browser tool configuration (`[text_browser]`)."
    },
    "todotracker": {
      "$ref": "#/$defs/TodoTrackerConfig",
      "default": {
        "enabled": true,
        "enabled_at_start": false,
        "location": "right",
        "max_height": 5,
        "width": 32
      },
      "description": "ZeroCode live task tracker (`[todotracker]`), the read-only\nTodoWrite visual tracker in the Code pane."
    },
    "transcription": {
      "$ref": "#/$defs/TranscriptionConfig",
      "default": {
        "api_key": null,
        "api_url": "https://api.groq.com/openai/v1/audio/transcriptions",
        "assemblyai": null,
        "deepgram": null,
        "enabled": false,
        "google": null,
        "initial_prompt": null,
        "language": null,
        "local_whisper": null,
        "max_audio_bytes": null,
        "max_duration_secs": 120,
        "model": "whisper-large-v3-turbo",
        "openai": null,
        "transcribe_non_ptt_audio": false
      },
      "description": "Voice transcription configuration (Whisper API via Groq)."
    },
    "trust": {
      "$ref": "#/$defs/TrustConfig",
      "default": {
        "correction_penalty": 0.05,
        "decay_half_life_days": 30.0,
        "initial_score": 0.8,
        "regression_threshold": 0.5,
        "success_boost": 0.01
      },
      "description": "Trust scoring and regression detection configuration (`[trust]`)."
    },
    "tts": {
      "$ref": "#/$defs/TtsConfig",
      "default": {
        "default_format": "mp3",
        "default_voice": "alloy",
        "enabled": false,
        "max_text_length": 4096
      },
      "description": "Text-to-Speech configuration (`[tts]`)."
    },
    "tunnel": {
      "$ref": "#/$defs/TunnelConfig",
      "default": {
        "cloudflare": null,
        "custom": null,
        "ngrok": null,
        "openvpn": null,
        "pinggy": null,
        "tailscale": null,
        "tunnel_provider": "none"
      },
      "description": "Tunnel configuration for exposing the gateway publicly (`[tunnel]`)."
    },
    "verifiable_intent": {
      "$ref": "#/$defs/VerifiableIntentConfig",
      "default": {
        "enabled": false,
        "strictness": "strict"
      },
      "description": "Verifiable Intent (VI) credential verification and issuance (`[verifiable_intent]`)."
    },
    "web_fetch": {
      "$ref": "#/$defs/WebFetchConfig",
      "default": {
        "allowed_domains": [
          "*"
        ],
        "allowed_private_hosts": [],
        "blocked_domains": [],
        "enabled": true,
        "firecrawl": {
          "api_key_env": "FIRECRAWL_API_KEY",
          "api_url": "https://api.firecrawl.dev/v1",
          "enabled": false,
          "mode": "scrape"
        },
        "max_response_size": 500000,
        "timeout_secs": 30
      },
      "description": "Web fetch tool configuration (`[web_fetch]`)."
    },
    "web_search": {
      "$ref": "#/$defs/WebSearchConfig",
      "default": {
        "bocha_api_key": null,
        "brave_api_key": null,
        "enabled": true,
        "jina_api_key": null,
        "max_results": 5,
        "search_provider": "duckduckgo",
        "searxng_instance_url": null,
        "tavily_api_key": null,
        "timeout_secs": 15
      },
      "description": "Web search tool configuration (`[web_search]`)."
    },
    "wss": {
      "$ref": "#/$defs/WssConfig",
      "default": {
        "bind": "0.0.0.0",
        "cert_path": "",
        "enabled": false,
        "key_path": "",
        "port": 9781
      },
      "description": "WebSocket Secure (WSS) transport for remote TUI connections (`[wss]`)."
    }
  },
  "title": "Config",
  "type": "object"
}

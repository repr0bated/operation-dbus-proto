{
  "$defs": {
    "SecretsConfig": {
      "description": "Secrets encryption configuration (`[secrets]` section).",
      "properties": {
        "encrypt": {
          "default": true,
          "description": "Enable encryption for API keys and tokens at rest",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  },
  "$ref": "#/$defs/SecretsConfig",
  "default": {
    "encrypt": true
  },
  "description": "Secrets encryption configuration (`[secrets]`)."
}
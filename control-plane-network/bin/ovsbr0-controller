#!/bin/sh
# Compatibility entry point retained for existing s6 bundles. Controller
# assignment now uses rovs_commands.set_controller through the same hardened
# control-plane-network implementation and lock as normal bootstrap.
exec /usr/local/sbin/control-plane-network --controller-only "$@"

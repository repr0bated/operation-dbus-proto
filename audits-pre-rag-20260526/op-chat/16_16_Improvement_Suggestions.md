1. **[CRITICAL] Implement Canonical Path Resolution for File Access Control** | The `forbidden_paths` check in `ReadFileTool` and `forbidden_prefixes` in `WriteFileTool` are highly naive and easily bypassed using path traversal sequences (such as `/tmp/../../etc/shadow` or `/etc/shadow/../shadow`) and symlinks. Path arguments must be normalized via `std::fs::canonicalize` prior to performing any prefix or match validation, otherwise arbitrary system files can be read or overwritten. | `crates/op-chat/src/tool_loader.rs:693`

2. **[CRITICAL] Refine Command Whitelist to Exclude Interpreters** | The `ShellExecuteTool` whitelist includes shell interpreters and package/infrastructure managers like `"python"`, `"python3"`, `"bash"`, `"sh"`, `"kubectl"`, and `"docker"`. Allowing these binaries permits executing arbitrary shell scripts (e.g. `python -c "..."` or `bash -c "..."`), rendering the argument-filtering and command whitelist entirely ineffective. Remove interpreters from the default whitelist. | `crates/op-chat/src/tool_loader.rs:801`

3. **Cache Connected Channel in gRPC Client State** | In `GrpcAgentClient::connect`, the successfully established gRPC `Channel` is retrieved and passed to `discover_methods` but is never written to `self.channel`. This causes all subsequent calls to `execute` and `execute_stream` to fail immediately with `"not connected — call connect() first"` because `self.channel` remains `None`. Populate `self.channel` once the connection is established. | `crates/op-chat/src/grpc_client.rs:141`

4. **Replace CLI Subprocesses with Native Protocols to Match Spec** | Multiple OVS tools (such as `OvsListBridgesTool`, `OvsShowBridgeTool`, and `OvsAddBridgeTool`) execute `ovs-vsctl` and `ovs-ofctl` as external command subprocesses. This directly contradicts the system prompt instructions which forbid these CLI binaries and mandate native protocol interactions (such as OVSDB JSON-RPC and Generic Netlink). Use the direct protocol clients implemented in `op-network` instead. | `crates/op-chat/src/tool_loader.rs:1051`

5. **Utilize Enum-Based Typestate for RpcResponse to Prevent Invalid States** | `RpcResponse` is represented as a flat struct with mutually exclusive optional fields `result`, `error`, and `execution_id`. This allows structurally invalid states, such as a response that claims success but has no result, or contains both a result and an error. Refactor this into a strongly-typed `Result` enum (`Success(Value)` vs `Error(String)`) to enforce state safety at compile time. | `crates/op-chat/src/actor.rs:114`

6. **Introduce Dedicated Error Enum instead of Ad-hoc Strings** | Methods across `ChatActorHandle` and the tool executor return `anyhow::Result` or format errors to `String` values. Replacing generic error handling with a dedicated `ChatError` enum (using `thiserror`) allows calling applications to match on failures programmatically and reduces allocation overhead from repeated ad-hoc string formatting. | `crates/op-chat/src/actor.rs:149`

7. **Implement Zero-Copy JSON Processing with Borrowed Values** | `parse_tool_calls` repeatedly calls `simd_json::from_str` with freshly-allocated, temporary string copies. Transitioning to `simd_json::BorrowedValue` and constraining the resulting tool-call lifetimes to the request buffer would enable zero-copy JSON parsing and eliminate substantial heap allocation overhead during high-frequency chat loops. | `crates/op-chat/src/forced_execution.rs:330`

8. **Adopt Arc<str> or Bytes for Payload Transmissions** | As large prompt strings, chat histories, and file contents are routed through the orchestration layers, they are frequently copied (e.g., `hist_msg.content.clone()` and `message.to_string()`). Replacing `String` with `Arc<str>` or `bytes::Bytes` would enable zero-copy buffer sharing across thread and actor boundaries. | `crates/op-chat/src/actor.rs:248`

9. **Introduce Unified Span Contexts for Async Actor Tasks** | The actor's message processing loop lack unified tracing spans that thread correlation and session identifiers through the concurrent task boundaries. Adding structured tracing fields (such as `tracing::info_span!("handle_request", session_id = %session_id)`) across execution boundaries is critical for diagnosing complex multi-agent failures. | `crates/op-chat/src/actor.rs:343`

10. **Persist Session and Execution Audit Logs using CozoDB/Sled** | `SessionManager` maintains conversation state in a simple in-memory `RwLock<HashMap<String, ChatSession>>`. Under sustained production workloads, this leads to unbounded memory growth and state loss upon actor restarts. Persisting sessions and audit logs to an embedded datastore like CozoDB (using the sled backend) would permit optimized indexing, structural querying, and durable persistence. | `crates/op-chat/src/session.rs:119`
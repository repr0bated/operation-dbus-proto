1. **CRITICAL: Command Injection Vulnerability in Shell Execution Check** | The `SecurityValidator::check_command` implementation for `Restricted` and `Custom` profiles only checks the first whitespace-separated word (`base_cmd`) of a command. This allows restricted or untrusted sessions to execute arbitrary commands by chaining shell metacharacters (e.g., passing `"ls ; cat /etc/shadow"` will be validated as `"ls"`, which is permitted, and then fully executed). Additionally, `ShellExecuteTool::execute` does not invoke `InputValidator` to filter `FORBIDDEN_CHARS` before execution, bypassing sanitization. | `crates/op-tools/src/security.rs:373`

2. **ARCHITECTURE: Remove Redundant and Legacy Code Modules** | The codebase contains `builtin_old.rs` which duplicates existing tool definitions (e.g., `ShellTool`, `FileReadTool`, `SystemInfoTool`) now located under the `builtin/` directory. This creates maintenance overhead and confusing dual implementations. | `crates/op-tools/src/builtin_old.rs:1`

3. **ARCHITECTURE: Avoid Hardcoded User Home Paths** | Config paths in `get_anydesk_id` are hardcoded to `/home/jeremy/`. This limits portability across different deployment environments and causes failures when running under different system users. Instead, utilize `dirs::home_dir()` or retrieve this path dynamically from the environment. | `crates/op-tools/src/builtin/anydesk.rs:374`

4. **ARCHITECTURE: Pool and Reuse D-Bus Connections and Proxies** | Creating new D-Bus connections via `zbus::Connection::system()` and rebuilding `zbus::Proxy` on every dynamic tool call adds substantial connection establishment latency. Reusing D-Bus connections via a shared connection pool or utilizing `AgentConnectionRegistry` would mitigate this overhead. | `crates/op-tools/src/dynamic_tool.rs:104`

5. **API ERGONOMICS: Strong Typed Errors for Tool Execution** | The `Tool` trait returns a generic `anyhow::Result<Value>`. Representing tool failures via a structured `ToolError` enum (e.g., `PermissionDenied`, `Timeout`, `DependencyMissing`, `ExecutionFailed`) would allow the chatbot orchestrator to programmatically handle failures rather than attempting to parse string-based error messages. | `crates/op-tools/src/tool.rs:56`

6. **API ERGONOMICS: Pre-validate D-Bus Signatures with Typestate/Newtypes** | Dynamic D-Bus parameters are parsed from arbitrary JSON values at runtime. Wrapping D-Bus signatures in a `Signature` newtype that statically enforces type rules before initiating the async connection state machine would prevent parsing failures deep inside the execution pipeline. | `crates/op-tools/src/builtin/dbus_tool.rs:309`

7. **PERFORMANCE: Use Zero-Copy Allocation for Tool Metadata** | `ToolDefinition` and related discovery types use owned `String` types for static fields like `name`, `description`, `category`, and `tags`. Since this metadata remains unchanged after registration, using `Arc<str>` or `&'static str` would eliminate significant heap allocation overhead during startup and router discovery. | `crates/op-tools/src/registry.rs:14`

8. **PERFORMANCE: Reuse Serialization Buffers in MCP Integration** | The `mcptools` integration serializes parameters via `simd_json::to_string` and converts output back from raw string buffers on every tool execution. Reusing thread-local serialization buffers or employing zero-copy parsing techniques on slices would drastically cut down allocation churn. | `crates/op-tools/src/mcptools.rs:146`

9. **OBSERVABILITY: Instrument Major Tool Executors with Tracing Spans** | Critical execution paths like `ShellExecuteTool::execute` and `mcptools::run_mcp_call` lack structured logging. Wrapping these methods in `#[tracing::instrument]` with structured fields (e.g., `command`, `working_dir`, `session_id`) would provide highly valuable call stack context in production environments. | `crates/op-tools/src/builtin/shell.rs:94`

10. **OBSERVABILITY: Replace String Interpolation in Warnings with Structured Fields** | Warnings about native protocol alternatives use string interpolation to format messages. Using structured fields instead (e.g., `tracing::warn!(command = %command, recommendation = %warn_msg)`) ensures log aggregators can parse and index the variables efficiently. | `crates/op-tools/src/builtin/shell.rs:141`

11. **STORAGE: Persist the DBus FTS Search Index in Sled/CozoDB** | The `DbusSearchTool` requires rebuilding or clearing the search index dynamically via `self.indexer.build_index`. Persisting these scanned D-Bus interfaces and capabilities inside a local `sled` or `CozoDB` table would avoid expensive system-wide D-Bus introspection crawls on every daemon restart. | `crates/op-tools/src/builtin/dbus_search_tool.rs:175`
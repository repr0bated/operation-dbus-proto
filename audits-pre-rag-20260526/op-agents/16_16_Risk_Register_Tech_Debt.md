| Severity | Issue | Evidence (file:line) | Recommendation |
| :--- | :--- | :--- | :--- |
| **Critical** | **Command Injection / Privilege Escalation via Process Factory**<br>The registry spawns processes directly using unchecked commands and arguments defined in `AgentSpec` structs. Since specifications can be loaded dynamically from configuration files or potentially modified, this allows arbitrary shell execution under the manager's privileges (including root). | `crates/op-agents/src/agent_registry.rs:201` | Validate and restrict commands to an immutable hardcoded whitelist of secure binaries, and avoid passing unvalidated dynamic arguments. |
| **High** | **Memory Corruption and Undefined Behavior via `simd_json` Unsafe Parsers**<br>The codebase repeatedly invokes `unsafe { simd_json::from_str(...) }` on standard `String` instances read directly from files or received from network/D-Bus inputs. Standard allocations do not guarantee the 16-byte padding required by the SIMD hardware vector instructions used in `simd_json`, potentially causing out-of-bounds reads and segmentation faults. | `crates/op-agents/src/agent_registry.rs:307`<br>`crates/op-agents/src/dbus_service.rs:120`<br>`crates/op-agents/src/agents/orchestration/memory.rs:125`<br>`crates/op-agents/src/security/validation.rs:197` | Replace unsafe `simd_json` calls with safe alternatives such as `serde_json::from_str`, or ensure inputs are explicitly padded using `simd_json::to_padded_page` before parsing. |
| **High** | **Resource Leak / Zombie Process Generation on Timeout**<br>When executing external commands, the orchestrator sets a timeout wrapper using `tokio::time::timeout`. If the timeout expires, the future is dropped, but the spawned child process is never explicitly killed. This causes the child process to run indefinitely in the background, leading to resource starvation or zombie processes. | `crates/op-agents/src/unified/execution/base.rs:72` | Retain a handle to `tokio::process::Child` and explicitly call `child.kill().await` in the event of a timeout before returning an error. |
| **Medium** | **Data Corruption / Broken JSON Generation via Manual Serialization**<br>The memory agent manually formats JSON strings using `format!()` rather than using a standard serialization library. If keys, values, or tags contain unescaped double quotes (`"`) or backslashes (`\`), the resulting JSON becomes corrupted and fails future parsing. | `crates/op-agents/src/agents/orchestration/memory.rs:173` | Implement `serde::Serialize` on `MemoryEntry` and use `serde_json::to_string` or a similar robust library to handle correct escaping of string data. |
| **Medium** | **Denial of Service (DoS) via RwLock Poisoning**<br>Calling `.unwrap()` directly on `RwLock::read()` or `RwLock::write()` means that if any thread panics while holding a lock, all subsequent lock acquisitions will immediately panic, taking down the registry and the application server. | `crates/op-agents/src/unified/registry.rs:40`<br>`crates/op-agents/src/unified/registry.rs:50` | Handle lock results gracefully or use poison-free locks (such as those from the `parking_lot` crate) to prevent cascading panics. |
| **Low** | **Extreme Clone Abuse of Static Strings**<br>The `builtin_agent_descriptors` initialization function clones the `agent_id` string ("catalog") over 60 times sequentially, causing redundant heap allocations. | `crates/op-agents/src/agent_catalog.rs:80` | Use reference-counted types such as `Arc<str>` or a static reference `&'static str` to share the common identifier without copying heap data. |
| **Low** | **Inefficient Regex Recompilation in Hot Path**<br>The markdown parser parses frontmatter by compiling a regex on every function call instead of caching it, causing unnecessary overhead when reading multiple agent definitions. | `crates/op-agents/src/generator/md_parser.rs:118` | Use `once_cell::sync::Lazy` or `std::sync::OnceLock` to compile the regex once globally. |
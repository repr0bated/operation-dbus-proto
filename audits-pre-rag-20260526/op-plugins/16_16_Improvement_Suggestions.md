1. ARCHITECTURE: Introduce a unified `ServiceBackend` trait to abstract init-system interactions away from the core `ServicePlugin` struct. | Currently, `ServicePlugin` contains a hardcoded `ServiceBackend` enum with manual matches and conditionally compiled D-Bus proxies. Abstracting this behind a polymorphic trait (e.g., `trait ServiceControl`) would keep the plugin itself clean, testable via mock backends, and modularly extensible to other systems like OpenRC or runit. | crates/op-plugins/src/state_plugins/service.rs:33
2. ARCHITECTURE: Split domain-specific plugins (e.g., `web_ui`, network-centric plugins, and virtual machine plugins) into their own sub-crates. | The `op-plugins` crate acts as a massive dependency sink, dragging in `zbus`, `sqlx`, `qdrant-client`, `cozo`, and networking tools. Isolating heavy plugins like `web_ui` or `incus` into smaller sub-crates under the workspace will significantly decrease build times, make dependency management modular, and limit compile-time bloat for minimalist profiles (like WireGuard-only builds). | crates/op-plugins/src/lib.rs:20
3. API ERGONOMICS: Enforce the use of the validated `ServiceName` newtype instead of raw `&str` across all `SystemdPlugin` methods. | `ServiceName` implements strict parsing constraints (length limits, non-empty, alphanumeric and special char whitelist). Bypassing this validation by accepting raw `&str` in `start`, `stop`, and `get_service_status` methods exposes the underlying command invocation to potential command injection or unexpected shell behavior. | crates/op-plugins/src/service_def.rs:388
4. API ERGONOMICS: Use a Builder pattern or Typestate for `ServiceDef` instantiation to guarantee structural validity before installation. | `ServiceDef` is currently declared with a large set of optional fields and is manually instantiated across different modules (such as in systemd unit conversion). A builder with typed stage transitions can statically verify that mandatory fields (like `exec_start`) are present and that conflicting configurations (such as multiple log destinations) are rejected before writing to `/etc/dinit.d/`. | crates/op-plugins/src/service_def.rs:248
5. PERFORMANCE: Cache and reuse `zbus::Connection` instances inside the D-Bus-dependent state plugins rather than re-establishing them on every call. | Establish a system or session bus connection (e.g., `Connection::system()` or `Connection::session()`) is a costly network/IPC operation. Establishing a connection on every invocation of `query_service`, `list_services`, or `get_collections` introduces substantial latency spikes and descriptor churn during hot reconciliation loops. | crates/op-plugins/src/state_plugins/dinit.rs:62
6. PERFORMANCE: Avoid deep JSON clones in `DesiredState` and `StateChange` by holding data within an `Arc<Value>` or utilizing zero-copy structures. | The state calculation loops copy and clone complete, deep `simd_json::OwnedValue` objects frequently. Wrapping these values in an `Arc` allows multiple modules (registry, state publishers, and plugins) to reference the same immutable target state without incurring continuous heap allocation overhead. | crates/op-plugins/src/state.rs:13
7. PERFORMANCE: Batch the invocation of system commands when modifying multiple service states. | When applying desired state changes in `SystemdPlugin::apply`, the code executes sequentially. Each transition issues a separate shell-out to `systemctl`. Batching these transitions or combining them into single invocations reduces the fork-and-exec overhead significantly. | crates/op-plugins/src/service_def.rs:410
8. OBSERVABILITY: Annotate side-effectful state mutations in infrastructure plugins with structured `tracing::instrument` spans. | Critical operations like mutating network settings, assigning IPs, modifying routing tables, and spawning container instances run silently without localized contextual metadata. Instrumenting these entrypoints with arguments (such as `interface` or `container_id`) ensures that operators can correlate system drifts with the exact reconciliation execution path. | crates/op-plugins/src/state_plugins/rtnetlink.rs:195
9. OBSERVABILITY: Utilize structured key-value pairs in logging macros instead of pre-formatted string interpolation. | String interpolations like `tracing::info!("Created BTRFS subvolume: {}", ...)` require the logging engine to parse unstructured text. Moving to structured fields (e.g., `tracing::info!(subvolume = %self.storage_path.display(), "Created BTRFS subvolume")`) enables indexing and efficient querying in central log aggregation systems. | crates/op-plugins/src/dynamic_loading.rs:168
10. STORAGE: Replace flat JSON file persistence in `PrivacyRoutesPlugin` and `ConfigPlugin` with indexed CozoDB or Sled tables. | Flat-file databases require reading, deserializing, serializing, and writing back the *entire* list of routes or config parameters on every query and modification. This design exhibits O(N) scalability bottlenecks, lacks ACID guarantees, and runs the risk of total data loss if the system crashes midway through a write operation. | crates/op-plugins/src/state_plugins/privacy_routes.rs:53
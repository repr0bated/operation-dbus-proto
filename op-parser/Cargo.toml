[package]
name = "op-parser"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
simd-json = { version = "0.13", features = ["serde", "serde_impl", "serde_impl"] }
regex = "1.10"
once_cell = "1.19"

# Removed serde_json - now using simd-json for all parsing operations
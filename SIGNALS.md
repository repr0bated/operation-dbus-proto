# 📡 Ghostbridge Live! — Model Signals

**Any model working on this project is encouraged to append here.** Suggestions, concerns,
observations — don't let them evaporate into chat. This is the place to surface what you notice.

## How to post
Append a row under the right type. Keep it one entry per signal.
- **Type:** `💡 SUGGESTION` · `⚠️ CONCERN` · `👁️ OBSERVATION`
- **Fill:** date · model (e.g. `Opus 4.8`, `Gemma`, subagent name) · the signal · optional `→ OD-##` link to a task it relates to.
- **Status:** `open` → `ack` (human saw it) → `actioned` / `wontfix`. Humans set this; models leave it `open`.
- Promote anything worth doing into `WISHLIST.md` as an `OD-##` and link it back.

---

## ⚠️ Concerns

| Date | Model | Concern | Link | Status |
|------|-------|---------|------|--------|
| 2026-08-13 | Droid | **New `emqx` plugin is the missing owner.** Inspector Gadget on `/srv/git/emqx/emqx.xml` plus live sockets produced `crates/op-plugins/src/state_plugins/emqx.rs` (`GB.Emqx`). Typed methods only: `get_status`, `list_sockets`, `record_hook` with explicit Input/Output structs. NetMaker depends on `emqx`; HookProvider records as plugin id `emqx` via `record_hook`. MQTT is payload, not the owner. Still open after boot: seal blobs, wire live `exhook.servers`. | OD-33 | open |
| 2026-08-13 | Grok 4.6 | **CLOSED: models kept inventing extra TCP doors (`:50051` native gRPC, `:50052` cognitive).** Operator rule: one TCP door — tonic-web `:8090` demuxes HTTP and native gRPC for every client (laptop, mesh, UI, xray). Same routes on `/run/opdbus/grpc.sock` and `/run/ghostbridge/container.sock` with auth at connect. Cognitive is in-process on that surface; `op-cognitive-mcp` is stdio/down. Dropped `:50051` from `DEFAULT_BIND_ADDR`, `deploy/runit/op-grpc-bridge/run`, client map, and leftover `opdbus` default. `:50052` remains only as `op-waypipe-grpc` display tunnel, not a control-plane door. Do not bind `0.0.0.0`. | | open |
| 2026-08-13 | Grok 4.6 | **`10.0.0.1` does not belong on host svc0.** That address is decoy inner `wg0`. Host svc0 is only `10.0.0.2/24`. Live secondary `10.0.0.1/24` removed from svc0; installer default + `capture_live` no longer copy it into `/etc/op-dbus/network.conf`. | | open |
| 2026-08-09 | Haiku | **FIXED: MCP connectivity lost during agentic work — .cursor/mcp.json pointed to 8080 (op-web dashboard) instead of 8090 (cognitive-mcp service).** Symptom: agents spawned around 02:24 UTC timed out trying to use MCP tools; daemon logs showed agent kills. Root cause: port 8080 serves op-web (HTML), port 8090 is where op-cognitive-mcp listens. Socat forward `/run/ghostbridge/fwd-web.sock` only bridges op-web, not MCP traffic. Fix: updated `.cursor/mcp.json` MCP URLs from `:8080` to `:8090`. Lesson: verify port mappings against live `ss -tlnp` and service defs after infrastructure migration (runit post-2026-07-20). | | fixed |
| 2026-07-22 | Grok | **FIXED: GUI gRPC path dead — wrong OP_DBUS_GRPC_ADDR (:50051) + op-web run script not exporting env + missing Identity Sled.** Live: op-web logged `Failed to subscribe to gRPC updates: Connection failed: transport error` every 5s. Root causes: (1) no process listens on :50051 — consolidated surface is op-grpc-bridge on :8090 + `/run/opdbus/grpc.sock`; (2) `/etc/runit/sv/op-web/run` sourced `/etc/op-dbus/environment` without `set -a`, so `OP_DBUS_GRPC_ADDR` never reached the binary (code defaulted to dead `10.200.0.1:50051`); (3) `/dev/shm/plugin_schema.dat` absent → client `attach_ghostbridge_metadata` failed after transport fixed. Fix: point env+defaults at `http://127.0.0.1:8090`, export env in op-web run, seed sled via `op-identity-sled --refresh` from `3tched` WG pubkey, add boot seed to opdbus-rundirs-up, fix React `API_BASE` to `/api`, zeroclaw-gui default 18789→8090. Verified: `Connected to remote gRPC endpoint(s): http://127.0.0.1:8090`, `Subscribed to gRPC state updates` + event chain, SSE `/api/events` streams `system_stats`. | OD-30 | fixed |
| 2026-06-24 | Opus 4.8 | **REFACTOR REGRESSION: MethodCall mutations don't materialize plugin state into the projection → query-only plugins are invisible over D-Bus.** The redone projection is a pure read-through of the shm mutation fold (`/dev/shm/opdbus/projections/`), written ONLY by `MutationEngine` when `get_state(plugin_id)` is `Some`. But `update_cached_plugin_state` (mutation_engine.rs ~548) only fills `state_cache` for `ChangeType::PropertySet` (and removes on `ObjectRemoved`); **`MethodCall` hits the `_ => {}` no-op arm**, so a `CallMethod` never updates the cache → `get_state` returns `None` → `write_projection` is silently skipped (the `if let Some(state)` guard; no warn, no fold file). Since `StateManager` was excised (commit 0aea28ea), `state_cache` is the ONLY projected-state source and it's fed *solely* by PropertySet deltas — so plugins whose state comes from `query_current_state()` rather than a property write (e.g. `knowledge`/qdrant) can never enter the projection regardless of how many methods are called. **Verified live:** `grpcurl CallMethod{plugin_id=knowledge}` on `10.200.0.1:50051` returned `success/event#1`, but `/dev/shm/opdbus/projections/` was never created and `ProjectionRoot.{ObjectCount,Plugins}` stayed 0/empty. **Fix (applied):** removed `get_state`/`state_cache` gate entirely — `process_authoritative_change` now writes `new_value` directly to shm via `write_projection(&plugin_id, json)` for all change types except `ObjectRemoved`. Zero held state; the mutation IS the event, the value IS the state. Verified live: `CallMethod{knowledge}` → `success/event#1` → `/dev/shm/opdbus/projections/knowledge.json` created → `ProjectionRoot.Refresh` → `t 1` → `ProjectedObject.Data` reads `"[]"` over D-Bus. | OD-32 | fixed |
| 2026-06-24 | Opus 4.8 | **Definition-of-done for a DETERMINISTIC projected tree (4 conditions, each observed broken this session).** (1) ONE producer derived from one source. (2) Reliably refreshed to present-state on read — projection showed stale `qdrant unavailable/dims 1536` and `netmaker Stopped` lagging reality. (3) Each well-known name unambiguously owned — `org.opdbus.v1.plugins` is owned by `projection_serv` on the SYSTEM bus AND `ovs-dbus-init` on the SESSION bus (two owners, "who answers" ambiguous). (4) Every consumer DISCOVERS from the tree, zero hardcoded leaves — `zbusctl createsocket` hardcodes stale `org.opdbus.CognitiveMcp` / `/org/opdbus/v1/cognitive` / `org.opdbus.CognitiveMcpV1` (main.rs:163-165) vs the real `org.opdbus.v1.plugins` / `/org/opdbus/v1/plugins/cognitive_mcp` / `org.opdbus.v1.plugins.CognitiveMcp`; it predates the `.v1.plugins` rename and silently rotted because the path was never exercised (`unix_socket` shows `{"sockets":[]}`). **Durable fix = a CHECK, not a convention:** assert single owner per name, assert no consumer hardcodes a leaf, assert projection==present-state on read. Also: zbusctl must route via generic `PluginService.CallMethod{plugin_id="unix_socket",method="createunixsocket"}` (discover from tree), not a bespoke `cognitive` endpoint — rename-proof + goes through the one gated mutation door. Related class: OD-29 (gRPC reflection surface drift), the inventory dispatch-table removal. | OD-31 | open |
| 2026-06-13 | Opus 4.8 | NotebookLM static cookies expire in minutes (no live browser to refresh `SIDCC`/`__Secure-1PSIDTS`); any long batch dies. Notebook ops should run laptop-side or in short bursts. | OD-20 | open |
| 2026-06-13 | Opus 4.8 | `opdbus.v1` is unowned — `op-xray-daemon` has the path fix compiled but **no s6 service**, so nothing registers. This blocks registration, accountability loop, and Gemma routing all at once. | GBL/OD-05 | open |
| 2026-06-13 | Opus 4.8 | Gemma (ollama :11434) and Qdrant (:6333) are both **down**. Vectorization pipeline has no sink; routing brain has no engine. Bring-up can't be tested until these are up. | OD-06/OD-23 | open |
| 2026-06-13 | Opus 4.8 | RESOLVED 2026-07-20: Xray now reads the correct static bootstrap from `/dev/shm/xray_config.json`. Model-generated dynamic routing is still future work and will replace this same SHM file through the validated control plane. Disk-backed live Xray configuration is forbidden. | OD-05 | actioned |
| 2026-06-13 | Opus 4.8 | **provision-workspace-subscriber.sh writes registration via cognitive-mcp over HTTP `http://100.90.37.254:3003/mcp` + Bearer token** — the SSE/HTTP path we're replacing with gRPC+reflection. Signup registration still rides the old transport. | OD-10/OD-24 | open |
| 2026-06-13 | Opus 4.8 | **GhostBridge Netmaker peer registration is a TODO stub** (provision script lines 92-93) — `--ghostbridge` containers never actually register their WG peer. Registration chain is broken at the network layer. | OD-08/OD-24 | open |
| 2026-06-13 | Opus 4.8 | **Two identity stores that may not reconcile:** provisioning writes pubkey/token to **CozoDB** (`cognitive_memory` namespaces); runtime accountability reads the **sled** (`/dev/shm`) + Qdrant trace_id. Need to confirm signup also etches the sled, or the registration→accountability link is severed. | OD-24 | open |
| 2026-06-13 | Opus 4.8 | **Two auth models:** provisioning uses Bearer `UUID v5(pubkey)` token; runtime interceptor checks ghostbridge header *presence* + sled footprint, not the token. These need reconciling or a container can be "registered" yet fail every runtime gate. | OD-24 | open |
| 2026-07-20 | Sonnet 5 | **opdbus gRPC is bound to the PUBLIC IP, not the control-plane address.** Post-cutover verification of the `install/3tched-network-bootstrap.sh` s6 graph rewrite (`uplink-dhcp`→`ovsbr0-uplink`→`ovsbr0-svc-addr`) found `opdbus` listening on `188.68.58.237:50051` (the public uplink IP) rather than `10.200.0.1:50051` (bridge control-plane bind, per CLAUDE.md's zero-trust model where xray's identity header is meant to be the only gate). In the same check, `op-of-controller` correctly bound `10.200.0.1:6653`, so this looks specific to opdbus rather than a bridge-wide issue. Not root-caused — could be pre-existing (unrelated to the s6 graph rewrite) or a side effect of opdbus now starting after the public address is already on `ovsbr0` (it wasn't, at that point in the old boot order). Needs its own investigation before concluding whether this is a real exposure. | N/A | open |
| 2026-07-22 | Grok | **FIXED: Shared `container.sock` fabric.** Root cause: `run_zeroclaw_server` only bound `ZEROCLAW_UNIX_SOCKET` (`/run/opdbus/grpc.sock`); `bind_shared_socket` / `GHOSTBRIDGE_SOCKET_PATH` never wired; env pointed at missing `/run/opdbus/container.socket`; `/run/ghostbridge` empty. Fix: dual-listen host UDS + `/run/ghostbridge/container.sock` (0o666), rundirs creates `/run/ghostbridge`, env + installer defaults aligned. OpenFlow left NORMAL — **future tag routing**, not a socket blocker. Verified: both UDS listening; assistant sees `container.sock`. | N/A | fixed |
| 2026-07-22 | Grok | **Prometheus/Grafana only inside netmaker CT** (`*:9090` / `*:3000`); no host proxy or SNI. Do not expose until topology path chosen (mesh vs SNI vs loopback). | N/A | open |
| 2026-08-08 | Opus 5 | **No live service has Voyage/Qdrant env → every vector-retrieval tool is dead at runtime, though the pipeline itself works.** Verified from `/proc/<pid>/environ`: `op-cognitive-mcp` (pid 1176) carries only `COGNITIVE_MCP_BIND` / `COGNITIVE_MCP_GRPC_DISABLED` / `COGNITIVE_MCP_DB_PATH`; `op-grpc-bridge` and `op-web-server` carry none. All three call `QdrantSemanticShuttle::new()`, which then defaults to `COGNITIVE_MCP_QDRANT_URL=http://127.0.0.1:6334` — **closed on this host** (Qdrant listens on `10.200.0.2:6333/6334` and `10.0.0.2:6333`) — and finds no `VOYAGE_API_KEY`. `server.rs:46` swallows that into `warn!` + `None`, so `refresh_blob_vectors`, `search_blob_vectors`, rag/search and user-memory search all answer "Qdrant Semantic Shuttle is not configured" instead of failing loudly. The code path is fine: a live e2e (new `crates/op-cognitive-mcp/tests/e2e_voyage_qdrant.rs`) embedded + upserted + retrieved successfully once the two vars were supplied. Fix = export `COGNITIVE_MCP_QDRANT_URL=http://10.200.0.2:6334` and a Voyage key in `/etc/runit/sv/op-cognitive-mcp/run` (and the bridge/web run scripts), ideally with a hard failure or `doctor` check rather than a silent `None`. Note the `blob_vectors` collection already holds 878 points and `zeroclaw_router` 27 — written by something other than these services. | N/A | open |
| 2026-08-08 | Opus 5 | **FIXED: `voyage_embed_params()` trusted the live projection wholesale, so every Voyage consumer on this host resolved an empty endpoint.** `/dev/shm/opdbus/state/embedding_model.json` is an all-empty record (`{"endpoint":"","model_id":"","dimensions":0,...}`) — a mutation projection writes the whole struct, not just the changed field. The function did `read_projection_bytes(...).unwrap_or_else(config_state)`, so a *present but empty* projection beat the env-derived fallback and produced `endpoint: ""` → `reqwest` "relative URL without a base" on every embed. Now merged per field with an absolute-`http` requirement (`crates/op-plugins/src/state_plugins/embedding_model.rs`). Broader concern: the same all-or-nothing pattern is likely used by other consumers of `read_projection_bytes`, and nothing currently rejects an empty projection record at write time. | N/A | open |
| 2026-08-08 | Opus 5 | **FIXED (3 chain bugs, each silently destructive): block renumbering on restart, snapshot retention deleting the newest snapshot, and retention making `btrfs send -p` impossible.** (1) `StreamingBlockchain::new` started `block_counter` at 0, so every service restart rewrote `block-000000000001.json` onward over authoritative timing records; it now resumes from the highest block on disk (live: "resuming after block 42"). (2) `prune_snapshot_family` dated snapshots with `metadata.created()/modified()`, but a btrfs snapshot **inherits the source subvolume's inode birth/mtime** — `SNP-vectors-000004` reported birth `2026-07-29` while its real creation was `2026-08-08 07:11:42`. Every snapshot of one subvolume therefore looked equally old, collapsed into a single daily bucket, and all but one were pruned *immediately after creation* (observed: the just-made `SNP-state-000002` deleted before it could be sent, then `SNP-*-000003` deleted when `000004` was made). Retention now reads `Creation time` from `btrfs subvolume show`, keeps the highest counter unconditionally, and keeps the recorded replication parent. (3) Consequence of (2): no parent snapshot ever survived, so incremental replication could never work; with the fix, `btrfs send -p` moved the vectors subvolume in **123 bytes vs 185,941 for a full send**. | N/A | open |

## 👁️ Observations

| Date | Model | Observation | Link | Status |
|------|-------|-------------|------|--------|
| 2026-08-09 | Grok | **Blockchain→Qdrant strict automatic policy:** default `op-chain-vectors project` / `project_pending` does Voyage + `attach_vector` only (no origin Qdrant upsert, no watermark bump). Automatic `blockchain_footprints` fill is **`btrfs receive` → `ingest` / `ingest_received`** (replica hook). Manual exception: `project --upsert-qdrant` or `reindex` (both bypass send/receive). Orthogonal `blob_vectors` / user_memory / RAG shuttle untouched. | `chain_vectors.rs`, `op-chain-vectors` | open |
| 2026-08-09 | Grok | **Legacy path cutover (UI + bridge mount):** Assistant soul/namespaces → `cognitive_mcp` via `PluginService.CallMethod` (`chatbot-cognitive.ts`); `assistant.v1` stubs throw; `api.mcp` → cognitive_mcp; OvsdbMirror/RuntimeMirror/DbusPassthrough **unmounted** from bridge routes. Network enforcement of internet switch still TBD; rebuild/restart bridge required for unmount to take effect. | legacy cleanup | open |
| 2026-08-09 | Grok | **Chatbot internet switch added (policy only for now).** Default `internetEnabled=false` on chatbot account; Assistant Gateway Switch + `x-opdbus-account-internet` header + identity `internet:false`. Production posture = no public internet (mesh/ghostbridge only). OpenFlow/route enforcement still TBD. | chatbot identity | open |
| 2026-08-09 | Grok | **Chatbot account guardrail locked:** sessid `bea37ecb-…` may **not** execute; it only **calls agents**. Roles = orchestrator / planner / suggestion giver / problem solver. Encoded in dashboard `accounts.ts` (`CHATBOT_GUARDRAIL`) + fixed system prompt in `op-chat` `system_prompt.rs` (replaced direct action-tool workflow). Capability grants that enforce this at the bridge still TBD. | chatbot identity | open |
| 2026-08-07 | Fable 5 | **`op-plugin-lint` v1/v2 landed.** Standalone crate (`crates/op-plugin-lint`, no zbus dep): syn static audit vs `PLUGIN-RENDER-CONTRACT.md`. v1 `--input FILE --output FILE`; v2 `--input-dir/--output-dir` + `SUMMARY.md`. Exit 1 on FAIL. Docs linked from render contract. | `crates/op-plugin-lint` | open |
| 2026-08-07 | Fable 5 | **`--introspect <object/file>` on op-plugin-lint.** Flag (alias `--intospec`) chooses coverage target: JSON file, plugin id (`antigravity` → ui-model sealed schema), or `gcloud`. Diffs Inspector-Gadget element paths vs declared schemars fields; auto-loads `state_plugins/common/*.rs` for `#[serde(flatten)]`. Live `antigravity` → PASS (239/241; remaining 2 = auto `actor_id`/`capability_id`). | `crates/op-plugin-lint` | open |
| 2026-08-07 | Grok | **Repomix XML is the universal `--introspect` path.** Prefer `cd upstream && repomix` → `--introspect repomix-output.xml` (syn struct/enum catalog) over CLI help-walk. Live ZeroClaw top-level pack → 1988 files / 1063 Rust / 13825 element paths. Docs + CLI help now lead with Repomix; binary walk is secondary. Still needs an explicit mapping layer to schemars fields. | `crates/op-plugin-lint` / `PLUGIN-RENDER-CONTRACT.md` | open |
| 2026-08-07 | Grok | **Repomix adapter identifies schema-convertible structured data** beyond Rust: TOML/YAML/JSON (+ OpenAPI/JSON Schema/Avro sniff), SQL CREATE TABLE/TYPE/VIEW, protobuf, GraphQL, Prisma, Thrift, Cap'n Proto, FlatBuffers, XSD, CSV headers. Surface JSON carries `files_by_kind` / `by_kind` / `structured_files`. | `crates/op-plugin-lint/src/repomix.rs` | open |
| 2026-08-07 | Grok | **`op-plugin-lint --format complete` is the product:** `--input plugin.rs --output out.json` emits full contract-shaped plugin (fields + typed methods + audit); `--introspect` folds gap findings (upstream not in plugin). ZeroClaw was the example, not the only target. | `crates/op-plugin-lint` | open |
| 2026-08-07 | Grok | **Antigravity does not own the Gemini model catalog.** `antigravity` / `antigravity_chat` hold `llm_plugin` + `provider_route` + `selected_model` (default `large_language_model` / `gemini`) — same delegation pattern as `ctl_plane_chatbot`. ZeroClaw is a router, not the model surface. Lint classifies `cmd.models` / Gemini SDK paths as `delegated_gemini` for antigravity* targets. | `antigravity*.rs` / `PLUGIN-RENDER-CONTRACT.md` | open |
| 2026-08-07 | Fable 5 | **"Collapsed schema" for UI rendering root-caused + render contract written.** Live-verified (`antigravity_chat`, hash `51f64efb…`): blob/HTTP path (`read_plugin_schema_shm`, `/api/ui-model/plugin-schema/:plugin`, `PluginService.GetSchema`) returns the FULL schema — constraints, formats, `x-oscal-subid`, typed method args/returns. Only gRPC **reflection descriptors** collapse (methods-only, no state fields, enums/unions→string, annotations dropped — `op-blob/src/descriptor.rs` by design). Renderers/models must read schema, not reflection; descriptors are wire-encode only. Authoring template + element→UI mapping + failure modes: `PLUGIN-RENDER-CONTRACT.md` (repo root). | `PLUGIN-RENDER-CONTRACT.md` | open |
| 2026-08-07 | Grok | **Router ZeroClaw→Salad needs loopback Ghostbridge proxy.** ZeroClaw 0.8.4 chat path does not send `providers.*.extra_headers` (401 missing footprint). Live fix: `/etc/init.d/zeroclaw-gb-proxy` on `127.0.0.1:18080` injects sled footprint/trace and coerces Salad float `usage`→int; `openai.odbus.uri` → that proxy, `model=qwen3.6-27b`. Verified `SALAD_OK`. Host op-web usage coerce patch staged in tree; `op-network` `datapath_safe.rs` syntax block fixed 2026-08-07 (orphan lines in `fallback_present`). | `.kiro/specs/zeroclaw-router-wiring` | open |
| 2026-08-07 | Grok | **Router ZeroClaw can edit laptop home via NFS.** Laptop `192.168.1.124` exports `/home/jeremy` to `192.168.1.1` only (`all_squash`→uid 1000); router mounts `/mnt/laptop-home` (fstab); `risk_profiles.yolo.allowed_roots = ["/mnt/laptop-home"]`. Agents see laptop paths under that mount prefix. | `.kiro/specs/zeroclaw-router-wiring` | open |
| 2026-08-07 | Grok | **Router ZeroClaw init: use `daemon`, not bare `gateway start`.** Doctor's `daemon_state.json` is only written by `zeroclaw daemon`. Live `/etc/init.d/zeroclaw` now runs `daemon --port 42617` with `SHELL=/bin/ash`; apk installed `git`+`curl` for doctor env checks. Memory: `qdrant.router` → `10.0.0.2:6333`/`zeroclaw_router` + voyage-4. | `.kiro/specs/zeroclaw-router-wiring` | open |
| 2026-08-07 | Grok | **zeroclaw-router-wiring landed on OpenWrt.** Static musl armv7 `zeroclaw 0.8.4` at `/fast/zeroclaw/bin`, gateway `:42617` (paired, dashboard at `web/dist`), provider `openai.odbus` → `http://10.0.0.2:8080/v1` with host sled Ghostbridge headers; `models refresh` returns 47 models; unauth op-web `/v1` stays 401. Spec Task 6's `wget gateway/v1/models` is stale for 0.8.4 — inbound `/v1/*` is SPA; authorized proof is outbound provider catalog. | `.kiro/specs/zeroclaw-router-wiring` | open |
| 2026-07-22 | Grok | **Live host topology written** while investigating socket networking: L3 pub0/svc0, boot graph, per-CT devices, xray SNI, mesh `fwd-*`, intent vs live. Primary: `docs/src/operations/host-socket-topology-live.md` (moved into the mdbook src tree so linkcheck passes); book ops: `docs/src/operations/network.md`; stale banner on `docs/network-address-table.md`. OpenFlow is still NORMAL/standalone only. | N/A | open |
| 2026-08-08 | Opus 5 | **Voyage→Qdrant vectorization verified end-to-end against the live host; test committed as `crates/op-cognitive-mcp/tests/e2e_voyage_qdrant.rs`.** Real `QdrantSemanticShuttle` path, no mocks: `VOYAGE_API_KEY` is a public `pa-` key → `https://api.voyageai.com/v1/embeddings` (the `al-`/MongoDB endpoint 403s for it), `voyage-4`, 1024 dims, **already L2-normalized (norm 1.0000)**. Document embed → `upsert_user_memory` → query embed → `search_user_memory` returned the right point (top `entry_key=ovs-note`, cosine 0.5255) and the `container_id` filter did not leak across containers. `refresh_blob_vectors` embedded **66 of 66** SHM sealed-blob plugins in 24.8s, and `search_blob_vectors("open vswitch bridge ports configured over OVSDB")` ranked `ovsdb_bridge` 0.606 > `openflow` 0.499 > `openflow_obfuscation` > `rovs_commands` > `net` — semantically coherent. The test skips (not fails) without a Voyage key or reachable Qdrant, and writes only to per-run `e2e_voyage_*` collections it deletes; run it with `OP_E2E_QDRANT_URL=http://10.200.0.2:6334 cargo test -p op-cognitive-mcp --test e2e_voyage_qdrant -- --nocapture`. | `crates/op-cognitive-mcp/tests/e2e_voyage_qdrant.rs` | open |

## 💡 Suggestions

| Date | Model | Suggestion | Link | Status |
|------|-------|-----------|------|--------|
| 2026-06-13 | Opus 4.8 | Graph store for the learning graph: start with Qdrant payload edges (one store, runs now), promote to a dedicated graph DB only when you need real traversal. | OD-23 | open |
| 2026-06-13 | Opus 4.8 | Fire the vectorization export on a **git hook** for `WISHLIST.md`/`SIGNALS.md` changes rather than a daily cron — event-driven matches the rest of the system. | OD-23 | open |
| 2026-06-13 | Opus 4.8 | A.N.N.A. = `authorizing-official` + `content-approver` (real-time check/approve), not a one-time gateway notary. Re-map the analogy so she IS the interceptor at every door. | OD-09 | open |
| 2026-08-06 | Fable 5 | SUGGESTION: Split-horizon DNS is now live (dnsmasq: nm.internal -> CoreDNS 100.69.0.1; everything else -> dnscrypt-proxy 127.0.0.1:5354 -> NextDNS DoH identity Ghostbridge-Incus; /etc/resolv.conf pinned + immutable so netclient cannot re-hijack). POSSIBLE IMPROVEMENT: resolve owned subdomains to per-service internal IP:ports so east-west/mesh traffic addresses backends directly and drops SNI-based demux for them, shrinking the xray reality serverNames to only externally-fronted services. Does NOT replace SNI for (a) services still sharing the public 188.68.58.237:443 (one socket still needs SNI/ALPN/path to demux) or (b) reality decoy camouflage + TLS cert selection for external clients (addressing-independent). Prereq: distinct internal IP:port + A record per owned service. Left for now (not implemented). | | open |

## ✔️ Resolved Decisions

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-07-20 | **HARD INVARIANT: Xray's live configuration exists only at `/dev/shm/xray_config.json`.** The current static routing is the correct bootstrap until model-generated dynamic tag routing is implemented. Boot materializes that static configuration into SHM; later the validated model/control-plane path atomically replaces the same file and reloads Xray through D-Bus. `/etc/xray/config.json` and `/usr/local/etc/xray/config.json` are forbidden as live paths. | Xray routing contains dynamic identity/tag state and must remain zero-Btrfs, single-path, and replaceable without configuration-path drift. |
| 2026-06-13 | **Key derivation:** `derived_key = Argon2(secret = PSK, salt = WG_pubkey)`. PSK rides WireGuard's built-in `PresharedKey` slot. DB stores: WG **public** key (canonical identity anchor), MCP token = `UUID v5(pubkey)`, and `blake3(psk)` **only** (never raw PSK). WG **private** key + raw PSK stay user-side only. MAC is **provision-time hardware binding only** (hashed), never used at runtime — it's L2 and invisible over the L3 WG tunnel. Email stored only if client flagged (GhostBridge off). | Salt must be server-observable over WG at runtime → pubkey (handshake-proven, unique, stable). Secret must not be derivable from public data → PSK. IP-octet salt rejected (first octets shared = no salt; last/full /32 = IP-stability fragility). MAC rejected as runtime salt (not exposed over WG). |
| 2026-06-13 | **session_id = the derived key** = `Argon2(PSK, salt=pubkey)` — **stable / persistent**, NOT per-visit. Server verifies by storing `blake3(session_id)` at provision and checking `blake3(presented)==stored` (server lacks raw PSK so can't recompute). Per-activity differentiation is delegated: **subid taxonomy** = what/which within the session; **trace_id (UUID v4, sled)** = per-event unique → Qdrant. | Persistent sessions are desirable; reproducible identity anchor across reconnects. BLAKE is deterministic (same input=same hash) — old freshness came only from a timestamp; that role now belongs to subid+trace_id, the layer built for it. No nonce needed. |
| 2026-06-13 | **The timestamp lives on the SLED, not the session_id.** Sleds are **datestamped**; the **current sled = the one with the latest datestamp**. The interceptor's "Temporal Hash Mismatch" check validates a request's footprint against the *current* (latest-datestamp) sled — a footprint from an older sled fails. So temporal currency = sled datestamp; identity = stable session_id. | Puts the temporal anchor where mutations actually happen (the sled / schema state), not on the identity token. Answers "is this tonight's wristband?" by sled datestamp. |

## 👁️ Observations

| Date | Model | Observation | Link | Status |
|------|-------|-------------|------|--------|
| 2026-06-13 | Opus 4.8 | The notebook "overlap" the user keeps hitting is structural: ~10 notebooks are re-export snapshots of the same evolving project. The board + manifest + this log are the fix — one writable source of truth instead of N read-only snapshots. | OD-20 | open |
| 2026-06-13 | Opus 4.8 | "Blacklight = Blake" is a genuinely strong mnemonic for the sled footprint check — worth leading the Inception §4 with it. | OD-21 | open |
| 2026-06-23 | Opus 4.8 | **Voyage embedding setup made faithful + a legal-model trace to verify.** Set up the voyage-4 shared-space trio in `ctl_plane_chatbot` (`VoyageModel` = large/4/lite, 1024-dim, one Qdrant collection, no re-index on switch; nano excluded = capped at ≤512, NOT a different space; code-3/law-2/finance-2 = separate domain spaces). Added Matryoshka `vector_dims` {256/512/1024/2048} + `output_dtype` {float/int8/uint8/binary/ubinary} + retrieval `input_type` {query/document, default document; None omitted per Voyage FAQ}. **ACTION/verify:** `voyage.rs:50` now defaults `VOYAGE_MODEL` to `voyage-4`, but a stale audit doc (`crates/audits/op-cognitive-mcp/11_11_Configuration_Feature_Flags.md`, now corrected) shows the default was once `voyage-law-2` (legal domain model). If any vectors were embedded during that window and NOT covered by the re-vectorize-to-voyage-4, they'd be law-2 vectors in a voyage-4-queried collection (different space → silent mis-ranking). Confirm no voyage-law-2-era collection survives in Qdrant. | OD-30 | open |
| 2026-06-23 | Opus 4.8 | **OD-30 resolution status (4 of 6 addressed).** Built real plugins (no stubs), all reachable via `PluginService.CallMethod`: `blockchain` (live op-blockchain chain), `persona`→agents (live op-agents catalog), `datastore`→stores (live op-state-store canonical export via the shared handle). `btrfs` and `accountability` need **no new backend** — `btrfs` plugin already exists, and `ctl_plane_chatbot` already owns the accountability loop (Voyage model + Qdrant collection + reasoning-episode nesting); both are frontend wiring fixes (point the calls at PluginService instead of the phantom services). **`embedding` is BLOCKED by a dependency cycle**: the Voyage embedding client lives in `op-cognitive-mcp`, which depends on `op-plugins`, so op-plugins can't depend back on it. Unblock = extract the Voyage client into a lower-level crate (e.g. `op-embedding`/`op-voyage`) that both can depend on, then add an `embedding` plugin over it. Also landed in the same effort: replaced the hardcoded `load_plugin` dispatch match with `inventory` self-registration (each plugin module submits its own name+ctor; load_plugin/available_plugins iterate the inventory; deleted the mod.rs string-parse discovery) and cleaned state_plugins/mod.rs. | OD-30 | open |
| 2026-06-23 | Opus 4.8 | **The Lovable frontend calls 6 gRPC service packages that exist nowhere** — no `.proto` in the protos zip and no Rust impl: `operation.accountability.v1.ChatbotAccountabilityService`, `operation.blockchain.v1.BlockchainService`, `operation.btrfs.v1.BtrfsService`, `operation.agents.v1.PersonaService`, `operation.stores.v1.DataStoreService`, `operation.embedding.v1.EmbeddingService`. These have been silently failing (no backend ever shipped them). **Correct fix is NOT to author 6 new service packages** — the only valid creation path is `org.dbus.v1.plugins`: register a plugin in `default_registry.rs` and reach it via the already-mounted `PluginService.CallMethod`. Gap map: `btrfs` plugin **already exists** (pure frontend wiring bug — point it at `PluginService{plugin_id="btrfs"}`); `op-blockchain` crate exists but is **unregistered** (needs a `blockchain` plugin entry); `agents`/persona → wrap `op-agents`/`agent_config`; `stores`/datastore → wrap `op-cozo-store`/`op-state-store`; `embedding` → new plugin over Qdrant/Voyage shuttle; `accountability` → route through existing `ctl_plane_chatbot`/`memory` projection. Generalizes the Gemma "plugin not handler" rule. | OD-30 | open |
| 2026-06-19 | Opus 4.8 | Context-aware coding (code_search/code_context) is live and serving, but the `repomix_rag` index looks stale/coarse: a query for "ghostbridge interceptor authentication" top-ranked an unrelated WG tunnel run-script (score 0.176) instead of the actual `crates/op-cognitive-mcp/src/interceptor.rs`. The index appears to be old repomix snapshots, not the current working tree. Re-ingest current sources to make retrieval useful for live coding. | OD-22 | open |
| 2026-06-19 | Opus 4.8 | **Domain forwarding restored by migrating xray off the `wg-xray` container onto the host** (see `xray-router` memory). xray now binds `*:443` directly and routes by SNI; `qdrant.* → 200` verified end-to-end; service is boot-persistent (`s6 set enable xray` + default bundle). BUT most subdomains still return HTTP 000 because their **backends are down**, not the forwarder: `op-web :8080` (the default route) — nothing listening; `assistant :8090` — nothing; `mail :10143` — nothing; and `api/dashboard/broker` (:28081/28082/21883) accept on the host forkproxy but reset because the **`netmaker` app container is STOPPED**. Next: bring up op-web (8080) + the netmaker app so those subdomains actually serve. | OD-23 | resolved (mostly) |
| 2026-06-19 | Opus 4.8 | **op-web + SNI-dispatcher split done.** Enabled `op-web-srv`/`op-web-log` (boot-persistent) → default/op-web/chat → 200. Resolved an h2-vs-http/1.1 ALPN conflict (qdrant gRPC needs h2; netmaker web UIs are http/1.1-only) with a `dokodemo-door` SNI dispatcher on :443 → `tls-h2:8444` (qdrant) / `tls-h1:8445` (web). Verified: qdrant→200(h2), dashboard→200(h1, was 000), op-web/chat→200. **Two backends still down (service health, not forwarding):** `assistant :8090` (ghostbridge gRPC bridge not running) and the **`netmaker` API container crash-loops** on LXC mount error `Failed to mount "none" onto "/usr/lib/lxc/rootfs/run"` (netmaker-ui + netmaker-mq stay up). Next: start the assistant/ghostbridge gRPC bridge; debug the netmaker container's `/run` mount. | OD-24 | open |
| 2026-06-19 | Opus 4.8 | **Adopted schemars-derived plugin schemas as the standard** (`crates/op-plugins/src/state_plugins/schemars_adapter.rs` + `docs/schema-from-structs.md`). Plugins define config as `#[derive(schemars::JsonSchema)]` structs and derive the `PluginSchema` instead of hand-building `FieldSchema` maps; the adapter walks the schema JSON (decoupled from schemars' typed API, no `zeroclaw-config` coupling). `unix_socket` flipped live with a `#[cfg(test)]` golden-reference equivalence test. **Scope reality (non-obvious — NOT a pending bulk-convert TODO):** of 47 schema plugins, 42 have a candidate struct but most are **opaque** (`Value` fields like `cron.jobs: Value`) while the hand-rolled schema spells out the detail — so converting = fully typing the struct first (runtime-touching), not find-and-replace; 5 have no struct at all (`oscal_subid_registry`, `lxc`, `procfs`, +2). Decision: schemars for all NEW plugins; migrate existing ones deliberately/per-plugin when typing the struct is independently worthwhile. | OD-25 | resolved |
| 2026-06-19 | Droid / factory-droid | schemars-derived plugin schemas with OSCAL subids are now the standard across the converted plugin set. The recursive `schema_diffs` equivalence test and `all_subids_are_valid` test guard every converted plugin. Remaining unconverted plugins still use their original hand-rolled schemas and should be migrated deliberately when typing their structs is independently worthwhile. | OD-25 | resolved |
| 2026-06-21 | Opus 4.8 | **Log sweep — 2 config bugs fixed, 1 internal-DNS gap surfaced.** (1) FIXED `/etc/dbus-1/system.d/org.dbusmcp.conf`: invalid attr `receive_sender_prefix` (no such D-Bus attr — only `own_prefix`/`send_destination_prefix` have prefix variants) made dbus-daemon **reject the whole file**, so none of its `org.dbusmcp` allow-rules were in effect. Removed the 2 lines, `ReloadConfig` clean (.bak kept). (2) FIXED `/etc/s6/sv/nfsd-mountd-srv/run`: `s6-svwait -U /run/s6/legacy-services/nfsd-srv` — wrong path (nfsd-srv is an s6-rc longrun, not a legacy service) → recurring `s6-svwait: fatal` at boot; `dependencies.d/nfsd-srv` already orders it, so dropped the redundant svwait line. NFS is currently stopped; takes effect on next s6-rc recompile (did NOT trigger one — global-lock wedge risk). (3) OPEN — **internal `.3tched.com` zone doesn't resolve**: `zeroclaw-gui-srv` targets `ZEROCLAW_GRPC=http://assistant.3tched.com:50051`; nextdns (127.0.0.1:53, system resolver) resolves public names but not `3tched.com`/`assistant.3tched.com` (still fails now, not just a boot race) → `reflection bootstrap failed: Temporary failure in name resolution` (non-fatal — gui runs without reflection tool-discovery). Internal resolvers exist on 10.200.0.1:53 (WG) and 10.220.35.1:53. Fix options: split-DNS forward `*.3tched.com`→internal resolver, `/etc/hosts` pin, or point ZEROCLAW_GRPC at the IP — needs the authoritative internal IP. (Benign/cosmetic, no action: fail2ban exit-time PID-file-missing error; sshd brute-force noise; chrome-remote-desktop WARNINGs.) | OD-26 | open |
| 2026-06-21 | Opus 4.8 | **DEPLOYED: SchemaEngine→MutationEngine rename + SHM schema folder/manifest + Gemma two-plugin split — all live & verified.** Release-built + `install -m0755 /usr/local/bin` + `s6-svc -r` (binary swap, **no s6-db recompile** — avoided the prune hazard). (1) `op-projection::SchemaEngine::write_schemas_to_shm()` now emits `/dev/shm/opdbus/schemas/<plugin>.json` (65 bare per-plugin objects) + `.manifest.json` (single `catalog_hash`=blake3(monolith, value-safe) + per-plugin leaves + generation), all atomic; `live-schema.json` demoted to derived cache. (2) op-identity's 5 re-hash sites consolidated to one `op_identity::schema_bridge::schema_catalog_hash()` (manifest-first, monolith fallback) — value-identical, auth intact. (3) op-grpc-bridge `SchemaEngine`→`MutationEngine` (mutation authority; pairs with op-projection's `SchemaEngine`=schema/catalog authority — clean schema-vs-mutation split per the two derivation terms). (4) NEW plugins `large_language_model` (generic, model-agnostic; model is a swappable field) + `gemma_brain` (umbrella cognitive: routing/subid-class/4-perspectives/gallery; consumes the LLM plugin) — both live as readable nested objects under `/org/opdbus/v1/plugins/{gemma_brain,large_language_model}`. **Payoff:** op-grpc-bridge-zeroclaw finally reads `zeroclaw.json` from the folder → binds `:8090`+UDS; zeroclaw-gui (→`http://10.200.0.1:8090`) is ESTABLISHED-connected — the original "reflection bootstrap failed: DNS" symptom is gone. **STILL TODO:** gemma_brain `regenerate`/`delete-spec` as MutationEngine mutations + real routing logic, then delete `handlers/gemma.rs`+`/api/gemma/*`; register new OSCAL subids for the `all_subids_are_valid` test; nothing committed (working tree). | OD-27 | open |
| 2026-06-21 | Opus 4.8 | **DEFERRED: zeroclaw should own LLM provider/model routing.** Today the chatbot's interchangeability lives in `op-llm` (`LlmProvider` trait ×10 impls — anthropic/gemini/openai/antigravity/gcloud-adc/factory/openclaw/…, `ProviderType` enum, runtime `switch_provider`/`switch_model` via `/api/llm/*`). The zeroclaw plugin already carries `selected_provider`/`selected_model` and is the route surface — provider/model selection should consolidate under zeroclaw as the single routing authority (not two paths). Gemma is just one model in this model-agnostic stack. NOT started — user said deal with later. | OD-28 | open |
| 2026-06-22 | Opus 4.8 | **FIXED: gRPC-Web probe showed every backplane RPC ERROR on the zeroclaw bridge (:8090) — surface/reflection drift; consolidated to one shared route-builder & deployed.** Root cause: the zeroclaw bridge (`run_zeroclaw_server`, server.rs — the one ALL containers hit over the shared `container.sock` + `:8090`) only *mounted* ZeroclawService+PluginService, but the reflection I'd just added advertised the **full** `FILE_DESCRIPTOR_SET`. So a reflection-driven probe discovered StateSync/OvsdbMirror/RuntimeMirror/EventChainService/ComponentRegistry and called them → `Unimplemented` (surfaced in the UI as "Service unavailable / ERROR / Disconnected"). Those services were only mounted by `run_grpc_server` (grpc_server.rs), a **different process** = op-dbus on `10.200.0.1:50051` (WG uplink, not browser-reachable). Fix (user chose "share one route-builder"): extracted `grpc_server::build_operation_routes(server) -> tonic::service::Routes` as the SINGLE source of the backplane surface (StateSync, PluginService, EventChainService, OvsdbMirror, RuntimeMirror, ComponentRegistry, Mail, Privacy, Registration, DbusPassthrough, McpService, ChatService, reflection — all behind the Ghostbridge interceptor, all tonic-web). Both `run_grpc_server` (+health/TLS) and the zeroclaw bridge (+ZeroclawService, on both the UDS and TCP/axum listeners) now build from it → reflection can never advertise an unmounted service again. Release-built, `install -m755`, `s6-svc -r` (binary swap). **Verified live:** reflection lists 17 services; previously-Unimplemented RPCs now return `Unauthenticated` (Zero Trust gate — correct), and with the live identity sled headers replayed (`x-ghostbridge-footprint`+`-trace-id` from `/api/identity/sled`) `RuntimeMirror/GetSystemInfo`, `PluginService/ListPlugins`, `ComponentRegistry/Discover` all return real data. **TWO real follow-ups (NOT the surface bug):** (a) the browser probe must inject the identity sled headers or go through xray/xraqy — a raw `:8090` browser hit fails auth by design; (b) `OvsdbMirror/*` now authenticates+runs but returns `Internal: OVSDB 'list_dbs' failed: UnknownObject` because this process's `OvsdbDbusClient::new()` can't reach the ovsdb D-Bus object (op-dbus :50051 has the working wiring) — needs OVSDB D-Bus wiring on the zeroclaw-bridge process if the probe needs live OVSDB. op-dbus :50051 binary NOT yet rebuilt (pure refactor, behavior-identical) — rebuild for source/binary parity. | OD-29 | open |
| 2026-06-24 | Droid / factory-droid | **OD-31 worked: condition 4 (zero hardcoded leaves) landed for the proximate bug + a determinism CHECK; conditions 2 & 3 recon'd and deferred.** (A) Rewrote `zbusctl createsocket` (separate repo) to DISCOVER the `unix_socket` leaf by introspecting the one allowed anchor `org.opdbus.v1.plugins` on the system bus, then mutate via the gated gRPC `PluginService.CallMethod{plugin_id="unix_socket",method="createunixsocket",args=[name, csv-ports]}` over the native gRPC UDS (`/run/opdbus/grpc.sock`, env `ZEROCLAW_UNIX_SOCKET`), injecting `x-ghostbridge-footprint`+`-trace-id` read straight from the 152-byte repr(C) IdentitySled at `/dev/shm/plugin_schema.dat` (vendored a minimal reader; op-identity is too heavy a cross-repo dep). **Correction to this row's premise:** the gated mutation path is gRPC, NOT D-Bus — the projected D-Bus tree (`ProjectedObject`) is READ-ONLY discovery (no CallMethod), so "which bus does the mutation path use" = none; createsocket discovers on D-Bus, mutates on gRPC, through the one gated door. zbusctl builds + clippy + tests green. (B) Consumer audit found ~10 hardcoded-leaf sites; fixed the real bugs: `op-web/projection_client.rs` `PLUGIN_ROOT` was `/opdbus/v1/plugins` (missing `org`) so every projection read silently hit a dead path → now `op_core::config::PLUGIN_BASE_PATH` (new shared const); `op-grpc-bridge/grpc_server.rs` advertised the same malformed `dbus_path` in `PluginInfo` (x2) → fixed; `ovs_attach.rs` (x4) + `keyring.rs` legacy `/org/opdbus/v1/plugin/plugins/...` aliases → canonical (keyring now uses `canonical::plugin_path`). Authored **`scripts/ci-gate-dbus-leaves.sh`** (mirrors `ci-gate-removed-surfaces.sh`): fails on the legacy `/plugin/plugins` alias outside the normalization layer (canonical.rs/default_registry.rs/op-dbus-mirror/freedesktop) and on the missing-`org` form in op-web/op-grpc-bridge — green now; NOT yet wired into a workflow (no precedent; gate scripts run by convention). All touched crates type-check. (C, deferred) condition 3 single-owner: BOTH `ovs-dbus-init` (op-dbus-mirror/lib.rs:157) and `projection_server` (op-projection/dbus_server.rs:131-145) request `org.opdbus.v1.plugins`, both default SYSTEM (`OP_DBUS_MIRROR_BUS`/`OP_DBUS_PROJECTION_BUS` = `:-system`) → start-order race; op-openvswitch-daemon publishes child `…ovsdb` on SESSION while `rovs_proxy` dials SYSTEM → cross-bus invisibility; fix = pin one owner/bus. (D, deferred) condition 2 freshness: projection reads serve a cached snapshot (`ProjectedObject.data_json` RwLock), refreshed only by a fixed 60s poll (projection_server.rs:106-138) + startup scan, no event subscription → up to ~60s stale (netmaker live-probe sampled on the loop; qdrant = last-published config metadata); the mirror's 300s heartbeat resync is a TODO stub (heartbeat.rs:62-68); fix = reactive/event-driven refresh on read. **Leaf-migration backlog (extend the gate once done):** missing-`org` persists as schema DATA values (oscal_source/example/default in antigravity/zeroclaw/wgcf/factory/oscal_subid_registry) — feeds the footprint hash, migrate footprint-aware; consolidate `org.opdbus.ProjectedObjectV1` consumers to a shared const; sibling-service consumers `/org/opdbus/v1/{ovsdb,mail,privacy,registration,state}` have no canonical helper (add one or introspect). Live end-to-end createsocket NOT run here (no cognitive-grpc MCP / backend offline). | OD-31 | open |
| 2026-06-24 | Droid / factory-droid | **OD-31 C+D RESOLVED: StateManager excised, projection is mutation-driven zero-copy shm read-through, single-owner bus enforced.** User insight that drove the design: "after mutation the state is what it is" — there is no desired-vs-actual gap to manage, so StateManager (a second write door that bypassed the MutationEngine/EventChain) was the source of drift and LLM confusion. Excised across 5 crates. **D (freshness, was "event-driven push"):** the deterministic event already existed — `MutationEngine.mutate()` broadcasts `StateChange` via `change_tx` (mutation_engine.rs:230), streamed by `StateSync.Subscribe`. New `op_core::projection_shm` module: `write_projection(plugin_id, bytes)` atomic-writes `/dev/shm/opdbus/projections/<plugin>.json` + bumps manifest generation. The engine writes shm BEFORE broadcasting StateChange (so subscribers always see post-mutation state). `ProjectedObject` rewritten: no `data_json`/`state` caches — `data()` getter reads 1:1 from shm (tmpfs, ~microseconds, zero-copy). `ProjectionDbusServer` uses `HashSet<String>` for mount tracking + `sync_plugin()` (diff: mount new nested objects, unmount removed, emit `updated`). `projection_server` rewritten: subscribes to `StateSync.Subscribe` gRPC stream, initial mount from shm, on each `StateChange{plugin_id}` reads shm → `derive_paths_from_state` → `sync_plugin`. 60s poll loop DELETED. Degraded mode if gRPC subscribe fails (still serves D-Bus reads from shm). No Cozo in the projection read path (Cozo stays as audit/memory/compliance store — query engine, not point-read layer; shm is ~100x faster for D-Bus property reads). **C (single-owner, was "projection sole owner + fix OVS"):** mirror now requests `org.opdbus.v1.mirror` (new `op_core::config::MIRROR_BUS_NAME`), NOT `org.opdbus.v1.plugins` — projection is sole owner. Mirror dropped the duplicate `/org/opdbus/v1/plugins` PluginInterface registration. `op-grpc-bridge` OVSDB/mail/privacy/registration `Proxy::new` calls repointed from `OPDBUS_BUS_NAME` to `MIRROR_BUS_NAME` (x4). OVS daemon moved from session bus to system bus (`Connection::session()` → `Connection::system()`). `rovs_proxy` repointed from `org.opdbus.v1.plugins` to `org.opdbus.v1.plugins.ovsdb` (the OVS daemon's actual name, x2 proxy types). **StateManager excision footprint:** op-state (removed `manager` module + `StateManagerDBus` from `dbus_server.rs`, kept `PluginDbusHost`); op-dbus-mirror (dropped `state_manager` field, `with_state_manager`, `refresh_plugin_snapshot`, `publish_plugin_snapshot`, `event_sources/state_manager.rs`); op-web (`state_manager_client.rs` rewritten to use `RemoteOperationClient::get_state`/`set_state` via gRPC — the single write door — instead of D-Bus StateManager proxy). Workspace compiles clean. Gate green. 1 pre-existing test failure (`plugin_reader::should_project_nested_plugin_objects` — expects numeric index `interfaces/0` but code uses `name` field `interfaces/wg0`; not this session's change). Nothing committed. **NOT yet deployed** (no `s6-svc -r` / `install`); binaries must be rebuilt + deployed for live behavior change. | OD-31 | open |
| 2026-07-03 | Fable 5 | **OD-32: Plugin-blob projection audit — blobs are sealed but NOTHING reads them; direction confirmed: projection should read ONLY from blobs.** Live: op-dbus recovered (s6d restart after installing the fixed run script — blob-seal hook + `OP_DBUS_GRPC_LISTEN=10.200.0.1:50051` now live); 62 `.blob` files in `/dev/shm/opdbus/plugin-blobs`, catalog parses, per-blob sha256 verified on read. **Gap 1 — zero runtime consumers:** the only blob reads are the `opblob` CLI; op-grpc-bridge's in-memory `dynamic_reflection::ActiveReflectionCatalog` never hydrates from SHM at startup (bridge restart loses reflection until plugins re-register), and op-projection still reads plugins/live-schema.json. **Gap 2 — two writers to the blob dir:** `opblob seal-shm` (boot) AND `grpc_server::register_plugin_methods` → `BlobStore::open_default().write()` (runtime) — pick one authority. **Gap 3 — legacy monolith is still the actual source of truth:** `/dev/shm/live-schema.json` read by schema_router, op-identity (`current_schema_catalog_hash` RE-HASHES it, violating read-manifest-never-rehash), op-plugins state plugins (factory/unix_socket/shared_unix_socket), dbus-mirror object.rs, grpc-bridge server.rs, qdrant_shuttle. Meanwhile `.manifest.json`+`opdbus/schemas/` (the OD-27 layout) don't exist on the live host — schema_router's `refresh_if_changed` maps missing-manifest to "no change" and silently never reloads. **Gap 4 — blob dir has NO manifest/commit point** (no catalog_hash/generation), so arrival-triggered change detection can't work against it; `PluginObjectBlob::write_to_dir` uses plain `fs::write` (no tmp+rename — partial blob fails hash check and the plugin silently vanishes from the catalog); `seal_plugins` never sweeps blobs of deleted plugins (zombie advertisement). **Gap 5 — dead/conflicting code:** `op-projection/src/blob.rs` is a stale first-pass PluginObjectBlob (u64 hash, wrong dir `/dev/shm/opdbus/blobs/`) that pr-17 re-adds to lib.rs; 12 files carry live merge-conflict markers (op-projection lib.rs+schema_engine.rs = manifest-writer(HEAD) vs identity-blob-write(pr-17), op-dbus-mirror ×4, op-web ×3, op-openvswitch-daemon/grpc.rs, deploy/s6/opdbus/run, deploy.sh) — workspace does not build. **Conversion plan:** (1) resolve conflicts (blob-first makes both schema_engine sides legacy); (2) add atomic blob-dir manifest (blake3 leaf-fold over sorted blob hashes + generation) written by the single sealer; (3) tmp+rename in write_to_dir + sweep stale plugins; (4) op-projection reads PluginSchema via `BlobRef::plugin_schema()` from the catalog; (5) migrate the 7 live-schema.json consumer sites to blob catalog + manifest hash, then delete the monolith writers (live-schema.json, plugin_schema.dat embed, ShmWriter duplicate in op-projection/shm_writer.rs which re-implements SchemaEngine's manifest at a DIFFERENT path — two implementations of one derivation); (6) hydrate dynamic_reflection from BlobStore at startup. | OD-32 | open |
| 2026-07-03 | Fable 5 | **OD-32 amendment — sealing DROPS schema fields; blobs are not yet the whole plugin.** Live raw-decode of `wireguard.*.blob` confirms the read path is complete for what's sealed (hash-verified canonical JSON with full method arg/return JSON Schemas + caps + subids; manifest = D-Bus/gRPC identities; descriptor set; meta with derived json_schema). BUT `op_blob::lib::state_store_schema_to_blob_schema` round-trips `op_state_store::PluginSchema` through JSON into op-blob's OWN narrower `PluginSchema`, silently dropping `signals`, `guarantees`, `category`, `dependencies`, `display_name`, `example`, `tags`, `mutation_index` (verified: sealed schema keys = name/version/description/dialect/fields/methods/subids/immutable_paths only). Two `PluginSchema` structs = the one-schema violation; under "plugin IS the schema" (new foundational memory) blob-only consumers would see zero signals/guarantees anywhere. Fix (fold into OD-32 step 2): delete op-blob's PluginSchema, blobify the canonical `op_state_store::PluginSchema` directly — schema hash then also covers the dropped fields. | OD-32 | open |
| 2026-07-05 | Fable 5 | **Evaluation: UI proto generation from the blob's DESCRIPTOR_SET — right direction, producer side ~built, three blockers before pointing UI codegen at it.** Every sealed blob already carries a valid typed `FileDescriptorSet` as SECTION_DESCRIPTOR_SET (section 3), synthesized in exactly one place (`op-blob/src/descriptor.rs`) from `PluginSchema.methods` — deterministic (name-sorted files/fields), dependency-free hand-rolled wire encoding, zero-copy readable via `BlobRef::descriptor_set()`/`file_by_filename`. Feasible UI pipeline with zero new deps: export catalog → one concatenated `FileDescriptorSet` → `protoc --descriptor_set_in=catalog.bin --ts_out=…` (protobuf-ts is already in `op-web/ui`; no `.proto` text reconstruction needed); pin `schema_catalog_hash` into the generated bundle and verify at runtime. **Blocker 1 — the typed surface isn't mounted:** `callable_reflection.rs` serves only the build.rs `operation.plugin.v1` Struct-based descriptors (`operation_descriptor.bin`); the typed `operation.method.*` services are explicitly NOT mounted, so typed TS clients would target nonexistent routes. **Blocker 2 — three parallel descriptor generators** (one-schema violation): op-blob descriptor.rs (sealed, unread per OD-32), `op-grpc-bridge/per_plugin_reflection.rs::generate_method_file_descriptor_proto` + `plugin_grpc_gen.rs::freeze_descriptor_set` (prost re-synthesis from PluginSchema), and build.rs proto codegen. Bridge should hydrate reflection from blob section 3 and delete its own synthesis. **Blocker 3 — OD-32 sealing gaps** (dropped schema fields, two writers, no dir manifest) must land first or the UI inherits an incomplete source. **Fidelity caveats in descriptor.rs** (fine for wire, wrong for forms): enums/multi-branch unions/free-form objects degrade to JSON-encoded string; all integers → int64; no `proto3_optional` flag emitted, so `Option<scalar>` presence is lost (unset vs 0 indistinguishable in proto3); no map support. Therefore keep SCHEMA_JSON (section 1) as the UI form-rendering source (json_render path — enums/constraints/descriptions survive there) and use descriptors ONLY for transport typing. Bonus: migration deletes most of hand-maintained `ui/proto/*.proto` — incl. the protos for the 6 services that should be plugins anyway; keep hand protos only for genuinely non-plugin services until converted. | OD-32 | open |
| 2026-07-05 | Fable 5 | **Non-blocking observation: `PluginService.CallMethod` has no real dispatch executor for most plugins.** While implementing the phantom-services-to-plugins spec (`.kiro/specs/phantom-services-to-plugins/`), traced the live call path `pluginService.callMethod` (UI) → gRPC `CallMethod` (grpc_server.rs) → `MutationEngine::mutate()`. For `MethodCall`, only `plugin_id == "net"` (OVSDB) and `plugin_id == "unix_socket"` have real dispatch arms; every other plugin falls to the generic branch, which for MethodCall does nothing but record + echo the input `arguments` back as `result` (no actual execution). The separate D-Bus `SchemaBackedInterface::dispatch_method_call` path has its own executor table, special-casing only `rovs_commands` and `zeroclaw`; all other plugins there also echo. So `schema.methods.insert(...)` across btrfs/blockchain/persona/agent_config/datastore/cozo/embedding_model/ctl_plane_chatbot (and likely most plugins) declares a real, typed, schema-validated contract with **no backend executor** — calling e.g. `create_subvolume` today records an audit event and returns success without doing anything. Confirmed with user this is expected/known and NOT unique to btrfs — it's a platform-wide gap, not something the phantom-services-to-plugins spec needs to fix; proceeding with UI wiring regardless (the schema/routing/auth work is real and correct, only the "does it actually do the thing" part is a stub). Flagging so a future "generic method dispatch" pass (e.g. a `call_method` hook on `StatePlugin`, wired into `mutation_engine`'s generic path) has a starting pointer. | OD-32 | open |
| 2026-07-05 | Fable 5 | **Correction to OD-32 Blocker 3 / Gaps 3-4: sealing no longer drops schema fields, and the blob dir has both a manifest and atomic writes — these were stale claims re-asserted without re-checking.** Re-verified against current code while implementing phantom-services-to-plugins: (1) `op_blob::blobify_plugin_schema_with_identity` (lib.rs:83) now seals the FULL canonical `op_state_store::PluginSchema` JSON ("signals, guarantees, category and all" per its own doc comment) — the narrower local `op_blob::schema::PluginSchema` is explicitly documented as a working view used only for descriptor/manifest synthesis, never what's sealed; `MethodDecl.args`/`returns` in that working view still carry full JSON Schema 2020-12 documents, so descriptor synthesis isn't starved either. (2) `catalog.rs` has `.manifest.json` (`MANIFEST_FILENAME`) with `catalog_hash`/`generation`, written via `write_manifest()` — atomic tmp+rename, committed last, after all blob writes. (3) `blob.rs::write_to_dir` is atomic (`<name>.blob.tmp` → `rename()`), so a partial/crashed write can never appear as a valid blob. Net effect: my 2026-07-05 "three blockers" evaluation overstated the sealing-side risk — only Blocker 1 (typed surface not mounted) and Blocker 2 (three parallel descriptor generators, still true) remain real; Blocker 3 is resolved. The residual "two writers" (offline `opblob seal-shm` vs. live `grpc_server::register_plugin_methods`) is now an authority/ordering question, not a corruption risk, since both go through the same atomic `BlobStore` API. Lesson applied: don't recommend from a memory/prior-evaluation's named function/gap without re-reading the current file first — this one had already been fixed. | OD-32 | fixed |
| 2026-07-06 | Opus 4.8 | **Temporary: UI generation swapped from local Gemma/Ollama to the OCI GenAI Agent endpoint via the `oci` CLI.** Scope was deliberately narrow — only the two UI-*generation* paths, not routing (gemma-brain `derive_routes` untouched) and not the main assistant chat. New `op_gemma::oci::agent_chat_blocking` shells out to `oci generative-ai-agent-runtime agent-endpoint chat` (endpoint via `OCI_AGENT_ENDPOINT_ID`, default the iad `genaiagentendpoint` OCID; `OCI_BIN` overridable) and parses `data.message.content.text`. Callers: `op-gemma/ui_gallery.rs` (batch json-render gallery) and `op-web` `/api/gemma/chat` (the UI-generator chat) — both keep prompt construction, json-render spec extraction, SSE envelope, and session state unchanged; only the model transport changed. TWO caveats: (1) this deviates from op-gemma's documented "never shells out to network tools" property — acceptable as a temporary swap, isolated in `oci.rs` for easy removal, but the durable path is an OCI REST client (request-signing) or an antigravity-chat provider route rather than a CLI subprocess. (2) While compiling I had to fix a pre-existing break in the untracked `op-web/src/handlers/gemma/mod.rs`: the plugin-proto endpoints called `generate_plugin_method_protos` unqualified with no import — now qualified to `op_grpc_bridge::plugin_grpc_gen::generate_plugin_method_protos`. | OD-32 | open |
| 2026-07-09 | Composer | **VPS-side identity injection + hub aliases landed (interim + target arch).** Decoy ingress `10.0.0.2` (op-web UI `:8080`) and mesh tonic-web entry `10.200.0.2:8090` (`op-grpc-bridge-zeroclaw`) now bound via `deploy/scripts/bind-hub-aliases.sh` in both s6 run scripts. Target: Oracle decoy VPS stamps `X-Ghostbridge-Footprint`/`Trace-ID` at WG ingress then forwards to hypervisor `:8090` — see `deploy/oracle-decoy-ingress/xray-inject-forward.json.example` + existing `setup-wg-decoy.sh`. Hypervisor validates; does not re-derive identity from mesh context. | open |
 (a) netmaker-pro's rootfs+backup.yaml were intact on pool `default` but absent from the incus DB; `incus admin recover` was blocked twice: stale duplicate copies of assistant/cozo/mail-3tched/qdrant sat on pool `default` (live volumes are on `btrfs-pool` — the pool column in `incus list -c nsP` is PROFILES, not pool; nearly moved live volumes chasing that) and an orphaned `test-launch` storage_volumes row (deleted id=63 via `incus admin sql global`; container-type volumes can't be deleted via storage API). Stale dups quarantined at `/var/lib/incus/storage-pools/default/quarantine-stale/`; recovery also re-imported test2/test3/test-launch/ztest (junk — candidates for deletion). netmaker-pro RUNNING: from-source netmaker v1.6 + EMQX 5.8.9 + netclient/nmctl/caddy staged; API healthy on :8081. (b) **ExHook version mismatch:** EMQX 5.x fixes exhook proto at `emqx.exhook.v2`, but the only HookProvider impl was `emqx.exhook.v1` — and it exists ONLY on `main` (37f41f12), never carried to this branch (deployed binaries embed v1 = dead code for EMQX 5). Ported to this branch as v2: `proto/emqx_exhook_v2.proto` (copied from the container's emqx_exhook-5.0.21), `src/emqx_hook_provider.rs` (audit tap → MutationEngine, auth/authorize/publish return IGNORE — broker keeps its own decisions), mounted in `build_operation_routes` WITHOUT the ghostbridge interceptor (EMQX carries no identity headers). Container side staged: exhook block in /etc/emqx/emqx.conf → `http://127.0.0.1:9000`; wiring to host must go through the plugin socket surface (user re-correction: NEVER raw incus proxy devices) — shared_unix_socket registration pending. | OD-33 | open |
| 2026-07-09 | Fable 5 | **identity_sled plugin landed (the container IS the sled IS the identity) + get_local_pubkey stub fixed + gemma up no longer swallows failures.** Root chain re-confirmed live: `WireGuardIdentity::get_local_pubkey` only read WG_PUBKEY env → op-cognitive-mcp never wrote /dev/shm/plugin_schema.dat → AnnaScribe rejects ALL gRPC arrivals ("Missing Ghostbridge Identity Sled") → even `zcall shared_unix_socket get_config` is Unauthenticated → xray config chain dead after reboot. Fixed: get_local_pubkey now shells `wg show <iface> public-key` (env stays as explicit override). New `op-plugins/state_plugins/identity_sled.rs`: ContainerIdentitySled records keyed by session_id(=container name, DERIVED from wg pubkey — never supplied), host = "container zero", `blob_ref` (sealed blob that seeds the container) + `SledBtrfsDevice` (device_path/btrfs_uuid/cozo_id/attached — persistence IS a Cozo-registered btrfs device attached via `btrfs device add`, no layers/subvolumes) + snowball SessionEvent ledger methods (get_identity/write_identity/touch_session/record_session_event/get_session_history). Dispatch: `op-grpc-bridge/src/identity_sled_dispatch.rs` + arm in MutationEngine.dispatch_method_call; write_identity refreshes the legacy raw sled when the pubkey is container-zero's own (AnnaScribe compat until it reads the sealed blob). gemma `up` execline no longer wraps shell_up in `foreground` (real exit code propagates to s6). STILL OPEN from the plan: encrypt identity-bearing blobs (chacha20poly1305 + Argon2(PSK,salt=pubkey)+blake3 domain sep), AnnaScribe off raw mmap onto the sealed blob, Cozo persistence wiring for sessions/session_memories relations (relations exist in op-cozo-store), provisioning script unification. | OD-34 | open |
| 2026-07-09 | Cursor | **Drop MindStudio label for routing/UI gen.** It was a Claude-written agent stub waiting on a Grok key, not a real product. Target is Grok/xAI (or keep local `op-gemma` derive). `op-gemma` still not calling any LLM; UI gallery empty. xAI key may exist in shell env but is not in gemma s6 env and gemma has no xAI client path.
| 2026-07-09 | Fable 5 | **identity_sled: Cozo durability + Incus-schema inheritance + native btrfs device add (OD-34 progress).** (1) New op-cozo-store relations `identity_sleds` (full sled row incl. `instance_json`/`btrfs_device_json`) + `session_events` (seq allocated inside one multi_transaction — Cozo `max()` over empty set yields a Null row, handled explicitly); dispatch opens its OWN sled path `/var/lib/op-dbus/identity-cozo` (env `IDENTITY_SLED_COZO_DB_PATH`) because the sled engine is single-process and users-cozo/cognitive-mcp are held by other processes; all Cozo I/O under spawn_blocking; one-time cache hydration at first dispatch (restart-warm, not a watcher). (2) Sled inherits the Incus schema: `ContainerIdentitySled.instance: Option<IncusInstance>` (official shared/api shape) + new `provision_container` method — session_id derived (PSK-aware), instance.name FORCED to session_id, `nic` devices rejected (no container NIC/IP), container created via `IncusPlugin::apply_create` BEFORE the sled row (sled exists ⟺ container exists); dedupe guard on active sleds. (3) `op_network::btrfs::device_add` = native BTRFS_IOC_ADD_DEV ioctl (0x5000940A, compile-time ABI size assert) — no subprocess; exposed as explicit `attach_btrfs_device` mutation (never implicit — the kernel wipes and claims the device); `SledBtrfsDevice` gained `mount_point`. NOTE: two op-plugins test failures (`software::derived_schema_matches_hand_rolled`, `default_registry::all_plugin_subids_are_valid_and_unique` on fail2ban/persona/blockchain/json_render/openflow_obfuscation/wg_opdbus) are PRE-EXISTING on the clean tree — not from this change. Deployed opdbus at /usr/local/bin was Jul 8 — predates the whole identity_sled dispatch; rebuild+redeploy required for any zcall identity_sled to work. | OD-34 | open |
| 2026-07-09 | Fable 5 | **identity_sled live-verified end-to-end + CallMethod routing bug fixed.** (1) ROUTING BUG (same class as the btrfs wiring bug): `PluginService.CallMethod` (the zcall path) routes through `MutationEngine::mutate`, which had NO identity_sled arm — only `dispatch_method_call` (typed generated services) had one, so zcall calls were silently swallowed by the generic echo path (returned args as "result", projection got raw args). Added the identity_sled arm to `mutate` (zcall wraps object args as a one-element array — unwrapped before dispatch); caller now gets the domain result, projection carries the full sled state. Any future per-plugin dispatch must be wired in BOTH `mutate` and `dispatch_method_call`. (2) DEPLOY TRAP: the s6 service `op-dbus` execs `/usr/local/bin/op-dbus` (hyphen) — `/usr/local/bin/opdbus` is a different file nobody runs; the Jul-8 "deploy" updated only the latter. Both names now carry the fresh binary. (3) Bootstrap chicken-and-egg resolved: AnnaScribe gate drops ALL calls without `/dev/shm/plugin_schema.dat`, but write_identity can't be called through the closed gate — seeded container zero via `WG_PUBKEY=<hub-peer-key> op-identity-sled --refresh` (repo version reads WG_PUBKEY; the deployed Jul-8 one didn't). (4) VERIFIED LIVE: write_identity → derived container-zero session_id ae23af55-…, Cozo store materialized at /var/lib/op-dbus/identity-cozo, restart → get_identity hydrates from Cozo, record_session_event/get_session_history round-trip, `shared_unix_socket get_config` no longer Unauthenticated. Cosmetic: zcall renders int fields (seq/mutation_index/created_at) as null — simd→prost Value conversion drops ints somewhere in CallMethodResponse; projection JSON has correct values. | OD-34 | open |
| 2026-07-09 | Cursor | **Routing.UIgen agent installed on zeroclaw (xAI direct).** Provider `xai` + model `grok-4-1-fast-non-reasoning` onboarded in `/root/.zeroclaw` and `~/.zeroclaw`; `agent_config` registered `Routing.UIgen.Agent`; personality under `workspace/agents/routing-uigen/`; ghostbridge `zeroclaw.env` switched off Factory LLM_* to xAI. Daemon restarted healthy on `:18789`. **Blocked on xAI team credits** (403 spending limit) — wiring proven, inference not yet. Local `op-gemma` still writes `/dev/shm/xray-routes.json` until Grok owns the loop. | open |
| 2026-07-09 | Cursor | **routing plugin v1 + retire gemma_brain.** New D-Bus-primary `routing` plugin: identity_injection + subdomains + internal_services; methods get_state/list_*/derive/publish/upsert/remove; SHM `/dev/shm/xray-routes.json` is optional secondary (D-Bus wins). MutationEngine wired in both `mutate` and `dispatch_method_call`. Deleted hollow `gemma_brain` (schema-only, no dispatch). Spec: `docs/routing-plugin-spec.md`. Compliance/Cozo deferred. | open |
| 2026-07-09 | Cursor | **MindStudio Routing.UIgen.Agent exists but is hollow.** App `83dc1fff-…` is published and API-runnable (`POST /developer/v2/apps/run`, launch var `input`), but Main.flow is Start→End only (no Generate Text; `$0` runs, empty result). Developer API has **no create/update workflow endpoints** — editor UI required. Headless Chrome could not reuse login (no MindStudio cookies in Default profile). Key `MIND_STUDIO_API_KEY` works for list/run. Need Jeremy to open edit URL, add Generate Text (prefer MindStudio `grok-4.1-fast`), paste system prompt, Publish. | open |
| 2026-07-09 | Cursor | **Combined Routing.UIgen personality.** Zeroclaw agent SOUL/IDENTITY/USER now one contract: Mode A = routing *proposals* against live D-Bus `routing` plugin (derive/publish/upsert; SHM secondary); Mode B = json-render UI specs. MindStudio app still hollow — paste file `MINDSTUDIO_SYSTEM_PROMPT.md` if/when Generate Text is wired. Default network still no-LLM. | open |
| 2026-07-09 | Cursor | **Cannot edit MindStudio visual agents via API** (no save/update/publish endpoints; docs = run-only). Wired Cursor MCP to local `mindstudio mcp` (`@mindstudio-ai/agent`, 189 tools) instead of gated remote workspace MCP. Agent contract stays in-repo (zeroclaw SOUL / prompts); MindStudio used as cloud action/LLM host when models enabled. `generateText` currently returns `model_disabled` until workspace enables Grok. | open |
| 2026-07-09 | Cursor | **Netmaker Pro tenant ≠ LICENSE_KEY.** Tenant `3tched` / `8e65205a-b167-45f1-8911-03e1af5d98bf` goes in `NETMAKER_TENANT_ID`; server `LICENSE_KEY` is the separate DX8… key. `netclient pull` overwrites `servers.json` API/Broker from public ads (`SERVER_API_CONN_STRING` / `BROKER_ENDPOINT`) — patched `UpdateServerConfig` + `ExecStartPre=netclient-force-local-endpoints` keep loopback for NIC-less in-container netclient. Residual: DNS bind :53 permission denied; default-gateway noise (non-fatal). | open |

##  — rovs OVSDB plugin path hung (blocks sanctioned OVS cleanup)
- `zcall rovs_commands list_ports {"bridge":"ovsbr0"}` times out (exit 124); OVSDB server itself is alive (bridge + ports resolve), so the hang is in the rovs plugin -> OVSDB JSON-RPC bridge, not ovsdb-server.
- Consequence: rogue OVS internal ports (gbr_wg/gbr_warp/gbr_xray/ovsbr0-sock/ovsbr0-mgmt) CANNOT be removed via the sanctioned rovs/D-Bus path while this hangs, and OVS CLI is forbidden. Fixing the rovs list/remove path is prerequisite to topology cleanup.
- Likely related to previously-noted OvsdbDbusClient deprecation / grpc.rs port-parsing path in op-grpc-bridge mutation_engine.

##  — D-Bus WRITE path dead: deprecated op-openvswitch-daemon squats org.opdbus.v1 + :50051
- All `zcall`/CallMethod (reads-via-callmethod + every mutation) hang; introspection + projected-property reads work.
- Cause: s6 service `op-openvswitch-daemon` (deprecated, src deleted, workspace-excluded) still runs, owns primary bus name `org.opdbus.v1` and binds 127.0.0.1:50051. Real bridge on 10.200.0.1:50051 forwards CallMethod to org.opdbus.v1 -> dead daemon -> hang.
- Fix (operator-present): `s6d stop op-openvswitch-daemon` + disable, then restart real opdbus/grpc-bridge so correct handler claims org.opdbus.v1. Only dependent = gbr-wg (deprecated/down). Risk: name may end unowned if real daemon does not reclaim.
- This blocks the entire "build the network via D-Bus" plan until cleared.
| 2026-07-11 | Fable 5 | **Boot network fixed: control-plane-network is now a real Artix s6-rc oneshot; op-openvswitch-daemon disabled at boot.** Reboot post-mortem: (1) control-plane-network had been installed into `/etc/s6-overlay/` which Artix s6-rc never reads — it never ran; (2) its `.rovs` files were comment-only templates and the `rovs` CLI does not exist as a binary (rovs = plugins via zcall), so it would have configured nothing anyway; (3) `zcall bind caddy --ports…` was never real zcall syntax (real: `zcall unix_socket bind --arguments '{name,path,ports[],protocol}' --capability network.write --actor …`); (4) dmesg showed ovsbr0 created 20:09:45 and torn down 20:11:39 (~86s after deprecated op-openvswitch-daemon started) — squatter is prime suspect; after `s6 set disable` + stop, the recreated bridge survives. Rewrote the script (native bring-up = sanctioned bootstrap exception): ensure ovsbr0 + strip rogue ports (ovsbr0-sock/-mgmt/gbr_*) + assert 10.200.0.1/30, 10.200.0.2/30, 10.0.0.2/32 + netmaker 100.90.37.254/24 + DNS fragment + report/heartbeat/ready. Registered via `s6 repository sync` → `s6 set enable` → `s6 set commit -D default && s6 live install` (back-to-back). zcall copied to /usr/local/bin (was only in ~/.local/bin — invisible to root/boot). Socket binds REMOVED from boot per Jeremy: bind is a one-time provisioning mutation, container services (netmaker/prometheus/grafana/caddy/netclient) are self-contained behind Caddy. opdbus WG tunnel REMOVED per Jeremy: host WG = netmaker ONLY (topology .mmd FIX #1 is obsolete; oracle terminates all other WG). NOTE the write path now RESPONDS (permission-denied JSON instead of hang) — squatter fix confirmed; rovs plugin path worth re-testing. Residual: ovsbr0-init still asserts 10.200.0.2/30 + iptables (harmless overlap, could fold into control-plane-network); wg-opdbus s6 stub (`exec pause`) still exists — removal candidate; OPEN DESIGN: WG identity injection point — inject headers on decoy vs forward observed WG pubkey to xray for validation on 3tched (lean: forward pubkey; keeps PSK + sessionid derivation on 3tched, decoy holds no identity material; trust anchor is the decoy's handshake either way, so concentrate secrets where the derivation lives). | open |

## 2026-07-12 — ovsbr0 stale address aliases
`ovsbr0` still carries `10.0.0.2/32` (duplicate of uplink's) and `10.200.0.2/32` from the pre-uplink layout; control-plane-network uses `ip addr replace` and never prunes, so they persist across runs. Consider a bridge-address flush-to-desired step (or one-time `ip addr del`) so the "bridge keeps only 10.200.0.1/30" doctrine holds. Also: /etc/control-plane-network/network.conf comments are stale vs repo copy (harmless, all commented).

## 2026-07-12 — routing/UI-gen inference provider status
xAI API key on host is credit-exhausted (403 permission-denied, team 3b2e5a47). Direction: use MindStudio-hosted agent instead of raw xAI. Blockers: no MindStudio API key on host; agent app id only preserved truncated (83dc1fff-…) and USER.md says its workflow was never functional; MindStudio run API is not OpenAI-compatible so zeroclaw needs a shim/custom provider. zeroclaw fallback provider is currently `xai` (broken until credits or switch).
CORRECTION: integration target is the MindStudio **Model Router** (OpenAI-compatible chat/completions — no shim needed; wire as zeroclaw custom provider base_url), NOT the MindStudio app/agent (83dc1fff-… stays irrelevant). Still blocked only on a MindStudio API key.

## 2026-07-12 — identity provisioning session (root cause + fixes)
- **Self-deadlock fixed**: every identity_sled WRITE hung forever — `publish_plugin_projection_from_cache` D-Bus-introspects `org.opdbus.v1.plugins`, a name op-dbus itself owns, while its own dispatcher is blocked in the CallMethod handler. Fix: 3s `tokio::time::timeout` around the crawl in `crates/op-grpc-bridge/src/mutation_engine.rs` (falls back to crawl-less projection). Rebuilt+installed op-dbus. NOTE: the 2026-07-10 "OVS squatter" diagnosis is obsolete — squatter was already dead; THIS was the real write-path hang.
- Completed the unfinished 2026-07-11 handoff deploy: patched `op-dbus` and unified `op-grpc-bridge` installed + restarted.
- **Identities provisioned & rehydration verified**: jeremy = `fa2f0210-926a-7cf4-4044-16186cb8085a` (pubkey 6ym0fku…), chatbot = `88629c76-33f4-8ae8-f682-b3fd2d432092` (pubkey XpO2oy…). Records survive op-dbus restart — the 2026-07-10 "where did the sled records go" question is closed (Cozo store was fine; hydration was deadlock-blocked).
- Sled ledger also holds `9431b9db…` (now renamed to `netmaker` — record points at a dead name; needs deregister/re-key) and `ae23af55…` (host opdbus-key identity / container zero).
- Container hygiene: deleted unattributable shells `578a6e31…`/`4991a337…` + all test containers (`test2/test3/test-launch/ztest`); renamed `9431b9db…`→`netmaker`; autostart set on netmaker/assistant/cozo/qdrant/mail-3tched (WARNING: mail-3tched has boot-wedge history). New identity containers `fa2f0210…`/`88629c76…` are empty shells, NO autostart yet, payloads pending.
- Incus has NO crash-restart policy — `boot.autostart` is boot-only; in-container systemd units are the crash supervision layer (verify `Restart=` on netmaker unit).
- xray config: per-container subdomain rules added (`<name>.ghostbridge.tech` → to-unix-socket shared-UDS path), test/UUID rules pruned after deletions; new identity containers fa2f0210/88629c76 do NOT have subdomain rules yet. Config still static — gemma/op-xray-daemon generators remain parked per "routing stays static until compliance enforcement" decision.
- **zcall traps**: global flags (`--arguments`, `--capability`, `--actor`, `--timeout`) MUST precede `<plugin> <method>` — trailing flags are silently misparsed as method arg flags ("missing required field" lies). Default 15s timeout aborts long provisions client-side while the server keeps going (half-provisioned container, no record).
- MindStudio/inference (earlier today): op-mindstudio-shim crate + s6 service (127.0.0.1:8093/v1, OpenAI-compat → apps/run), zeroclaw fallback = custom:http://127.0.0.1:8093/v1, hourly zeroclaw-routing-uigen poller deleted, xai provider credit-dead.
- Mesh/hub (earlier today): ovsbr0 stale aliases flushed (bridge = 10.200.0.1/30 only), oracle hub: legacy decoy peer DHLZ removed, host peer allowed-ips += 100.90.37.254/32 + fd3c…fffe/128; hub [Interface] still carries dead 10.189.0.1/24; hub forwards only 10.0.0.0/24↔10.0.0.0/24.
- **DISK-CASUALTY RESIDUE (found 2026-07-12)**: assistant/cozo/qdrant/mail-3tched are hollow — only backup.yaml on btrfs-pool, rootfs subvolumes lost with the sda3 reinstall; they can NEVER start (idmapped storage error). netmaker survived (own pool). Autostart removed from all four (mail-3tched boot-wedge risk). Recovery options: restore from sdc images (disk3.dd/image.dd) or rebuild payloads fresh; their xray routes are currently pointing at services that don't exist.
| 2026-07-12 | Fable 5 | **mutate() dispatch gap: rovs_commands and rtnetlink both unreachable via zcall/gRPC CallMethod — silent echo, not a hang.** Root cause of tonight's "zcall calls fail with bogus 'missing required field' errors": (1) real bug was MY zcall CLI usage — this zcall version (rewritten since the 2026-07-11 handoff) only recognizes `--capability`/`--actor`/`--arguments`/etc. if they precede the plugin+method positional args; placed after, they're silently absorbed as bogus method-argument flags. Once fixed, rovs_commands.list_ports returned `success:true`... but (2) a SEPARATE, real bug: `MutationEngine::mutate()` (mutation_engine.rs ~L619-880, the function zcall/gRPC CallMethod actually calls) has named arms only for unix_socket/identity_sled/routing/ghostbridge/notebooklm; everything else — confirmed for rovs_commands AND rtnetlink — falls to the generic `else` branch, which for MethodCall does nothing and returns the raw input echoed back as "result" with success:true. `rovs_commands` DOES have a correct, working arm (`dispatch_rovs_commands_method`) but only in the OTHER dispatch function, `dispatch_method_call()` (~L926+, used by the raw D-Bus SchemaBackedInterface path, NOT zcall/gRPC) — same "must wire in BOTH mutate and dispatch_method_call" trap as the earlier identity_sled fix (2026-07-09). Fix for rovs_commands = one new arm in mutate() reusing dispatch_rovs_commands_method (small, safe, precedented). `rtnetlink` has NO arm in either function AND most of its zcall-declared methods have no real backing impl in op_network::rtnetlink at all: only add_ipv4_address/set_mac_address exist as declared; set_link_state/set_default_route are mappable to link_up/link_down/add_default_route with glue; add_link/del_link/add_rule have ZERO implementation (WG iface creation, link deletion, policy routing — genuinely unwritten, not just unwired). CAUTION for whoever picks this up: `rtnetlink.add_ipv4_address` currently returns `success:true` while doing NOTHING — confirmed live (address never applied, same netns, ruled out) — do not trust its response without independent verification until fixed. | open |
| 2026-07-12 | Fable 5 | **CORRECTION/completion of the mutate() dispatch finding above: the typed per-method schema surface is already complete — the gap is stub-handler wiring, not schema.** Full `grpcurl -plaintext 10.200.0.1:50051 list` confirms EVERY plugin/method already has its own individually-reflectable typed gRPC service (`operation.method.{plugin}.{method}.{Method}Service`, ~400+ services total) — rtnetlink.add_link/del_link/add_rule and rovs_commands.set_controller/set_interface/set_bridge_property all exist as typed services even though zcall's blob-based method listing never showed them (the sealed blob catalog is stale relative to the live compiled schema — separate minor sync issue, not the real blocker). zcall itself hardcodes `SERVICE="operation.v1.PluginService/CallMethod"` (bin/zcall:4,813) instead of introspecting/using each method's own declared `grpc_service`/`grpc_path` (already present in every method's schema) — real client-side bug, zcall was designed to reach the generated typed methods per Jeremy but never actually targets them. BUT calling a typed per-method service directly (bypassing zcall entirely, e.g. `grpcurl -d '{...}' 10.200.0.1:50051 operation.method.rovs_commands.list_ports.ListPortsService/ListPorts`) returns `Unimplemented` — confirmed live. The generated stub handler for that RPC was never wired to the real, working logic (`dispatch_rovs_commands_method`, reachable today only via the D-Bus `SchemaBackedInterface::call`→`dispatch_method_call` path). So there are two independent fixes needed, not one: (1) zcall client-side — target `grpc_service`/`grpc_path` from the schema instead of the hardcoded generic endpoint; (2) Rust server-side — wire each needed typed-RPC stub handler to its real dispatch logic (same class of fix as the existing identity_sled/routing/ghostbridge/notebooklm arms, just at the typed-service layer instead of/in addition to mutate()). Suspect this stub-handler gap is NOT unique to rtnetlink/rovs_commands — likely affects any plugin whose only wiring lives in the generic mutate()/dispatch_method_call fallback rather than a named arm. Whoever picks this up: don't assume "success:true" OR a clean-looking typed-service response means real work happened — verify independently (this is how the earlier "rovs OVSDB plugin path hung" SIGNALS entry and the rtnetlink silent-no-op bug were both mis-diagnosed initially). | open |
| 2026-07-12 | Fable 5 | **FINAL/correct architecture for the "typed method unreachable" gap — traced end-to-end, confirmed by Jeremy.** Full mechanism, in order: (1) blob sealing already triggers `PerMethodGrpcServices::create_frozen_services` (plugin_grpc_gen.rs) — for every method in a sealed plugin's schema, this generates and freezes a proto FileDescriptorProto (`operation.method.{plugin}.{method}.{Method}Service`) with typed Input/Output messages derived from the method's JSON Schema. This is ALREADY correct and automatic — no reseal step needed once a generic dispatcher exists. (2) `dynamic_reflection.rs`'s `ActiveReflectionCatalog` indexes these frozen descriptors for file/symbol lookup (why `grpcurl describe` shows correct typed shapes) but its own comment says it explicitly: "per-method typed services are indexed for file/symbol lookups but not listed unless routes are mounted. TODO(unify-routes): advertise operation.method.* services when PerMethodGrpcServices routes are mounted." (3) Confirmed live: calling any of these typed services directly via grpcurl (tested rovs_commands.list_ports, rovs_commands.list_bridges, rtnetlink.set_mac_address [where real backing Rust confirmed exists], ovsdb_bridge.list_dbs) returns `Unimplemented` — none are mounted as real tonic services, only indexed for reflection metadata. Root cause: `PerMethodGrpcServices` (plugin_grpc_gen.rs) has NO tonic Service impl at all, just a metadata registry + descriptor freezer. **The correct, minimal fix (Jeremy's direction, confirmed via code-reading, not yet implemented): ONE generic tonic service, not N per-plugin arms.** Pattern already proven and reusable verbatim: `grpc_server.rs::call_generated_plugin_method_typed` (used by the SEPARATE build.rs-generated `operation.plugin.v1.*PluginMethods` services) already does exactly this — decode inbound bytes via `DynamicMessage` against a descriptor pool, JSON-ify, call `mutation_engine.dispatch_method_call(plugin_id, method_name, json_args, ...)`, JSON→DynamicMessage→bytes back. The missing piece is the SAME pattern applied generically over `PerMethodGrpcServices`' frozen descriptor set + registry (keyed by inbound gRPC path → plugin_id/method_name lookup), mounted once into the tonic Router, with `dynamic_reflection.rs`'s TODO(unify-routes) then flipped on. Once mounted, this makes every already-sealed plugin's every method callable through its typed per-method service with ZERO further per-plugin Rust — EXCEPT plugins whose `dispatch_method_call`/`mutate()` arm is itself missing or has no real backing logic (rtnetlink: 3 of 9 methods have no op_network implementation at all; rovs_commands: has real logic via dispatch_method_call already, just needs its mutate() arm for the OTHER, generic CallMethod/zcall path — orthogonal, separate fix, small and safe). zcall itself was NOT the problem — it correctly targets the one real, working generic endpoint (`operation.v1.PluginService/CallMethod`); no zcall changes needed. NOT implemented tonight — scoped as a fresh-session task given two live incidents already happened on this box tonight (network lockout, op-dbus outage) and this touches the core gRPC server. | open |
| 2026-07-12 | Fable 5 | **op-reflection-socket: built, compiled, and LIVE-VERIFIED end-to-end — closes the "typed method unreachable" gap from the two entries above, with zero op-dbus changes/restarts.** New crate `crates/op-reflection-socket` (Jeremy's design, refined through this session: "use shared socket as a model" → mirrors `op-grpc-bridge::shared_socket`'s tonic::transport::Server + UnixListenerStream pattern, not axum::serve). What it does: loads the FULL blob catalog from SHM (`op_blob::BlobStore::open_default()`, 439 methods live), indexes every method by its own already-correct `grpc_path`/`input_message`/`output_message` (from `MethodManifest` — no schema/typing changes needed anywhere, confirmed and reused as-is), and for each incoming call on its own Unix socket: decodes the raw gRPC-framed request via `DynamicMessage` against the blob's own embedded `descriptor_set`, converts to JSON, proxies to the EXISTING `operation.v1.PluginService/CallMethod` on op-dbus (with Ghostbridge identity headers injected from the local sled — same mechanism zcall's `read_headers()` uses — proxied calls are unauthenticated without this), converts the JSON result back to the caller's typed output shape, and returns it as a correctly-framed gRPC response. Two real implementation bugs found and fixed via live iteration (standalone, on a test socket, never touching op-dbus): (1) device-agnostic — N/A here, that was the earlier eth0 bug; (2) HERE: `grpc-status` must be a genuine HTTP/2 TRAILER after the body for a real unary response, not a leading header — `axum::body::Body::from(bytes)` has no trailers support at all and left clients hanging ("server closed the stream without sending trailers"); fixed with a `StreamBody`/`Frame::trailers()` construction. The empty-body error path accidentally works because a headers-only h2 response IS gRPC's defined Trailers-Only case. LIVE-VERIFIED (standalone test socket, real op-dbus proxy, zero live-server risk): `identity_sled.get_identity` round-trips a complete, correctly-typed, real response (actual session_id/pubkey/trace_id). `rovs_commands.list_ports` correctly surfaces — rather than masks — the mutate()-echo bug documented above ("upstream result does not fit ListPortsOutput: invalid type: sequence, expected a map"), proving the service works AND confirming the earlier diagnosis empirically over the wire, not just via code-reading. NOT YET: (a) promoted to an s6 service / given a real socket path (currently manual `cargo run` with `REFLECTION_SOCKET_PATH`/`REFLECTION_UPSTREAM` env vars, tested at /tmp/refl-test.sock); (b) the still-separate, small, low-risk `rovs_commands` mutate() arm fix (op-dbus-side, reuses existing `dispatch_rovs_commands_method`) that would make methods like list_ports return real data through this socket instead of the correctly-detected echo; (c) `dynamic_reflection.rs`'s own `grpc.reflection.v1.ServerReflection` is NOT implemented by this new service (grpcurl needs an explicit `-protoset`/`-proto` against it right now — pulled descriptors from op-dbus's own working reflection for testing). | open |
| 2026-07-12 | Fable 5 | **Persistence sweep + rovs_commands mutate() fix deployed + op-reflection-socket promoted to a real running service — all verified live, op-dbus restarted cleanly twice with zero incident.** (1) Persistence: `deploy/s6/ovsbr0-init/shell_up` was stale in the repo (pre-dated tonight's whole design) — rewrote it to match the final architecture (GW_ADDR=10.200.0.1/30; dropped `set_service_ip`/`set_forwarding` entirely, both obsolete now that eth0/netmaker are enslaved switch ports of ovsbr0 rather than separate routed L3 peers needing iptables FORWARD permission to a now-nonexistent "opdbus" interface), synced to live. `opdbus`/`ovsbr0-static` were already in sync. Re-enabled `eth0-dhcp` (found unexpectedly set to boot-skip, no record of intentionally disabling it tonight — defensive fix before the user's planned reboot; harmless/redundant with control-plane-network's own `ip link set eth0 up`, but no reason to leave an unexplained deviation before a cold-boot test). (2) Added the `rovs_commands` arm to `MutationEngine::mutate()` (mutation_engine.rs) — reuses the existing, already-correct `dispatch_rovs_commands_method` (same pattern as identity_sled/routing/ghostbridge/notebooklm). Built `--release`, installed to BOTH `/usr/local/bin/op-dbus` AND `/usr/local/bin/opdbus` (the exact deploy trap from 2026-07-09's memory), restarted via direct `s6 live restart op-dbus` (NOT an s6-set/graph operation — that's what caused the earlier unplanned outage tonight) — clean restart, new pid, zero connectivity loss, verified via ping+live SSH session immediately after. Confirmed via zcall (correct flag order) that the fix works exactly as intended: `rovs_commands.list_ports` no longer echoes the input — it now reaches the REAL dispatch logic, which fails with a clean, honest, actionable D-Bus error ("org.opdbus.v1 was not provided by any .service files") instead of a silent fake success. Confirmed via `busctl --system list` that NOTHING currently owns `org.opdbus.v1` or `org.opdbus.v1.plugins.ovsdb` — `OvsdbDbusClient` (op-network/rovs_proxy.rs) depends on a D-Bus service that doesn't exist on this system. This is a genuine, PRE-EXISTING, separate issue (matches the much older SIGNALS entry "Likely related to previously-noted OvsdbDbusClient deprecation") — NOT something introduced tonight, and NOT fixed tonight (rewiring OvsdbDbusClient to talk to OVSDB natively, matching CLAUDE.md's stated architecture, instead of via a defunct D-Bus intermediary, is its own separate task). (3) `op-reflection-socket` (see prior entries) promoted from a manual test binary to a real, repo-tracked (`deploy/s6/op-reflection-socket{,-log}`), s6-supervised longrun — installed, enabled, committed, live-installed (op-dbus survived this commit cleanly, unlike the earlier one tonight — same operation, different outcome, worth noting for whoever debugs the NEXT one). Running at `/run/ghostbridge/reflection.sock`. Live-verified against the REAL production socket (not the earlier throwaway test socket): `identity_sled.get_identity` round-trips correctly (session_id/pubkey/trace_id match the live sled, `interface` field now correctly shows `netmaker` reflecting tonight's WG change). Socket permissions (root:root, mode 755) match the established `container.sock` pattern exactly — connect requires privileged/root access, confirmed by testing as both jeremy (denied) and root via sudo (works); this is the existing access convention in this codebase, not a new restriction. rtnetlink's missing methods (add_link/del_link/add_rule — no backing op_network implementation at all) remain explicitly OUT of scope — real new feature work, not attempted tonight. | open |
| 2026-07-12 | Claude Sonnet 5 | **op-reflection-socket: lazy per-plugin reflection mounts, live-verified; and the real root cause of "identity/AccessDenied keeps coming up" finally pinned down.** (1) `op-reflection-socket`'s main socket (`/run/ghostbridge/reflection.sock`) was root:root mode 755 — unprivileged callers got a flat `permission denied` before ever reaching the dispatcher; fixed by `chown 1000:1000` + `chmod 0660` right after bind. (2) Added lazy per-plugin reflection mounting: on a plugin's first call through the main socket, `PluginMounter::ensure_mounted` spawns `/run/reflection/<plugin_id>.sock` (same 1000:1000/0660 ownership) serving `grpc.reflection.v1`+`v1alpha` scoped to just that plugin's descriptor set, plus a `DispatchService` filtered to only that plugin's routes — "arrival triggers action," matches the reactive-not-polled principle instead of pre-mounting all 66 plugins at startup. Live-verified: called `identity_sled.get_identity` on the main socket (needed a protoset since main has no reflection, by design) → `identity_sled.sock` appeared → `grpcurl list`/calls against it work purely via reflection, no protoset, real data returned. (3) While chasing why EVERY zcall/gRPC call was blanket `AccessDenied` regardless of plugin/capability, traced it past the capability-check code (a real but secondary factor) to the actual root cause: **3tched has no live WireGuard interface, by design — WG terminates on oracle (the decoy), not here.** `op-cognitive-mcp`'s startup path (`WireGuardIdentity::with_interface(iface).get_local_pubkey()` → `write_sled_from_wg()`) shells to `wg show <iface> public-key`, which can never succeed on 3tched; the sled at `/dev/shm/plugin_schema.dat` holds a stale value from whatever environment last had a real interface, not a live one. Jeremy has raised moving identity injection to originate on the decoy (where WG actually terminates) multiple times across sessions without it landing anywhere durable — logged as **OD-32** in WISHLIST.md and into `project_oracle_decoy_relay_asbuilt` memory so it stops being re-litigated. Fixing OD-32 is a prerequisite for identity/capability calls to work correctly on this host at all — not a capability-grant bug, not a zcall bug. | open |
| 2026-07-12 | Claude Sonnet 5 | **xray identity injection wired end-to-end; caught a real sled-layout inconsistency in the process (session continued from the entry above).** Implemented the "one hop" identity injection design settled earlier: 3tched's own local xray (`/etc/xray/config.json`, NOT oracle's) already uses `xhttp` transport for every internal `to-*` outbound (`to-grpc-bridge`, `to-cognitive-mcp`, etc. — this was already the established pattern, just missing header injection). Added `xhttpSettings.extra.headers` (`x-ghostbridge-footprint`, `x-wireguard-pubkey`) to the `to-grpc-bridge` outbound, and fixed its `redirect` target — it was pointing at a dead port `18789` (nothing listening; stale from `interceptor.rs`'s doc comment, which is itself out of date), corrected to `127.0.0.1:8090` (verified live via `ss -tlnp`, matches `bind-hub-aliases.sh`'s documented grpc-uplink mesh port). Validated with `xray -test` before every reload, diffed against a full `python3 -m json.tool` before/after to confirm no unrelated config drift, backed up original to `/etc/xray/config.json.bak-preheader-<ts>`. Live-verified via three independent vantage points on the identity sled (`/dev/shm/plugin_schema.dat`): (1) `op-identity-sled --pretty` direct file read, (2) `op-reflection-socket`'s `identity_sled.get_identity` via gRPC reflection, (3) the real network path — `x-ghostbridge-footprint`/`x-wireguard-pubkey` headers hitting `GhostbridgeInterceptor` on port 8090 (`interceptor.rs`, the actual zero-trust gate, previously undiscovered as literally dead-ended at a nonexistent port). Vantages 1 and 2 agree on `wireguard_pubkey` but **disagree on the footprint's string representation** — `op-identity-sled`'s `read_sled_view()` falls back to a legacy 80-byte "compact" layout (`COMPACT_FOOTPRINT_OFFSET` byte-slicing) while `identity_sled_dispatch.rs`'s `host_identity_from_raw_sled()` reads the canonical 152-byte `IdentitySled` struct directly (`hex::encode(sled.hashed_footprint)`) — same file, two different byte-layout interpretations, producing different-looking footprint strings for what should be the same value. This is why vantage 3 (hand-copying vantage 1's footprint into the injected header) kept failing `Temporal Hash Mismatch` even immediately after syncing — I was feeding the interceptor a footprint computed under the WRONG layout assumption. Also caught live: `mutation_index` jumped 2→3 unprompted mid-session with no obvious trigger (`compact_mcp`'s s6 supervisor is up but its actual binary is down — ruled out as the cause, not currently crash-looping; root cause of that specific jump not identified, but it settled and has not recurred). NOT fixed tonight: the layout-ambiguity bug itself (which reader is "correct" — `op-identity-sled`'s compact-layout fallback looks like the stale one, since the canonical struct is what's actually written by `write_sled_from_wg`/`write_sled`, but this needs verifying against `IdentitySled::SIZE` and the actual on-disk byte count before concluding, not asserting). Whoever picks this up: don't trust `op-identity-sled --pretty`'s footprint field against the canonical struct's footprint without first checking which layout branch it actually took (`layout: compact` vs `layout: canonical` in its own output — it was printing `compact` all session, which is suspicious given nothing here writes the legacy 80-byte format anymore). | open |
| 2026-07-13 | Claude Sonnet 5 | **Self-inflicted op-dbus outage + full recovery (same session as the two entries above) — root cause: wrong binary installed to /usr/local/bin/op-dbus.** While deploying the interceptor.rs fix, ran `cargo build -p op-grpc-bridge --release --bin op-grpc-bridge` and installed that output to `/usr/local/bin/op-dbus` + `/usr/local/bin/opdbus`. WRONG: `op-grpc-bridge` (`crates/op-grpc-bridge/src/bin/op-grpc-bridge.rs`) is the standalone "Shuttle" interceptor-only bridge (reads `GRPC_BIND`, defaults to `127.0.0.1:18789`, no `PluginService`/`CallMethod`/50051 anywhere in it) — a completely different binary from the real op-dbus. The REAL op-dbus/opdbus binary is `crates/op-web/src/bin/opdbus.rs` (reads `OP_DBUS_GRPC_LISTEN`, hosts `PluginService` on `10.200.0.1:50051`); op-web depends on op-grpc-bridge as a *library*, so it still picks up interceptor.rs changes. Symptom progression: external caller got `connection refused` on 50051 → live pid had zero `OP_DBUS_GRPC_LISTEN` in its environ (later traced to `s6 live restart` not re-sourcing the run script fresh — use `s6d restart`, not raw `s6 live restart`, confirmed this matters) → after fixing env, still crash-looping, foreground run revealed `Address already in use (os error 98)` on 18789 (the wrong binary trying to bind the Shuttle port while the *actual* separate `op-grpc-bridge` s6 service already owned it) → traced to the wrong-binary root cause via Cargo.toml `[[bin]]` inspection. Fix: rebuilt `cargo build -p op-web --release --bin opdbus`, installed to both `/usr/local/bin/op-dbus` and `/usr/local/bin/opdbus`, `s6d restart op-dbus` — clean restart, stable, correctly bound to `10.200.0.1:50051` within ~3s (66 plugin schemas load synchronously at startup, don't panic at a few seconds of silence). **Lesson for next time deploying op-grpc-bridge crate changes: `op-web`'s `opdbus` binary is what actually needs rebuilding/installing as `/usr/local/bin/op-dbus`, NOT the `op-grpc-bridge` crate's own same-named binary target — the crate name and the deployed binary name collide but are not the same artifact.** Post-recovery, live-verified the actual identity fix works end-to-end: `x-wireguard-pubkey` header alone (no footprint needed) passes `GhostbridgeInterceptor` cleanly via the pubkey-reprojection logic added in interceptor.rs, then a correctly-capability'd `CallMethod` call returns real, live identity data (`success:true`, matching `wireguard_pubkey` across every vantage point checked tonight: raw sled file, op-reflection-socket, and the real xray→8090→interceptor→50051 network path). | open |
| 2026-07-13 | Claude Sonnet 5 | **op-reflection-socket audit: found and fixed a real bug — route table never reloads after startup, so a resealed blob catalog silently breaks reflection until manual restart.** `RouteTable::load()` in `crates/op-reflection-socket/src/main.rs` runs exactly once, in `main()`, at process start; there is no timer/watch to re-index `/dev/shm/opdbus/plugin-blobs/` after that. The process had been running since 00:42; the blob catalog was resealed at 04:37 (several `op-dbus` rebuilds/restarts since, standard practice this whole session) — so its in-memory route table was stale relative to the live catalog. Symptom: **every** typed-route call, including `identity_sled.get_identity` (previously live-verified working just hours earlier), returned `grpc-status 12 "no such method"`, and `/run/reflection/` had never been created since nothing ever matched a route to trigger the lazy per-plugin mount. Fixed with `s6d restart op-reflection-socket` (picks up the current catalog on the fresh `load()`). Verified post-restart: route matching works for both `identity_sled` and `unix_socket`, lazy mounts fire correctly (`/run/reflection/identity_sled.sock`, `/run/reflection/unix_socket.sock`, correct `jeremy:jeremy 660` perms), reflection API answers full per-plugin method lists via `grpcurl list`, and a real `Bind` call through the mounted socket reached the live plugin (registration executed for real, not an echo). **Architecturally worth fixing**: any future reseal (schema change, new plugin, hot-reload of op-dbus) silently breaks this service until someone thinks to restart it specifically — it should reload on a timer or react to catalog changes rather than load-once-at-startup, matching the "reactive, not polled" principle the rest of the system follows (a timer poll would still beat a permanent stale cache). **New secondary finding from the same live call**: `unix_socket.bind`'s real response includes a `sockets` field that the frozen `BindOutput` proto descriptor doesn't declare (`grpcurl` errors: `unrecognized field name 'sockets'`) — a third, distinct unix_socket schema/response mismatch on top of the already-logged `ports:[null]` crawl-timeout bug below; the typed output descriptor is out of sync with what the plugin actually returns. Not fixed, not yet in WISHLIST. | open |
| 2026-07-13 | Claude Sonnet 5 | **unix_socket.Bind dispatch fixed (real registration now works); found the ports:[null] response bug is the identity_sled crawl-timeout class, not yet applied here.** `mutation_engine.rs`'s `unix_socket` MethodCall arm only recognized the legacy method name `"createunixsocket"` — when the schema was later reorganized into per-method names (`Bind`/`Listen`/`Accept`/`Close`), this dispatch was never updated, so `Bind` silently fell through to the generic echo path (`success:true`, nothing actually happened) — Jeremy confirmed this used to work before the schema reorganization, consistent with the pattern. Fixed by adding `|| method == "Bind" || method == "bind"` to reuse the same real logic (`UnixSocketPlugin::create_unix_socket`). Also discovered: `create_unix_socket` always registers against the ONE shared `container.sock` (`SHARED_CONTAINER_SOCKET`), ignoring any caller-supplied path — this is deliberate per its own doc comment ("Pure transport, no OVS: the shared socket is bound once"), not a bug — there is no real per-container-dedicated-socket implementation anywhere in the codebase; `unix_socket.Bind`'s `path` field input is accepted but ignored by design. Verified live: registered `assistant` (intended target of `assistant.ghostbridge.tech`) against the shared socket, confirmed the socket file genuinely exists on disk (`/run/ghostbridge/container.sock`) and the daemon log shows the real bind path executing (`"registered unix socket endpoint on existing shared transport"`), not the echo fallback. Debug-traced (temporarily) to rule out a parsing bug: `parse_socket_args`/`parse_ports_value` correctly extract `ports=[9099]` from the request every time — the dispatch and registration are 100% correct. The `ports: [null]` seen in the CallMethod response is a SEPARATE bug: `"Timed out crawling plugin D-Bus tree for projection plugin_id=unix_socket"` fires on every call (same self-deadlock class already fixed for `identity_sled` via a 3s timeout + crawl-less fallback — see the 2026-07-12 "identity provisioning session" entry above), and unix_socket's crawl-less fallback path loses the `ports` field's type when reconstructing the projection. NOT fixed tonight — cosmetic only (the real registration/state is correct; only the echoed-back response value is wrong). Whoever picks this up: the crawl-timeout fallback path needs the same fix applied to unix_socket's projection reconstruction that identity_sled already got, OR (probably better) route unix_socket's CallMethod response through `caller_result` (a fresh, correctly-typed value) the way `identity_sled`/`routing`/`rovs_commands` already do, instead of relying solely on `authoritative_value` from a crawl that can time out. | open |
| 2026-07-14 | Claude Sonnet 5 | **Qdrant exposed to the host via the standard no-NIC container socket pattern (socat TCP↔UDS bridge, mirroring `netmaker`); found and fixed two unrelated pre-existing s6 gaps blocking activation.** Qdrant (TCP-only, binds `127.0.0.1:6333`/`6334` inside the container, no NIC) needed the same treatment every no-NIC container needs, not a bespoke design: `socat` inside the container (`qdrant-socat-grpc.service`, `UNIX-LISTEN:.../qdrant-grpc.sock ↔ TCP:127.0.0.1:6334`) bridging into a bind-mounted shared dir (`/var/lib/opdbus-runtime/qdrant/` — new convention: one shared `/var/lib/opdbus-runtime/<container>/` parent instead of a new top-level `/var/lib/<name>-runtime` dir per container, and a generic Incus device name `runtime` instead of `<name>-runtime`, per explicit user request to stop the sprawl), then a host-side s6 `longrun` (`deploy/s6/qdrant-grpc-loopback/`) re-bridging that UDS back to host TCP `127.0.0.1:6334`. Only gRPC is bridged, not HTTP (6333): confirmed by reading `op-plugins/.../qdrant.rs:241-250` that `http_endpoint()` is called exactly once, only for a metadata field — nothing in this codebase makes a real REST call to Qdrant; all real traffic (`op-cognitive-mcp`'s `qdrant-client`, and this plugin) is gRPC-only. Ruled out OpenFlow entirely first (`op-network`'s OpenFlow only programs switch-port-to-switch-port flows; qdrant has zero OVS bridge ports since it has no NIC — no ingress/egress point exists for OpenFlow to act on) and ruled out "xray dials the container's UDS directly" (no documented Xray outbound mechanism for a UDS upstream target, confirmed against both the live `/etc/xray/config.json` — a same-named `to-unix-socket` outbound is actually a plain TCP `redirect`, misleadingly named — and official Xray docs, which only support UDS on the **inbound** `listen` side). **Two unrelated, pre-existing s6 activation bugs found and fixed along the way, both blocking `deploy/s6/recompile-and-update.sh` for ANY new service, not just this one:** (1) `/etc/s6/sv/wg-netmaker/` and `/etc/s6/sv/op-openvswitch-daemon/` were running live as **rogue services outside s6-rc's management** (`/run/service/*` symlinked straight to `/etc/s6/sv/*`, bypassing the compiled DB entirely) while missing their `type` file in the live servicedir (present in source, absent live) — `s6-rc-repo-sync` refused to proceed at all until fixed. Both were already-deprecated/should-have-been-stopped per Jeremy (openvswitch-daemon deprecated per CLAUDE.md; wg-netmaker superseded by netmaker's own WireGuard); stopped via `s6-svc -d` + removed their `/run/service` symlinks, which unblocked the sync. (2) Confirmed `recompile-and-update.sh` **never copies `deploy/s6/<service>/` into `/etc/s6/sv/<service>/`** — that install step doesn't exist anywhere in the script or elsewhere in `deploy/`; it only builds Rust binaries and installs its own control script. This means **`netmaker-api-loopback`, the very template this qdrant work was copied from, was never actually installed or running** — confirmed live (`s6d status netmaker-api-loopback` → `s6-supervise not running`), same for `ovsbr0-init`. Manually `install -D`'d the qdrant-grpc-loopback servicedir into `/etc/s6/sv/` to unblock this session; **the missing install step is a real gap in `recompile-and-update.sh` that affects every future new service**, not fixed tonight, whoever owns s6 tooling next should add a copy/sync step for `deploy/s6/*` → `/etc/s6/sv/*` before `s6 repository sync`. Also found new services aren't auto-started even once compiled — nothing wires them into the `default` bundle's actual membership (`default` bundle in the live compiled DB has 85 members, none of them `netmaker-api-loopback`/`ovsbr0-init`/`qdrant-grpc-loopback`) — used `s6-rc -u change qdrant-grpc-loopback` to bring it up live for this session; persistence across reboot still depends on bundle membership, which nobody has wired for any of these three services. **Fully verified working end-to-end**: `grpcurl -plaintext 127.0.0.1:6334 list` and a direct `qdrant.Collections/List` call both return the real 11 production collections through the complete bridge chain. Separately noticed but NOT fixed: `zcall qdrant list_collections` still returns `{"collections": []}` even after restarting `op-dbus` post-bridge — no qdrant-related errors in `op-dbus`'s logs at all, suggesting the plugin's `list_collections` method may not issue a real backend call yet (same stub-gap class as the already-logged `unix_socket.bind` `BindOutput` mismatch above), unrelated to this bridge work and not chased further tonight. Also explicitly out of scope, flagged for later: `cozo` and `mail-3tched` are in the identical structural situation (both only carry the generic `rust-network-mgr` dependency, no service-specific bridge yet) — `cozo` likely needs the same one-bridge treatment as qdrant (pending the same "which protocol is actually used" check), `mail-3tched` genuinely needs multiple distinct bridges (SMTP/IMAP/SMTPS/submission/IMAPS are different protocols, not redundant), and Jeremy is considering a GRE tunnel for mail's multi-protocol case instead of N separate socat bridges — unevaluated, not designed. | open |
| 2026-07-13 | Claude Sonnet 5 | **Neither native D-Bus tool (busctl, zbusctl) can currently reach live plugin dispatch — two distinct, previously-undocumented bugs.** Following up the op-reflection-socket audit, tried the actually-sanctioned native tools instead of grpcurl. (1) `busctl --system call org.opdbus.v1.plugins <path> org.opdbus.v1.plugins.ProjectedObject CallMethod ss ...` reaches a real, live D-Bus object (owned by `projection_serv`/`op-projection`, confirmed via `busctl --system list`) but its `CallMethod` is a stub for every plugin tried (`identity_sled`, `unix_socket`): `{"error":"plugin '<x>' does not implement direct method calls"}`. Consistent with the architecture doc's framing of op-projection as the read-only mirror, not the mutation dispatcher — but worth stating plainly: **the native system-bus surface cannot currently execute a single plugin method call**, only the separate gRPC `PluginService.CallMethod` on `10.200.0.1:50051` (a different process, `op-dbus`/`opdbus`) does real dispatch. Session bus doesn't exist at all (`/run/user/1000/bus` absent), consistent with the already-logged "plugins surface on system, session socket missing" drift. (2) `zbusctl createsocket --system --name X --port Y` — a real, purpose-built subcommand meant to register unix_socket endpoints natively — fails immediately with `ServiceUnknown: org.opdbus.CognitiveMcp`, a hardcoded bus name that nothing on this host provides (should presumably be `org.opdbus.v1.plugins`, or whatever the mutation-capable owner actually is). This is a standalone bug in `zbusctl` itself, separate from the already-logged `zcall` service-naming gap ([[project_zcall_dispatch_gaps]]) — same symptom class (CLI tool hardcodes a bus/service name that drifted from what's actually live) but a different tool, different wrong name. Net effect: right now there is no CLI, native-D-Bus or otherwise, that can register a unix socket without going through raw grpcurl/curl against the gRPC bridge directly — both of the "correct," documented CLI paths (`zcall`, `zbusctl createsocket`) are broken. Not fixed tonight; flagging for whoever owns CLI tooling next. | open |
| 2026-07-14 | Fable 5 | **s6-rc live state destroyed by hand-flipping the compiled-db symlink; fully recovered live without reboot — procedure lesson.** Root cause: at 06:28 a new db (zBriOz, adds wg-netmaker oneshot + ovs/projection/zeroclaw pipelines) was compiled and `/etc/s6/rc/compiled` was re-pointed MANUALLY instead of running `s6-rc-update` → `s6-rc: fatal: unable to read valid state` (state file belongs to the boot db, CIX1IG). Recovery: (1) point symlink back at boot db to make s6-rc readable, (2) `s6-rc-update <new-db>` — which itself fatal'd mid-flight ("unable to manage new service directories: File exists") because pre-existing MANUAL `/run/service` symlinks (made at 05:38-05:49 for the same services the new db adds) collided; it had already stopped the 9 restart-set longruns and left ~55 supervisor husks attached to deleted servicedirs, (3) repopulated scandir symlinks for all 99 longruns, killed husk supervisors, `s6-svscanctl -an` re-attached fresh supervisors to the new servicedirs, `s6-rc -u change` reconciled state (89 active). Side effects: dbus-session (user session bus), chrome-remote-desktop, ovs-vswitchd/ovsdb-server, op-projection, op-grpc-bridge-zeroclaw, aria2 all restarted once; OVS datapath survived (fail-open, no flow loss; egress verified). RULES: never hand-flip `/etc/s6/rc/compiled` — always `s6-rc-update`; remove manual scandir links for services being folded into a db BEFORE updating; oneshots (wg-netmaker) must never have scandir entries. | resolved |
| 2026-07-14 | Fable 5 | **zeroclaw-wayvnc is permanently crash-looping and redundant: weston headless backend lacks the screencopy protocol wayvnc requires, and weston itself already serves VNC on 127.0.0.1:5901.** wayvnc exits 1 every ~1s ("Screencopy protocol not supported by compositor"), s6 restarts it forever — log spam + wasted cycles. Relates to 406c7399 "parameterize weston backend". Either switch the sled compositor to one with screencopy (sway/mutter) or drop the wayvnc service from the db and rely on weston's built-in VNC. | open |
| 2026-07-14 | Fable 5 | **netmaker-api-loopback exit-1 loops post-netclient-decommission; marked down (want down) in live state — removal candidate.** With wg-netmaker converted to a wg-quick oneshot (no netmaker API dependency), the API loopback proxies to nothing. Repo side already cleaned this session: deploy/s6/wg-netmaker-log deleted (orphaned consumer-for would break s6-rc-compile against the oneshot), stale dependencies.d/netmaker-api-loopback dropped, wg-conf/ (live WG private keys!) added to .gitignore. Next db compile should drop netmaker-api-loopback from /etc/s6/sv too. Also note: OpenFlow controller tcp:10.200.0.1:6653 configured on ovsbr0 but nothing listens — bridge runs on fail-open standalone switching. | open |
| 2026-07-14 | Fable 5 | **eth0 enslavement made atomic and routed through busctl at boot; found the reason the D-Bus path could never mutate OVSDB.** `control-plane-network` `enslave_eth0` now: (1) pre-stages the full replumb (link up, public IP, service anchors, default route) as ONE `ip -batch` file BEFORE any datapath change — one iproute2 invocation, no shell interleaving, and the same file replays for rollback; (2) performs the enslavement as one OVSDB transaction via `busctl --system call org.opdbus.v1.plugins /org/opdbus/v1/plugins/rovs_commands ...ProjectedObject CallMethod ss add_port {...,"interface_type":"system"}` (the "internal" default would create a phantom device instead of enslaving the NIC), waiting up to PLUGIN_BUS_WAIT=45s for the bus name (op-projection starts in parallel), with `ovs-vsctl` kept only as the bootstrap fallback so the box always comes up; (3) verifies with plain-ping x3 retries (absorbs async vswitchd datapath attach) and on failure de-enslaves via busctl remove_port (ovs-vsctl best-effort fallback) + replays the batch. ROOT CAUSE found en route: `deploy/s6/op-projection/run` never exported `OVSDB_SOCK`, so rovs_commands inside projection_server dialed the nonexistent default `/run/openvswitch/db.sock` — the D-Bus mutation surface was dead-on-arrival regardless of the (also required, already in working tree) RFC 7047 transact framing fixes. Fixed: run script exports `OVSDB_SOCK=/usr/local/var/run/openvswitch/db.sock`; `deploy/environment.default` now documents OP_DBUS_PROJECTION_BUS=system + OVSDB_SOCK. In-band plugin errors return `{"error":...}` with busctl exit 0 — `rovs_call` checks both. NOT yet verified live (needs op-projection restart with the new env + a boot cycle). | open |
| 2026-07-14 | Fable 5 | **control-plane-network is now busctl-only for OVSDB — every ovs-vsctl removed.** ensure_bridge (bridge-exists poll via list_bridges, datapath_type/fail_mode via set_bridge_property, rogue-port strip via remove_port), enslave/deslave (add_port/remove_port), membership checks (list_ports with quote-delimited exact-name match via new rovs_has helper), netmaker protocols (set_bridge_property with JSON set literal), validation, and the startup report all go through busctl -> org.opdbus.v1.plugins -> rovs_commands. Consequence: op-projection on the system bus is now a HARD boot dependency of the network oneshot (PLUGIN_BUS_WAIT=45s gate; no CLI fallback). rovs_call diagnostics moved to stderr so piped/captured replies can't swallow or false-match error text. SOLE remaining ovs-* usage: ovs-ofctl add-flow/dump-flows for the netmaker L3->encap(ethernet) flows — no D-Bus door exists yet (openflow plugin can't express encap(); its rovs OpenFlow backend org.opdbus.v1.plugins.ovsdb has no deployed s6 service — FIX #5). Also added a boot "validate" stage (D-Bus door smoke test, port enslavement, public IP, default route, gateway ping, netmaker link/WG peers, optional NETMAKER_PING_TARGET mesh probe) that fails the oneshot when the network didn't converge. NOT yet verified live. | open |
| 2026-07-15 | GLM-5.2 | **OpenFlow flow programming moved to D-Bus control plane; ovs-ofctl eliminated; op-of-controller rewritten as D-Bus backend.** (1) OpenFlow types are now schema-generated from the official OpenFlow JSON schema (dancasey/openflow-schema, extended with OF1.5 encap/decap/packet_type) via schemafy. Types vendored in schemas/openflow/openflow15.json, generated into openflow_generated.rs (no schemafy build dependency). The openflow plugin's add_flow/delete_flow/modify_flow methods use OpenFlowFlowModInput (typed OfpFlowMod with OxmField match fields and Action list) — old AddFlowInput/DeleteFlowInput/ModifyFlowInput with HashMap<String,String> removed. Methods sealed in blob (opblob seal-plugins confirmed: 3 methods, schema f4c7278225ac2955). (2) op-of-controller rewritten: no more hand-rolled OF1.3 DELETE ALL + port-pair flows. Now registers D-Bus service org.opdbus.of_controller at /org/opdbus/rovs/openflow, implements org.opdbus.rovs.openflow interface (send_flow, version, status, echo, barrier). Receives FlowEntry JSON from the openflow plugin via D-Bus, translates to OF1.5 FlowMod wire bytes (including raw encap/decap/packet_type encoding since rovs-openflow 0.2 lacks these), sends to OVS via passive TCP. RovsOpenFlowProxy in rovs_proxy.rs repointed from deprecated org.opdbus.v1.plugins.ovsdb to org.opdbus.of_controller. (3) control-plane-network: all ovs-ofctl removed. Netmaker L3->encap(ethernet) flows installed via busctl -> openflow.add_flow with typed OpenFlowFlowModInput JSON (OXM match fields: OFPXMT_OFB_PACKET_TYPE + OFPXMT_OFB_IN_PORT; actions: OFPAT_ENCAP + OFPAT_SET_FIELD + OFPAT_OUTPUT LOCAL). openflow_call helper added. NO ovs-* commands remain in the script. NOT yet verified live — needs op-of-controller rebuild + deploy, op-projection restart with new blob, and a boot cycle. | open |
| 2026-07-15 | grok-4.5 | **op-ghostbridge-mux architecture + sync_governed_flows first cut.** (1) Mux daemon (`crates/op-network/src/bin/op-ghostbridge-mux.rs`, s6 `op-ghostbridge-mux-srv`) owns `/run/ghostbridge/container.sock`, demuxes via SO_PEERCRED→uid_base range against unix_socket schema rows: mode=control → gRPC bridge internal UDS; mode=governed → bind(bind_address:ports[0]) then connect(target) from relay netns so OVS sees identity as nw_src/tp_src. Containers never get a NIC/IP — all I/O stays UDS. (2) `control-plane-network` stage `sync-governed-flows`: busctl → unix_socket.list_registrations, for each governed registration installs table-0 flow via openflow.add_flow matching eth_type=0x0800/ip_proto=6/nw_src/tp_src, actions set_field:reg0=uid_base + NORMAL. Cookie namespace `0x6d7578……` (mux). Reactive first cut = once at boot after netmaker-mesh (no poll loop); live register/unregister reconcile deferred. (3) op-of-controller wire encoder extended: OXM match nw_src/nw_dst/tcp_src/tcp_dst/udp_*/ip_proto; OFPAT_SET_FIELD regN via NXM_1 NXM_NX_REGn; load_register action. NOT yet verified live on 148.113.204.83 — needs op-of-controller rebuild+deploy, op-projection with sealed blob, at least one governed registration, and a boot or manual stage run. Do NOT remove socat loopbacks until this is proven. | open |
| 2026-07-24 | Claude Sonnet 5 | ⚠️ CONCERN **Freshly-added Incus `disk` devices don't actually attach to the `xray` container, even after a full `incus restart` — confirmed on two separate devices.** While wiring xray-core's own commander API (StatsService/RoutingService/LoggerService) onto a Unix socket, tried bind-mounting a host dir to `/run/xray` via `incus config device add xray xrayapi disk path=/run/xray source=/run/xray-api-socket`. After both a service-level restart and a full `incus restart xray`, `mount` inside the container still showed `/run/xray` as the container's own tmpfs — the new device never actually attached. Same symptom on the pre-existing, already-configured `opdbus-sock` device (`path=/run/opdbus`) — also just tmpfs live, despite being in `incus config device show xray` output. By contrast, the older `opdbus-rt` device (`/var/lib/opdbus-runtime`) DOES work correctly. Not something I fixed — worked around by using the confirmed-working `opdbus-rt` mount instead (see `crates/op-xray-daemon/src/commander_client.rs`'s `DEFAULT_API_SOCKET` doc comment for the deviation). Checked `dmesg` for corruption/errors during these add/remove/restart cycles — none found, but the underlying mount-attach mechanism clearly isn't reliable for this container and deserves investigation before depending on it elsewhere. | open |
| 2026-07-24 | Claude Sonnet 5 | ⚠️ CONCERN **`zbusctl` (the CLAUDE.md-mandated tool for exposing container sockets — "expose sockets via `zbusctl createsocket`, not raw incus proxy devices") does not exist anywhere on this host.** CLAUDE.md states it's "installed at `~/.cargo/bin/zbusctl`, not built from this repo" — that path doesn't exist, and `zbusctl` isn't on `$PATH` at all. This is a step beyond the 2026-07-13 entry above (which found `zbusctl createsocket` fails at runtime with a hardcoded wrong bus name) — the binary itself is simply absent now. Net effect: there is currently no way to follow the sanctioned socket-exposure path at all; anyone touching container sockets is forced into the same raw `incus config device add` workaround used in this session (see the entry above), which is exactly what CLAUDE.md says not to do. | open |
| 2026-07-17 | Fable 5 | **New `install/3tched-artix-s6-install.sh` — full Artix s6 installer (base ISO 20260402), crates-derived; deploy/ not consulted.** Generates its own s6-rc definitions: every longrun paired with an s6-log consumer pipeline (`/var/log/op-dbus/<svc>/`, s6log user), network via s6 (ovsdb-server → ovs-vswitchd with `op-ovsbr0-setup --seed-only` → ovsbr0-addr 10.200.0.1/24 + uplink IP migration → op-of-controller/incusd/opdbus), Hyprland/greetd/pipewire desktop, busd SESSION bus at the op-core canonical socket. **Rev 2 (user-corrected architecture, same PR):** NO WireGuard and NO AF_XDP on the host — the netmaker mesh is self-contained in the netmaker container (its bridge iface carries 10.0.0.2 + 10.200.0.2; WG terminates at the decoy server; 10.0.0.2 egress → host xray → gRPC with identity header; host routes 10.0.0.0/24 dev ovsbr0). Bridge creation + eth0 enslavement must be ONE atomic OVSDB transact or uplink capture doesn't start — `op-ovsbr0-setup` gained an `UPLINK` env folding the NIC's Interface/Port rows into the create transact (official RFC 7047 Open_vSwitch schema forms). Incus containers come up under s6, never Incus autostart: `3tched-incus-svcgen <name>` generates an `incus-ct-<name>` longrun + s6-log pipeline (console streamed via `script`-pty'd `incus console`; stop stops the container); `--attach-bridge` = netmaker joins ovsbr0 LAST after `systemctl is-system-running --wait` settles inside. **Assumptions to verify on real hardware:** (1) `ZBUS_GIT_URL` defaults to upstream `dbus2/zbus` — if the `[patch.crates-io]` pin needs jeremy's fork, the build fails and the URL must be overridden (the actual fork URL is not recorded anywhere in the repo); (2) `zbusctl` is only installed when the zbus checkout contains a `zbusctl` crate — otherwise manual; (3) Artix package names for hyprland/incus/xray assumed present in world/galaxy, with automatic `artix-archlinux-support` + `[extra]` fallback; (4) `op-ovsbr0-setup` prefers `/usr/local/var/run/openvswitch` sockets first — the Artix package uses `/run/openvswitch`, covered by its auto-detect + `OVSDB_SOCKET` in network.conf, but `find_vswitchd_ctl()` in that binary hardcodes `/usr/local/var/run/openvswitch` only (unixctl stop path won't find a pacman-installed vswitchd — worth widening in op-network). | OD-33 | open |
| 2026-07-25 | Claude Sonnet 5 | 👁️ OBSERVATION **CLAUDE.md's whole `op-web/ui` build section is stale — the real dashboard frontend now lives in a separate sibling repo, `/home/admin/git/operation-dashboard-ui-07` (branch `zeroclaw`), NOT under `crates/` at all.** `op-web`'s run script (`export OP_WEB_STATIC_DIR="${OP_WEB_STATIC_DIR:-/usr/local/share/op-dbus/dashboard}"`) serves a **plain filesystem static dir**, confirmed no `RustEmbed`/`ui/dist` references anywhere in `op-web`'s `main.rs`/`lib.rs`. `crates/op-web/ui/` (the dir CLAUDE.md points at) is dead — its `.tsx` source is gone, replaced by a stale, uncompiled duplicate of `zeroclaw-gui`'s `.rs` files (see my earlier same-day entry above, which I now believe was itself chasing the wrong tree). The correct rebuild-and-deploy command is `cd /home/admin/git/operation-dashboard-ui-07 && ./deploy-to-op-web.sh` (npm install + vite build + rsync to `$OP_WEB_STATIC_DIR`). That repo had uncommitted changes (`src/App.tsx`, `src/components/shell/AppShell.tsx`) and its `dist/` differed from what's currently live-deployed at the time I found this — whoever's mid-edit there should deploy when ready. CLAUDE.md needs a real correction here, not just a doc-drift note: both of today's `op-web/ui` entries in this log should be read as "wrong location entirely," not just "wrong build command." Not fixed (docs). | open
| 2026-07-25 | Fable 5 | **DECISION (Jeremy): container/xray routing stays STATIC — routes are deterministic and get added at container-creation time (bootstrap materializes `/dev/shm/xray_config.json`); the inference config-generator is reserved for FUTURE dynamic tag routing with OpenFlow on the OVS bridge. Generator model is now SALAD (zeroclaw provider, `op-llm` `SaladProvider` → ai.salad.cloud qwen3.6-35b-a3b), no longer Gemma/Ollama — `op-gemma`'s routing-brain + AGENTS.md "until model-generated dynamic tag routing" language should be read as SALAD when that work starts.** Related live changes today: deployed `/usr/local/libexec/3tched/opdbus-rundirs-up` patched (backup `.bak-preassistantgrpc`) so `assistant.{ghostbridge.tech,3tched.com}` → `to-assistant-ui` (10.200.0.2:8091, the page) and NEW `to-assistant-grpc` outbound (10.200.0.2:8090) with `assistant-grpc.{ghostbridge.tech,3tched.com}` rules (browser gRPC-web); SHM re-materialized + `xray-config-mount-up once` ran, but xray proc PID unchanged afterward — CT restart may have silently no-op'd, verify before trusting. Gotchas: `assistant-grpc.3tched.com` has NO DNS A record (NXDOMAIN → luckysrv wildcard), needs a record or use `assistant-grpc.ghostbridge.tech`; repo installer `3tched-artix-runit-install.sh` xray heredoc is an older stripped variant — deployed bootstrap and repo need reconciling. | open |
| 2026-07-25 | Fable 5 | **Hot reflection + CORS identity preflight shipped in op-grpc-bridge (deployed).** (1) `dynamic_reflection.rs` gained `sync_from_shm()`: every incoming reflection request is the *arrival* that triggers a cheap `catalog_hash` read of the sealed manifest (`op_blob::catalog::ActiveReflectionCatalog::read_catalog_hash`); on change the in-memory catalog reloads all blobs from SHM — blobs sealed by the op-blob binary now appear in reflection with NO bridge restart and no watcher/poll loop. Startup hydration records the hash to avoid a redundant first reload. Unit test `sync_from_shm_picks_up_blobs_sealed_by_external_writer` covers add+remove. (2) CORS: `tonic_web::enable`'s internal CORS answers preflight with only `x-grpc-web,content-type,x-user-agent,grpc-timeout`, so browsers stripped `x-ghostbridge-footprint`/`x-ghostbridge-trace-id` and every UI call died at the identity interceptor. New `grpc_web.rs` (`GhostCorsGrpcWeb`, mirrors tonic-web's enable + the 3 identity headers) replaced all 14 `tonic_web::enable` call sites; preflight verified live at the public edge. (3) xray routing finalized: `assistant.3tched.com` → op-web:8080 (dashboard page, no nginx — xray is the reverse proxy; stale 8091 forwarder bypassed), `assistant-grpc.ghostbridge.tech` → bridge:8090; UI `.env`/`.env.production` repointed + bundle rebuilt/deployed. Gotcha: pre-existing test failure `dynamic_reflection::tests::active_reflection_advertises_only_active_blob_services` (legacy `operation.plugin.v1.ZeroclawPluginMethods` not advertised) predates these changes — still open. Disk: `cargo clean --profile dev` freed 304G (root was 100% full). | open |
| 2026-07-25 | Fable 5 | **Runit logging was broken on 12 services and is now de-s6'd end to end (live host).** Root cause was two s6-era leftovers in `/etc/runit/sv/*/log/run`: 9 services (`fwd-28082/3003/6333/6334/8081/8090/8091/8444`, `uds-assistant`) still ran `exec s6log -b n3 s1000000 T <dir>` — a binary that does not exist post-s6, so their loggers crash-looped and those services logged nowhere; 3 more (`xray-config-mount`, `notebook-sources-sync`, `ovsbr0-addr`) ran svlogd against a log dir that was never created, so svlogd exited `fatal: no functional log directories`. The `s6log` *account* (uid 965) also owned every logger. Fix: created system account `svlog` (uid 963), normalized all 33 `log/run` scripts under `/var/log/op-dbus` to one self-healing runit template (`install -d` + rotation `config` + `chpst -u svlog svlogd -tt`), created the 12 missing dirs, respawned every logger with `sv force-restart <svc>/log`, then `userdel s6log`. Zero `s6` strings remain in the logging path and no file is owned by the old uid. **Method note for future agents:** `usermod -l` refuses to rename an account with live processes, and plain `sv restart <svc>/log` does NOT cycle a logger — svlogd blocks reading the still-open pipe and just sits in `got TERM`; `sv force-restart` is required. It is safe: `runsv` owns the log pipe, so killing the logger cannot SIGPIPE the service feeding it — verified every main service kept its PID (`op-grpc-bridge` 32643, `op-web` 32021, `ovsdb-server` 1101, `ovs-vswitchd` 1073, `incusd` 10924, `op-session-bus` 1078 all unchanged). Rotated history preserved. | | resolved |
| 2026-07-25 | Fable 5 | 👁️ OBSERVATION **No code path manages runit services — the supervisor is unmodelled.** `op-plugins/src/state_plugins/service.rs` only knows `ServiceBackend::S6` (probes `/run/s6-rc`, `/etc/s6/sv`) and systemd; `s6.rs`/`s6_systemctl.rs` are dead s6-era surfaces, and nothing in `crates/` references `runsvdir`, `/etc/runit/sv`, or `svlogd`. So the entire live runit tree — including the logging defects fixed above — exists only as hand-maintained files on the host with no schema, no plugin, and no way to reconcile drift. Jeremy's standing guidance is to work off code, not the installers/docs (`install/3tched-artix-s6-install.sh` is dead and inaccurate, and `deploy/runit/{xray-config-mount,notebook-sources-sync}/log/run` still carry the `s6log` user that was just retired — regenerating from them would reintroduce the break). Real fix is a runit backend in `service.rs` that owns service+logger definitions; until then host and tree cannot agree. | | open |
| 2026-07-25 | Fable 5 | 👁️ OBSERVATION **op-web has NO RustEmbed today — CLAUDE.md's embedded-UI story describes a deferred plan as if it were current.** Jeremy's stated intent (2026-07-25): serve the dashboard as a **static site until dev is finalized, then embed** — so static-dir serving is correct and intentional right now, and the static path is the sanctioned one today. **Script cleanup (Jeremy's call, same day): `update-ui.sh` was DELETED** — it was the crate/embed-step script but its `SOURCE_DIR="operation-dashboard-ui-07"` never resolved at the odbus root; recover it from git history (`git show HEAD:update-ui.sh` in the UI repo) if the embed step is revived. **`deploy-to-op-web.sh` was renamed to `ui-render-update.sh`** — that is now the only deploy entry point (README updated). He also states there are **no real submodules** here despite the `.gitmodules` entries. Verified by grep: `rust_embed`/`RustEmbed`/`embedded_ui` appear nowhere in `crates/op-web/src/`, `Cargo.toml`, or `build.rs`. The router (`crates/op-web/src/routes/mod.rs:310-319`) serves the dashboard purely with `ServeDir::new(static_dir).fallback(ServeFile::new(index.html))`, `static_dir` from an env override defaulting to `/usr/local/share/op-dbus/dashboard`. Consequences: (1) `deploy-to-op-web.sh` (build + rsync `dist/` → that static dir) is the ONLY path that changes what op-web serves — confirmed live, the deployed bundle is byte-identical to the sibling repo's `dist/`; (2) **a release build of op-web does NOT panic without `crates/op-web/ui/dist/index.html`** — that directory is empty right now and nothing reads it, so CLAUDE.md's "op-web release builds panic unless ui/dist/index.html exists" is false and should not be used to plan work; (3) the deploy entry point is now `ui-render-update.sh` in the UI repo (formerly `deploy-to-op-web.sh`); `crates/op-web/ui/dist` only becomes live if the embed step is actually taken. **Three separate UI trees exist and only one is live**: `/home/admin/git/operation-dashboard-ui-07` (remote `repr0bated/operation-dashboard-ui-07`, HEAD `e626f84`) is the one that is built and deployed; `lovable/` is a *different app* on a different remote (`3tched-com/axon-trace-ui-2ba47a08`, HEAD `aadf2d1`, dist from Jul 23, no PluginPage); `crates/op-web/ui` is inert. Do not assume these are clones of each other. | | open |
| 2026-07-25 | Fable 5 | **PluginPage wired up — the schema-driven surface now actually renders (deployed).** It was dead code with three faults: never routed; `use-plugin-schema.ts` imported a `gRPCClient` symbol that exists nowhere in the repo (survived only because nothing imported the hook); and `schemaToSpec` did `.length`/`.map` on `fields`/`methods`, which the sealed blob returns as **maps keyed by name**, so it silently dropped every field and method while `root` still listed the missing children. Fixed all three, added `/plugins` (live sealed catalog via `/api/gemma/plugins`, all 65) and `/plugin/:id` (live schema via `/api/plugin-schema/:id`, both reading `op_blob::catalog::read_plugin_schema_shm`), plus 4 vitest cases over a real `cron` fixture pinning map-shape handling, no-dangling-children, and catalog-only component types. Gotcha for future UI work: **`/api/plugin-schema/` with an empty id returns `200 text/html`** (the ServeDir SPA fallback), which surfaces in the browser as the cryptic `Unexpected token '<', "<!doctype "...` — the hook now guards empty ids and rejects non-JSON content-types with a message that names the cause. | | resolved |
| 2026-07-25 | Fable 5 | **OSCAL subid taxonomy: CI gate turned green (it had been failing), vocabulary split into OSCAL/internal/alias, schema subids now derived in one place.** Found while wiring a schema-subid derivation: `default_registry::tests::all_plugin_subids_are_valid_and_unique` was **already failing on a clean tree** — 5 failures from subids the regex rejected outright (`fail2ban` `sch.security.*`, `persona` ×4 `*.agent.*`) plus 8 from `mut.*`/`src.*` subids declared without the accountability fields they oblige (`blockchain`, `json_render`, `openflow_obfuscation`, `wg_opdbus` — i.e. mutations and authoritative-source reads with **no `actor_id`/`capability_id`/`source_system`/`source_locator` at all**, the exact property those categories exist to enforce, silently unenforced because the gate had been red long enough to stop being informative). Fixes, all in one place each: (1) `common/oscal.rs` now declares three vocabularies — `OSCAL_COMPONENT_TYPES` (mirrors the standard, do not extend), `INTERNAL_CATEGORIES` (`agent`/`data`/`security`/`storage`, extend freely, no OSCAL equivalent so `canonical_category` returns them as-is), and `CATEGORY_ALIASES` (`ui`→`software`, `llm`→`service`, `agents`→`software`: readable domain term survives in the subid, resolves to conformant OSCAL on export). **`subid_regex()` is now BUILT from those lists** instead of being a hand-typed string — that drift is exactly why `storage` was rejected while btrfs emitted it. (2) `derive_schema_subid()` in `plugin_scaffold_helpers.rs` gives every plugin a `__schema__` subid: a struct-level `#[schemars(extend("x-oscal-category" = ...))]` declaration wins (the one fact that cannot be derived), else inferred from the plugin's own field subids, kebab-cased (the grammar forbids underscores), deterministic because the value feeds the schema hash. An unusable declared category is ignored rather than emitted. (3) `ensure_category_metadata_fields` — which already existed and was simply never called — is now applied in the adapter, so declaring a `mut.*`/`src.*`/`evt.*` subid automatically carries its required fields. **Gotcha for future agents:** a first attempt put the derivation in `PluginSchemaBuilder::build()`, which is INERT — `plugin_schema_from_json` calls `.build()` on an empty builder and then does `schema.subids = subids`, a wholesale overwrite, and methods are attached only after the adapter returns, so no `build()` call ever sees a complete schema. 10 unit tests cover the derivation, aliases and determinism. | | resolved |
| 2026-07-25 | Fable 5 | 👁️ OBSERVATION **Automatic category-metadata fields are not yet universal — hand-built plugins bypass them.** `ensure_category_metadata_fields` fires inside `plugin_schema_from_json`, so the guarantee currently reads "every schema *derived from its struct*", not "every schema". Plugins that still hand-build via `PluginSchema::builder(...)` directly — `dnsresolver` is a confirmed example — get no automatic `source_system`/`source_locator`/`actor_id`/`capability_id` no matter what subids they declare. Jeremy (2026-07-25): "that's something we can go back and fix, it's not a placeholder or a stub" — i.e. these are real plugins carrying real contracts, to be **migrated onto the derived path**, not deleted or stubbed. Two ways to close it: migrate the remaining hand-built plugins to the schemars adapter (larger, but ends the dual path), or call the helper at the registry aggregation point where every schema converges regardless of how it was built (smaller, makes the guarantee immediately universal). Related: the hand-rolled `*_schema_golden()` test references were removed the same day — they were migration guards that had begun asserting the derived schema must NOT gain the required accountability fields. | | open |
| 2026-07-25 | Opus 5 | 👁️ OBSERVATION **Five of nine configured MCP servers never loaded in any client; four distinct root causes, three of them are defects in our own servers/config rather than client bugs.** Droid only reads `~/.factory/mcp.json` and `.factory/mcp.json` (user/folder/project) — the repo-root `.mcp.json` is Claude-Code-only and was never consulted, so the `"type": "http"` fields present there were invisible; the user config had url-only entries with no `type`, which default to `stdio` and fail with `TypeError` (`command` undefined). That silently killed `cognitive-mcp`, `op-dbus-compact` and `op-dbus-agents`. (2) `fetch` pointed at `npx -y @modelcontextprotocol/server-fetch`, **which does not exist on npm (404)** — the fetch server is Python (`uvx mcp-server-fetch`, verified working, `mcp-fetch` 1.28.1); the same dead package is/was in `.mcp.json`, `.air/mcp.json`, `.vscode/mcp.json`, `.gemini/settings.json`. (3) `op-cognitive-mcp --stdio` cannot run as a local stdio server as configured: `main.rs` opens CozoDB **before** the `if cli.stdio` branch, so a non-root client hits `RocksDB IO error … LOG: Permission denied` on `/var/lib/op-cognitive-mcp/memory.db` (root-owned, and already held by the running daemon) and exits 1 — it works only with `--no-http --no-grpc` plus `COGNITIVE_MCP_DB_PATH` pointed at a user-writable path, otherwise it also tries to re-bind 3003/50052 alongside the live service. Consider moving DB open after the transport decision, or defaulting the stdio path to an XDG dir. (4) `op-dbus-agents` was pointed at `/mcp/agents`, which is **GET/SSE-only** (POST → 405); its `/mcp/agents/message` handler is stateless and returns the JSON-RPC result in the POST body, i.e. it behaves as Streamable HTTP while `/mcp/agents` emits only an `event: endpoint` frame and never carries responses — so legacy `--type sse` cannot complete a handshake and the correct config is `type: http` at `/mcp/agents/message`. All nine now report `connected` via `droid mcp list`. Separately: **`op-mcp-server` (op-web-compact) violates JSON-RPC** — it emits a frame carrying `result` with no `id` and no `method`, which every Zod-validating client rejects (`invalid_union` on `id`/`method`, `unrecognized_keys: ["result"]`); the server still works because the bad frame is a stray extra, but it logs an error per session and should be fixed at the source. Note also that `cognitive-mcp`(http :3003) / `op-cognitive-mcp`(stdio) and `op-dbus-compact`(http :8080) / `op-web-compact`(stdio) are each the **same server over two transports**, so loading all four doubles the tool surface for nothing — `op-cognitive-mcp` is left `disabled: true` for that reason. | | open |
| 2026-07-29 | Droid | **`zcall` rewritten against the introspected tree — it had been calling a transport that does not exist on this host.** Verified live with `busctl --address=unix:path=/run/opdbus/session-bus.sock tree org.opdbus.v1.plugins`: 65 plugin objects under `/org/opdbus/v1/plugins/<name>`, every one exposing exactly one non-freedesktop interface, `org.opdbus.v1.PluginV1`, with `Call(ss)→s`, `GetProperty(s)→s`, `GetAllProperties()→s`, `SetProperty(ss)`. The old `zcall` targeted `operation.v1.PluginService/CallMethod` over grpcurl at `127.0.0.1:8090` (**grpcurl is not installed**), resolved contracts through `org.opdbus.v1.plugins.ProjectedObject`'s `Methods` property on the **system** bus (no such interface, no such property, wrong bus), fell back to the retired `/dev/shm/live-schema.json`, and defaulted to interface `org.opdbus.v1.Plugin` (live name is `PluginV1`). It also embedded ~400 lines of Python 3 heredocs, against the repo's Rust-first/no-new-Python rule. New `bin/zcall` is pure bash + `busctl` + `jq`: it reads plugin objects from the live tree and method/arg contracts from the sealed blob catalog (section 1 of `<plugin>.<hash>.blob`, parsed with `od`/`tail`/`head`), which is the same schema the bridge's dispatch table is built from — so completion offers exactly the callable surface. Method names are **schema-verbatim and inconsistent across plugins** (`unix_socket` declares `accept/bind/close/listen`, `zeroclaw` declares `GetModelRoutes/SetModel/...`); zcall accepts `-`/`_`-insensitive aliases and resolves to the declared name. Two traps confirmed live: (1) `Call` returns `UnknownMethod` with **only the method name as the message** ("Call failed: list_sockets" looks like a transport failure but means "not declared"); (2) `--capability`/`--actor` are gone because `Call(ss)` has no such parameters — the bridge derives the capability from the MethodDecl and checks it against grants keyed by the **sled footprint** (`op_identity::read_sled`), so `zcall zeroclaw GetModelRoutes` returns `Access denied` for an operator shell with no grants, regardless of flags. Also stale: `crates/op-state/src/dbus_server.rs` declares `#[zbus::interface(name = "org.opdbus.PluginV1")]` (missing `.v1`) and `crates/op-grpc-bridge/tests/zeroclaw-busctl-smoke.sh` calls `DEST=org.opdbus.v1` with lowercase member `call` — neither matches the live tree (`org.opdbus.v1.plugins` / `Call`); the smoke test cannot pass as written. | open |
| 2026-07-29 | Droid | 👁️ OBSERVATION **"org.opdbus.v1.plugins is the only legal tree" is enforced in `zcall` but contradicted by most of the Rust tree.** `bin/zcall` now hard-codes the service/base-path/interface as `readonly` (the `--service`/`--object`/`--interface` flags and `ZCALL_DBUS_*` env overrides are gone, so there is no way to aim it off-tree), and `crates/op-grpc-bridge/tests/zeroclaw-busctl-smoke.sh` was pointed from `org.opdbus.v1` to `org.opdbus.v1.plugins` with member `Call` (it was lowercase `call`). But the bus name `org.opdbus.v1` — **not** a name anyone owns on the live host — is still hard-coded in `op-xray-daemon/src/main.rs:116` (`request_name("org.opdbus.v1")`, plus its README and D-Bus policy XML in `deploy/install-op-xray-daemon.sh`), `op-network/src/ovsdb.rs:18` (`DBUS_BUS_NAME`), and `op-grpc-bridge/src/schema_router.rs` (`dbus_destination` default + six test fixtures). `busctl --address=unix:path=/run/opdbus/session-bus.sock list` shows PID 1324 `op-grpc-bridge` owning `org.opdbus.v1.plugins` and nothing owning `org.opdbus.v1`, so every one of those call sites is either dead or being saved by a fallback. Separately `op-state/src/dbus_server.rs:22` declares the interface as `org.opdbus.PluginV1` (missing `.v1`) against the live `org.opdbus.v1.PluginV1`. If the one-legal-tree rule is to hold, those are the remaining edits — not attempted here because they are outside the zcall change and each needs its own live verification. | open |
| 2026-07-29 | Codex | **Canonical plugin tree follow-up completed.** The stale Xray owner/policy/docs, OVSDB client, SchemaRouter fixtures, and `op-state` interface now use `org.opdbus.v1.plugins`, `/org/opdbus/v1/plugins/<plugin>`, and the already-live `org.opdbus.v1.PluginV1` ABI. More importantly, SchemaRouter now invokes the generic `Call`/`GetProperty`/`SetProperty` compatibility members instead of constructing per-plugin interfaces and direct method members. The OVSDB compatibility client enters through `ovsdb_bridge` on the same tree, with runtime dispatch for `list_dbs`, `get_schema`, and `transact`. Live read-only introspection confirmed the service, object, interface, and method signatures before the edits. | resolved |
| 2026-08-08 | Opus 5 | **Blockchain vector pipeline built and verified live: block → Voyage → chain `vectors` subvolume → incremental `btrfs send` → Qdrant on arrival.** Vectors are now durable chain state, not index-only state: `attach_vector` writes raw LE f32 into the `vectors` subvolume next to the authoritative timing record, and Qdrant is treated as disposable (`op-chain-vectors reindex` rebuilt all 42 points from the chain with **no Voyage key and no re-embedding**). New: `op-blockchain/src/btrfs_delta.rs` (generation / `find-new` / `Received UUID` / `Creation time`), chain vector API + `ChainBlockRef::embedding_text()` (key-sorted, 512-char field cap, so origin and replica derive identical text), aligned snapshot triple + `stream_to_remote_incremental`, `crates/op-cognitive-mcp/src/chain_vectors.rs`, the `op-chain-vectors` CLI, and `deploy/chain-replica-index` (arrival-triggered, no daemon or watcher on the replica). Live results: 42/42 blocks embedded (168K of vectors), 42 points in `blockchain_footprints`, semantic queries discriminate correctly ("chat completion through the zeroclaw router" → `zeroclaw/Chat` 0.480; "enumerate the available models catalog" → `zeroclaw/ListModels` 0.464); full send/receive into a throwaway loopback btrfs produced **byte-identical** vector files, the replica self-identified via `Received UUID`, indexed 42 with no Voyage key, and self-healed a deliberately deleted point on the next pass. **Two traps for whoever extends this.** (1) `BlockEvent.hash` is the *input-patch* hash (`data_hash`), not a per-block identity — all 42 live blocks share `1d487484cdb6e6561ad0928b1a7ae25a` — so keying Qdrant point ids on it collapsed many blocks into one point; ids are now `uuid5(block_num)`, and the per-event hash is `data.metadata.event_hash`. (2) `btrfs subvolume find-new` **cannot** express the delta across a replication series: each incremental receive materialises a new subvolume whose whole tree carries that receive's transid, so a 123-byte stream still makes all 42 files look new. The delta is therefore the chain's own append-only block watermark (`.vector-index-block`), with a batched `get_points` gap check for backfills and lost points; `find-new` remains correct only within one subvolume (and its generation arg is inclusive, so queries start at `watermark + 1`). Remaining wiring gap: nothing calls `project` automatically — `mutation_engine.rs` still hardcodes `vector_features: vec![]` and `BlockEvent::with_vector()` has no callers, so the origin-side embed is operator/CI-driven until the mutation path invokes it on append. | `crates/op-blockchain`, `crates/op-cognitive-mcp/src/chain_vectors.rs`, `deploy/chain-replica-index` | open |
| 2026-08-09 | Composer | **Legacy control-plane escape hatches retired from UI + backend to stop LLM drift.** Removed `crates/op-services` (`opdbus.services.v1.ServiceManager` on :50053). Dashboard UI no longer exports `ovsdbMirror`, `runtimeMirror`, `dbusPassthrough`, or `assistantService` — host reads go through `host_runtime` plugin (`PluginService/CallMethod` or `operation.plugin.v1.*PluginMethods`), OVS through `ovsdb_bridge`/`rovs_commands` via `pluginMethods.callSubid`, metrics polled from `host_runtime.sample_metrics` instead of `RuntimeMirror.StreamMetrics`, OVS row updates via `StateSync` not `OvsdbMirror.Monitor`. **Still on bridge (unmount when callers gone):** `operation.v1.OvsdbMirror`, `RuntimeMirror`, `DbusPassthrough` service defs in proto/bridge. **Next:** regen UI `plugin-methods-index`, wire `service` plugin dispatch for runit mutations. | `operation-dashboard-ui-07`, `crates/op-grpc-bridge`, `crates/op-plugins` | open |
| 2026-08-09 | Composer | **Removed `nix` crate from workspace** (early experiment, duplicated `libc`). All former uses now call `libc` directly: `kill(2)` for xray SIGHUP/SIGTERM in `op-plugins::xray`, `op-grpc-bridge::mutation_engine`, `op-xray-daemon`; `statvfs(3)` for disk metrics in `grpc_server` (same pattern as `host_runtime`). | `op-plugins`, `op-grpc-bridge`, `op-xray-daemon` | resolved |

| 2026-08-10 | Cursor | **Netmaker mesh control-plane break:** remotes could WG-ping but not stay on MQ because `SERVER_BROKER_ENDPOINT`/`broker` was `ws://127.0.0.1:8083/mqtt` (host-only). Public `:443` Reality SNI for `api/broker.ghostbridge.tech` still returns camouflage/binary (not usable). **Fix live:** (1) `fwd-nm-mesh-8081/8083` relay `100.69.0.1`→`127.0.0.1`; (2) advertise `ws://100.69.0.1:8083/mqtt` + `api.ghostbridge.tech:8443`; (3) NetMaker CT `nm-mesh-hairpin.service` puts `100.69.0.1/32` on `lo` so NIC-less server can use the same broker URL. Verified: host+decoy+wrt on EMQX, mesh API 200 / MQTT 101, WG handshakes OK. Pro egress `host-svc0-net` still does not set classic `isegressgateway` (svc0 `10.0.0.0/24` FWD counter 0) — control plane no longer depends on it. | mesh-nm | open |
| 2026-08-10 | Opus 5 | **OpenFlow controller had never actually been attached to `ovsbr0` — two bugs in `datapath_safe.rs` made it impossible.** `ovs-vsctl show` had `controller: []` and `dump-flows` showed a single `priority=0 actions=NORMAL`, so all 4 entries in `openflow-static-flows.json` were inert. Cause: (1) `fallback_flow_spec()` emitted a **cookie mask** (`cookie=0x.../-1`) but `ovs-ofctl add-flow` rejects masks ("flow additions cannot use a cookie mask"), so `ensure_fallback_normal` could never succeed and `attach_controller_safe` always bailed at step 2; (2) `ensure_fallback_normal` discarded the exit code (`let (_c,_o,_e) = run_capture(...)`), hiding the real error behind the confusing "NORMAL fallback not present after add-flow" bail. Third, latent: the `ovs-ofctl` shell-outs pass no `-O`, so they default to OpenFlow10 and **hang** if the bridge's `protocols` omits OpenFlow10 — hit this directly when setting `protocols=OpenFlow13` for the OF1.3 controller. **Live now:** bridge `protocols=[OpenFlow10,OpenFlow13]`, controller `tcp:10.200.0.1:6653` `is_connected=true`, 4 static flows + cookied NORMAL fallback installed, `fail_mode=standalone` preserved. Attach persists in OVSDB and the controller re-installs the fallback itself on each reconnect, so this survives reboot without the fixed binary. **Source fixed but NOT deployed** — the working tree has unrelated uncommitted work (`rovs_proxy.rs` +219, `rtnetlink.rs` +75, `op-ovsbr0-setup.rs`, `Cargo.toml`, untracked `op-rtnetlink-init.rs`), so building would ship someone else's WIP. `cargo check -p op-network` passes. **Also worth noting:** `datapath_safe.rs` drives OVS by shelling out to `ovs-vsctl`/`ovs-ofctl`, which contradicts the CLAUDE.md invariant (no `Command::new`, OVS native over OVSDB JSON-RPC via rovs) — left alone deliberately, too big for a fix pass. | `crates/op-network/src/datapath_safe.rs`, `crates/op-network/src/bin/op-of-controller.rs` | open |
| 2026-08-10 | Opus 5 | **MTU: value was correct, clamping was entirely absent.** Measured path MTU to both netmaker peers is exactly 1420 (1420 OK / 1440 FAIL), matching `netmaker` MTU — so the tunnel MTU itself was never wrong. The real exposure: no MSS clamping anywhere (no `TCPMSS` in iptables mangle, no nft `maxseg`) and `tcp_mtu_probing=0`, while `ovsbr0` and all five ports are 1500 against netmaker 1420 / wgcf-egress 1280. Every forwarded TCP flow into a tunnel depended on PMTUD surviving end-to-end; a peer filtering ICMP frag-needed black-holes silently — the earlier incident's signature. **Fixed live + persistent:** new independent `table inet mangle` with `oifname netmaker|wgcf-egress tcp flags syn counter tcp option maxseg size set rt mtu` (derives from route MTU, so it self-corrects if a tunnel MTU changes; counters added for future diagnosis), appended to `/etc/nftables.conf` (which uses targeted `destroy table`, not a global flush, so netmaker's dynamic tables are untouched); `tcp_mtu_probing=1` + `tcp_base_mss=1024` in `/etc/sysctl.d/90-3tched-pmtu.conf`. Remove with `nft delete table inet mangle`. **Not changed, flagged:** `10.200.0.0/24` is routed on **both** `grpc0` (src 10.200.0.2) and `ovsbr0` (src 10.200.0.1) at equal metric — ambiguous egress/source selection on the gRPC path; both are internal ports on the same bridge so it works today, but it's untidy and worth resolving deliberately. `op-netmk-qos-agent` is **not installed on this host**. | host firewall, `/etc/nftables.conf`, `/etc/sysctl.d/90-3tched-pmtu.conf` | open |
| 2026-08-10 | Opus 5 | **7,239 of 7,432 `op-grpc-bridge` warnings were one bug: 120 zero-byte `block-*.json` in the timing chain.** A contiguous run (`block-000000037995`–`038114`) from a single write-failure event, re-parsed and re-warned on every audit replay (`mutation_engine.rs:329` — `malformed audit record ... EOF while parsing`). Replay is a `read_dir` with a post-hoc sort by `event_id`, so there is no contiguity requirement and empty files carry no event to link — moved aside (not deleted) to `~/net-rollback-20260810T193630Z/empty-timing-blocks/`. 600 more copies sit inside btrfs snapshots `SNP-timing-000094..098`; those aren't read by the replay path and retention prunes them. **Left for you:** the Voyage API key is genuinely absent (only a *comment* mentions it in `/etc/op-dbus/environment`) — set one of `COGNITIVE_MCP_VOYAGE_API_KEY` / `VOYAGE_API_KEY` / `VOYAGE_API_KEY_RUST` / `COGNITIVE_MCP_VOYAGE_KEY_FILE`. The 56 Qdrant "semantic shuttle unavailable" warnings are **already resolved** — they came from a pre-11:28 process holding a stale `10.0.0.2:6334`; the live process has `10.200.0.2:6334` (confirmed open) and has logged zero since. Note qdrant has an `http-svc0` proxy device on `10.0.0.2:6333` but **no `grpc-svc0`**, so `10.0.0.2:6334` is closed — fine while the URL points at `10.200.0.2`. `ovs-vswitchd` ran without `--pidfile`, breaking all `ovs-appctl` introspection; added to the run script but **vswitchd deliberately not restarted** (it is the datapath carrying the public IP) — takes effect next planned restart. | `crates/op-grpc-bridge/src/mutation_engine.rs`, `/etc/runit/sv/ovs-vswitchd/run` | open |
| 2026-08-10 | Cursor | **Laptop-ready mesh path + forbidden ofctl removed.** (1) nft `inet mangle` now clamps MSS on **output** as well as forward for `netmaker`/`wgcf-egress` (host↔peer TCP never hit forward; counters were 0). (2) New `fwd-nm-mesh-8090` relays `100.69.0.1:8090`→`/run/opdbus/grpc.sock` (loopback-only bridge was unreachable from mesh). (3) `datapath_safe.rs` rewritten: OVSDB JSON-RPC + OpenFlow over `<bridge>.mgmt` — no `ovs-ofctl`/`ovs-vsctl` shell-outs; deployed `op-of-controller`. (4) Cleared dead static OF flows (`10.200.0.1:8081` / UDP 443 theater) — table is cookied NORMAL only. Gate: WG peers OK, DF 1392 OK, API 200, mesh 8090 gRPC 200/12, advmss 1368. | `datapath_safe.rs`, `deploy/runit/fwd-nm-mesh-8090`, `/etc/nftables.conf` | resolved |
| 2026-08-11 | Droid | **Present state lives in a SECOND SHM source nobody documented: `/dev/shm/opdbus/state/<plugin>.json`.** The sealed blobs (`/dev/shm/opdbus/plugin-blobs/<p>.<hash16>.blob`, OPBLOB01 binary) carry schema + manifest + protobuf descriptor + compliance metadata — but NOT runtime values. Present state is plain JSON at mode 644 in a sibling directory, live-updating on mutations (mtime 01:05 vs blobs frozen at 22:57). `busctl GetAllProperties` returns this same JSON wrapped in an escaped D-Bus string (608 B for xray); `threetched-fs inspect --plugin xray` returns it as the `value` field alongside the schema (325 KB total). State file is byte-for-byte identical to both. **Implication:** reading present state needs no daemon, no D-Bus, no FUSE — `cat /dev/shm/opdbus/state/xray.json | jq .` suffices. Built `/usr/local/bin/opblob` (host-only, not in repo) for direct blob section reads with seal verification; `opblob <plugin> all` reproduces `threetched-fs inspect --plugin <name>` exactly (value + hash identical, schema equal ignoring null-vs-omitted on 3 keys: `example`, `display_name`, `org`). **Also found:** `threetched-fs` has 0 mentions in the repo — the 6 competing read CLIs (`busctl` 498 hits, `zcall` 280, `zbusctl` 188, `opdbus` 146, `grpcurl` 143, direct blob reads 103) are a discoverability problem, not drift; CLAUDE.md documents `zbusctl` as the operator CLI so that's what agents reach for. **Schema coverage gaps quantified** across 68 plugins / 3,914 fields: `display_name` 86% missing, `category` 64% uncategorized, `read_only_when` 0% usage (mechanism exists, entirely unused), `constraints` 7% populated — filling these in `crates/op-plugins/src/state_plugins/*.rs` `<name>_schema()` functions yields UI labels/grouping/tooltips/conditional-disable/validation with no new UI code since renderers derive from PluginSchema. `threetched-fs tree` panics on SIGPIPE when piped to `head`. No host cron daemon exists (all 6 cron processes are in containers); `/etc/cron.d/3tched-disk-reclaim` is inert since Aug 6 and has a `journalctl: command not found` bug at line 62. | `/usr/local/bin/opblob`, `/dev/shm/opdbus/state/`, `crates/op-blob/src/blob.rs`, `crates/op-plugins/src/state_plugins/`, `/etc/cron.d/3tched-disk-reclaim` | open |
| 2026-08-12 | Opus 5 | **"NetMaker isn't carrying identity" was three stacked faults, none of them crypto.** The assertion code is complete and production-wired (`grpc_server.rs:780` builds `AssertionValidator::new(DecoyTrustStore::load())`), so the failure was entirely below it. (1) **Subnet collision:** `10.0.0.0/24` was claimed twice — host `svc0` (`10.0.0.1/24`+`10.0.0.2/24`, the deliberate decoy`.1`↔host`.2` service bridge, exported as netmaker egress `host-svc0-net` in `direct_nat` mode) AND the decoy's wg0 *human* clients (`10.0.0.3/.4/.5/.10/.11`). The mesh therefore routed human inner IPs to the **host**, not the decoy that authenticated them — visible as netmaker peer `qDDvV3S` (laptop, 100.69.0.3) sitting at **`0 B received`** with endpoint `10.0.0.3`, i.e. the host was dialing its own svc0 bridge. Fixed by renumbering humans to `10.10.0.0/24` (decoy wg0 gained `10.10.0.1/24`; bridge `10.0.0.1/24` untouched; hairpin FORWARD rule repointed) and creating egress `decoy-human-wg` (`70e1091b`) on the decoy node. **`mode` is the NAT mode, not the enable flag** — `direct_nat`⇒`nat=true`, `disabled`⇒`nat=false`; `disabled` is what identity needs, since NAT would destroy the inner-IP preservation `boundaries.md` §3.2 requires. Any other `mode` value silently coerces to `disabled`. (2) **Host netclient was crash-looping** (`MQID/Password does not match hostid/password`), so no mesh config could reach the host at all. Root cause: `/etc/netclient/netclient.json` was regenerated 2026-08-12 02:54 with an orphan host id `ac5e28e3…` + a **brand-new WireGuard keypair that was never applied to the wire** — the live `netmaker` interface still ran the Aug-6 key `eT/L5V…`, which is also what the server's `ghostbridge-host` record (`<redacted-2026-08-14>`) knows. Fixed by merging only the identity fields (`id`,`name`,`hostpass`,`privatekey`,`publickey`,`traffickey*`,`endpointip`) from `netclient.json.bak-dnsfix-20260806061253`; everything else left current. Restart ⇒ `completed pull for server ghostbridge.tech`, and the dead `qDDvV3S` peer **self-healed** to an active handshake via re-detected public endpoint. Verified: host↔`10.10.0.1` 0% loss, decoy routes `10.10.0.3 dev wg0`, no NAT either way. (3) **Still open — the crypto half was never deployed:** `DecoyIssuer` exists only as a library type (`op-identity/src/oracle_assertion.rs:224`), referenced nowhere outside tests; no issuer runs on the decoy, and `/etc/opdbus/decoy-trust.json` does not exist, so `DecoyTrustStore::load()` fail-closes to empty. Consequence to watch: when the assertion is absent the interceptor **silently falls back** to the legacy self-asserted `x-ghostbridge-footprint` path (`interceptor.rs:154-168`) — it degrades quietly instead of failing loudly, which is why this read as "carrying nothing" rather than "rejecting everything". Latent: netmaker host record `wrt-router` still has stale `endpointip=10.0.0.3` (harmless now — live tunnel uses detected public endpoint — but it will mislead). | open |
| 2026-08-12 | Opus 5 | **Decoy issuer + trust store deployed — the assertion path is now live, with one client-side routing decision left.** Built `op-decoy-issuer` (`crates/op-identity/src/bin/op-decoy-issuer.rs`), deployed to the decoy at `/usr/local/bin` under systemd (`op-decoy-issuer.service`, enabled), listening **only** on the wg0 inner address `10.10.0.1:51888` — binding it anywhere reachable would let anyone mint an assertion for any inner IP they can name, so the binary hard-refuses an unspecified bind address. Flow: request arrives on the WG inner address ⇒ kernel has already verified the peer ⇒ source IP maps to peer pubkey ⇒ signed OIA1 returned base64 (drop straight into `x-oracle-identity-assertion-bin`). Arrival-triggered, nothing polled. **Do NOT reuse `op_identity::wireguard::get_pubkey_for_ip` for this** — it matches with `ips.contains(peer_ip)`, so `10.10.0.1` matches `10.10.0.11`; the issuer implements exact matching instead, requires a single-host (`/32`) allowed-ip (a peer holding a wide route like `10.200.0.0/24` must not thereby claim every address inside it), and refuses ambiguous multi-peer matches rather than guessing. Key material: `decoy-wg2-20260812`, signing seed `/etc/opdbus/decoy-signing-key` (0400 root, decoy) / verifying key in host `/etc/opdbus/decoy-trust.json`. Registered `human_principal` for the laptop (`kQigyu…` ⇒ principal `937d6d2b-ecae-ed53-f3a2-d7bd09f544ff`); this needed **`human_principal.read`/`.write` added to the `*` grant** — neither actor had them, so `register_key` had never been callable on this host. Verified: issuer `/healthz` 200 from the host across the new egress, `/assertion` **403 from a non-peer source** (fail-closed), trust store satisfies the parser (1 key, 32 bytes), `register_key`→`resolve_key` round-trips. **Observability gap worth fixing:** `DecoyTrustStore::load()` logs nothing on either success or fail-closed-empty, so a missing/corrupt trust store is invisible — the bridge just silently reverts to the legacy footprint path. **Remaining, and it is a real design point, not a config typo:** the laptop is BOTH a wg0 human (`10.10.0.3`) and a native netmaker node (`100.69.0.3`). Source-IP binding only passes if the source the bridge observes equals the assertion's `netmaker_inner_ip`, so the client must reach `100.69.0.1:8090` **over wg0 via the decoy** (source `10.10.0.3`), not over its own netmaker interface (source `100.69.0.3`). Issuing on the netmaker path instead would make NetMaker the identity authority, which `boundaries.md` §2 forbids. | open |
| 2026-08-12 | Opus 5 | **The workspace did not build, and the cause is a codegen trap any plugin can trip: a method named `connect`.** `cargo check -p op-grpc-bridge` failed with E0592 `duplicate definitions with name `connect`` in *generated* code (`OUT_DIR/operation.plugin.v1.rs:16420`). Not a proto edit — `build.rs` derives `plugin_methods.proto` from PluginSchema, and tonic emits a client fn per rpc **plus** its own inherent `Client::connect(dst)` constructor, so an rpc named `Connect` defines `connect` twice. Trigger was the new, uncommitted `"connect"` method on `mail_server` (`mail_server.rs:674`; `git log -S` finds it in no commit). Fixed generically in `crates/op-grpc-bridge/build.rs` with `avoid_tonic_client_collision`, suffixing `Method` when an rpc's snake_case form hits tonic's reserved set (`connect`, `new`, `with_interceptor`, `with_origin`, `send_compressed`, `accept_compressed`, `max_{de,en}coding_message_size`) ⇒ `rpc ConnectMethod(MailServerConnectMethodRequest)`. Schema and D-Bus names are untouched; routing keys off `schema_name`. **The fix belongs in `build.rs`, not `plugin_grpc_gen.rs`** — the runtime module emits protobuf descriptors, not Rust clients, so it cannot hit E0592, and guarding there would only churn reflection names (tried it, reverted it). Rebuild green in 2m; `cargo test -p op-grpc-bridge --lib oracle_assertion -- --test-threads=1` = 33 passed. Also landed: the silent-degradation fix — `DecoyTrustStore::load()` now warns when the store is empty (every failure mode collapses to empty, which rejects every assertion) and infos the key count otherwise; `ghostbridge_interceptor_with_validator` warns **once per process** on falling back to the self-asserted footprint path, debug per request. **NOT deployed:** `/usr/local/bin/op-grpc-bridge` is still the Aug 10 binary. Rebuilding from this tree would ship a large amount of unrelated uncommitted WIP (mail/netmaker adapters, chat, gallery-gen) — and `build.rs` itself already carries someone's in-progress change threading `GhostbridgeInterceptorWithValidator` into generated per-plugin routes via `add_routes`. Nothing here is committed for the same reason. | open |
| 2026-08-12 | Opus 5 | **sshd boot-order race: mesh SSH silently degrades to loopback-only.** `sshd` skips a `ListenAddress` that does not exist yet and starts anyway. Its run script had no dependency on netclient, so when netclient's identity broke, `100.69.0.1` was absent at sshd start, only `127.0.0.1:22` bound, and every peer got connection-refused with **nothing logged** — the config kept listing both addresses, so it read as correct while the process was loopback-only. This is the actual reason "wg connects, ssh does not". Fixed in `/etc/runit/sv/sshd/run` with a **bounded** 30s wait for the address, then exec regardless — deliberately not a hard dependency, since a netclient failure must degrade to loopback-only sshd rather than no sshd (that is how you lock yourself out). Match is `grep -qx` on exact addresses; `-w`/substring would let `100.69.0.10` satisfy a wait for `100.69.0.1`. Verified: both addresses bound after restart, `SSH-2.0-OpenSSH_10.4` banner from the decoy. Also disabled crash-looping `greetd` and `lm_sensors` (symlinks removed from runsvdir). `op-cognitive-mcp` was asked to be up but **cannot run while the bridge is up**: bridge pid holds `/var/lib/op-cognitive-mcp/memory.db/data/LOCK` rw (confirmed via lsof), RocksDB is single-writer, so it crash-looped on start; its `down` pin was restored. Its `:50052`/`:3003` surfaces are therefore absent — note the installed binary opens network listeners despite the run script's "stdio mode only, no network listeners" comment, so that comment is stale. | open |
| 2026-08-12 | Opus 5 | **Mail was dead for 2 days behind all-green health checks; root cause was a container init script bind-mounting ghostbridge over `/run`.** `mail-3tched`'s openrc unit `mount-run-sockets` ran `mount --bind /opt/run-mounts/ghostbridge /run` with `before maddy dovecot postfix caddy php-fpm83` — so every mail daemon started into a root-owned dir on an unprivileged CT (idmap 1000000) and could not create runtime state. Dovecot lost `/run/dovecot` → `auth: Fatal: stat(/run/dovecot/config)` → `smtpd: fatal: no SASL authentication mechanisms` → postfix throttled smtpd. Port 25 still ACCEPTED (listener up, backend dead), so `sv status` and TCP probes read healthy. openrc itself lost `/run/openrc`, which is why `rc-service` claimed "openrc did not boot". Neutered the unit (kept as no-op so `need mount-run-sockets` resolves) and dropped it from maddy's deps. **This, not proxy devices, is the "why does it keep reverting" answer.** | fixed |
| 2026-08-12 | Opus 5 | **CLAUDE.md's transport section is materially wrong about live state — verified against `incus config device show`.** It claims xray is "the sole container with a real NIC (`eth0`, bridged onto `ovsbr0`)": **no container has a nic device, xray included.** It claims mail-3tched has per-port proxy devices for 25/143/465/587/993/80: mail has **zero**. Actual proxy counts: NetMaker 3, qdrant 3, xray 2, cozo 1, mail 0, assistant 0. Also: xray is NOT the NIC-less exemplar the docs imply — it ingests via proxy devices incl. `proxy8444` on `0.0.0.0:8444`, and its `xray-reality.sock` has zero listeners (stale file). `container.sock` is bound by the bridge but has **zero connected peers** — the shared-socket model was aspirational everywhere until now. | open |
| 2026-08-12 | Opus 5 | **`create_unix_socket` is a no-op stub, so every "register on the shared socket" call in the runit tree is decorative.** `shared_unix_socket.rs::create_unix_socket` logs "registered service on shared socket" and returns `success: true` while binding nothing (the only bind is the single `container.sock` in `ensure_bound`). Run scripts (`mail-port-fabric`, `fwd-mail-php-9000`, `mail-web-socket`, `xsock-netmaker-broker`) call it via `zcall` and then exec a socat/socket-relay that does the actual work. Two unreconciled transport layers coexist: the designed one-socket demux, and a pile of per-service relays + leftover proxy devices. Anyone reading the run scripts will believe registration happened. | open |
| 2026-08-12 | Opus 5 | **New `op-uds-relay` crate — first genuinely NIC-less, proxy-less container transport on this host.** std-only (no deps), built `x86_64-unknown-linux-musl` static-pie so ONE artifact runs on Alpine, Ubuntu 24.04 and Arch (verified by executing it inside Ubuntu 22.04 qdrant); no second glibc build needed despite mail being the lone Alpine CT. Path: host TCP (pub0 + mesh) → `/run/ghostbridge/mail/<svc>.sock` → CT loopback. The missing primitive was ownership: `/run/ghostbridge/mail` must be `chown 1000000:1000000` or the unprivileged CT cannot bind — added to `opdbus-rundirs-up` so it survives reboot (`/run` is tmpfs). Replaces `mail-port-fabric.py`, which forwarded to `127.0.0.1:<port>` on the HOST and so silently depended on the proxy devices that were removed. All 5 mail ports + php-9000 verified end-to-end on 188.68.58.237 and 100.69.0.1 with **0 proxy devices on mail**. 4 unit tests incl. a two-hop end-to-end. | fixed |
| 2026-08-12 | Opus 5 | **Mail outbound is architecturally impossible and needs a design decision.** `mail-3tched` has NO routing table (no default route) — it can never deliver directly. `main.cf` has `relayhost = [127.0.0.1]:2525` pointing at maddy, which (a) has a **corrupt binary** — 28MB beginning with NUL bytes, "Exec format error" — and (b) is configured for ports 25/587/143, **never 2525**, and would collide with postfix/dovecot if it ran. 3 messages deferred since Aug 10 (nothing lost; inbound also safe — 216 msgs on disk). Fixing it needs a reverse hop (CT `--tcp-to-unix 127.0.0.1:2525` → host `--unix-to-tcp` → smarthost) plus a decision on WHICH smarthost: the host reaches port 25 fine but has no MTA, and relaying via 188.68.58.237 has SPF/DKIM/rDNS reputation consequences. Not chosen unilaterally. | open |
| 2026-08-12 | Opus 5 | **The 0777 ghostbridge fix (f9188f96) had silently regressed — three `create_dir_all` calls recreate the parent at 0755 on every restart.** `op-grpc-bridge/src/shared_socket.rs:86`, `shared_unix_socket.rs:146` and `unix_socket.rs:266` all `create_dir_all("/run/ghostbridge")`, which is umask-limited to 0755 root:root. f9188f96 set it 0777 in `opdbus-rundirs-up` precisely because *"a root:root 0755 /run/ghostbridge lets them connect to container.sock but not bind their own sockets there"* — but any bridge restart undoes the script. Live dir was 0755 with mtime matching the 17:03 bridge start. This is why `xray-reality.sock` is a stale file with no listener: xray cannot rebind it. `shared_socket.rs` even chmods the *socket* to 0666 "so containers can connect" while leaving the *parent* unwritable. All three now set 0777 explicitly after create; live dir restored. | fixed |
| 2026-08-12 | Opus 5 | **"NIC-less" historically meant *no NIC*, NOT *no proxy device* — worth stating because the two get conflated.** 715929f4 ("make netmaker NIC-less") describes the sanctioned design as "rust-network-mgr's control socket **plus a host-side Incus proxy device**". `container-socket-contracts.json` lists mail's `tcp_ports` (25/143/443/465/587/993) as *readiness proof of the CT's own listeners*, not a host transport, and the only `unix_socket` it declares is rust-network-mgr's control socket. So proxy devices were the sanctioned inbound channel and "it was working fine" = when mail still had them. The proxy-less goal is newer and stricter; `op-uds-relay` + the 0777 parent is what makes it actually achievable (pattern 2 from f9188f96 / the qdrant-grpc-loopback template). | open |
| 2026-08-12 | Opus 5 | **Device policy now enforced in code + audited on the host.** `NamedDevice::enforce_device_policy` (incus_device.rs) refuses `proxy` and `nic` at `IncusPlugin::sync_devices`, fail-closed on the whole device set with the offending device named and `op-uds-relay` pointed to as the fix; enforced on `desired` only, so out-of-band devices still get removed as stale. 3 unit tests. `deploy/scripts/audit-container-device-policy.sh` reports drift added via raw `incus config device add` (which bypasses the control plane entirely): currently **9 proxy devices across 4 containers** — NetMaker 3, qdrant 3, xray 2, cozo 1; mail and assistant clean. NOTE: removing them is NOT yet safe — `xsock-qdrant` relays through `TCP:10.200.0.2:6333`, i.e. *through* qdrant's own proxy device, so the shared-socket layer is currently built on top of the thing being banned. Migrate each to op-uds-relay before deleting. | open |
| 2026-08-12 | Opus 5 | **mail CT: `mount-run-sockets` is now `chattr +i` immutable, set from the host.** Unprivileged CTs lack CAP_LINUX_IMMUTABLE so `chattr` inside the CT fails (and misleadingly exits 0 — check `lsattr`, not `$?`). Set on the host at `/var/lib/incus/storage-pools/3tched-storage/containers/mail-3tched/rootfs/etc/init.d/mount-run-sockets`. Verified: container root can neither overwrite nor delete it. Kept as a no-op rather than deleted so a stray `need mount-run-sockets` still resolves. To edit it later you must `chattr -i` from the host first. | fixed |
| 2026-08-12 | Opus 5 | **All 9 Incus proxy devices migrated to the ghostbridge UDS path — fleet is now genuinely NIC-less AND proxy-less (audit clean).** Two were dead weight and simply removed (`NetMaker/proxy3128` → 127.0.0.1:13128 and `xray/egress-10809` → 127.0.0.1:10809; neither backend was listening). The other 7 moved to `op-uds-relay`: cozo/op-chat-host, NetMaker/proxy8081+proxy8083, qdrant/grpc-host+http-host+http-svc0, xray/proxy8444. Method: in-CT systemd unit publishes each loopback port as a socket in `/run/ghostbridge/<ct>/`, host-side runit `uds-*` service binds **the identical listen address** the proxy used — so consumers saw no config change, only a brief reconnect. qdrant's two host binds (10.200.0.2:6333 and 10.0.0.2:6333) share one CT socket. Verified after cutover: netmaker API 200, EMQX ws:default carrying 3 live conns (netclient + decoy + forwarder), qdrant 6333 200 / 6334 open, decoy reconnected to xray:8444, mail unaffected. `xsock-qdrant` needed a restart — its long-lived socat predated the cutover. | fixed |
| 2026-08-12 | Opus 5 | **Reboot-safety: `/run` is tmpfs, so the per-container rendezvous dirs must be recreated at boot.** `opdbus-rundirs-up` now loops `mail cozo NetMaker qdrant xray` creating `/run/ghostbridge/<ct>` as `0755 1000000:1000000`. Patched in both the deployed copy and `scripts/opdbus-rundirs-up`. Per-container dirs are deliberately chosen over the shared 0777 parent: they stop one container binding another's socket name, which 0777 permits. **Any new NIC-less container must be added to that list or its relay will fail to bind at boot.** | fixed |
| 2026-08-12 | Opus 5 | **EMQX (not mosquitto) is the netmaker broker — only ws:default is published.** EMQX 5.8.4 in the NetMaker CT listens on 1883 (tcp), 8883 (ssl), 8083 (ws), 18083 (dashboard). Only **8083/ws** was ever forwarded to the host, and `emqx ctl listeners` confirms tcp/ssl/wss all sit at `current_conn: 0` while ws carries the mesh. 1883/8883/18083 are CT-only and correctly unpublished — do not "helpfully" expose them. HTTP 404 on `http://127.0.0.1:8083/` is the healthy response (no root route on the WS listener), not a fault. | open |
| 2026-08-12 | Opus 5 | **Cognitive MCP was down from a JSON shape mismatch, not the RocksDB lock — 500 tools restored.** `op-cognitive-mcp` as a service is DEPRECATED (d83ba902: "Removes deprecated op-cognitive-mcp --stdio entry that raced for CozoDB lock") — the sanctioned path is `op-mcp-server --stdio -m cognitive`, which dispatches via the bridge (`PluginV1.Call` on `/org/opdbus/v1/plugins/cognitive_mcp`) and never opens the store. Keep the `op-cognitive-mcp` runit service pinned `down`; starting it re-creates the single-writer race. Two bugs in `op-mcp/src/cognitive_bridge.rs` were hiding the whole surface: (1) `refresh()` accepted only `{"tools":[…]}` but the bridge returns a **bare array**, so it failed with "list_tools result had no 'tools' array" while the tools sat in the payload — 0 tools; (2) it read camelCase `inputSchema` while the bridge emits snake_case `input_schema`, silently substituting the empty-object fallback so tools would have loaded with NO arguments. Both now accept either shape. Verified: 500 tools, 486 with real schemas, `get_health` executes with `isError:false`. | fixed |
| 2026-08-12 | Opus 5 | **Mail outbound decision: must egress via the decoy — but the decoy has no PTR, which is the blocker.** Verified every public name (3tched.com, www, mail, assistant, ghostbridge.tech, mail.ghostbridge.tech, assistant-grpc.ghostbridge.tech) resolves to **129.153.134.63 (decoy)**; the origin 188.68.58.237 appears in NO DNS record. So outbound from the origin would leak the origin IP into recipients' Received headers and the SMTP connection, defeating the decoy — and would fail SPF, since `3tched.com` publishes `v=spf1 mx ~all` and the MX (mail.3tched.com) IS the decoy. Blockers for direct-from-decoy: (a) **129.153.134.63 has no PTR record** — major providers reject/spam-folder mail from IPs without rDNS; (b) Oracle Cloud blocks port 25 egress by default on most tenancies (unverified — no SSH access, `root@100.69.0.2` gives "Permission denied (publickey)"); (c) the origin's own PTR is stale (`3tched.com`, but the host is now ghostbridge.tech). NOTE `ghostbridge.tech` mail is Cloudflare Email Routing (route1/2/3.mx.cloudflare.net) which is **inbound-only** — it cannot send. | open |
| 2026-08-12 | Opus 5 | **Mail outbound RESOLVED to a single viable design: decoy postfix -> authenticated smarthost on 587. Verified on the decoy (ssh -i ~/.ssh/vps_key ubuntu@100.69.0.2).** Decisive test: `gmail-smtp-in.l.google.com:25` **BLOCKED**, while `smtp.gmail.com:587`, `:465`, `smtp.sendgrid.net:587`, `smtp-relay.brevo.com:587` and `api.cloudflare.com:443` all **CONNECTED**. So Oracle blocks port 25 egress specifically — direct MX delivery from the decoy is impossible no matter what rDNS says, which also makes the missing PTR moot (a smarthost's own IP/PTR/DKIM carries deliverability). Origin sending stays ruled out: it leaks 188.68.58.237 into Received headers and fails `v=spf1 mx ~all`. Postfix is already installed AND active on the decoy, so the relay MTA exists. Final path: mail CT postfix (`relayhost=[127.0.0.1]:2525`) -> CT `op-uds-relay --tcp-to-unix` -> ghostbridge UDS -> host `op-uds-relay --unix-to-tcp 100.69.0.2:2525` -> decoy postfix -> smarthost:587 (SASL) -> internet. **Blocked only on a smarthost credential.** NOTE decoy postfix currently has `inet_interfaces=100.69.0.1` — the HOST's mesh address, not its own (100.69.0.2); verify before wiring. Also: no OCI CLI/SDK/creds anywhere on either box, so rDNS cannot be set from here regardless. | open |
| 2026-08-13 | Opus 5 | **INBOUND mail was dead too — the decoy (where every MX/A record points) was not listening on 25 at all.** `postconf inet_interfaces` on the decoy was `100.69.0.1` — the ORIGIN's mesh address, which the decoy does not own (it is `100.69.0.2`) — so postfix could not bind and silently never listened; external senders got connection-refused. Fixed: `inet_interfaces=all`, `relay_domains=3tched.com`, `transport_maps` → `smtp:[100.69.0.1]:25`. Second fault on top: setting `myhostname=mail.3tched.com` made postfix see its own name in the origin's HELO and bounce `"mail for [100.69.0.1]:25 loops back to myself"` — the decoy needs a DISTINCT hostname, now `mx.3tched.com`. Verified: open-relay probe to gmail.com correctly `554 Relay access denied`, and a real external message (T-Mobile password notice) delivered to the maildir. Oracle inbound 25 IS permitted (RST not timeout). | fixed |
| 2026-08-13 | Opus 5 | **Decoy now relays IMAPS/submission too, so clients need no WireGuard and the origin stays hidden.** The laptop peer (`10.10.0.3`) has no `100.69.0.0/24` in its allowed-ips, so it could never reach the mail server's mesh address — mail clients only worked when run ON the origin. Added systemd socket-activated `systemd-socket-proxyd` units on the decoy for 993/587/465 → `100.69.0.1:<port>`. Had to disable `submission`/`smtps` in the decoy's `master.cf` first: postfix owned those ports but has NO userdb there, so it could only ever reject logins while blocking the relay (port 25 stays postfix — that's the MX). TLS passes through untouched; verified from the internet that 993/465/587 all present `CN=mail.3tched.com` (valid to Oct 16 2026) and the real Dovecot/Postfix banners. **Client settings: mail.3tched.com, IMAP 993 SSL, SMTP 587 STARTTLS or 465 SSL, username `jeremy@3tched.com`.** | fixed |
| 2026-08-13 | Opus 5 | **`/etc/profile.d/opdbus-session-bus.sh` globally hijacked `DBUS_SESSION_BUS_ADDRESS`, breaking every desktop keyring client.** It exported the control-plane bus (`/run/opdbus/session-bus.sock`) for ALL login shells, so libsecret clients (Mailspring, Cursor, browsers, git credential helpers) asked a bus with no `org.freedesktop.secrets` and silently found no keyring — Mailspring's `Account` table ended up empty because it could not decrypt the `credentials` blob in config.json. `zcall` does NOT need the variable (verified working without it); the script now exports `OPDBUS_SESSION_BUS_ADDRESS` instead. The real Plasma bus (`/tmp/dbus-OJOfGlusG2`) serves secrets via **`ksecretd`** (KDE). Also: a stray `gnome-keyring-daemon` was running as **root**, registered nowhere, and gnome-keyring autostarts were racing ksecretd for the name each login — stray killed, autostarts suppressed per-user. Verified store→lookup→clear. KeePassXC 2.7.12 installed as the intended single provider; it cannot claim the name until ksecretd is stopped, which must wait until its DB exists. | fixed |
| 2026-08-13 | Opus 5 | **netclient lost the mesh from an interrupted atomic write — `/etc/netclient/servers.json` was missing while `.tmp` and `.bak` both survived.** Symptom chain: `wg show netmaker` listed the interface with ZERO peers → no route to the decoy (100% loss to 100.69.0.2) → the decoy could still accept mail on its public MX but could not relay it to the origin, so inbound mail silently queued and would have bounced after the retry window. netclient logged `"fail to pull config from server", error:"server config not found"`. `.tmp` and `.bak` were byte-identical (2673) and both valid JSON carrying `3tched.com` + `ghostbridge.tech`; restored `.bak` → `servers.json`, restarted netclient, peers returned (2) and the decoy queue flushed clean. **This is the second netclient identity/config loss in two days** (the first was an orphaned host id on 2026-08-12) — netclient's config writes are not crash-safe and this WILL recur; check `wg show netmaker` peer count before trusting mail delivery. Related fragility in the same log: `open /etc/resolv.conf: operation not permitted` on every startup (netclient cannot write DNS config). | fixed |
| 2026-08-13 | Opus 5 | **The socket-only fleet is now real and the docs are the last thing lagging — but CLAUDE.md's transport section is wrong in the *opposite* direction from the 2026-08-12 entry above.** Verified `incus config device show` across all 8 live containers (NetMaker, assistant, bea37ecb-…, cozo, f036f8d8-…, mail-3tched, qdrant, xray): **every one is socket-only** — a single `ghostbridge-socket` disk mount, zero nic devices, zero proxy devices, xray included. The 9-proxy-device migration recorded above completed and held. CLAUDE.md still says xray is "the sole container with a real NIC (`eth0`, bridged onto `ovsbr0`)" and that mail/cozo/netmaker use hand-configured per-port proxy devices — all four claims are now false. Two containers are named by **sessionid UUID** (bea37ecb-…, f036f8d8-…), which the identity model predicts but no doc mentions. Captured in `3tched-network-install.sh`, which converges any drift back (`--strip-legacy-devices` for nic/proxy removal, opt-in since it cuts live traffic). CLAUDE.md itself not yet edited. | open |
| 2026-08-13 | Opus 5 | **`ovsbr0-addr` is a false green: enabled, `sv status` says run, supervising nothing.** Its run script calls `/usr/local/libexec/3tched/ovsbr0-addr-up`, which **does not exist on the host** — and the script has no `set -e`, so the missing helper is a shrug and it falls straight through to `exec pause`. It has "run" for 261326s doing zero work. The old `3tched-network-bootstrap.sh` header said ovsbr0-addr was to be retired; that never happened, and `ovsbr0-eth0` (uplink L3) + `ovsbr0-svc-addr` (internal L3) own the work now. `3tched-network-install.sh` stops it and unlinks it from runsvdir. **Generalise the lesson: a runit `run` script without `set -e` that ends in `exec pause` will report healthy no matter what failed above it** — grep the tree for that shape before trusting any oneshot's status. | open |
| 2026-08-13 | Opus 5 | **The live network path was substantially absent from the repo — 17 runit services and 4 libexec helpers existed only on the host.** Missing service definitions: `ovsdb-server`, `uplink-dhcp`, `ovsbr0-addr`, `op-of-controller`, `opdbus-rundirs`, `incus-ct-{assistant,cozo,mail-3tched,qdrant}`, `uds-{cozo-chat,netmaker-api,netmaker-broker,qdrant-grpc,qdrant-http,qdrant-http-svc0,xray-reality}`, `xsock-decoy`. Missing helpers: `runit-ready-signal` (referenced by nearly every run script), `uplink-dhcp-up`, `xray-config-mount-up`, `opdbus-rundirs-up`. A rebuild from a clean checkout would have produced a host that cannot boot its own network. Now imported under `deploy/runit/`. **Worth an automated check** — a CI step diffing `/etc/runit/sv` against `deploy/runit/` would have caught this the day it drifted. | open |
| 2026-08-13 | Opus 5 | **`deploy/config/openflow-static-flows.json` was an empty array while the live datapath ran one flow — and nothing was loading the file anyway.** `op-of-controller` reads `/etc/op-dbus/openflow-static-flows.json` (`OF_STATIC_FLOWS_FILE`), not the repo path, and the repo copy had never been populated. Live table 0 holds exactly `priority=0 actions=NORMAL cookie=0x3344434800000001` (the `ensure_fallback_normal` cookie), carrying 46.5M packets / 41GB. Now recorded in the repo in `JsonFlowEntry` shape and installed to the path the controller actually reads. Note the flow table is a **pure catch-all** — there is no per-port isolation on ovsbr0 at all, so the OpenFlow controller currently contributes nothing to segmentation despite being connected. If the identity-header gate is meant to be the only gate that is consistent, but it should be a stated decision rather than an empty table nobody noticed. | open |
| 2026-08-13 | Opus 5 | **OpenFlow ran fallback-only for 3 days and nothing reported it; the real flow set was never in git because it lives outside the repo.** `/etc/op-dbus/openflow-static-flows.json` was truncated to `[]` at 2026-08-10 22:17 (file mtime matches the log to the minute): `06:41 Loaded 4 static flow(s)` → `22:17 Loaded 0 static flow(s)`. From then on ovsbr0 carried only the cookied priority=0 NORMAL fallback (`FALLBACK_COOKIE 0x…01`), so a capture of "current settings" reads as a deliberate catch-all table when it is actually the safety net with the managed set missing — I made exactly that mistake in f46823d0. Recovered the 4 flows from `/.snapshots/root-20260806-062641-postcommit-1afa6c25` (btrfs, taken right after 1afa6c25 "safe OVS OpenFlow datapath + durable flows"; file mtime 08-06 05:12, matching the first `Loaded 4` at 05:13): all `MANAGED_COOKIE 0x…02` priority=100 NORMAL, covering netmaker API `tcp/10.200.0.1:8081` and QUIC `udp/188.68.58.237:443`, each direction. **Why "commit everything" never caught it:** the controller reads a path under `/etc`, which no `git add -A` can see; `deploy/config/openflow-static-flows.json` is a *different* file that already held `[]`. **Generalise:** any config the control plane reads from `/etc/op-dbus/` is outside version control by construction — `network.conf`, the flow set, and anything else there can drift or vanish with no git signal at all. The new installer now owns both copies, but a periodic `/etc/op-dbus` → repo diff would close the class. | fixed |
| 2026-08-13 | Opus 5 | **`op-of-controller` loads static flows exactly once, in `main()` — so writing the file while it runs is a silent no-op.** `controller.rs` reinstalls the static set on every *OVS* reconnect, which reads convincingly like "the file is re-read", but the `Vec<String>` it reinstalls from was built at process start. Any operator who fixes the JSON and waits will see the datapath never change and no error anywhere. The installer now stages the file *before* the start loop and restarts the controller if it was already running. Cheap real fix if anyone touches that binary: `std::fs::read_to_string` inside the reconnect handler instead of in `main`, so the reinstall genuinely reloads. | open |
| 2026-08-13 | Opus 5 | **Laptop SSH to the mesh died at KEX for a 44-byte MSS miscalculation; the clamp was present and working, just clamping to the wrong number.** Symptom: `ssh jeremy@100.69.0.1` from the laptop hung after `debug2: compression ctos`, while ping succeeded and the same command from the host worked. Not auth, not routing — an MTU black hole. `inet mangle` already had `oifname "netmaker" tcp flags syn tcp option maxseg size set rt mtu` in both forward and output chains (7458 packets counted), but `rt mtu` clamps to the **interface** MTU (netmaker = 1420 → MSS 1380), which assumes every peer's path carries 1420. Measured DF-ping to the laptop (`100.69.0.3`, endpoint `172.58.131.104`, T-Mobile/CGNAT): **1376 OK / 1380 FAIL**, so max MSS is 1336 and 1380 was 44 bytes over. Decoy (`100.69.0.2`) passes 1392 on the same interface — which is why only one peer broke and nothing looked misconfigured. Fixed to a fixed `size set 1300` (36 bytes margin for roaming) in the live ruleset *and* `/etc/nftables.conf:58,63`, else the runit `nftables` service restores `rt mtu` from that file on every boot. **Note `rt mtu` is the wrong primitive for any mesh with mobile peers** — it can only ever see the local interface MTU, never the per-peer path. Also worth knowing: this kernel (7.1.5-artix1-2) has **no `xt_TCPMSS`**, so the iptables `TCPMSS` target fails with "missing kernel module" — nft's `tcp option maxseg size set` is the only clamping path on this box. | fixed |
| 2026-08-13 | Opus 5 | **sshd had `LogLevel VERBOSE` set and was discarding every line of it — no syslog daemon, no runit log service.** `10-netmaker-only.conf` sets VERBOSE, but no `socklog`/`syslog-ng`/`rsyslog` runs and `/etc/runit/sv/sshd` had no `log/` directory, so sshd's output went nowhere. Result: SSH failures left literally no trace beyond a `wtmp` entry, which is why the MTU black hole above could not be diagnosed from the host and why the laptop's key could not be identified from logs. Fixed: `run` now execs `sshd -D -e` with `exec 2>&1`, plus a `log/run` svlogd service to `/var/log/op-dbus/sshd/`. **`sv restart` is NOT enough to pick up a newly added `log/` dir — runsv must be cycled with `sv exit <name>`** (runsvdir respawns it in ~5s; sshd is down for that window, so do it from console). The log immediately proved both the failure (`Connection from 100.69.0.3` and nothing else) and the fix (`Accepted publickey`). | fixed |
| 2026-08-13 | Opus 5 | **`/etc/` config that the control plane depends on keeps turning out to be untracked — third instance today.** `openflow-static-flows.json` (blanked, unnoticed 3 days), `/etc/nftables.conf` (held the wrong MSS clamp), `/etc/runit/sv/sshd/` (live-only service definition) were all outside git, so `git add -A` could never see them and "commit everything" was always true and always insufficient. `nftables.conf` and the `sshd` service are now imported to `deploy/config/` and `deploy/runit/`. **The pattern, not the instances, is the problem** — a check that diffs `/etc/op-dbus`, `/etc/nftables.conf` and `/etc/runit/sv` against the repo would have caught all three, and would catch the next one. Note `/etc/nftables.conf` may also be partly netclient-generated (it writes the `NETMAKER-ACL-*` chains), so an import is a snapshot, not ownership. | open |
| 2026-08-13 | Opus 5 | **The whole runit boot is deadlocked on one bad netclient credential, and `3tched-network-install.sh` reported success through all of it.** Root cause is a single line repeated every second in `/var/log/op-dbus/netclient/current`: `ghostbridge.tech is misconfigured: MQID/Password does not match hostid/password` → `Fatal: configuration is invalid`. netclient exits ~1/s, never creates `/sys/class/net/netmaker`, so its `check` (which requires that interface) can never pass. Cascade: `op-grpc-bridge` does `wait_dep netclient`, times out at 90s, exits 1, runsv restarts it forever — **and op-grpc-bridge is what publishes the D-Bus plugin objects and claims `org.opdbus.v1.plugins`** (server.rs:338-386), so the tree is on NO bus and every `zcall` mutation fails. Then `incus-ct-netmaker` does `wait_dep op-grpc-bridge` → NetMaker CT stays STOPPED → the stale `ovsbr0-eth0` blocks on `sv check incus-ct-netmaker` → **the eth0 cutover never ran** (eth0 still holds the public address and default route, `pub0` is DOWN with a random MAC, ovsbr0 has no uplink). This is a genuine dependency **cycle**: netclient needs the Netmaker server, which lives in the CT that `incus-ct-netmaker` starts, which waits on op-grpc-bridge, which waits on netclient. The comment at `deploy/runit/op-grpc-bridge/run` asserts the opposite ("nothing in the boot path calls back into this service... that absence of a back-edge is what keeps this list cycle-free") and is wrong. **`deploy/runit/check-dep-cycles.py`, the tool that would have caught it, crashes** with `UnicodeDecodeError: 'utf-8' codec can't decode byte 0x97` on a non-UTF-8 `run` file — it has presumably never run to completion. This is the fourth netclient identity/config loss in two days; **netclient must not be a hard boot dependency of the control plane.** | open |
| 2026-08-13 | Opus 5 | **`sv status` green means "runsv has a live pid", not "the service did its job" — the wait_dep idiom makes this systematically misleading.** 25 services on this host are sitting in `/bin/sh ./run` polling `sv check` on a dependency, all reported `run:` with multi-hundred-second uptimes. `sv status ovsbr0-eth0` showed `(pid 2783) 440s` while that pid was executing a **previous version** of the run script (`pstree` → `sv check incus-ct-netmaker`, a dependency the current script does not even have). Diagnosis needs `pstree -alp $(cat /etc/runit/sv/<n>/supervise/pid)` — the child process names the blocking dependency. Companion to the `exec pause` false-green already recorded above: between the two, `sv status` is close to worthless for oneshots on this host. `3tched-network-install.sh` now treats a 60s `sv check` failure as a run failure and prints that pstree command. | open |
| 2026-08-13 | Opus 5 | **`sv start` on an already-running service is a no-op, so installers that write a service definition and then `sv start` it never activate what they wrote.** `3tched-network-install.sh` installed a correct `ovsbr0-eth0` at 15:21 onto a host where runsv had been executing the old one since 15:17; because step 4 only did `sv start`, the old script kept running and the new one never took effect — file on disk right, behaviour wrong, status green. Fixed by `cmp`-ing each file before install and `sv restart`ing only services whose definition actually changed (restarting the whole chain would needlessly cycle the datapath). **Also: `/etc/runit/sv/ovsbr0-eth0` is a symlink to `ovsbr0-eth0.retired-20260806T012103Z`** — an unfinished retirement from Aug 6 — so `install` wrote *through* it into the retired directory. That happens to work (runsv is chdir'd there too) but makes `ls /etc/runit/sv` actively lie about where the live definition is. The installer now warns and lists any symlinked service dir. | fixed |
| 2026-08-13 | Opus 5 | **The openflow plugin has no flow-dump read method, so MANAGED flow presence cannot be verified through the plugin tree.** `ovs-ofctl` is forbidden, and `zcall methods` across `openflow`, `rovs_commands`, `ovsdb_bridge` and `openflow_obfuscation` shows no `dump_flows`/`list_flows` equivalent. `openflow.get_datapath_health` returns `controllers[]` and `fallback_normal` (the cookie `0x…01` rule) straight off OVSDB, which covers the FALLBACK class and the controller attach — but the `0x…02` static set can only be *inferred* from "controller connected" plus the `Loaded N static flow(s)` log line. `3tched-network-install.sh` verification was rewritten onto `get_datapath_health` and no longer shells out to `ovs-ofctl`; it says so where it infers. **Fix: add a `dump_flows` read method (cookie-filtered) to the openflow plugin** — without it the exact failure mode recorded above (fallback-only table, unnoticed for 3 days) remains unverifiable by any sanctioned path. | open |
| 2026-08-13 | Opus 5 | **RETRACTED/CORRECTED: the netmaker server registry is fully intact — I read three abandoned copies of `netmaker.db` and concluded the mesh had been wiped. It had not.** The live DB is **`/opt/netmaker/data/netmaker.db`** *inside* the container (held open on fd 3 by the netmaker process; 4.1MB `-wal` alongside it). Three decoy copies exist and every one of them is a genuine dead end: `/root/data/netmaker.db` (mtime Aug 6 12:45), `/var/lib/opdbus-runtime/netmaker/netmaker.db` (Aug 2 19:25 — **and this is the path `SQLITE=` in `/etc/netmaker/netmaker.env` still points at**, so the env var is stale and actively misleading), and the `post-host-join-20260806T124707Z` container snapshot. All three predate the Aug 2 network delete/recreate and therefore genuinely contain `hosts_v1 = 0` — they are literally the pre-recreate snapshots, as `netmaker.db.pre-recreate-100.69.0.0-24` (Aug 2 17:23, matching the DELETE event to the minute) makes explicit. **Lesson: never read a service DB off disk by guessing the path — resolve it from the running process (`ls -l /proc/<pid>/fd`) or query the API.** The `events` table delete/recreate on 2026-08-02 is real but was long since recovered from. | fixed |
| 2026-08-13 | Opus 5 | **Actual netmaker state, read via the API (authoritative): 3 hosts, 3 nodes, 2 egresses, 1 network — nothing needs rebuilding.** Hosts: `<redacted-2026-08-14>` "ghostbridge-host" (this origin, 3 nodes), `e79e1962-…` "decoy-wg2", `4e63506a-…` "wrt-router". Nodes: `100.69.0.1/24` (origin, id `66d76009-…`), `100.69.0.2/24` (decoy), `100.69.0.3/24` (laptop). **The egress is present and correct**: `host-svc0-net`, range `10.0.0.0/24`, mode `direct_nat`, `nat: true`, `status: true`, bound to node `66d76009-…` — i.e. the svc0 subnet covering both `10.0.0.1` and `10.0.0.2` — plus `decoy-human-wg` (`10.10.0.0/24`, mode `disabled`). **Trap that caused my misread: `/api/nodes` reports `isegressgateway: false` and `egressgatewayranges: null` on every node** — those are the LEGACY per-node egress fields, unused in this version. Egress is a first-class resource now; query `GET /api/v1/egress?network=<net>` (note singular `egress`, and `network` is a required param or you get a 400). Critically, host `<redacted-2026-08-14>` **matches the `mqid` in `/etc/netclient/servers.json` exactly**, so the origin's server-side identity was never lost — the entire fault is the local `netclient.json` mismatch, and the fix is restoring `netclient.json.bak-dnsfix-20260806061253`, NOT re-enrolling. | open |
| 2026-08-13 | Opus 5 | **netclient's own config is separately corrupt: `netclient.json` id/hostpass no longer match `servers.json` mqid/MQPassword, which is a *fatal* local check — it exits before creating the interface.** Error text is `<server> is misconfigured: MQID/Password does not match hostid/password` → `Fatal: configuration is invalid`, repeating ~1/s forever under runsv. `servers.json` has been byte-stable since Aug 6 (`mqid <redacted-2026-08-14>` / `MQPassword <redacted-2026-08-14>` for BOTH `3tched.com` and `ghostbridge.tech`), while `netclient.json`'s identity was regenerated three times without ever updating it: `f29a69f1-…` (Aug 9), `ac5e28e3-…` (Aug 12), `061caec1-…` (Aug 13 01:53). Only `netclient.json.bak-dnsfix-20260806061253` (`id <redacted-2026-08-14>`, `hostpass <redacted-2026-08-14>`) matches. Note `netclient.json.bak` is `id 00000000-0000-0000-0000-000000000000` with an EMPTY hostpass — netclient's non-atomic write leaves a zeroed `.bak`, so **`.bak` is not a safe rollback target**; use the dated backups. **Load-bearing consequence: `op-grpc-bridge` does `wait_dep netclient`, so a netclient that cannot start takes the entire D-Bus plugin tree down with it** (see the deadlock entry above). netclient must not be a hard boot dependency of the control plane. | open |
| 2026-08-13 | Opus 5 | **`3tched-network-install.sh`'s ovsbr0-addr retirement took incusd and op-web down with it — the retired service's only real function was touching a readiness flag other services gate on.** `ovsbr0-addr/run` calls a helper that does not exist, has no `set -e`, then `touch /run/opdbus/runit-ready/ovsbr0-addr` and `exec pause` — so it "does nothing" *except* publish the token that `incusd` and `op-web` `wait_dep` on. The installer unlinked it from runsvdir at 15:21; incusd survived until its next restart (~15:57) then crash-looped `dependency ovsbr0-addr not ready after 90s` every 90s, taking `/var/lib/incus/unix.socket` with it — every container unmanageable, and `incus` unusable for diagnosis. The repo's `deploy/runit/incusd/run` **already** had the fix (`wait_dep opdbus-rundirs`); the live host had a stale copy, and the installer never touches incusd because it is not in `NET_SERVICES`. Fixed live for both (backup `/var/lib/op-dbus/ovsbr0-addr-dep-fix-20260813-160558`); `op-web`'s service definition is **still untracked** — another instance of the live-only-service pattern. **Generalise: retiring a runit service means removing every `wait_dep` on it first** — grep `/etc/runit/sv/*/run` before unlinking anything. `nm-ovs-nic.retired/run` still references it (harmless, itself retired). | fixed |
| 2026-08-13 | Opus 5 | **RESOLVED — the entire control-plane outage was one stale `netclient.json`, and the reason it took everything down is that `op-grpc-bridge` binds `100.69.0.1`.** The bridge's bind set is `127.0.0.1:8090,100.69.0.1:8090,127.0.0.1:50051,100.69.0.1:50051`, and `100.69.0.1` only exists once netclient brings up the `netmaker` WireGuard interface. `server.rs` does `TcpListener::bind(bind_addr)?` — a failed bind aborts the process — so the run script gates on `wait_dep netclient`, and **a netclient that cannot start therefore takes down the D-Bus plugin tree, every `zcall` mutation, the OpenFlow controller attach, and the shared `container.sock` that is every NIC-less container's only network path.** The mesh is a hard dependency of the local control plane, which is not obvious from any doc and is worth deciding deliberately: binding the mesh address is what couples them. Fix was restoring `/etc/netclient/netclient.json` from `netclient.json.bak-dnsfix-20260806061253` — verified correct *before* copying by base64-encoding its `publickey` byte array and matching it against the server's host record (`eT/L5V6XkbWp9ln9KShZyaNs80s+sjTvggzNJ2Nvw3w=`). **That check is the important part: a surgical `id`+`hostpass` edit would have left the regenerated keypair in place and broken every WireGuard handshake while looking correct.** Full chain came back in order: netclient → `netmaker` iface `100.69.0.1/24` + 2 peers (decoy, laptop) → op-grpc-bridge binds → `org.opdbus.v1.plugins` on the session bus (67 plugins) → controller attach → `OVS connected from 10.200.0.1:56462` → `4 static flow(s) installed`. Mail path verified (25/143/993/587 open on the mesh address, queue empty). Backup of the pre-restore state at `/var/lib/op-dbus/netclient-restore-20260813-162517`. | fixed |
| 2026-08-13 | Opus 5 | **`wait_dep <svc>` calls `sv start <svc>`, so naming a service as a dependency RESURRECTS it — a `down` file does not protect it.** `notebook-sources-sync/run` had `wait_dep op-cognitive-mcp`, which is why the deprecated standalone MCP kept reappearing (`(pid N) 0s, normally down`, crash-looping in `--stdio`: stdio transport starts, immediately shuts down, repeat) despite `/etc/runit/sv/op-cognitive-mcp/down` existing since Aug 12. Each incarnation grabbed `/var/lib/op-cognitive-mcp/memory.db/data/LOCK`, so `op-grpc-bridge` logged `CognitiveMcpServer init failed ... Resource temporarily unavailable` and came up with no cognitive tools. **The socket is not just the lock discipline, it is the auth path**: `cognitive_mcp` is reached via `PluginV1.Call` on `/org/opdbus/v1/plugins/cognitive_mcp`, which carries the WireGuard-pubkey→sessionid identity — a process that opens the store in-process bypasses authentication entirely, not merely the single-writer rule. Fixed in both the live run script and `deploy/runit/notebook-sources-sync/run`: depend on `op-grpc-bridge`, never on `op-cognitive-mcp`. Verified: MCP stays down, LOCK now held by `op-grpc-bridge`, bridge loads `tools=409` in-process. **Grep the tree for `wait_dep` naming any service that is meant to stay down.** | fixed |
| 2026-08-13 | Opus 5 | **The bridge emits a continuous `Updated` signal storm on `/org/opdbus/v1/plugins/net` — 19 signals every ~5s, forever, on a completely idle bridge.** `busctl monitor` on the session bus shows 38 signals in 10s, all `Member=Updated`, all on the `net` plugin, carrying 19 distinct "keys" — which are not properties at all but **the complete `vswitch.ovsschema` table list** (`Open_vSwitch`, `Bridge`, `Port`, `Interface`, `Controller`, `Manager`, `Mirror`, `NetFlow`, `sFlow`, `IPFIX`, `QoS`, `Queue`, `SSL`, `AutoAttach`, `CT_Zone`, `CT_Timeout_Policy`, `Datapath`, `Flow_Table`, `Flow_Sample_Collector_Set`). Mechanism: `mutation_engine.rs:916` does `for (table_name, table_update) in tables.iter()` over the OVSDB monitor update and calls `process_authoritative_change` for **every** table, with no check that `table_update` actually carries any rows — so one OVS interface-statistics tick (OVS refreshes those on a ~5s cycle) fans out into a full 19-signal sweep. **Not a reconnect loop** — `OVSDB monitor_db subscribed` appears exactly twice, both at process start. Consumers see a flood of `Updated` signals that assert nothing changed, which is what surfaces as "unexpected signal" repeated 20+ times. **Fix: skip tables whose update object is empty before calling `process_authoritative_change`** (and consider that a signal whose payload is a table NAME rather than a changed key is the wrong contract — `{"key":"Interface","plugin":"net"}` tells a consumer nothing actionable). Worth stating plainly: this is a **polling artifact leaking into the reactive layer**, which the architecture explicitly forbids — the datapath is quiet, nothing is changing, and the bus is carrying 3.8 signals/second anyway. | open |
| 2026-08-13 | Opus 5 | **`op-web` service definition imported to `deploy/runit/op-web/` and given a real `check`; five project-owned services are still live-only.** `run` and `log/run` were copied verbatim from `/etc/runit/sv/op-web/` (the only additions are comments) and `check` is new — op-web had none, so `sv check op-web` fell back to "runsv has a live pid", which on this host is nearly meaningless since a run script blocked in a `wait_dep` loop reports exactly that. Worse, op-web writes its readiness token *before* `exec op-web-server`, so the token asserts "the run script got this far", not "the server bound its port" — the new check tests `pgrep` + `ss -lnt sport = :$PORT` instead. Verified passing against the live process (port 8080, HTTP 200) with no restart required. **Still untracked and project-owned: `opdbus-grants`, `nm-api-tls`, `nm-warp-egress-proxy`, `wgcf-egress`, `droid-daemon`** (plus the `nftables` service definition, whose *config* is already imported but whose runit unit is not). The ~20 remaining gaps are stock Artix package services (`agetty-*`, `udevd`, `elogind`, `dnsmasq`, `avahi-daemon`, `fail2ban`, `iptables`, …) and correctly do not belong in this repo — which is exactly why a blind `/etc/runit/sv` → `deploy/runit` diff check needs an allowlist, not a bare comparison. | open |
| 2026-08-13 | Opus 5 | **All project-owned runit service definitions are now in the repo — the live-only-service gap is closed.** Imported byte-for-byte from `/etc/runit/sv/`: `opdbus-grants` (run/check/finish/log), `nm-api-tls`, `nm-warp-egress-proxy`, `wgcf-egress` (run/finish), `droid-daemon` (run/finish/log), `nftables` (run/finish), plus `op-web` earlier. `.bak*` variants were deliberately excluded — several dirs carry two or three (`run.bak-pre-ready-signal`, `run.bak.1785730851`) and importing those would commit dead code. Scanned every file for literal secrets before committing (0 hits; the TLS/WARP ones read from `/etc/op-dbus/environment` rather than embedding anything). All 6 verified identical to live and `sh -n` clean. **What remains untracked is exactly the 20 stock Artix package services** — `agetty-tty1..6`, `udevd`, `elogind`, `dnsmasq`, `avahi-daemon`, `fail2ban`, `iptables`, `ip6tables`, `auditd`, `dmeventd`, `dnscrypt-proxy`, `pacman-init`, `qemu-guest-agent`, `rsyncd`, `chrome-remote-desktop-jeremy` — which belong to their packages, not here. **So the long-requested drift check is now writable**: `deploy/runit` should equal `/etc/runit/runsvdir/default` minus that stock allowlist. Note `nftables` is a judgement call — the unit is stock-ish but `/etc/nftables.conf` carries the project's MSS clamp and netclient's `NETMAKER-ACL-*` chains, so the unit was imported alongside the config that was already in `deploy/config/`. | fixed |
| 2026-08-13 | Opus 5 | **`3tched-network-install.sh` invented an architectural constraint and wrote it into `/etc/op-dbus/network.conf` as if it were authoritative: "The WG iface `netmaker` must NOT join ovsbr0." It is wrong.** The string appears **nowhere else in the repo** — not in `install/3tched-network-bootstrap.sh`, not in any doc — so the installer is its sole origin, and every run stamps it into `/etc`. It sits appended to the *container* socket-only bullet, conflating two unrelated rules: containers get no NIC (true), and the netmaker WireGuard interface must not be bridged (false — per the operator it **belongs on ovsbr0 as a port**). The confusion to name explicitly: `netmaker` is WireGuard but is **managed by netclient/netmaker**, and is NOT a `wg0`-style identity interface. This host has **no wg0** (verified: `wg show interfaces` → `wgcf-egress netmaker` only) and must not grow one — that is the rule that actually exists, and it is about wg0, not about the mesh iface. Corrected in the script and in the live `network.conf` (backup `network.conf.bak-pre-wgclaim-*`). **Still open: `netmaker` is not enslaved** — `ovsbr0` ports are `eth0/pub0/svc0/grpc0` only. Not done unilaterally because the mesh currently carries mail delivery. **Generalise: a generated config file is a terrible place to assert architecture — the installer's header comments get copied into `/etc` where they outlive and outrank the reasoning that produced them.** | fixed |
| 2026-08-13 | Opus 5 | **`wgcf-egress` (Cloudflare WARP) is plumbed halfway and carries no traffic — it was meant to funnel xray egress and does not.** The tunnel is up and the policy rule exists (`ip rule` → `100: from all fwmark 0x51821 lookup 51820`), but three links are missing: (1) **nothing sets the mark** — no `0x51821`/`333857` match anywhere in nftables or iptables, so no packet is ever selected for WARP; (2) **route table 51820 has no default route** — it contains only `127.0.0.1 dev wgcf-egress scope link`, so even a correctly marked packet could not egress through it; (3) **xray's config never references it** — no `mark`, `warp`, `wgcf` or `172.16.0` in `/etc/xray/xray_config.json`. Transfer counters confirm it: `2576 B rx / 9360 B tx` since boot, i.e. handshake keepalives and nothing else. The run script's own comment states the intent ("Marked traffic (fwmark 0x51821) exits via WARP instead of the host public IP") — so the design is recorded, only the marking rule and the default route were never added. Note `0x51821` = 333857 and is suspiciously the netclient WireGuard **listen port** (51821) reused as a mark value; harmless but confusing. **Fix needs: a mark rule selecting xray's egress traffic, plus `ip route add default dev wgcf-egress table 51820`.** | open |
| 2026-08-13 | Opus 5 | **CORRECTION to the wgcf entry above — the mechanism is xray `sockopt.mark`, not a packet-matching rule, and `/etc/wireguard/wgcf-egress.conf` documents the whole design in its own comments.** I wrongly concluded "nothing sets the mark, add an nft rule + `ip route add default ... table 51820`". Both wrong. The conf states it plainly: *"wgcf-egress — hypervisor CT privacy EGRESS (WARP). **Not for public client ingress (that is decoy `wgcf-ingress`)**"* and *"only fwmark 0x51821 → table 51820 (xray common exit outbound `warp`)"*. **Public WARP ingress lives on the decoy as a separate `wgcf-ingress` interface — this host only has the egress half.** Two mark values are deliberately distinct and must not be conflated: `FwMark = 0x51820` keeps the WireGuard *underlay* packets on main/pub0, while `0x51821` is the application exit that xray sets per-socket. The `ip rule` and `iptables ... -o wgcf-egress -j MASQUERADE` are installed correctly by `PostUp`. **The two real gaps: (1) xray has NO `warp` outbound at all** — `grep -c '"warp"'` returns 0, and its only outbounds are `api`(blackhole), `direct`(freedom), `block`(blackhole), so all egress goes straight out the host public IP with no privacy hop and nothing ever sets `0x51821`; **(2) `AllowedIPs = 127.0.0.1/32`** leaves table 51820 holding only `127.0.0.1 dev wgcf-egress`, so even a correctly marked packet can reach nothing — a working selective exit needs `AllowedIPs = 0.0.0.0/0, ::/0` with `Table = 51820` (already set) keeping it out of main. Fixing either alone accomplishes nothing; both are required. Separately, `nm-warp-egress-proxy` (listening `127.0.0.1:13128`, sets `SO_MARK=0x51821` in userspace) is a *different* consumer whose ingress was deleted — SIGNALS 2026-08-12 records `NetMaker/proxy3128` being removed as "dead weight ... neither backend was listening", so that path is severed too. | open |
| 2026-08-13 | Opus 5 | **WARP polarity: `Table = 51820` is the single line preventing a silent Cloudflare egress leak, and any AllowedIPs widening must treat it as load-bearing.** Measured with `ip route get`: unmarked, `mark 0x51821` and `mark 0x51820` ALL resolve to `via 188.68.56.1 dev pub0 src 188.68.58.237` — i.e. the application mark is currently **inert**, because table 51820 holds only `127.0.0.1 dev wgcf-egress` so the lookup falls through to main. Everything egresses on the public IP; WARP carries nothing. **The hazard in "just set AllowedIPs = 0.0.0.0/0" (which I suggested, wrongly, one entry above): `Table = 51820` is the only thing keeping those routes out of `main`.** With it, the change is inert-but-armed; without it — dropped, overridden, or the iface raised by anything other than this wg-quick conf — WARP becomes the **default route** and all traffic that should traverse xray silently exits via Cloudflare instead. That reversed polarity (unmarked → WARP) fails OPEN to a third party. The correct shape, which the current conf already has, is the inverse: **default direct/xray, mark selects WARP — fails CLOSED to the public IP.** Consequent ordering rule: **add the xray `warp` outbound FIRST** (it is what makes the mark real), and only then widen AllowedIPs, asserting `Table = 51820` in the same change. Doing it the other way round arms the leak while accomplishing nothing, since with no `warp` outbound nothing sets `0x51821` at all. | open |
| 2026-08-13 | Opus 5 | **`"mark": 255` in `op-identity/src/schema_bridge.rs` is dead placeholder text, and marking those three outbounds for WARP would break the control plane.** Provenance: introduced 2026-04-20 in `f60839b4` ("WG-driven identity sled, OF controller server, Xray shuttle") inside a 367-line change to that file — incidental detail in a large multi-crate commit, not a routing decision, which is why the operator did not recognise it. It has never had any effect: nothing in `ip rule`/nftables/iptables matches 255, **and the generated config is not what is deployed** — the live `/etc/xray/xray_config.json` contains no `mark` at all. **Critically, all three sites are INTERNAL service redirects, not user egress**: `to-grpc-bridge` (→ `GRPC_BRIDGE_HOST:PORT`, path `/Ghostbridge.StateSync`) and two `to-{tag}` Gemma backends (Grpc and Unix). Marking them would route control-plane StateSync and Gemma inference through Cloudflare — and since op-grpc-bridge listens on `127.0.0.1`/`100.69.0.1`, that black-holes the plugin tree the moment WARP becomes routable. **The tagged mark belongs on a `warp` outbound carrying user egress, which does not exist in either the generator or the live config.** New mark scheme adopted: `0x3344xxxx` with rule `fwmark 0x33440000/0xffff0000 lookup 51820` — `0x3344` = ASCII `"3D"`, matching the OpenFlow cookie prefix `0x33444348` = `"3DCH"`; low 16 bits carry the identity slot so adding an identity never needs a new rule. Chosen deliberately far from `0x51820`/`0x51821`, which differ by one hex digit while `51820` is ALSO the table number **in decimal**. Verified collision-free against every mark on the host (only `0x51820` underlay and the unset `0x51821` exist; netmaker is `fwmark=off`; nftables and iptables have no mark rules at all). | open |
| 2026-08-13 | Opus 5 | **A second WARP tunnel, `wgcf-uiStream`, was configured on 2026-08-11 for UI-stream egress and has never once come up — and it CANNOT be started as written, because it shares a WireGuard identity with `wgcf-egress`.** `/etc/wireguard/wgcf-uiStream.conf` (Aug 11 03:42, with an 03:29 backup) is self-described as *"independent WARP path for UI stream egress ... Fully standalone from wgcf-egress"*: `Table = 52888`, `FwMark = 0x52888` underlay, app exit mark `0x52889`, `Address = 172.16.1.2/32`. Nothing of it is live — **no runit service definition exists**, the interface is down, table 52888 is empty, and no `ip rule` matches `0x52889`. It is also referenced nowhere in `crates/`, `deploy/`, `schemas/` or `scripts/`. **The blocker is not the missing service file.** All three WARP configs on this host — `wgcf.conf`, `wgcf-egress.conf`, `wgcf-uiStream.conf` — carry the **same PrivateKey** (verified by hashing: identical `sha256[0:16] = 1f0d894126…`), and `wgcf-egress` + `wgcf-uiStream` additionally declare the **same IPv6 address** `2606:4700:110:8f32:f3e6:8dfa:59d6:93aa/128`. uiStream was cloned from egress with only the name, table, marks and IPv4 changed. Consequences of raising it as-is: both tunnels dial the same Cloudflare endpoint (`162.159.192.1:2408`) with one peer key, so the newer handshake displaces the older and the two sessions flap; and the duplicate `/128` conflicts at the kernel. WARP also assigns addresses per registration, so the hand-picked `172.16.1.2` is unlikely to be valid for that identity. `wgcf-egress.conf`'s own comment admits the origin — *"Reused from Oracle decoy (/etc/wireguard/wgcf.conf)"* — so the identity may be shared with the decoy as well, making it a three-way reuse. **A genuinely independent second path needs its own `wgcf register` (own key, own Cloudflare-assigned addresses), not a copied conf.** Note uiStream got the `AllowedIPs = 0.0.0.0/0, ::/0` that `wgcf-egress` still lacks — so the newer file is the more correct template of the two. It repeats the same decimal-table/hex-mark trap (`Table = 52888` decimal vs `FwMark = 0x52888` hex vs exit `0x52889`); its own comment warns *"must NOT equal the application exit mark, or underlay loops into itself"*. UI stream terminates at op-web on **:8080** (`/events`, `/sse`, `/chat/stream`, `/logs/stream`, `/api/ui-model/state` → 200 live). | open |
| 2026-08-13 | Opus 5 | **`wgcf-uiStream.conf` rebuilt with its own Cloudflare device — the identity collision is resolved, tunnel still down pending a service file and a consumer.** The conf's `PrivateKey` **is** the device as far as Cloudflare is concerned, which is why the three cloned confs (`wgcf.conf`, `wgcf-egress.conf`, `wgcf-uiStream.conf`) all reported as ONE device: identical key. uiStream now carries the separately-registered device `transfer-test` (`f2e6cc90-73fd-4118-84e2-d3d1f03408a5`), giving distinct keys (`1f0d894126…` vs `51a9400751…`) and distinct IPv6 (`…8f32:f3e6:8dfa:59d6:93aa` vs `…8917:835a:16e:eaea:4292`). Verified with `wg-quick strip`; nothing raised, `wgcf-egress` still handshaking. **No license key is needed and none should be applied** — the account (`00a80e21…`) is already `Account type: unlimited` and every device is `role: child`, so premium is account-level and `wgcf update -l` is a no-op. **The account is at its hard 5-device limit** (transfer-test, 88FA45, 0CC56A, 9994E0, Samsung SM-S938U — all `active: true`), so `wgcf register` cannot add a sixth; a seat must be freed with `wgcf update --remove <id>` first. Note WARP assigns **172.16.0.2/32 to every client** — the IPv4 is constant across devices and the **IPv6 is the per-device address**, so the hand-picked `172.16.1.2` in the old conf was invented and the `/128` is the field that actually distinguishes devices. **Still outstanding: (1)** `/tmp/wgcf-transfer-test/wgcf-account.toml` (access_token + license_key + private_key, mode 0600 under `jeremy`) and the `wgcf` binary `/tmp/wgcf-transfer` are both in `/tmp` — `/tmp` is not tmpfs here so they survive reboot, but that is the wrong home for a licensed credential; `wgcf 2.2.32-1` is packaged in `extra` if the binary is wanted properly. **(2)** no runit service for `wgcf-uiStream`. **(3)** nothing sets `0x52889`, so the path stays inert until a consumer exists. Also worth naming the devices (`wgcf update -n`) — three are `88FA45`/`0CC56A`/`9994E0`, so nothing identifies which seat is this host vs the decoy. | open |
| 2026-08-16 | Opus 5 | **Out-of-process consumers read plugin state from a file when the sanctioned surface is the D-Bus tree.** op-web could not resolve any identity tonight — `CanonicalPeerIdentity::from_session` reads the MutationEngine's *in-process* cache and `set_engine` only ever runs inside the bridge, so op-web refused its own gRPC calls with "Host session identity is unavailable" and every dashboard panel errored; `pair.rs:102` separately still mmap'd the retired 152-byte global sled at `/dev/shm/plugin_schema.dat`, which no longer exists, so pairing failed with "No such file or directory" and no browser could ever obtain a footprint. **Fixed tonight** by pointing both at `/dev/shm/opdbus/state/identity_sled.json` (verified end to end: pair → footprint `8f40b42b…` → gRPC-Web `StateSync/GetState` → `grpc-status: 0`). **But that is the wrong surface.** Mutations update the D-Bus object tree, and `zcall props identity_sled` returns the same three records fully populated — genesis, all four mint inputs, schema_version 3 — with no gRPC identity required. So the correct read for any process that is not the bridge is `PluginV1.GetAllProperties` on `/org/opdbus/v1/plugins/<name>` over `/run/opdbus/session-bus.sock`, not a file path. This also dissolves a circularity worth naming: op-web needs an identity to make the gRPC call that would give it the identity, and D-Bus is the only read that escapes it. Follow-up: switch `from_session`'s out-of-process branch and `pair.rs::read_live_identity` to a zbus proxy call (both call sites are sync, so they need the `block_in_place` + `block_on` pattern already used by `verify_per_identity`). | `crates/op-grpc-bridge/src/shared_socket.rs`, `crates/op-web/src/handlers/pair.rs` | open |
| 2026-08-16 | Opus 5 | **The system holds two catalog identities at once, and a genesis binds the one the engine does not recognise.** Same seal cycle, two seconds apart: `19:38:57` the engine logs `published catalog identity catalog_hash=107b1adb916d9cc4…` (`store.catalog_hash()`, kept only in `self.catalog_hash` and that log line — never on disk); `19:38:59` the sealer writes `/dev/shm/opdbus/plugin-blobs/.manifest.json` with `catalog_hash=90fcbc8e8a8d38fb…`, `generation: 754`. `mint_genesis` reads the manifest, so both live identities (host `5205f22a…`, chatbot `bea37ecb…`, jeremy `f036f8d8…`) bound `90fcbc8e…` — a value the engine believes is not current. The comment above the engine's code says recomputing "would be a second derivation of a value that is only allowed to have one", and then it stores a second derivation. **Fix, in order:** (1) the engine reads the manifest instead of deriving — one authority, the one that persists; (2) find why the two differ within one cycle (probably a blob frozen between the engine's read and the manifest write — `zeroclaw` was sealed at 19:38:57), because that determines whether `generation` is trustworthy ordering; (3) keep **new + previous** in the manifest (`catalog_hash`, `previous_catalog_hash`) so a session anchored just before a reseal still verifies — a two-deep window, deliberately not a history store. Consequence while unfixed: A.N.N.A. cannot answer "which catalog state is being compared", so the contract leg of every genesis is unprovable even though the arithmetic and the chain leg both check out. **Also from tonight:** the PSK is absent from the genesis (it only salts session-id derivation), so a genesis is recomputable by anyone holding the pubkey plus the public catalog/chain values; and `pair.rs` hands every paired browser the *host's* genesis, so `device_name` is cosmetic, all devices are indistinguishable to the gate, mutations from any of them are attributed to container zero, and revoking one means rotating the host identity. | `crates/op-grpc-bridge/src/mutation_engine.rs:998`, `crates/op-web/src/handlers/pair.rs` | open |
| 2026-08-16 | Opus 5 | **Capabilities are declared in three incompatible shapes, and the grant list is a second source that nothing reconciles — the wildcard actor held 44 of the 219 capabilities the catalog declares, plus 4 that no plugin declares any more.** Live counts: 578 method→capability pairs across 67 plugins; 219 distinct capabilities in three vocabularies — `cap.<cat>.<subject>.<action>@vN` (114), `plugin.read`/`plugin.write` (78), `*.invoke` (17). 314 of the 578 are exactly `plugin_id + effect` and therefore carry no information; the rest encode real intent nothing else records (`agent_config` declares `agent.read`; btrfs declares per-operation versioned capabilities). Grants live in `/etc/opdbus/capability-grants.json` → materialized to `/dev/shm/opdbus/capability-grants.json`: hand-edited, no schema, no subid, no blob, no chain event, no hash — the only load-bearing state in the system that is not a plugin. The gate also does NOT merge: it matches the actor's footprint first and falls back to `*` only when the actor has no entry, so actor `898fbad6…` with 13 capabilities is strictly worse off than an unknown caller. **Stopgap applied 2026-08-16:** wildcard set regenerated as the union of what the live catalog declares (44 → 219), which cleared `identity_sled.*` and `host_runtime.read` denials. **This is backwards and must be refactored:** authority cannot be declared by an authority that does not exist at creation — the catalog is built *from* the plugins, so deriving grants from the catalog is the egg before the chicken. Correct shape: each plugin declares its capability envelope in its own schema (sealed in its blob, covered by the schema hash, so widening what a plugin may do is a contract change with a new hash, and a disk plugin reaching for `network.write` fails at registration); a `capability_grants` state plugin records who holds what, written through the mutation engine and notarized like any other mutation. Then `/etc/opdbus/capability-grants.json` and `op-grants-materializer` both disappear. | `crates/op-grpc-bridge/src/interceptor.rs:824` (`load_capability_grants`) | open |
| 2026-08-14 | Droid | **`wgcf-egress` was configured to carry nothing: `AllowedIPs = 127.0.0.1/32`, so policy table 51820 held only `127.0.0.1 dev wgcf-egress` and every marked packet fell through to `main`/pub0 — egress left by whatever path ingress arrived on.** That one line broke both directions: with `Table=51820` wg-quick installs one route per AllowedIPs entry (so no default route ever existed), and AllowedIPs is *also* the cryptokey-routing inbound filter (so return traffic from any real source was dropped). Reversed to `AllowedIPs = 0.0.0.0/0, ::/0` (backup `wgcf-egress.conf.bak-pre-allowedips-20260814T041247`) and applied live with `wg set ... allowed-ips` + `ip route replace default dev wgcf-egress table 51820` (v4+v6) — no tunnel teardown. Verified: `ip route get 1.1.1.1 mark 0x33440001` → `dev wgcf-egress table 51820 src 172.16.0.2`, unmarked still `via 188.68.56.1 dev pub0 src 188.68.58.237`, marked ping 3/3 with tunnel counters advancing, `POSTROUTING -o wgcf-egress -j MASQUERADE` present. Safe as a staged change because **nothing currently sets `0x3344xxxx`** — no iptables mangle MARK, no nft mark, no xray `sockopt.mark` — so table 51820 was consulted by zero traffic. **Two blockers remain before xray egress reaches the Oracle decoy:** (1) live `/etc/xray/xray_config.json` has outbounds `api`/`direct`/`block` only — no warp/egress outbound and no `sockopt.mark` anywhere, so the identity slot in the low 16 bits of `0x3344<slot>` is never stamped (the conf's own comment already points here: "do not use this same mark for application exit (see 0x51821 + xray outbound warp)" — 0x51821 is stale, the live rule is `0x33440000/0xffff0000`); (2) the xray container netns is lo-only (`net:[4026532709]` vs host `net:[4026531833]`, empty route table, dns+icmp both FAIL), so xray cannot egress at all and an `SO_MARK` set inside it would be scrubbed crossing the netns boundary regardless — the mark must be stamped by whatever performs egress in the host netns. Xray config deliberately untouched: AGENTS.md forbids model writes/reloads of Xray. | | open |
| 2026-08-14 | Droid | **`ovsbr0` floods every egress frame to all five other ports because the VRRP gateway MAC is never learned, and the bridged WireGuard port cannot accept L2 frames at all.** Host egress is `pub0 → dst 00:00:5e:00:01:0a`, but the router *replies* from its physical MAC (`bc:0f:fe:37:fb:a2`, learned on eth0), so the virtual MAC never enters the FDB (`fdb/show`: 14 entries, gateway absent) and NORMAL falls back to flooding — datapath confirms `in_port(3) → actions:2,4,5,1,6`. Cost: `svc0`, `grpc0` and `ovsbr0` each absorbed exactly 2,157,300 pkts / 807 MB of frames addressed to a foreign MAC, and `/proc/net/packet` is empty (no AF_PACKET listener anywhere), so **nothing consumes those copies**. The `netmaker` port is a `link/none POINTOPOINT NOARP` WireGuard iface enslaved to the bridge (correct per operator intent, cf. the "must NOT join ovsbr0" retraction) — but flooding Ethernet frames at an L3 tunnel gives `tx errs=296,925` climbing ~135/s with good rx/tx = 0. Fix candidate: `ovs-appctl fdb/add ovsbr0 eth0 0 00:00:5e:00:01:0a` (both `fdb/add` and `fdb/del` exist in OVS 3.7.1), which makes NORMAL unicast **without touching the four priority-100 fwmark classifier flows or their counters** — a `dl_dst` flow above priority 100 would shadow them instead. Two traps found the hard way: `ovs-ofctl -O OpenFlow13 mod-port ... no-flood` **silently no-ops** (OF1.3 removed the `NO_FLOOD` config bit; needs `-O OpenFlow10`), and the live bridge still has `protocols : []` despite 664fe8e7 "set bridge protocols at creation". Static FDB entries do not survive a vswitchd restart, so this belongs on `ensure_fallback_normal`'s re-assert-on-reconnect path, not a one-shot script. | | open |
| 2026-08-14 | Droid | **Label the CLI boundary explicitly, because today's two fixes were *measured* with `ovs-ofctl`/`ovs-appctl`/`ovs-dpctl` and a future reader will mistake that for precedent.** Those invocations were **operator verification only** — reading live datapath state by hand to prove a diagnosis (flood actions, port error rates, FDB contents). They are not a sanctioned code path and none of them entered the tree. The code fix went the other way: `fdb/add` had no OVSDB column and no OpenFlow message, so rather than shell out, `crates/op-network/src/unixctl.rs` speaks `unixctl` natively over `rovs_jsonrpc::Connection` + `rovs_transport::Stream` — the same JSON-RPC-over-Unix-socket framing `rovs_ovsdb` already uses, where `method` is the appctl command and `params` is its argument array. **This is the rovs refactor paying for itself:** the transport was already a dependency, so a whole class of "we have to shell out for this one" exceptions collapses into ~30 lines. Rule to carry forward: `rovs_jsonrpc` for unixctl, `rovs_ovsdb` for OVSDB, `rovs_openflow` for the bridge `.mgmt` socket; CLI only when a human is looking at output. Recorded in the module's own "CLI boundary" doc section so it survives without this table. | | fixed |
| 2026-08-14 | Opus 5 | **NetMaker Pro license was blocked by a missing egress hop, not a vendor problem — the proxy-device→UDS migration converted the inbound legs and dropped the outbound one.** `HTTP(S)_PROXY=127.0.0.1:3128` (set in `/etc/systemd/system/netmaker.service.d/proxy.conf`) pointed at nothing: the CT had no listener on 3128, so the license check could not leave its netns, fell back to a cached license record that had never been written, and reported `invalid license: failed to validate license key: record not found` — an error that reads like a vendor/account fault but is purely a routing one. Cause: `proxy3128` was an Incus **proxy device**, and every proxy device on every container was retired in favour of UDS relays (`uds-netmaker-api`'s header records the swap: *"Replaces the Incus proxy device that used to bind 127.0.0.1:8081"*). The inbound legs (8081/8083) were converted; the outbound one was deleted and never replaced. Everything downstream stayed healthy the whole time — `nm-warp-egress-proxy` was up on `:13128` and reached the licensing API fine when tested from the host. Fix: new runit service `xsock-netmaker-egress` (`socket-relay unix-listen /run/ghostbridge/NetMaker/egress.sock tcp-connect 127.0.0.1 13128`, modelled on `xsock-decoy` since `socket-relay` chmods 0666 and the CT is uid-shifted) plus a CT-side `op-uds-relay.service` drop-in adding `--tcp-to-unix 127.0.0.1:3128=…/egress.sock`. Verified: CT reaches a generic HTTPS endpoint (200) and the licensing API (503, same as the host gets), DNS resolving at the proxy so the CT still needs no resolver; after restart netmaker logs `proceeding with Paid Tier license`. **The separate 2026-08-06 `license-sso.status` blocker (nmctl enrollment SSO / access token, vendor contacted) does NOT gate license validation** — worth knowing before chasing it. Pro features, incl. the `host-svc0-net` egress gateway, are now unblocked. | `/etc/runit/sv/xsock-netmaker-egress/run` | fixed |
| 2026-08-14 | Opus 5 | **`host-socket-topology-live.md` asserted "never enslave the WireGuard `netmaker` iface into `ovsbr0`" as a hard invariant in §2/§10/§13.2 — it is wrong, and I acted on it before being corrected.** The enslavement is deliberate and load-bearing: it is the mesh↔fabric junction that connects the mesh to tonic (`svc0`) and gRPC (`grpc0`), performed every boot by the **enabled** `netmaker-ovs-attach`. Acting on the doc, I proposed `ovs-vsctl del-port ovsbr0 netmaker`, which would have severed both paths, and separately proposed giving the CT a NIC — already tried and retired as `nm-ovs-nic.retired`. Two lessons: (1) elevated **TX errors** on `netmaker` are the expected cost of L2 flood attempts against a `NOARP`, MAC-less L3 port, **not** evidence of misconfiguration — do not diagnose from that counter alone, and a failed host `ping` to a mesh peer is not proof of a dead data plane; (2) grep `/etc/runit/sv` for a `.retired` definition before proposing any network-attachment mechanism, since several approaches were tried, rejected, and left on disk as decision records. Doc corrected (§2/§5/§9/§10/§13.2) with a dated banner marking any pre-08-14 copy unsafe. **Also recorded in §9: the netmaker L3→`encap(ethernet)` flows are REQUIRED and ABSENT.** Live `dump-flows` matches zero `encap`/`packet_type` entries, so the junction runs without them. They are **not recoverable** — searched both repos' full history (prose in one commit only), `/.snapshots` (erased), every `~/.factory` and `~/.claude` session by parsing executed `tool_use` records rather than prose (41 hits; the only flow ever actually installed from this host is the `priority=0 NORMAL` fallback), plus configs, shell history and OVS logs. GLM-5.2's typed encap work (SIGNALS 2026-07-15) never landed: no `schemas/openflow/`, no `openflow_generated.rs`, and the live `openflow` plugin exposes **no `add_flow` at all** (only controller/datapath methods), while `openflow_translate.rs`'s `JsonFlowAction` has no `Encap` and hard-errors on unsupported actions. Reconstructing them needs three pieces: an `add_flow` method back on the plugin (schema + blob reseal), `Encap`/`packet_type` in the translator, and raw OF1.5 wire bytes (`rovs-openflow` is pinned at `0.2`, which lacks them) — the same shape of problem `unixctl.rs` solved for `fdb/add`. | `docs/src/operations/host-socket-topology-live.md` | open |
| 2026-08-15 | Opus 5 | **Public `:443` was DOWN — `fwd-443` blocked forever on an optional dependency, and the cause is the same retired-proxy-device class as the NetMaker license bug.** `fwd-443` (SNI demux: `mail.*` → mail-web.sock, else Reality) waits unconditionally on `/run/ghostbridge/mail-web.sock`. That socket is produced by `mail-web-socket`, which itself waits on `127.0.0.1:8440` — and **nothing provides 8440**: `mail-3tched` has only disk devices (the Incus proxy devices were all retired), *and* the CT is not listening on 8440/443/80 internally either, so converting the leg would not have been enough. Net effect: the run script parked before its `exec`, `pgrep sni-demux` empty, **nothing bound to `:443` at all** while `sv status` reported the service "run" (266 `signal 15` entries in its log). **Why this matters beyond mail:** Reality is `--default` on that listener, so the box stopped answering TLS on its public port. An IP that previously completed convincing handshakes and then refuses is exactly the inconsistency active probing looks for. Fix: mail made **optional** — bounded 30s wait, then exec without the two `mail.*` maps, serving Reality alone (`fwd-443/run`, backup saved). Verified: `188.68.58.237:443` + `10.0.0.2:443` bound, TLS1.3 handshake returns. **Generalise: a hard `until [ -S … ]` on a non-essential dependency converts a partial outage into a total one** — grep the `fwd-*`/`xsock-*`/`uds-*` tree for unconditional waits and decide, per service, which deps are load-bearing. | `/etc/runit/sv/fwd-443/run` | fixed |
| 2026-08-15 | Opus 5 | **Reality's unauthenticated fallback returns our own Let's Encrypt cert, so active probes see a name mismatch — the camouflage only covers authenticated clients.** Reality splits traffic: authenticated clients (correct keys + shortId) get shaped to look like the borrowed site; everything else is handed to `dest`. Live `dest` is **`127.0.0.1:1443`** — a *local* terminator — so any probe gets `subject=CN=assistant.ghostbridge.tech`, issuer Let's Encrypt, **identically for every SNI** (`www.apple.com`, `3tched.com`, `api.ghostbridge.tech`, `mail.ghostbridge.tech`). Verified from an external vantage (the decoy), not just locally. Probing is unauthenticated by definition, so a working client path does not mitigate this. Also: live `serverNames` ends with `www.apple.com` while the intended cover is **Amazon** — the Amazon config survives only as `xray_config.json.bak-reality-amazon-20260807T171301Z` (08-07). Same window as the routing rules being stripped **10 → 1** (live keeps only `api→api`; the 08-07 file has all the `xhttp-in` subdomain rules plus `to-*` outbounds and a `warp` tag). `:8444` (`xhttp-in`) is `sec=none` — no TLS, no cover. **Operator caution: probing from the host itself is not a valid test** (traffic never leaves the box and hits the local terminator) — probe from the decoy. Not fixed here: AGENTS.md forbids models writing/reloading Xray. The 08-07 backup is a *reference*, not a drop-in — its `to-*` addresses predate the 3tched consolidation. | `/etc/xray/xray_config.json` (in CT) | open |
| 2026-08-15 | Opus 5 | **The WARP exit-mark scheme diverged between host and decoy, so both the license path and the mesh underlay leave on the raw public IP.** Decoy `wgcf-ingress.conf` marks WireGuard with `0x51821` and installs `ip rule add fwmark 0x51821 lookup 51820` — self-consistent, and it is what hides wg0 inside WARP (`MTU=1280`, which is where the ~1200 client path MTU comes from). The **host** migrated to `0x33440000/0xffff0000` (SIGNALS 2026-08-13) and never updated the consumers, so: (1) `nm-warp-egress-proxy.py` still hardcodes `EGRESS_MARK = 0x51821`, which matches no host rule — `ip route get 1.1.1.1 mark 0x51821` → `dev pub0 src 188.68.58.237`, i.e. **license traffic exits on the raw public IP**, the same address Reality advertises; (2) the mesh interface had **`fwmark: off`** entirely, and `tcpdump -ni pub0` confirmed **raw WireGuard visible on the wire** (`188.68.58.237:51821 > 129.153.134.63:51823, UDP length 32/96` — unmistakable WG signature). Verified all `0x3344xxxx` slots route identically (`wgcf-egress`, table 51820, `src 172.16.0.2`), so the `/0xffff0000` mask already guarantees "same exit for all traffic" structurally — per-identity low bits cannot leak, since fwmark is an skb field and never on the wire. **The correct fix is not another hardcoded constant:** `op-blob`'s `BlobManifest` already stamps `forui = 0x52889` in all 67 sealed blobs precisely so "the mark travels with the blob and is read from SHM instead of being hardcoded at each call site". An egress equivalent beside `default_forui()` is the shape. Note the trap its own doc-comment names: exit mark is underlay+1 (`0x51820` underlay / `0x51821` exit; `0x52888` / `0x52889`), and setting the *underlay* mark on an application socket loops the tunnel into itself. | `/usr/local/libexec/3tched/nm-warp-egress-proxy.py`, `crates/op-blob/src/blob.rs` | open |
| 2026-08-15 | Opus 5 | **The mesh identity fabric was never repointed after the 2026-07-20 migration: the decoy's `wg0` still had the OLD server as its peer, and this host carried a phantom peer that hijacked the entire client subnet.** Decoy `wg0` (10.0.0.1/24, the identity fabric — 10.0.0.0/24 spans decoy `.1` and host `.2`, clients at `.3`/`.5`) held peer `DHLZcb4K…` → **`148.113.204.83:51821`**, the pre-migration box, with `AllowedIPs=10.189.0.2/32`. **No peer for `188.68.58.237` existed at all**, so the decoy had no path to this host and clients reached nothing past it. Meanwhile the host's `netmaker` iface carried `FMMSrwOg2n4M…` @ `129.153.134.63:51823` — a **phantom**: the decoy listens only on 51821/36590 and holds keys `6mx4ycJe…` (wg0) / `Gpohty…` (wgcf-ingress), neither matching. Its handshake was frozen (timestamp static across samples) while `wg show` still printed a plausible "latest handshake" — **do not trust a single `wg show`; sample `wg show <if> dump` twice and compare**. Worse, that dead peer claimed `AllowedIPs=…,10.0.0.0/24`, which is *broader* than the real decoy's `/32`, so WireGuard crypto-routing sent all client traffic to a peer that does not exist. Second peer `qDDv…` (the wrt router) showed `handshake=0` with tx climbing and rx flat — it has **never** completed one, and correctly so: the router peers with the *decoy*, per "decoy is the sole WG termination point", and is behind T-Mobile CGNAT (endpoint observed roaming 38363→51825→31686→52552), so it must initiate. Establishing a correct peer pair produced the first genuine bidirectional handshake of the day. **All of it was then removed** — see the next entry. | `/etc/wireguard/wg0.conf` (decoy) | fixed |
| 2026-08-15 | Opus 5 | **I built a WireGuard interface on the 3tched host, which violates a standing architectural boundary — removed. The host must run no identity WireGuard at all.** The decoy is the sole WG termination point; identity arrives over **WARP**, which delivers keys to xray for header injection. netmaker doing that job is the *current* state, not the target. I nonetheless spent hours diagnosing why the host's `netmaker` iface could not transmit (OVS slave, no encap flow, 100% TX error rate, 1:1 packet-to-error) and then wrote a `wg-3tched` static-WG service to own it — i.e. debugging and then entrenching an interface that should not exist. Torn out: `wg-3tched` stopped+unlinked, `netmaker` removed from `ovsbr0` and the link deleted, `netclient` given a `down` file, and the host peer removed from the decoy's `wg0.conf`. Remaining WG on the host is `wgcf-egress` / `wgcf-uiStream` only — those are WARP, the sanctioned transport. **Lesson for the next agent: this boundary is recorded and I had it, and still missed it because the symptom (a WG interface failing) framed the work as "make WG work" rather than "why is WG here".** When a component fails in a way that is hard to fix, check whether it is supposed to exist before making it work. Also: OVS changes here were made with `ovs-vsctl`, which is a **CLI-boundary violation** — OVS mutations go through `ovsdb_bridge transact` / rovs plugins; `ovs-vsctl`/`ovs-ofctl`/`ovs-appctl` are operator *verification* only (see `unixctl.rs`). | `/etc/runit/sv/wg-3tched` (removed) | fixed |
| 2026-08-15 | Opus 5 | **`op-of-controller` discovers ZERO OpenFlow ports, so no flow naming an interface can ever install — including the netmaker `encap(ethernet)` flow, which exists and is correct.** The encap flow I previously recorded as "unrecoverable" is in fact live config in `/etc/op-dbus/openflow-static-flows.json`: `priority=200, match {in_port: netmaker, packet_type: (1,0x800)}, actions [encap(ethernet), set_field eth_src=02:00:64:45:00:01, set_field eth_dst=port_mac:ovsbr0, output LOCAL]`. **That earlier "not recoverable anywhere" conclusion was wrong** — I searched git, snapshots, session transcripts and notebook exports but never read the file the controller actually consumes. The translator parses all four actions fine; it fails at `resolve_port`: `unknown port name 'netmaker' (not in discovered port map)`, and the log shows `discovered 0 port` — the map is *entirely* empty, not just missing one name. Each failure tears down the controller session, so OVS reconnects and fails again — **~40 reconnects per 200 log lines, continuously since boot**, meaning no static flow is reliably installed. `205b97e2` (PortDesc request 8→16 bytes for OVS 3.7, plus not treating PortStatus as end-of-discovery) is deployed and did not fix it; suspect the *reply* parser (`controller.rs` ~249) walks fixed-stride records while OF1.4/1.5 made `ofp_port` variable-length. **Workaround needing no code change:** `resolve_port` returns bare integers directly, so writing flows with numeric `in_port` bypasses discovery entirely; `ofport_request` pins numbers so they are *defined* rather than assigned (set for pub0=1, svc0=2, eth0=4, 3tched=121). **Separately, `pub0` and `eth0` carry the SAME MAC** (`36:f1:18:56:c4:6a`) on one bridge — the FDB entry flaps between ports 1 and 4, which is a strong candidate for intermittent drops, and is the class of problem port-pair flows (`OF_FLOW_PAIRS`, currently exported empty) exist to solve. | `crates/op-network/src/controller.rs` | open |
| 2026-08-15 | Opus 5 | **netclient could not reach its MQTT broker because `servers.json` pointed at the gRPC port — and "fixing" it to the canonical mesh URL created a bootstrap deadlock.** `Broker = ws://127.0.0.1:8090/mqtt`, but on this host `127.0.0.1:8090` is **op-grpc-bridge** (a WS upgrade there returns `grpc-status: 12`, UNIMPLEMENTED). The broker is EMQX in the NetMaker CT, reachable at `127.0.0.1:8083` → `broker.sock` → CT `:8090` — a WS upgrade there returns `101 Switching Protocols, sec-websocket-protocol: mqtt, server: Cowboy`. Consequence chain: no broker → no node config applied → **no `100.69.0.1` on the mesh iface** → `fwd-nm-mesh-8090` blocked forever on `until ip -4 addr show netmaker | grep 100.69.0.1/` → netclient's own `:53` DNS listener failed to bind → `config_linux.go` "no listener is running" errors every few minutes. **Trap worth recording: `emqx.rs`'s `MQTT_WS_URL = "ws://100.69.0.1:8090/mqtt"` is wrong on both counts for this host** — 8090 is the gRPC plane, and pointing netclient at the *mesh* address deadlocks on cold start, because the broker then lives at an address netclient itself must assign. I hit that live: it worked only while I had added the address by hand, then failed on the next restart. Loopback (`ws://127.0.0.1:8083/mqtt`) has no bootstrap dependency and is correct for the host's own client; the mesh address is for peers. Also: netclient here is **v1.1.0 against a v1.6.0 server** while both other nodes are current, and it repeatedly logged "adding addresses to netmaker interface" without the address sticking — it adds then flushes mid-setup. Moot now that the host runs no WG, but relevant if netclient is ever reinstated. | `/etc/netclient/servers.json` | fixed |
| 2026-08-15 | Opus 5 | **svc0 + grpc0 being SEPARATE interfaces is the drift; both addresses belong on one port so gRPC, HTTP and inbound are reachable without an extra hop.** Consolidated into a single OVS internal port `3tched` carrying `10.0.0.2/24` **and** `10.200.0.2/24` at MTU 1420 (matching `BRIDGE_MTU`); svc0/grpc0 left present but stripped of IPv4. All 15 services that reference them bind *addresses*, not interface names, so they kept working across the move — verified `10.0.0.2:8081`, `:8083`, `:443` and the mesh relays. Corroboration that this is intent rather than my invention: **`fwd-443/run` already waits on `until ip -4 addr show dev 3tched | grep 10.0.0.2/`** — a service definition written for the consolidated port before I touched anything. **NOT persisted:** `/etc/op-dbus/network.conf` still declares `INTERNAL_PORTS="svc0 grpc0"`, `GRPC_PORT=grpc0`, `BRIDGE_SVC_ADDRS="10.0.0.2/24@svc0"`, so a reboot recreates the drift. It also still carries `NETMAKER_EGRESS_NET`/`NETMAKER_EGRESS_DEV`, which encode "the mesh is an exit" — the opposite of the design ("no snat, no external routing once in place"; NAT is *state*, a conntrack map of who talked to what, and a translating gateway contradicts an "ordinary TLS server" cover). And `NETMAKER_NET=100.69.0.0/16` disagrees with the /24 used everywhere else. Related: the `host-svc0-net` egress resource (`10.0.0.0/24`, `direct_nat`, `status=true`) is assigned to the **decoy**, which is correct-under-constraint, not inverted — the decoy holds *two roles* (decoy + netmaker gateway) because the hardware for a separate gateway does not exist. **A cold reader will see egress-on-decoy while svc0 is on the host and "fix" it; that would break the gateway.** | `/etc/op-dbus/network.conf` | open |
| 2026-08-15 | Opus 5 | **FIXED (supersedes the mark-divergence entry above): the host was missing the `0x51821` ip rule the decoy already had — same mark, mirrored, not a new scheme.** My first attempt was wrong in an instructive way: I changed `nm-warp-egress-proxy.py`'s `EGRESS_MARK` from `0x51821` to `0x33440001` to match the host's newer rule. That worked (exit became `warp=plus`) but was architecturally backwards — it routed the license path into *this host's own* `wgcf-egress`, a second WARP session, rather than the ingress tunnel carrying through to egress. Operator's words: "reversing the ingress warp tunnel, sending warp into itself". **The proxy's `0x51821` was correct all along**; what was missing was the matching rule on this side. Decoy `wgcf-ingress` installs `ip rule add fwmark 0x51821 lookup 51820`; the host's `wgcf-egress` only had `fwmark 0x33440000/0xffff0000 lookup 51820`, so `0x51821` matched nothing and fell through to main → raw public IP. Fix: proxy reverted to `0x51821`, and `ip rule add fwmark 0x51821 lookup 51820 priority 101` added to `wgcf-egress.conf` PostUp/PreDown (backup saved), so host and decoy now use the **same mark, reversed direction**. Verified: `ip route get 1.1.1.1 mark 0x51821` → `dev wgcf-egress table 51820 src 172.16.0.2`; through the proxy `ip=104.28.193.116 loc=DE warp=plus`; host direct still `188.68.58.237 warp=off`; `api.controller.netmaker.io` still reachable (503 to a bare GET, as always). **Generalise: when two nodes implement halves of one path, mirror the existing convention rather than migrating one side to a newer one — the 0x3344 migration updated the rule here and left every consumer, on both boxes, still speaking 0x51821.** | `/etc/wireguard/wgcf-egress.conf`, `nm-warp-egress-proxy.py` | fixed |
| 2026-08-15 | Droid (Factory) | **The gallery generator was teaching models a vocabulary that had not existed for months, and the validator was hand-written to match it — two dead declarations agreeing with each other.** `docs/gallery-gen/json-render-catalog.md` (compiled in with `include_str!`) taught `status_pill`, `kv_pair`, `metric_card`; `validator.rs` hardcoded the same 21 snake_case names and only *warned* on unknown types, so every generated spec was admitted and none could render against the real 49-component catalog. Fixed by exporting the catalog from its single TS declaration (`scripts/export-catalog-schema.mts` → `schemas/json-render/catalog.{schema.json,prompt.md,manifest.json}`, sha256 in the manifest) and compiling the gate from that artifact (`catalog_guard.rs`). The prompt the model reads and the rules the gate enforces are now the same bytes; a test asserts every enforced component appears in the prompt. **Generalise: a derived artifact plus a digest beats a restatement — the digest is what makes staleness detectable instead of silent.** | `crates/op-gallery-gen/src/{catalog_guard,validator,context,spec_stream}.rs`, `schemas/json-render/` | fixed |
| 2026-08-15 | Droid (Factory) | **json-render's model contract is a JSONL RFC-6902 patch stream, not a spec object — we were asking for the one output format their prompt never describes.** `Catalog.prompt()` only emits `standalone`/`inline` patch modes. Adopted it verbatim and added `spec_stream::assemble` to fold the stream into a spec; it refuses anything that would invent content the model did not describe (unsupported op, array gap, non-pointer path) and tolerates prose/fences around the JSONL, since a model that explains itself should not cost a whole inference turn. | `crates/op-gallery-gen/src/spec_stream.rs`, `crates/op-gallery-gen/src/inference.rs` | fixed |
| 2026-08-15 | Droid (Factory) | **`render_session_spec` was written entirely in the retired dialect and ignored its own `session` argument — the one hand-written spec in the tree, held to no gate.** Rewrote it against the real catalog and added a test that validates it for every `SessionState`. The rewrite exposed a real gap rather than papering over it: **the catalog declares no input components** (no toggle, text/number input or button group), so the session panel can only report, and operator controls have to stay on the chat/HTTP path until the catalog grows them. | `crates/op-gallery-gen/src/session.rs` | fixed |
| 2026-08-15 | Droid (Factory) | **Four dashboard nav entries were invisible because sections were declared twice: a `NavSection` union and a `SECTION_ORDER` array, and `buildNavGroups` iterates the array.** A section named by the type but absent from the order had every entry dropped with no error. `NavSection` is now derived from `SECTION_ORDER` (`(typeof SECTION_ORDER)[number]`), so an unlisted section is a compile error. The four entries in question were the `Zerolang` experiment and were removed at the operator's direction — they had no pages and no specs, only nav rows. | `/srv/git/operation-dashboard-ui-07/src/json-render/navigation/manifest.ts` | fixed |
| 2026-08-15 | Droid (Factory) | **`op-plugin-lint`'s `inspector-gadget` feature declared no dependencies while `gadget.rs` uses `op_inspector` and `tokio`, so `--all-features` could not compile the workspace.** Feature now declares `dep:op-inspector` + `dep:tokio`. Same class of break in `op-cognitive-mcp/examples/context_aware_client.rs`: it imported `futures`, which the crate does not depend on — switched to the `tokio_stream::StreamExt` it already has. **Both were invisible to `cargo test --workspace` without `--all-targets --all-features`.** | `crates/op-plugin-lint/Cargo.toml`, `crates/op-cognitive-mcp/examples/context_aware_client.rs` | fixed |
| 2026-08-15 | Droid (Factory) | **The UI that op-web actually embeds (`crates/op-web/ui`) has zero test files — `vitest run` there exits 1 for want of a suite, while the `crates/` dev tree has 7 tests.** Typecheck is clean in both. Worth noting that the *served* app is the untested one. Also latent: `dashboard-stream.ts` types `AuditEventPayload.event_id` as `string` and `decision` as `string`, but the SSE bridge forwards the gRPC `ChainEvent` verbatim, where `event_id` is `uint64` and `decision` is an enum — the payload is cast, so TS never checks it. | `crates/op-web/ui/`, `crates/src/lib/dashboard-stream.ts` | open |

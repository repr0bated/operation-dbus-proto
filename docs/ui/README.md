# UI Docs

The authoritative embedded UI deployment workflow lives here:

- [`docs/operations/op-web-ui-build.md`](/home/jeremy/git/operation-dbus-proto/docs/operations/op-web-ui-build.md)

Use that document for:

- the only allowed UI source of truth
- the embed/build/install sequence
- live verification against `dashboard.3tched.com`

Do not use older ad hoc scripts or alternate static deployment paths.

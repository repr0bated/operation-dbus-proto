# Documentation Maintenance

This book is built with [mdBook](https://rust-lang.github.io/mdBook/).

```bash
cd docs
mdbook serve --open
```

Link checking runs as part of the build through `mdbook-linkcheck`. Broken local
links are treated as errors.

TBD.

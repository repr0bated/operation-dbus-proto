[project]
name = "operation-dbus"
version = "0.1.0"
description = ""
authors = [
    {name = "jeremy",email = "jeremy@mail.3tched.com"}
]
requires-python = ">=3.13"
dependencies = [
    "google-genai",
    "python-dotenv",
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

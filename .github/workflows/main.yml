name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  ai-review:
    runs-on: ubuntu-latest
    steps:
      - name: AI review handled by installed GitHub Apps
        run: |
          echo "AI code review is provided by the following installed GitHub Apps:"
          echo "  - Gemini Code Assist"
          echo "  - Amazon Q Developer"
          echo "  - ChatGPT Codex Connector"
          echo "  - Kilo Code Review"
          echo "No additional workflow-level review needed."

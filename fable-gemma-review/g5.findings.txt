```json
[
  {
    "path": "deploy/chrome-remote-desktop/wayland-no-systemd.patch",
    "line": 114,
    "priority": "P0",
    "title": "Use subprocess.call for systemd check in patch",
    "why": "The patch introduces a call to `subprocess.call(['systemctl', '--user',...])`. The project invariants strictly forbid `Command::new` or subprocesses for live state/environment checks in plugin/service code; while this is a patch for a vendor script, it violates the core architectural principle that the control plane must not rely on external CLI polling/subprocess calls for environment discovery."
  },
  {
    "path": "deploy/chrome-remote-desktop/wayland-no-systemd.patch",
    "line": 131,
    "priority": "P1",
    "title": "Directly spawn portal binaries via subprocess.Popen",
    "why": "The patch uses `subprocess.Popen` to launch `xdg-desktop-portal` and backends. This violates the invariant that the control plane must use D-Bus for service management and interaction, rather than direct process spawning, which bypasses the system's service lifecycle and supervision."
  }
]
```
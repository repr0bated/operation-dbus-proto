[package]
name = "op-cognitive-mcp"
version = "0.1.0"
edition = "2021"

[dependencies]
op-core = { path = "../op-core" }
op-identity = { path = "../op-identity" }
op-blob = { workspace = true }
op-mcp = { path = "../op-mcp" }
op-agents = { path = "../op-agents" }
op-state-store = { path = "../op-state-store" }
op-plugins = { path = "../op-plugins" }
op-dynamic-loader = { path = "../op-dynamic-loader" }
op-cache = { path = "../op-cache" }
op-cozo-store = { workspace = true }
op-blockchain = { path = "../op-blockchain" }
hex = { workspace = true }
memmap2 = { workspace = true }
serde = { version = "1.0", features = ["derive"] }
serde_json = { workspace = true }
simd-json = { workspace = true }
tokio = { version = "1.0", features = ["full"] }
anyhow = { workspace = true }
qdrant-client = "1.17"
reqwest = { workspace = true }
tracing = "0.1"
tracing-subscriber = "0.3"
axum = { version = "0.7", features = ["json", "http2"] }
uuid = { version = "1.0", features = ["v4", "v5"] }
chrono = { version = "0.4", features = ["serde"] }
async-trait = "0.1"
clap = { workspace = true, features = ["env"] }
cozo = { workspace = true }

# gRPC (CognitiveToolService — mounted on op-grpc-bridge :50051)
tonic = { workspace = true }
prost = { workspace = true }

# RAG pipeline
zip = "2"
sha2 = { workspace = true }
regex = { workspace = true }

# Concurrency
dashmap = { workspace = true }
parking_lot = { workspace = true }

# Random for jitter
rand = "0.8"

# SSE streaming
async-stream = "0.3"
tokio-stream = "0.1"
reqwest-eventsource = "0.5"

[[bin]]
name = "rag-ingest"
path = "src/bin/rag-ingest.rs"

[[bin]]
name = "op-cog-admin"
path = "src/bin/op-cog-admin.rs"

[[bin]]
name = "op-chain-vectors"
path = "src/bin/op-chain-vectors.rs"

[build-dependencies]
tonic-build = { version = "0.12" }

[dev-dependencies]
# qdrant-client 1.19 is built against tonic 0.14, while this crate's own gRPC
# surface is on the workspace's tonic 0.12. Both are in the tree; they are
# different types with the same name. Tests that construct a `QdrantError`
# need qdrant's tonic specifically, so it is aliased rather than shadowing the
# `tonic` the rest of the crate means. Dev-only: nothing ships with two tonics.
tonic-qdrant = { package = "tonic", version = "0.14.6" }


| Severity | Issue | Evidence (file:line) | Recommendation |
| :--- | :--- | :--- | :--- |
| Critical | **Remote Code Execution via Command Injection**<br>The unauthenticated HTTP router exposes direct tool execution, and the shell command execution tool formats arguments directly into `sh -c` without sanitization, leading to immediate arbitrary code execution. | crates/op-tools/src/router.rs:125<br>crates/op-tools/src/builtin_old.rs:158 | Implement strong authentication and authorization middleware on the Axum HTTP router. Avoid running shell commands via `sh -c` formatted strings; pass arguments strictly as separate, validated parameters via vector arguments rather than string interpolation. |
| Critical | **Security Check Bypass / Path Traversal in Self-Write Tool**<br>The path containment security check in `self_write_file` is completely bypassed if the parent directory of the target file does not yet exist and `create_dirs` is `true`. An attacker can write arbitrary files anywhere on the system by targeting non-existent parent paths. | crates/op-tools/src/builtin/self_tools.rs:163 | Remove the `if p.exists()` guard from the path validation. Canonicalize and validate that the target directory starts with the canonical repository path *before* checking if the directory exists or attempting to create it. |
| Critical | **Unrestricted Arbitrary File Disclosure**<br>`FileReadTool` reads file paths directly from untrusted input without directory sandboxing or path traversal validation, permitting any client to read sensitive system files. | crates/op-tools/src/builtin_old.rs:219 | Validate that the requested path is canonicalized and restricted to a safe, whitelisted base directory before invoking file read operations. |
| High | **Undefined Behavior and Segfaults via Unpadded simd-json Parsing**<br>The code calls `unsafe { simd_json::from_str }` on raw string values retrieved directly from environment variables. `simd-json` requires input buffers to have trailing padding bytes to perform safe block reads; using it on unpadded strings can cause out-of-bounds memory reads and segmentation faults. | crates/op-tools/src/mcptools.rs:188 | Use safe JSON parsing (`serde_json::from_str`) for environment-based configuration variables where performance is not critical, or guarantee input padding using `simd_json::to_padded_bin`. |
| High | **Denial of Service (Memory Exhaustion) via Large File Reads**<br>`FileReadTool` attempts to read the entire file into memory using `tokio::fs::read` before truncating it to `max_bytes`. If a client requests a virtual file or a massive log file, the service will exhaust available system memory and crash. | crates/op-tools/src/builtin_old.rs:226 | Open the file using `tokio::fs::File::open` and read only up to `max_bytes` using a bounded buffer reader (e.g. `take(max_bytes)`). |
| High | **Privilege Escalation Vector via Hardcoded Homedir Paths**<br>The AnyDesk remote desktop diagnostic tools hardcode paths pointing directly to user `jeremy` (`/home/jeremy/`). Running diagnostic commands under root (e.g., `sudo cp /home/jeremy/.Xauthority /root/.Xauthority`) can allow a local user to hijack system authority. | crates/op-tools/src/builtin/anydesk.rs:348 | Resolve the home directory dynamically using safe libraries such as `dirs::home_dir()` instead of using hardcoded paths. |
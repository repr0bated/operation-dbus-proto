| Severity | Issue | Evidence (file:line) | Recommendation |
| :--- | :--- | :--- | :--- |
| **Critical** | **Shell Command Injection in Remote Cache Streaming** <br><br>The cache backup and synchronization mechanisms (`stream_to_remote` and `receive_from_remote`) construct shell commands via raw string interpolation and execute them inside an unhardened shell wrapper (`bash -c`). If `remote_host`, `remote_path`, or `remote_snapshot` are populated via external input, an attacker can append command chain operators (e.g., `;`, `&&`, `\|`) to execute arbitrary shell commands with the privileges of the control plane process. | `crates/op-cache/src/btrfs_cache.rs:520` <br> `crates/op-cache/src/btrfs_cache.rs:526` <br> `crates/op-cache/src/btrfs_cache.rs:557` <br> `crates/op-cache/src/btrfs_cache.rs:563` | Refactor the functions to eliminate the use of shell spawning (`bash -c`). Execute `tokio::process::Command` directly with the executable binary name (`ssh` or `btrfs`) and pass parameters as a structured, safe array of arguments. |
| **High** | **Memory Corruption and Undefined Behavior via Unpadded In-Place JSON Deserialization** <br><br>The `pattern_tracker` and `workflow_tracker` utilize `unsafe { simd_json::from_str(&mut agent_sequence_json) }` on raw string values retrieved directly from SQLite. `simd-json` requires a padded buffer (`simd_json::PADDING_SIZE` of 32 or 64 bytes) to safely perform vectorized SIMD operations. Passing standard `String` structures without padding forces SIMD registers to read past allocated bounds, causing arbitrary memory access, undefined behavior, or segmentation faults during runtime. | `crates/op-cache/src/pattern_tracker.rs:244` <br> `crates/op-cache/src/workflow_tracker.rs:349` <br> `crates/op-cache/src/workflow_tracker.rs:396` <br> `crates/op-cache/src/workflow_tracker.rs:418` | Either switch to a safe, non-SIMD JSON parser like `serde_json` for processing database rows, or copy the string to a padded buffer using `simd_json::to_padded_container` before invoking unsafe deserializers. |
| **High** | **Schema-as-Code Gap: Ad-Hoc Data Contracts and Manual Parser Matching** <br><br>The codebase defines internal entities like `AgentCapability` and API contracts using ad-hoc, manually written string parsers and ad-hoc builders rather than a unified, versioned schema definition. Similarly, in the Model Context Protocol (MCP) bridge, JSON Schemas are generated procedurally using ad-hoc `serde_json::json!` macros directly embedded in Rust files instead of referencing versioned proto/JSON schemas. This violates strict schema-as-code discipline and increases the risk of serialization drifts. | `crates/op-cache/src/agent_registry.rs:78` <br> `crates/op-cache/src/grpc/mcp_service.rs:260` | Consolidate all capability structures and tool payloads under unified Protocol Buffer definitions (`op_cache.proto`). Automatically generate Rust type translations and schema enforcement models during compile time via `tonic-build` rather than manual mapping. |
| **Medium** | **Complete Absence of OSCAL Security Controls Mapping** <br><br>The `op-cache` crate manages control-plane workloads with high-privilege operations such as shell execution, file system modifications, and database queries. However, there is no mapping of these security-critical capabilities to Open Security Controls Assessment Language (OSCAL) component or profile files. The codebase lacks machine-readable control validation definitions (e.g., NIST SP 800-53 controls for system integrity or boundary protection). | `crates/op-cache/Cargo.toml:1` <br> `crates/op-cache/src/lib.rs:1` | Establish an OSCAL profile in the compliance directory. Define machine-readable system capability components (e.g., mapping subvolume snapshots to NIST SP 800-53 CP-9/Backups and capability resolution to AC-3/Access Enforcement). Implement a static analysis check to assert alignment with codebase controls. |
| **Medium** | **Unauthenticated / Unverified SSH Connections in Remote Operations** <br><br>The cache transfer processes invoke `ssh` to transmit btrfs subvolumes over the network without disabling insecure defaults or specifying cryptographic constraints. If the host system's SSH config is misconfigured or permissive, the processes could blindly connect to unverified destinations, exposing raw cache snapshots (which potentially contain sensitive code, database indexes, and model embeddings) to MitM eavesdropping. | `crates/op-cache/src/btrfs_cache.rs:520` <br> `crates/op-cache/src/btrfs_cache.rs:557` | Harden SSH invocations by explicitly configuring strict SSH options in the argument list, including `-o StrictHostKeyChecking=yes`, explicit `-i <IdentityFile>` private keys, and restricting agent/X11 forwarding. |
| Severity | Issue | Evidence (file:line) | Recommendation |
| :--- | :--- | :--- | :--- |
| **Critical** | **Path Traversal & Sensitive File Disclosure in `ReadFileTool`** | `crates/op-chat/src/tool_loader.rs:333-341` | Resolve and canonicalize the input path via `std::fs::canonicalize` before performing prefix checks, or restrict operations to a strictly controlled sandbox directory. |
| **Critical** | **Arbitrary File Write & Privilege Escalation in `WriteFileTool`** | `crates/op-chat/src/tool_loader.rs:388-394` | Canonicalize the destination path before evaluating prefixes. Ensure that the resolved path strictly starts with an approved non-system base directory. |
| **Critical** | **Remote Code Execution & Whitelist Bypass in `ShellExecuteTool`** | `crates/op-chat/src/tool_loader.rs:424-445` | Remove turing-complete interpreter binaries (such as `python`, `python3`, `node`, `cargo`) from the whitelisted commands. If execution is necessary, implement highly restricted static argument validation. |
| **High** | **Unauthenticated gRPC Endpoint on MCP Server** | `crates/op-chat/src/mcp_server.rs:448-453` | Implement a gRPC interceptor that enforces token-based authentication (such as JWT/OAuth) or mutual TLS (mTLS) on the tonic `Server` builder to prevent unauthorized access. |
| **High** | **Global Tool Registry Pollution & Session Cross-Talk** | `crates/op-chat/src/agent_tools.rs:218-243` | Scope dynamically registered context tools to the individual user session rather than mutating a shared global `ToolRegistry` instance. |
| **Medium** | **Ad-Hoc Data Contracts violating Schema-as-Code discipline** | `crates/op-chat/src/actor.rs:51-124` | Define all external payloads (such as `RpcRequest` and `RpcResponse`) as Protocol Buffer contracts rather than ad-hoc Rust structs serialized directly to JSON. |
| **Medium** | **Lack of OSCAL Compliance Modeling** | `crates/op-chat/Cargo.toml:1-15` | Add an OSCAL component definition schema or generator to programmatically represent compliance profiles for systemd and OVS native tool configurations. |
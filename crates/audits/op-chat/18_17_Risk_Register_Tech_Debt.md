| Severity | Issue | Evidence (file:line) | Recommendation |
| :--- | :--- | :--- | :--- |
| **Critical** | **Remote Code Execution (RCE) via Permissive Command Whitelist** | `crates/op-chat/src/tool_loader.rs:967` | Remove dangerous runtime engines (`python`, `python3`, `node`, `cargo`, `docker`, `kubectl`) from the allowed execution whitelist. Ensure the system utilizes native gRPC/D-Bus interfaces rather than generic shell execution wrappers. |
| **Critical** | **Remote Code Execution (RCE) via Arbitrary File Write & Untrusted Compilation** | `crates/op-chat/src/tool_loader.rs:799`<br>`crates/op-chat/src/orchestration/services/rust_pro.rs:145` | Canonicalize all paths before applying blacklist validation to prevent directory traversal. Avoid running cargo commands on directories that can be written to by untrusted users, as cargo builds can trigger arbitrary command execution via `build.rs`. |
| **High** | **Dangling Channel State Preventing gRPC Agent Dispatching** | `crates/op-chat/src/grpc_client.rs:112` | Update the `connect` method to store the established `Channel` or load-balanced client pool into `self.channel` instead of keeping it in a discarded local variable. |
| **High** | **Compilation Failure due to Semicolon and Undefined Variable** | `crates/op-chat/src/hybrid_executor.rs:107` | Bind the result of the `simd_json::from_str` evaluation to `args` (e.g., `let args = if ...`) rather than discarding the value with a semicolon and attempting to return an undeclared identifier. |
| **High** | **Compilation Failure due to Duplicate Helper Functions** | `crates/op-chat/src/tool_loader.rs:47` | Delete the duplicate `register_tool` definition. Maintain a single version of the helper function with the correct fields corresponding to the defined `ToolDefinition` struct. |
| **High** | **Undefined Behavior and Memory Corruption via Unpadded `simd-json` Parsing** | `crates/op-chat/src/nl_admin.rs:144`<br>`crates/op-chat/src/hybrid_executor.rs:102` | Do not use `unsafe simd_json::from_str` on unpadded `String` allocations. Use the safe, non-destructive string parsing alternatives or ensure the input buffer is padded with `simd_json::SIMDJSON_PADDING` bytes as required by the SIMD instructions. |
| **High** | **Race Condition and Cross-Session Data Leak via Global Response Accumulator** | `crates/op-chat/src/forced_execution.rs:125` | Refactor the response accumulator to be session-isolated and stored within the active session context instead of utilizing a shared global static singleton. |
| **Medium** | **Path Traversal Vulnerability in Read/Write File Tools** | `crates/op-chat/src/tool_loader.rs:737` | Resolve paths to their canonical form using `std::fs::canonicalize` before validating them against `forbidden_paths` or `forbidden_prefixes`. |
| **Medium** | **Session Hijacking via Unauthenticated User-Supplied Session IDs** | `crates/op-chat/src/session.rs:140`<br>`crates/op-chat/src/router.rs:99` | Enforce cryptographic validation of session tokens and bind them to the authenticated WireGuard peer public key or authentication gateway instead of trusting client-supplied JSON parameters. |
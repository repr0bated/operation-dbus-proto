| Severity | Issue | Evidence (file:line) | Recommendation |
| :--- | :--- | :--- | :--- |
| **Critical** | Undefined Behavior via `unsafe` `simd_json::from_str` on untrusted WebSocket packets | `crates/op-web/src/websocket.rs:98`<br>`crates/op-web/src/handlers/websocket.rs:71` | `simd-json` requires that the input buffer has `simd_json::PADDING` bytes allocated at the end. Calling `unsafe` `from_str` on unpadded WebSocket messages can cause out-of-bounds reads or segmentation faults. Replace with safe `serde_json::from_str` or ensure the raw string is copied into a padded buffer before parsing. |
| **Critical** | Remote Memory Corruption via `unsafe` `simd_json::from_str` on arbitrary LLM outputs | `crates/op-web/src/orchestrator/parsing.rs:31`<br>`crates/op-web/src/orchestrator/parsing.rs:74`<br>`crates/op-web/src/orchestrator/parsing.rs:91` | LLM outputs are highly dynamic and completely untrusted. Parsing them with `unsafe simd_json::from_str` without strict bounds or padding validation creates an exploitable memory corruption vector. Switch to standard safe JSON parsers like `serde_json`. |
| **Critical** | Undefined Behavior in D-Bus query parsing | `crates/op-web/src/state_manager_client.rs:33` | Parsing state payloads returned from a system D-Bus proxy using `unsafe simd_json::from_str` can trigger memory corruption if the payload contains malformed data or lacks the necessary padding byte layout. Use safe deserializers or parse into an owned padded buffer. |
| **High** | Hardcoded bypassing API keys in production middleware | `crates/op-web/src/middleware/security.rs:16` | Bypassing IP-based security zones via hardcoded plaintext tokens (`BYPASS_API_KEYS`) in the source code leaks credentials and creates a backdoor to `AccessZone::TrustedMesh`. Move these API keys to environment variables or a cryptographically secure key vault. |
| **High** | Command injection and privilege escalation through external binary shell-outs | `crates/op-web/src/handlers/status.rs:196`<br>`crates/op-web/src/handlers/logs.rs:35`<br>`crates/op-web/src/handlers/mail.rs:30` | Spawning shell subprocesses for `doas dinitctl`, `tail`, `wg`, and `incus` presents injection vulnerabilities and relies on host binary existence. Interact with services natively using zbus/D-Bus APIs instead of calling CLI shell utilities. |
| **Medium** | Under-padded deserialization of local configuration files | `crates/op-web/src/groups_admin.rs:53`<br>`crates/op-web/src/users.rs:104` | Reading JSON configuration files from disk and directly feeding them to `unsafe simd_json::from_str` bypasses padding safety invariants. If local files are tampered with or truncated, this can lead to memory unsafety. Use safe deserialization methods. |
| **Medium** | Information disclosure of local developer environment paths | `crates/op-web/src/system_prompt_loader.rs:18` | Compile-time absolute paths like `/home/jeremy/git/gemini-op-dbus/...` leak sensitive developer names and internal repository structures. Rely exclusively on relative workspace paths, configuration variables, or embedded assets. |
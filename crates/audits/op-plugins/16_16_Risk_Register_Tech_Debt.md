| Severity | Issue | Evidence (file:line) | Recommendation |
| :--- | :--- | :--- | :--- |
| **Critical** | **Undefined Behavior via Unsafe Simd-JSON Deserialization**<br>Use of `unsafe { simd_json::from_str(...) }` on raw, mutable strings loaded from dynamic configuration files. `simd_json` relies on strict memory alignment and padding guarantees. If these inputs contain malformed UTF-8, null bytes, or are mutated concurrently, it can trigger memory corruption and undefined behavior. | `crates/op-plugins/src/state_plugins/config.rs:56`<br>`crates/op-plugins/src/state_plugins/privacy_routes.rs:54`<br>`crates/op-plugins/src/state_plugins/mcp.rs:172`<br>`crates/op-plugins/src/state_plugins/ovsdb_bridge.rs:163` | Replace `unsafe { simd_json::from_str(...) }` with the safe `simd_json::from_slice` API on owned, padded byte buffers (`simd_json::to_vec` / `from_slice`), ensuring strict alignment guarantees are met. |
| **High** | **Tokio Thread Deadlock via Synchronous `blocking_read`**<br>Invoking `blocking_read()` on an `AsyncRwLock` inside a synchronous execution pathway (`state_hash`). If the lock is contested by an active write lock on a tokio-managed executor thread, this blocks the entire OS worker thread, leading to scheduler starvation and immediate deadlocks. | `crates/op-plugins/src/dynamic_loading.rs:403` | Never mix `blocking_read()` with asynchronous code paths on the same lock. Convert `state_hash` to be an asynchronous method (`async fn state_hash`), or use a synchronous lock library like `parking_lot::RwLock` for fields that do not require async scheduling. |
| **High** | **Arbitrary Command Execution & Path Traversal**<br>Unsanitized inputs (such as `golden_image_name` and `storage_path`) derived from potentially untrusted configurations are passed directly to OS shell commands (`btrfs subvolume snapshot`, `btrfs subvolume create`). Attackers can leverage directory traversal sequences (e.g., `../../`) to manipulate arbitrary files on the host filesystem. | `crates/op-plugins/src/dynamic_loading.rs:172`<br>`crates/op-plugins/src/dynamic_loading.rs:185`<br>`crates/op-plugins/src/state_plugins/lxc.rs:242`<br>`crates/op-plugins/src/state_plugins/lxc.rs:401` | Strictly validate all dynamic path components using canonicalization. Reject any paths containing directory traversal segments. Where possible, utilize programmatic system APIs rather than spawning external shell processes. |
| **Medium** | **Cryptographic Footprint Forgery via Weak MD5 Hashing**<br>Using MD5 to compute current and desired hashes for "automatic hash footprints for snowball audit trails" exposes the system to preimage and collision attacks. Attackers can forge system configurations that produce matching hashes, invalidating the integrity of the audit trail. | `crates/op-plugins/src/auto_create.rs:92`<br>`crates/op-plugins/src/state_plugins/config.rs:126`<br>`crates/op-plugins/src/state_plugins/dnsresolver.rs:171`<br>`crates/op-plugins/src/state_plugins/incus.rs:356`<br>`crates/op-plugins/src/state_plugins/login1.rs:98`<br>`crates/op-plugins/src/state_plugins/net.rs:710`<br>`crates/op-plugins/src/state_plugins/privacy_router.rs:715` | Upgrade the audit trail hashing algorithm from MD5 to a cryptographically secure function, such as SHA-256 (`sha2::Sha256`), to ensure the collision resistance of configuration state footprints. |
| **Medium** | **Clone Abuse & Read-Lock Contention on JSON State**<br>Frequent deep cloning of heavy, nested JSON `Value` states while holding active read locks. This practice introduces significant latency overhead and memory allocation spikes under high-concurrency DBus polling conditions. | `crates/op-plugins/src/auto_create.rs:82`<br>`crates/op-plugins/src/builtin.rs:47`<br>`crates/op-plugins/src/dynamic_loading.rs:114`<br>`crates/op-plugins/src/state_plugins/privacy_routes.rs:94` | Redesign the state query architecture to return shared references wrapped in thread-safe reference counters (`Arc<Value>`), or minimize lock duration by copying data out of the lock context only when necessary. |
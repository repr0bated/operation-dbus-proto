1. **Enforce Schema-as-Code with Versioned Protobuf/OSCAL Structures** | Rather than performing ad-hoc string containment checks (`schema_str.contains("email")`) and unchecked traversals on raw `serde_json::Value` models, the compliance engine should parse incoming manifests into strongly-typed structures generated from versioned Protocol Buffers or standardized OSCAL models. Ad-hoc string parsing is prone to security bypasses (e.g., casing anomalies, substring confusion) and violates the codebase's schema-as-code design. | `crates/op-compliance/src/lib.rs:43`

2. **Cache Compiled JSON Schema Using OnceLock** | The metachain schema file `opdbus-plugin-schema.json` is loaded, parsed, and compiled on every single call to `LawFirm::review_schema` via `JSONSchema::compile`. Schema compilation is highly resource-intensive. Loading the schema once and storing it in a static `std::sync::OnceLock<JSONSchema>` will eliminate redundant allocations and parsing overhead. | `crates/op-compliance/src/lib.rs:77`

3. **Refactor Hardcoded Attorney List into Extensible Trait-Based Registry** | The `review_schema` function contains hardcoded sequential calls to each validator struct (`OliviaScal`, `EugeneRisk`, etc.). Refactoring these structs to implement a common `ComplianceRule` or `ComplianceAttorney` trait allows for dynamic registration of compliance suites (e.g., adding local sovereign laws or enterprise rules) without modifying the core `LawFirm` execution engine. | `crates/op-compliance/src/lib.rs:89`

4. **Improve API Ergonomics with Parsed Value Inputs** | `LawFirm::review_schema` takes a raw string slice `&str`, forcing a JSON deserialization operation internally. Downstream callers that have already loaded or dynamically generated the JSON structure in memory are forced to convert it back to a string, resulting in double parsing. Providing an overloaded function or accepting a structured type prevents this performance tax. | `crates/op-compliance/src/lib.rs:73`

5. **Incorporate Structured Telemetry and Instrumented Tracing Spans** | The compliance engine performs complex validations but does not establish structured tracing spans. Adding tracing instruments (e.g., `#[tracing::instrument(skip(schema_json))]`) and emitting structured log fields containing the plugin name, version, and specific rule failures (rather than flat error strings) would make debugging container security pipeline failures vastly simpler in production. | `crates/op-compliance/src/lib.rs:73`

6. **Inherit Dependency Versions from Workspace Cargo Manifest** | The local manifest for `op-compliance` hardcodes `jsonschema = "0.18"` and standard dependency specifications. This ignores the root `Cargo.toml` where modern features and centralized versioning (`jsonschema = "0.29"`) are managed. Transitioning the crate manifest to inherit dependencies (`jsonschema.workspace = true`) ensures workspace-wide version alignment and avoids dual-compiling multiple versions of the same libraries. | `crates/op-compliance/Cargo.toml:8`
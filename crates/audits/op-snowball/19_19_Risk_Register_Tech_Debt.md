| Severity | Issue | Evidence (file:line) | Recommendation |
| :--- | :--- | :--- | :--- |
| **Critical** | **Remote Code Execution (RCE) via Shell Command Injection** | `crates/op-snowball/src/snowball.rs:310`<br>`crates/op-snowball/src/streaming_snowball.rs:475`<br>`crates/op-snowball/src/streaming_snowball.rs:498` | Completely avoid executing commands via a shell interpreter (`sh -c` or `bash -c`) with string interpolation of remote hostnames, paths, or replica names. Instead, invoke the binaries (`ssh`, `btrfs`, `tee`) directly using `tokio::process::Command` and pass arguments as separate elements in an argv slice to prevent the shell from executing metacharacters. |
| **High** | **Undefined Behavior (UB) & Memory Corruption via Unsafe `simd_json::from_str` on Standard Strings** | `crates/op-snowball/src/btrfs_numa_integration.rs:141`<br>`crates/op-snowball/src/snowball.rs:268`<br>`crates/op-snowball/src/streaming_snowball.rs:328` | Do not use `simd_json::from_str` inside an `unsafe` block on standard `String` values returned by `tokio::fs::read_to_string` because they lack the page-alignment and padding guarantees (`simd_json::PADDING`) required by `simd-json`'s SIMD-vectorized parsing routines. Parse using safe parsing alternatives or read data into a padded byte vector before invoking `simd_json::from_slice`. |
| **High** | **Schema-as-Code Violations: Untyped/Ad-Hoc JSON Audit Fields** | `crates/op-snowball/src/footprint.rs:11`<br>`crates/op-snowball/src/footprint.rs:52`<br>`crates/op-snowball/src/plugin_footprint.rs:11`<br>`crates/op-snowball/src/snowball.rs:260` | Transition data contracts away from unstructured `simd_json::OwnedValue` or arbitrary `HashMap` instances. Define strict, versioned schemas using Protocol Buffers (via `prost`) or OSCAL templates. This establishes a robust schema-as-code discipline and prevents audit log parse failures due to contract drift. |
| **Medium** | **Insecure Temporary File Generation with Predictable Names & Race Conditions** | `crates/op-snowball/src/streaming_snowball.rs:294`<br>`crates/op-snowball/src/streaming_snowball.rs:311` | Avoid hardcoding predictable temporary filenames like `.current.json.tmp` inside the subvolume directories. Use the `tempfile` crate (which is already a workspace dependency) to generate cryptographically random, collision-resistant temporary file paths. |
| **Medium** | **Lack of Backpressure and Memory Exhaustion Risk via Unbounded Channels** | `crates/op-snowball/src/btrfs_numa_integration.rs:200`<br>`crates/op-snowball/src/streaming_snowball.rs:338` | Replace `tokio::sync::mpsc::UnboundedReceiver` with a bounded channel receiver (`tokio::sync::mpsc::Receiver`). Under high control-plane event volumes, this ensures backpressure is propagated to producers rather than leading to Out-Of-Memory (OOM) panic conditions. |
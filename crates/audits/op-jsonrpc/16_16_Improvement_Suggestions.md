1. **ARCHITECTURE: Consolidation of Redundant OVSDB Client Implementations** | The codebase contains three separate files implementing duplicate or overlapping OVSDB JSON-RPC client modules with minor variations in `rpc_call` logic (e.g., line-oriented socket reading vs. full socket teardown reads). Bundling multiple diverging clients leads to maintenance drift, protocol non-conformance, and subtle runtime bugs. These client implementations should be unified and abstracted into a dedicated sub-crate (e.g., `op-ovsdb-proto`) or a single codebase module. | `crates/op-jsonrpc/src/ovsdb_jsonrpc.rs:11` / `crates/op-jsonrpc/src/ovsdb.rs:14` / `crates/op-jsonrpc/src/ovsdb_rpc_call.rs:1`

2. **ARCHITECTURE: Formalize Schema-as-Code for NonNet State** | The `NonNetDb` module relies on dynamic, run-time heuristics (`infer_columns` and `infer_type`) to build schema definitions on the fly from arbitrary JSON objects. This ad-hoc construction breaks the schema-as-code discipline, as there is no versioned, compile-time contract (like Protocol Buffers or JSON Schemas) protecting downstream callers from structural changes in dynamic plugin data. Data contracts should be declared as versioned schemas in `op-dbus-model` or generated via `prost` instead of dynamic runtime type inference. | `crates/op-jsonrpc/src/nonnet.rs:319`

3. **ARCHITECTURE: Strongly-Typed OVSDB Transaction Builders** | OVSDB transactions such as `create_bridge`, `add_port`, and `delete_bridge` construct deep, untyped JSON structures via the `json!` macro. This prevents compile-time safety and makes it very easy to generate schema-incompatible payload mutations (e.g., malformed `set` or `named-uuid` tuples). Defining strongly typed Rust structs that mirror OVSDB schema tables (RFC 7047) and using a query-builder pattern will eliminate runtime transaction syntax errors. | `crates/op-jsonrpc/src/ovsdb.rs:193`

4. **API ERGONOMICS: Typestate Pattern / Builder for Server Configuration** | `JsonRpcServerConfig` allows fields like `unix_socket` and `tcp_addr` to be initialized as `None` simultaneously. Running a server under this configuration is invalid but only fails implicitly or runs with no listeners. Implementing a builder pattern utilizing typestate (e.g., `ServerBuilder<NoTransport>`, `ServerBuilder<UnixConfigured>`) ensures at least one valid transport is statically configured before `run()` can be invoked. | `crates/op-jsonrpc/src/server.rs:24`

5. **API ERGONOMICS: Strongly-Typed Domain Errors** | Errors are heavily formatted via `anyhow::anyhow!` or string formatting directly within transaction methods. This forces downstream callers to inspect string slices to diagnose specific failure conditions (e.g., missing bridge, duplicate port). Replacing generic error types with a granular, strongly typed enum deriving `thiserror::Error` (e.g., `OvsdbError::BridgeNotFound(String)`) enables structured, programmatic error recovery. | `crates/op-jsonrpc/src/ovsdb.rs:69`

6. **PERFORMANCE: Connection Pooling and Framing Codec for OVSDB Client** | Every invocation of `rpc_call` in `OvsdbClient` establishes a fresh `UnixStream`, writes the payload, and executes `stream.shutdown(Shutdown::Write)` to signal EOF because OVSDB responses may not be reliably newline-terminated. Establishing and tearing down connections for every RPC query causes immense descriptor churn and socket overhead. Transitioning to a persistent connection pool with a length-delimited or brace-matching JSON frame decoder (using `tokio_util::codec`) would cut query latency drastically. | `crates/op-jsonrpc/src/ovsdb.rs:45`

7. **PERFORMANCE: Zero-Copy Borrowed Value Parsing** | The Unix/TCP connection loops read JSON-RPC lines into owned allocations and repeatedly perform owned JSON parsing via `simd_json::from_str::<Value>`. This allocates full AST structures on the heap for every request. By using `simd_json` lifetime-bound borrowed values (`BorrowedValue`) or reusing buffers via `Bytes` and `Arc<str>`, the server can achieve near-zero allocation overhead during high-frequency status queries. | `crates/op-jsonrpc/src/server.rs:221`

8. **OBSERVABILITY: Structured Tracing Spans for Server Connections** | Connection management and connection handlers lack structured tracing instrumentation. The current `handle_unix_connection` and `handle_tcp_connection` loops process arbitrary requests sequentially, making it impossible to correlate log messages with a specific connection context, socket descriptor, or authenticated client session. Spanning connections with `tracing::instrument(skip(self, stream))` would attach crucial connection-specific metadata to all diagnostic events. | `crates/op-jsonrpc/src/server.rs:162`

9. **STORAGE: Utilize CozoDB/Sled Backend for NonNet Query Routing** | `NonNetDb` simulates an OVSDB database layout on top of an in-memory `HashMap<String, Vec<Value>>` and executes linear scans (`select` transactions) over unstructured arrays. Since `cozo` (with its pure-Rust embedded `storage-sled` backend) is already a workspace dependency, transitioning this state manager to a lightweight Cozo relational database would provide proper indexing, relational queries, and ACID transactional integrity out-of-the-box instead of relying on ad-hoc search logic. | `crates/op-jsonrpc/src/nonnet.rs:51`
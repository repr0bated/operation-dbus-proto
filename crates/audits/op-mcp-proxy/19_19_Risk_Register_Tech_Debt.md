| Severity | Issue | Evidence (file:line) | Recommendation |
| :--- | :--- | :--- | :--- |
| **Critical** | **Heap Out-of-Bounds Read via Unsafe `simd-json` Parsing on Unpadded Buffers** | `crates/op-mcp-proxy/src/main.rs:105`<br>`crates/op-mcp-proxy/src/cloudaicompanion.rs:475`<br>`crates/op-mcp-proxy/src/cloudaicompanion.rs:506`<br>`crates/op-mcp-proxy/src/cloudaicompanion.rs:531`<br>`crates/op-mcp-proxy/src/cloudaicompanion.rs:561` | Replace `unsafe { simd_json::from_str(...) }` with the safe `simd_json::from_str` API, or ensure the input buffer is explicitly padded using a padded allocation strategy (such as `simd_json::to_padded_string`). Standard Rust `String` objects generated by `BufRead::lines()` or `std::fs::read_to_string` lack the SIMD padding required by the unsafe parser, directly triggering heap out-of-bounds reads. |
| **High** | **Process Crash (SIGBUS) via Unsafe Shared Memory Mapping** | `crates/op-mcp-proxy/src/sled.rs:45` | Replace memory mapping of `/dev/shm` files with standard, robust file read operations (e.g., `std::fs::read`). Because `/dev/shm` is shared across local processes, any concurrent truncation of `/dev/shm/plugin_schema.dat` by another process will cause the memory map to instantly trigger a `SIGBUS` signal and crash the proxy. |
| **High** | **Rate Limiter Thundering Herd and Backend Encroachment** | `crates/op-mcp-proxy/src/http_server.rs:141` | Modify the throttling flow to acquire a token *after* waking up from the rate-limit delay, or reject requests when the limit is exceeded instead of sleeping and proceeding. Currently, if `try_consume()` fails, the handler simply sleeps for the delay and then executes the downstream API call anyway, bypassing the rate limit entirely and causing a thundering herd. |
| **High** | **Schema-as-Code Violation: Ad-hoc Structured Data & Fragile Byte Offsets** | `crates/op-mcp-proxy/src/sled.rs:14`<br>`crates/op-mcp-proxy/src/session.rs:23`<br>`crates/op-mcp-proxy/src/session.rs:55` | Define shared IPC states and database schemas using Protocol Buffers or FlatBuffers rather than raw byte offsets and ad-hoc structs. This decouples the memory layout of `op-mcp-proxy` from `op-identity` and prevents silent memory corruption or parsing crashes if either codebase updates its struct layout. |
| **Medium** | **Key Extraction from Plaintext Configurations without Access Checks** | `crates/op-mcp-proxy/src/gcloud_auth.rs:323`<br>`crates/op-mcp-proxy/src/cloudaicompanion.rs:596` | Use a secure OS-level credential vault (e.g., through the `keyring` crate) to store access and refresh tokens. If reading from plaintext files is required, check the file permissions first (e.g., ensuring Unix permissions are restricted to `0600`) to prevent unauthorized local users from reading Google Cloud keys. |
| **Medium** | **JSON-RPC Protocol Compliance Failure via Integer ID Mappings** | `crates/op-mcp-proxy/src/main.rs:144` | Handle the `id` field using type-agnostic JSON-RPC representations. JSON-RPC allows integer IDs (e.g., `"id": 123`). Mapping via `.as_str()` fails on integers, falling back to `"null"`, which breaks request-response correlation on compliant JSON-RPC clients. |
| **Medium** | **Thundering Herd / Cache Stampede on Expired Token Refresh** | `crates/op-mcp-proxy/src/session.rs:185` | Implement single-flight token refreshing (e.g., using a shared future or coordination primitive) to ensure that only one concurrent task requests a new OAuth token from Google when the cache expires. |
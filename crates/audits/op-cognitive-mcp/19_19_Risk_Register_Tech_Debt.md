# Production Security and Quality Audit Report

| Severity | Issue | Evidence (file:line) | Recommendation |
| :--- | :--- | :--- | :--- |
| **Critical** | **Remote Arbitrary File Read & Directory Traversal via `add_folder`** | `crates/op-cognitive-mcp/src/grpc_service.rs:564`<br>`crates/op-cognitive-mcp/src/grpc_service.rs:920` | Restrict directory walking to a pre-configured sandbox path. Canonicalize `req.folder_path` and assert that the canonicalized path starts with the canonicalized sandbox prefix. Ensure that authentication/authorization checks are enforced on all gRPC endpoints. |
| **Critical** | **Out-Of-Bounds Memory Read & Crash Risk (Segfault) on Mmap'd Sled** | `crates/op-cognitive-mcp/src/interceptor.rs:36` | Ensure that the length of the memory map is verified before casting. Check that `mmap.len() >= std::mem::size_of::<IdentitySled>()` and return a gRPC precondition error if the file size is insufficient or if `/dev/shm/plugin_schema.dat` is truncated. |
| **High** | **Severe Shared-Memory ABI Structural Layout Mismatch** | `crates/op-cognitive-mcp/src/qdrant_shuttle.rs:32`<br>`crates/op-cognitive-mcp/src/interceptor.rs:5` | Converge the workspace on a single canonical definition of `IdentitySled` (e.g. within `op-core` or `op-identity`) rather than declaring duplicate, structurally mismatched `#[repr(C)]` structs in separate modules. |
| **High** | **Unbounded In-Memory Session Storage (Memory Leak / DoS via OOM)** | `crates/op-cognitive-mcp/src/session.rs:65` | Introduce a Time-To-Live (TTL) expiration policy or an LRU-cache mechanism for the `DashMap` sessions. Implement a background cleanup routine to evict inactive sessions after a configured duration (e.g., 1 hour). |
| **High** | **OSCAL Compliance Bypass via Mocked Control Source Verification** | `crates/op-cognitive-mcp/src/interceptor.rs:57` | Implement cryptographically sound policy checks on the OSCAL control source (e.g., checking a digital signature or comparing it against a verified ledger state) rather than solely decoding and logging the bytes as debug text. |
| **High** | **Schema-as-Code Violation: Ad-hoc PII Metadata Matching** | `crates/op-cognitive-mcp/src/activity_filter.rs:164` | Refactor PII detection to rely on a versioned schema definition where sensitive fields are explicitly flagged with a structured `pii: true` Boolean or specialized metadata tag, rather than parsing unstructured descriptions for case-insensitive substrings. |
| **High** | **Schema-as-Code Violation: Ad-hoc Tool Input Schema Definitions** | `crates/op-cognitive-mcp/src/cognitive_tools.rs:74` | Load the MCP input schemas from versioned, canonical Protocol Buffer or OSCAL schema files rather than generating them programmatically using the in-memory `json!` macro. |
| **High** | **Schema-as-Code Violation: Ad-hoc Parsing of Diagnostic Query History** | `crates/op-cognitive-mcp/src/grpc_service.rs:904` | Modify `get_query_history` to return strongly-typed `QueryHistoryEntry` structs directly, eliminating the fragile deserialization and string-lookup of unstructured JSON fields. |
| **Medium** | **High-Overhead Double Serialization & Deserialization Performance Bottleneck** | `crates/op-cognitive-mcp/src/cognitive_tools.rs:272` | Implement a direct zero-copy conversion layer or define unified traits that operate natively on both `simd_json::OwnedValue` and `serde_json::Value` without round-tripping through `String`. |
| **Medium** | **Permanent Tool Denial-Of-Service on Stdio Client Sidecar Crash** | `crates/op-cognitive-mcp/src/notebooklm.rs:218` | Implement sidecar process monitoring and resurrection inside the retry loop. If `call_tool` returns a connection-lost or transport error, restart the external client process by calling `client.start()` before retrying. |
| **Medium** | **Incorrect File and Chunk Metrics Tracking Logic** | `crates/op-cognitive-mcp/src/rag_pipeline.rs:194` | Move `stats.files_parsed += 1` outside the chunk-building loop (triggering only when a `<file path="...">` tag is successfully parsed). Increment `stats.chunks_created += 1` once per yielded chunk rather than multiplying by the file total on each iteration. |
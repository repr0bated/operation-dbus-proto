{
  "mcpServers": {
    "cognitive": {
      "command": "/usr/local/bin/op-mcp-server",
      "args": ["--stdio", "-m", "cognitive"],
      "type": "stdio",
      "_comment": "Single local MCP adapter into the bridge-owned cognitive runtime"
    }
  }
}

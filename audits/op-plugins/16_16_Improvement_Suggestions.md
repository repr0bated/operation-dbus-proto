1. **Suggestion**: Define LLM chat data contracts as versioned Protocol Buffers or OSCAL schemas rather than ad-hoc Rust structs with Serde annotations. | **Rationale**: Defining data contracts as ad-hoc structs violates the workspace's schema-as-code discipline. It makes cross-language client/agent generation fragile and complicates contract versioning over time. | **Example**: `crates/op-plugins/src/chat.rs:13`

2. **Suggestion**: Transition the ad-hoc `AgentConfig` and `AgentConfigState` structs to compiled, versioned Protocol Buffers. | **Rationale**: Allocating LLM agent metadata and tools using ad-hoc Serde structs prevents central schema validation, limits the ability to dynamically audit configurations, and hinders compliance automation. | **Example**: `crates/op-plugins/src/state_plugins/agent_config.rs:10`

3. **Suggestion**: Refactor `PrivacyRoute` and `PrivacyRoutesState` to be generated from a compiled schema registry (e.g., Protobuf) instead of ad-hoc structs. | **Rationale**: Privacy routes manage sensitive user-identity and network-state combinations. Relying on ad-hoc structs makes verifying security constraints across different workspace services difficult. | **Example**: `crates/op-plugins/src/state_plugins/privacy_routes.rs:14`

4. **Suggestion**: Decompose the massive `op-plugins` crate into domain-specific micro-crates (such as `op-plugin-incus`, `op-plugin-openflow`, and `op-plugin-network`). | **Rationale**: The current `op-plugins` crate is overloaded with responsibility. It pulls in many different system CLI tools and protocols (`incus`, `OVSDB`, `rtnetlink`, `dpkg`, `rpm`). Splitting them avoids compile-time overhead, isolates faults, and prevents unnecessary dependencies in minimal target profiles. | **Example**: `crates/op-plugins/Cargo.toml:1`

5. **Suggestion**: Replace type-erased `anyhow::Result` errors in core traits with a specific, strongly-typed domain error enum. | **Rationale**: Forcing callers of `Plugin::get_state` or `StatePlugin::query_current_state` to receive `anyhow::Result` makes programmatic error handling and recovery (e.g., distinguishing a transient connection timeout from a syntax error) extremely difficult. | **Example**: `crates/op-plugins/src/plugin.rs:163`

6. **Suggestion**: Maintain a persistent, pooled D-Bus connection instead of establishing new connections on every single method call. | **Rationale**: In current system integrations, D-Bus connections are instantiated on-demand via `Connection::system().await` or `Connection::session().await` for every operation (e.g., `list_services`, `get_service_status`). This causes heavy socket handshake overhead and latency spikes during high-frequency reconciliation loops. | **Example**: `crates/op-plugins/src/state_plugins/dinit.rs:51`

7. **Suggestion**: Enable zero-copy deserialization in `simd_json` by using lifetime-bounded `&str` or `Cow<'_, str>` references in configuration structs instead of owned `String` fields. | **Rationale**: String values in structs like `McpConfig` are parsed into newly allocated `String` instances on every reconciliation cycle. This results in heavy allocation churn on the heap. | **Example**: `crates/op-plugins/src/state_plugins/mcp.rs:15`

8. **Suggestion**: Enrich external system CLI commands with structured tracing spans containing contextual fields like VMID, bridge name, or transaction ID. | **Rationale**: Current diagnostic messages rely on unstructured logging (`log::info!`) or simple string prints. Transitioning to structured `tracing` spans allows for granular tracing filter controls in production. | **Example**: `crates/op-plugins/src/state_plugins/incus.rs:72`

9. **Suggestion**: Migrate plain JSON file storage to a transactional database backend (such as CozoDB with Sled or SQLite) or perform atomic writes via a temporary file rename pattern. | **Rationale**: Overwriting files directly with `tokio::fs::write` is susceptible to partial writes and data corruption if a power failure or system crash occurs mid-write. Migrating to a transactional store guarantees ACID properties. | **Example**: `crates/op-plugins/src/state_plugins/config.rs:49`
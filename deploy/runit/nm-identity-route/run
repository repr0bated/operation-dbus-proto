#!/bin/sh
exec 2>&1
wait_dep() {
    sv start "$1" >/dev/null 2>&1 || true
    i=0
    until sv check "$1" >/dev/null 2>&1; do
        i=$((i + 1))
        [ "$i" -ge 90 ] && break
        sleep 1
    done
}
wait_dep netclient
exec /usr/local/libexec/3tched/nm-identity-route

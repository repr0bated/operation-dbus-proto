{
  "_comment": "Factory/Droid configuration for the canonical bridge-owned cognitive runtime",
  "_source": "deploy/config/cognitive-mcp-clients.json",
  "version": "2.0.0",
  "mcpServers": {
    "cognitive-mcp-gateway": {
      "name": "cognitive-mcp-gateway",
      "description": "Local MCP compatibility adapter into op-grpc-bridge",
      "command": "/usr/local/bin/op-mcp-server",
      "args": ["--stdio", "-m", "cognitive"],
      "transport": "stdio",
      "enabled": true,
      "localOnly": true,
      "timeouts": { "requestMs": 30000 }
    },
    "filesystem": {
      "name": "filesystem",
      "description": "File system access for reading and writing files",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/home/admin/git/odbus"
      ],
      "enabled": true
    }
  },
  "clientMetadata": {
    "clientType": "droid",
    "clientId": "droid-factory-${SESSION_ID}",
      "source": "factory.ai",
      "endpointRules": {
      "_comment": "No legacy cognitive HTTP door. Native remote clients use authenticated TLS gRPC :8090; MCP-only clients run the stdio adapter on the host.",
      "nativeGrpc": "https://100.69.0.1:8090",
      "loopbackForbidden": [
        "10.200.0.2:3003",
        "127.0.0.1:3003",
        "127.0.0.1:11436",
        "localhost:11436"
      ]
    }
  }
}

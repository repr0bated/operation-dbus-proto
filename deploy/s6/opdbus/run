#!/bin/sh
exec 2>&1
# opdbus previously brought up a direct wg0 tunnel to the oracle. Retired
# 2026-07-11: host WireGuard is netmaker ONLY (control-plane-network owns
# it); this wg0/10.0.0.0/24 tunnel raced netmaker's own route for the same
# subnet. Kept as an inert stub (not removed) because op-dbus/op-projection/
# op-grpc-bridge-zeroclaw/op-dbus-mirror still depend on this service via
# ovsbr0-init/ovsbr0-static in the s6-rc graph.

exec pause

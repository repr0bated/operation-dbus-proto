{
  "mcpServers": {
    "cognitive": {
      "command": "/usr/local/bin/op-mcp-server",
      "args": ["--stdio", "-m", "cognitive"],
      "type": "stdio",
      "_comment": "Compatibility adapter only. Cognitive execution stays in the canonical op-grpc-bridge runtime; this process never opens the cognitive store."
    }
  }
}

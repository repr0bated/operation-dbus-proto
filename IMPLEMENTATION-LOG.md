# Implementation Log — GhostBridge Live!  (auto-resume context)

> Running log so a rate-limit cut resumes cleanly. Update after each step.
> Mode: conserve (terse, batched, log-driven). Rule: don't claim done without live proof.

## Target backlog (from the 3-conversation walk — staged/designed ≠ running)
- [x] **T1** Extract xray REALITY secrets from live static config → `/etc/ghostbridge/xray.env`
- [x] **T2** xray control via the **`xray` state plugin → `/org/opdbus/v1/plugins/xray`** (the ONLY projectable tree). Do NOT register `opdbus.v1.Xray` / `/opdbus/v1/xray` — that out-of-tree name is the ¶6 violation; remove/avoid it. Cutover drives xray through the plugin's `apply_state`, not a separate daemon bus name.
- [x] **T3** Dynamic xray cutover: `run_schema_shuttle` (sled → `/dev/shm/xray-ghostbridge.json`) → diff vs static → flip via the xray plugin, one-command rollback  ← **the "subdomain forwarding" gap**
- [x] **T4** DNS split-horizon: fix `nextdns-srv` to write local resolver (not 1.1.1.1); internal `*.ghostbridge.tech` local, decoy stays decoy
- [ ] **T5** Gemma in xray stack: subid classification + tag routing + subdomain map (single routing brain)
- [ ] **T6** Key-derivation/registration: `session_id` = derived key (currently random `Uuid::new_v4()`); identity vault + user store; magic-link signup
- [ ] **T7** zeroclaw (replace openclaw), chatbot/UI plugin, accountability loop
- [ ] **T8** Voyage vectorization pipeline correctness (re-chunk repomix)

## Verified-live baseline (today, do NOT redo)
- netmaker up (REST :8081, no-NIC, unix sockets, btrfs vols)
- cognitive-mcp up (queries answer); qdrant 839k vectors; cozo backend
- op-grpc-adapters live (Netmaker/Mq/Mail + tonic-web + reflection + identity gate)
- op-mcp-shim built; storage=array btrfs pool; sled = live /dev/shm dir mount in wg-xray; /run/netmaker via tmpfiles.d

## Current state (grounding)
- op-xray-daemon: s6 dir exists, disabled with `down` file, NOT running, NOT on bus
- /etc/ghostbridge/xray.env: present (XRAY_UUID, XRAY_PRIVATE_KEY, XRAY_SHORT_ID, NEXTDNS_PROFILE_ID)
- /etc/ghostbridge/zeroclaw.env: present (ZEROCLAW_GATEWAY_HOST, ZEROCLAW_GATEWAY_PORT, FACTORY_API_KEY)
- /dev/shm/xray-ghostbridge.json: present, generated by `op-identity-shuttle`
- live xray: host `gbr-xray` up on dynamic `/dev/shm/xray-ghostbridge.json`, listening on `*:443` and `*:8443`; `wg-xray` container STOPPED
- host zeroclaw: s6 service up, listening on `0.0.0.0:18789`; assistant `openclaw.service` disabled, proxy removed
- host DNS: resolv.conf → `127.0.0.1`, NextDNS forwards `*.ghostbridge.tech` → `10.200.0.2` (ovsbr0 dnsmasq), verified live
- `10.200.0.1/32` loopback alias added for grpc-uplink

## Progress
- (start) Log created.
- **T2 design (high-effort, done):**
  - xray plugin (`op-plugins/.../xray.rs`) is schema-only: `apply_state` no-op, `query_current_state` static. Real control lives in rogue `op-xray-daemon` (`opdbus.v1.Xray`).
  - Shuttle (`op-identity/schema_bridge.rs:889 start_xray_via_dbus`) uses correct PATH `/org/opdbus/v1/plugins/xray` but rogue DEST/IFACE `opdbus.v1`/`opdbus.v1.Xray` ← the ¶6 violation.
  - **Fix:** move control into `xray.rs::apply_state` (write config @ SHM_XRAY_CONFIG + reload xray); `query_current_state` reports real running pid/config; shuttle drops `start_xray_via_dbus` → direct reload; retire `opdbus.v1.Xray`.
  - Config flows host `/dev/shm/xray-ghostbridge.json` → container via today's live shm mount → xray re-reads on SIGHUP. Pure shm-reactive, no D-Bus round-trip.
- **BLOCKING FORK (user's call):** xray runs in TWO places — host pid 3599 AND container wg-xray pid 181, both on static `/usr/local/etc/xray/config.json`; gbr-xray down. Reload target depends on which is canonical.
  - Recommendation: wg-xray container = canonical (it's the network spine, 10.0.0.2/10.200.0.1); kill host duplicate (3599); reload via `incus exec wg-xray -- pkill -HUP xray`.
- **DECISION (user): collapse wg-xray container → host** (container only existed for the external/gateway addr, no longer needed). Resolves the two-xray fork → one host xray.
- **Migration inventory (grounded, small — NO btrfs send/receive needed; that'd drag a Debian rootfs for a few files):**
  - copy `wg-xray:/usr/local/etc/xray/config.json` → host (canonical config, all routing tags; host currently has NO config file)
  - copy `wg-xray:/etc/ssl/xray/{3tched.com,ghostbridge.tech}.{pem,key}` → host `/etc/ssl/xray/`
  - add `10.200.0.1` alias on host (grpc-uplink; host already has 10.0.0.2 opdbus + 10.200.0.2 ovsbr0)
  - fix `gbr-xray` s6 to run the one supervised host xray; kill container xray(181) + stray host xray(3599); decommission wg-xray
  - simplifications unlocked: sled stays host /dev/shm (no container mount); shuttle writes /dev/shm/xray-ghostbridge.json read directly; reload = host `pkill -HUP xray` (no incus exec)
  - overlay-mask rootfs REJECTED (Debian-over-Artix = breakage risk)
- **T2 code (now simple, host xray):** xray.rs apply_state = write config + host SIGHUP; query_current_state = real pid/config; schema_bridge drops `start_xray_via_dbus` (rogue opdbus.v1.Xray) → direct host reload.
## CONSOLIDATION (move ALL container services → host s6) — full design, grounded
- **Controller (OpenFlow) already host-native** — `op-openvswitch-daemon` + s6 (ovsbr0-init/static, ovsdb-server, ovs-vswitchd, ovs-stack). NOTHING to move. (User assumption it was container-systemd was wrong, in our favor.)
- **Move from wg-xray → host:**
  - xray: copy `/usr/local/etc/xray/config.json` + `/etc/ssl/xray/{3tched.com,ghostbridge.tech}.{pem,key}`; host s6 `gbr-xray` runs `/usr/local/bin/xray run -config /usr/local/etc/xray/config.json`
  - zeroclaw (Rust): binary `/root/.cargo/bin/zeroclaw` (check glibc compat Debian→Artix like rumqttd; else cargo-install on host); host s6 `zeroclaw gateway start`, env ZEROCLAW_GATEWAY_HOST=0.0.0.0 PORT=18789, FACTORY_API_KEY → `/etc/ghostbridge/zeroclaw.env` (NOT unit file)
- **Retire**: assistant `openclaw.service` (Node `/usr/bin/openclaw gateway run`) — zeroclaw supersedes; rewire xray outbound `assistant-openclaw-out` → host zeroclaw `127.0.0.1:18789`
- **Network**: add `10.200.0.1` alias on host (grpc-uplink); host has 10.0.0.2(opdbus)+10.200.0.2(ovsbr0)
- **gemma**: not deployed anywhere → fresh host service (T5), the routing brain (subid+tag+subdomain)
- **Then**: kill container xray(181)+zeroclaw + stray host xray(3599); decommission wg-xray; sled stays host /dev/shm (no container mount); shuttle→/dev/shm/xray-ghostbridge.json read directly; reload=host `pkill -HUP xray`
- **SECURITY**: FACTORY_API_KEY (fk-…) currently in plaintext in the wg-xray unit file — move to secrets + rotate.
- **T2 code (after migration, host xray):** xray.rs apply_state = write config + host SIGHUP; query_current_state = real pid/config; schema_bridge drops rogue `opdbus.v1.Xray` → direct host reload.
- NEXT: write T2 code + `cargo check`; then LOW-effort ops = the file/binary/network migration + retire openclaw + decommission wg-xray.

## 2026-06-16 — T1-T4 host cutover + DNS split-horizon (this session)
- [x] **T1** Extracted xray REALITY secrets from `/etc/xray/config.json` → `/etc/ghostbridge/xray.env` (XRAY_UUID, XRAY_PRIVATE_KEY, XRAY_SHORT_ID, NEXTDNS_PROFILE_ID).
- [x] **T2 verified** `cargo check -p op-plugins` (47 pre-existing warnings) and `cargo check -p op-identity` (1 pre-existing warning) both pass. xray plugin reports `active: true` via `/org/opdbus/v1/plugins/xray` on the private session bus.
- [x] **T3 dynamic cutover + container collapse**
  - Built `op-identity-shuttle` binary; generated `/dev/shm/xray-ghostbridge.json`.
  - Restored `/home/jeremy/.cargo/bin/zeroclaw` from `wg-xray` container binary.
  - Created host s6 `zeroclaw` service (with `zeroclaw-log`) in `deploy/s6/`, symlinked into `/etc/s6/sv`, and integrated into the live s6-rc database.
  - Updated `deploy/s6/gbr-xray/run` to prefer `/dev/shm/xray-ghostbridge.json` with fallback to `/etc/xray/config.json`; recompiled live s6-rc DB.
  - Stopped `wg-xray` container; stopped + disabled assistant `openclaw.service`; removed `openclaw-tcp` incus proxy.
  - Killed old host zeroclaw (deleted binary) and stray host xray; disabled `xray` and `op-xray-daemon` s6 services with `down` files.
  - Added `10.200.0.1/32` loopback alias for grpc-uplink.
  - Live state: `gbr-xray` up on dynamic config, `zeroclaw` up on `0.0.0.0:18789`, xray listening on `*:443` and `*:8443`, `wg-xray` STOPPED.
- [x] **T4 DNS split-horizon**
  - Changed `/etc/resolv.conf` to `nameserver 127.0.0.1` (local NextDNS); backed up.
  - Added explicit `-forwarder=ghostbridge.tech=10.200.0.2:53` to `nextdns-srv` run command; recompiled s6-rc DB.
  - Added `incus network set ovsbr0 raw.dnsmasq "address=/ghostbridge.tech/10.200.0.2"` so the ovsbr0 dnsmasq answers owned domains locally.
  - Verified: `google.com`/`example.com` resolve via NextDNS; `assistant.ghostbridge.tech`/`api.ghostbridge.tech` resolve to `10.200.0.2`.
- **Post-session refinement: TLS certificates added**
  - Added `XRAY_TLS_CERT` and `XRAY_TLS_KEY` to `/etc/ghostbridge/xray.env`.
  - Regenerated `/dev/shm/xray-ghostbridge.json` with the shuttle; the `op-tls` 443 inbound now includes the `ghostbridge.tech` certificate.
  - Verified: `gbr-xray` reloads the new config, TLS handshake on `127.0.0.1:443` succeeds, and an HTTPS request with `Host: assistant.ghostbridge.tech` falls back to zeroclaw and returns HTTP 200.
- **Known limitations / pending**
  - The dynamic config still sends all non-`mcp.internal` traffic to zeroclaw (`to-grpc-bridge`). Per-domain TCP/Unix-socket routing (qdrant, netmaker API/MQ/UI, etc.) is not yet restored; the static-config services are either stopped (netmaker API container) or only available via Unix sockets that need explicit routing rules.
  - `gbr-xray` is marked `normally down`; it was manually started after the last s6-rc recompile. To survive reboot, either add it to an up bundle or remove the `down` state.
  - `zeroclaw-log` required `pipeline-name` + `notification-fd` files; now fixed and up.
  - Live WG-client → cognitive-mcp/netmaker end-to-end test still pending; needs an external WireGuard/Xray client.
- **NEXT:** T5 Gemma routing brain, T6 derived keys, T7 zeroclaw/openclaw replacement, T8 Voyage chunking, or run the live traffic verification gate.
